# T1 — Resolver D-001: sentinela único em `situacao_cru`

## 1. Identificação

- **ID**: T1
- **Nome da Task**: Resolver D-001 — `situacao_cru` com sentinela ambíguo nas dataclasses de Resultado
- **model**: sonnet
- **risk**: low
- **gates**: [qa, tech_review]  # path em Critical Path (payments — domínio de cobrança bancária)
- **Status**: Concluído
- **Fase**: 2 (núcleo canônico — sequencial)
- **Dependências**: Nenhuma
- **Símbolos públicos criados**: N/A
- **Símbolos consumidos de outras tasks**: N/A
- **Critério de Conclusão**: anotação padronizada nas 3 dataclasses sem expansão de escopo; suíte existente passa sem regressão; gates aprovam.
- **Feature**: integracao-bancaria-configuravel
- **Versão**: v2-debits
- **Parent task**: T1 (de v1)
- **Débito origem**: D-001 (code_quality, BAIXO) — classificação: `recomendado_corrigir`
- **Source**: agent-spec-debt-resolution

---

## 2. Objetivo da Task

Resolver o débito técnico D-001 padronizando o sentinela de `situacao_cru`, **sem expandir escopo** e **sem regressão** na suíte de testes.

---

## 3. Arquivos Impactados

### 3.1 Arquivos a Criar

_Nenhum._

### 3.2 Arquivos a Modificar

| Arquivo | Descrição |
|---------|-----------|
| `app-sync/locacao_automation/locacao_automation/cobranca_bancaria/modelo.py` | Trocar `situacao_cru: str \| None = ""` por `situacao_cru: str = ""` nas dataclasses `ResultadoEmissao`, `ResultadoBaixa` e `ResultadoConsulta` |

> ⚠️ **Arquivo compartilhado**: `modelo.py` também é tocado por **T5** (normalização de `mapear_situacao_boleto`) e **T4** (campo `codigo_erro`). Essas são tasks separadas — **não** as antecipe aqui.

### 3.3 Arquivos de Referência

- `docs/specs/features/integracao-bancaria-configuravel/v1/_run/run-report.md` — débito original (§2, linha 29).
- `docs/specs/features/integracao-bancaria-configuravel/v1/tasks/T1.md` — task que gerou o débito.

---

## 4. Detalhes de Implementação

### 4.1 Contexto do Débito

**Origem**: task `T1` da `v1` — registrado em `docs/specs/features/integracao-bancaria-configuravel/v1/_run/run-report.md` (linha 29).

**Descrição original**: em `ResultadoEmissao`/`ResultadoBaixa`/`ResultadoConsulta` o campo é `situacao_cru: str | None = ""` — o tipo admite `None` mas o default é string vazia, criando **dois sentinelas** para "sem texto cru". Sem efeito funcional (o cru é sempre preenchido pelo produtor do resultado), mas consumidores futuros podem checar `is None` vs `not situacao_cru` de formas divergentes.

**Categoria**: `code_quality` (BAIXO)

**Classificação do especialista**: `recomendado_corrigir`
**Justificativa**: "Custo: 2min (3 anotações de tipo em dataclasses simples, sem validação runtime). Risco: nenhum, campo sempre populado pelo produtor. Valor: elimina sentinela duplo e evita propagar o padrão para novos ResultadoX."
**Custo estimado**: ~2min
**Risco de regressão**: `nenhum`

### 4.2 Correção

- [ ] Padronizar para `situacao_cru: str = ""` nas três dataclasses de resultado (`ResultadoEmissao`, `ResultadoBaixa`, `ResultadoConsulta`) em `modelo.py`.

> Não há subtarefas adicionais. Cleanup é cirúrgico.

### 4.3 Guardrails de Execução

**DEVE**:

- Ler o débito original no `_run/run-report.md` da `v1` (linha 29) antes de editar.
- Aplicar a correção literalmente: apenas a anotação de tipo e o default do campo `situacao_cru`.
- Verificar por grep que nenhum ponto do código depende de `situacao_cru is None` — se depender, **pare e reporte** em vez de alterar a lógica.
- Rodar a suíte completa antes de retornar a task.

**NÃO DEVE**:

- NÃO alterar outros campos das dataclasses nem outras dataclasses.
- NÃO antecipar os débitos que têm task própria no MESMO arquivo: **D-005/T5** (`mapear_situacao_boleto`) e **D-004/T4** (campo `codigo_erro`). Cada um na sua task.
- NÃO adicionar testes novos.
- NÃO alterar comportamento observável.

---

## 5. Testes

**N/A — task é cleanup técnico**. Não cria testes novos.

O Gate 1 (QA) DEVE executar a **suíte completa** após a modificação:

```
docker compose exec -T backend bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend run-tests --app locacao_automation'
```

(a partir de `/opt/frappe`; **não existe `bench` no host** — só dentro do container. Site `frontend` é produção; `FrappeTestCase` faz rollback.)

- ✅ Os **114 testes** continuam passando.
- ❌ Qualquer teste regredindo → task rejeitada.

---

## 6. Notas / Observações

_Vazio. Tasks de débito não geram débito novo._

---

## 7. Checklist Final

- [ ] Correção aplicada exatamente como descrita em §4.2 — sem expansão de escopo
- [ ] Diff afeta APENAS `cobranca_bancaria/modelo.py`
- [ ] Nenhuma antecipação de T4/T5 (mesmo arquivo, tasks próprias)
- [ ] Suíte completa passa sem regressão (114 testes)
- [ ] Gates aprovaram (QA + Tech Review)
- [ ] Staged para commit (git add pelo orquestrador)
