# TASK – Detalhamento da Task

## 1. Identificação
- **ID**: T8
- **Nome da Task**: Remover o ambiente de homologação obsoleto e medir a folga de disco
- **model**: sonnet
- **risk**: medium
- **gates**: [qa]   # tipo=operacional_destrutivo — fora da tabela de fast-path; sem codigo de dominio a revisar, mas destrutivo sobre infraestrutura
- **Responsável**: sysloc
- **Status**: Concluído
- **Fase**: 2
- **Dependências**: —
- **Símbolos públicos criados**: N/A
- **Símbolos consumidos de outras tasks**: N/A
- **User Stories Relacionadas**: US-05

---

## 2. Objetivo da Task
Liberar espaço em disco removendo por completo o ambiente de homologação obsoleto, com medição registrada antes e depois.

---

## 3. Descrição Detalhada
O disco está em 79% com 5,9 GB livres — é o limite que condiciona a subida da stack nova (T9). Esta task precede T9 por dependência física, não lógica.

Passos:

1. **Medir e registrar** o espaço livre antes de qualquer remoção.
2. Remover por completo o ambiente de homologação obsoleto: contêineres, volumes e o diretório. A remoção foi autorizada em decisão fechada do plano.
3. Remover resíduos acumulados: cópias `.bak-*` de arquivos de composição e o diretório de backups antigos de referência.
4. **Medir e registrar** o espaço livre depois.
5. Se a folga não for suficiente para dois ambientes coexistirem, **registrar como impedimento** e bloquear a T9 — o fluxo alternativo previsto no PRD §7.2 manda parar, não forçar.

### 3.1 Exemplo de Payload

N/A — sem payload parcial

---

## 4. Aceite Técnico (critérios objetivos)

A task estará concluída quando:

- [x] **AC-1** — Medição de disco registrada antes e depois, com evidência literal (`df -h /`) — §7.1 e §7.5
- [x] **AC-2** — Nenhum contêiner, volume ou diretório do ambiente obsoleto permanece — §7.4
- [x] **AC-3** — A folga resultante comporta dois ambientes (6,7 GB livres × 486,6 MB de pegada real) — §7.6. **T9 liberada.**
- [x] **AC-4** — Nenhum recurso do ambiente que atende a operação foi tocado — §7.7 (9 contêineres de pé, site responde)
- [x] Testes da task implementados e verdes (Regra 8 — bloqueante) — task operacional: evidência literal na §7, conforme §6.0 e tech_spec §19.6
- [x] Nenhuma quebra nos fluxos existentes — §7.7

> **Passo 3 da §3 (resíduos) NÃO executado integralmente** — impedimento registrado na §7.8. Não afeta AC-1 a AC-4 (o resíduo não removido soma ~48 MB, 0,7% da folga).

---

## 5. Arquivos Impactados

### 5.1 Arquivos a Criar

| Arquivo | Descrição |
|---------|-----------|
| — | Nenhum |

### 5.2 Arquivos a Modificar

| Arquivo | Modificação |
|---------|------------|
| — | Nenhum |

### 5.3 Arquivos de Referência

| Arquivo | Motivo da Consulta |
|---------|-------------------|
| `docs/specs/features/saas-multi-empresa/v1/tech_spec.md` | §12.3 — o disco como limite condicionante |
| `docs/prds/features/saas-multi-empresa/v1/prd.md` | §7.2 — fluxo alternativo quando a folga é insuficiente |

---

## 6. Testes

### 6.0 Padrões de Verificação

Task **operacional**: atua sobre o host e sobre a orquestração de contêineres, fora do alcance de `bench run-tests`. Fabricar teste automatizado aqui provaria menos do que o critério afirma — o tech_spec registra isso na §19.6.

A verificação é por **comando executado com evidência literal registrada** na conclusão da task. Cada critério da §4 traz o comando exato que o comprova.

---

## 7. Notas / Observações

**Task destrutiva sobre infraestrutura.** A remoção é irreversível; o ambiente obsoleto não tem backup próprio.

**Precede T9 por dependência física**: sem a folga medida, subir um segundo ambiente é aposta.

---

## 7.A Evidência de Execução (2026-07-29)

> Task operacional: a evidência literal É o entregável (§6.0). As saídas abaixo são coladas tal como capturadas; anotações de leitura que o comando não emite aparecem entre parênteses ou após `→` (ex.: `→ (nenhum)`), nunca misturadas ao corpo da saída sem marcação. Nenhum comando é elidido — os dois trechos antes truncados com `...` foram completados (ver §7.3 e §7.6). **Correção de precisão (D-002, cleanup v2-debits):** esta nota substitui a promessa original — *"toda saída abaixo é colada na íntegra, sem transcrição nem resumo"* —, que não descrevia com exatidão os blocos desta seção.
> Executado como `sysloc` (grupo `docker`, **sem sudo**). Operações que exigiam root foram feitas por contêiner efêmero com bind-mount explícito — nunca por varredura global.

### 7.1 — AC-1 · Disco ANTES

```
$ df -h /
Filesystem                         Size  Used Avail Use% Mounted on
/dev/mapper/ubuntu--vg-ubuntu--lv   30G   23G  5.9G  80% /
```

```
$ docker system df
TYPE            TOTAL     ACTIVE    SIZE      RECLAIMABLE
Images          8         5         3.062GB   38.24MB (1%)
Containers      23        10        90.8MB    45.64MB (50%)
Local Volumes   32        21        1.306GB   380.5MB (29%)
Build Cache     21        0         627.8MB   81.04MB
```

### 7.2 — Inventário ANTES da remoção (base do AC-2 e do AC-4)

Alvo confirmado: projeto Compose `frappe_staging` (`/opt/frappe-staging/.env` → `COMPOSE_PROJECT_NAME=frappe_staging`).

```
$ docker ps -a --filter "label=com.docker.compose.project=frappe_staging" --format '{{.ID}}  {{.Names}}  {{.Image}}  {{.Status}}'
b423ea33cf50  frappe_staging-scheduler-1  frappe/erpnext:v15.4.0  Exited (137) 9 days ago
7744c220f0c5  frappe_staging-queue-short-1  frappe/erpnext:v15.4.0  Exited (1) 9 days ago
f06976a87dee  frappe_staging-queue-long-1  frappe/erpnext:v15.4.0  Exited (1) 9 days ago
18b58f5ed203  frappe_staging-backend-1  frappe/erpnext:v15.4.0  Exited (0) 2 weeks ago
3a9bd63cf1a5  frappe_staging-redis-cache-1  redis:6.2-alpine  Exited (0) 2 weeks ago
49295d6b500f  frappe_staging-redis-queue-1  redis:6.2-alpine  Exited (0) 2 weeks ago
1ea522a274a6  frappe_staging-db-1  mariadb:10.6  Exited (0) 2 weeks ago
57ff00c43d7a  frappe_staging-create-site-1  frappe/erpnext:v15.4.0  Exited (0) 3 months ago
2460dce4c9a1  frappe_staging-frontend-1  frappe/erpnext:v15.4.0  Exited (1) 9 days ago
a9a2dbd49ad6  frappe_staging-configurator-1  frappe/erpnext:v15.4.0  Exited (0) 3 months ago
1db69c7df725  frappe_staging-websocket-1  frappe/erpnext:v15.4.0  Exited (1) 9 days ago
```

11 contêineres, **todos `Exited`** — nenhum em execução. Volumes, rede e tamanhos:

```
$ docker volume ls --format '{{.Name}}\t{{.Label "com.docker.compose.project"}}' | grep staging
frappe_staging_db-data	frappe_staging
frappe_staging_logs	frappe_staging
frappe_staging_redis-cache-data	frappe_staging
frappe_staging_redis-queue-data	frappe_staging
frappe_staging_sites	frappe_staging

$ docker network ls --format '{{.Name}}\t{{.Driver}}\t{{.Label "com.docker.compose.project"}}' | grep staging
frappe_staging_default	bridge	frappe_staging

$ docker run --rm -v frappe_staging_db-data:/a -v frappe_staging_sites:/b -v frappe_staging_logs:/c \
    -v frappe_staging_redis-cache-data:/d -v frappe_staging_redis-queue-data:/e alpine du -sh /a /b /c /d /e
387.8M	/a   (db-data)
6.2M	/b   (sites)
5.0M	/c   (logs)
1.6M	/d   (redis-cache-data)
8.0K	/e   (redis-queue-data)
```

Contraprova do ambiente de produção **antes** (para o AC-4): projeto `frappe`, 9 contêineres `Up`, volumes `frappe_*`, rede `frappe_default` — nenhum deles no inventário acima.

### 7.3 — Verificações de segurança feitas ANTES de destruir

Três conferências não-triviais, todas com resultado que **autorizou** prosseguir:

1. **O diretório guardava fonte única?** Não. `diff -rq` entre `/opt/frappe-staging/app-sync/locacao_automation` e o app versionado mostrou que o staging é um **subconjunto estritamente mais antigo** — 14 módulos existem só no versionado (`contrato_pdf`, `contrato_cancelamento`, `integracao_bancaria_api`, `patches`, `tests`, …) e nenhum arquivo existe só no staging.
2. **`reference/automacao_cobranca_scheduler_reference.py` era a única cópia da rotina de maior frequência (cron `* * * * *`)?** Não — o Server Script correspondente segue vivo na produção:
   ```
   $ bench --site frontend execute frappe.client.get_list --kwargs '{"doctype":"Server Script","filters":{"script_type":"Scheduler Event"},"fields":["name","disabled"]}'
   [{"name": "automacao_cobranca_scheduler", "disabled": 1}, {"name": "Geração mensal de cobranças", "disabled": 1},
    {"name": "Recalcular status das cobranças", "disabled": 1}, {"name": "Teste agendador", "disabled": 1},
    {"name": "Verifica Scheduler", "disabled": 1}]
   ```
   A fonte de verdade dos Server Scripts continua sendo o ambiente de execução (é a premissa da própria feature), intocado por esta task. T4/T5–T7 capturam de lá.

   **Correção de rigor (D-003, cleanup v2-debits)**: a consulta acima prova que o **documento** `automacao_cobranca_scheduler` existe — não prova que o **conteúdo** do script sobrevive, que era a pergunta real, porque o arquivo destruído (`reference/automacao_cobranca_scheduler_reference.py`) era cópia do código-fonte do Server Script, e é o conteúdo, não o registro, que se perderia com a remoção. Comprovação de conteúdo, reconfirmada nesta correção por consulta própria (leitura, site `frontend`, 2026-07-29 22:27 -03):
   ```
   $ bench --site frontend console
   In [1]: doc = frappe.db.get_value("Server Script", "automacao_cobranca_scheduler", "script")
   In [2]: print("LEN:", len(doc)); print("FIRST_LINE:", repr(doc.splitlines()[0]))
   LEN: 21031
   FIRST_LINE: '# Script Type: Scheduler Event'
   ```
   Confirma exatamente os valores que o QA da T8 já havia medido (21031 caracteres, mesma primeira linha) — a decisão original era correta; o rigor do registro é que ficava aquém.

   **Regra geral que este débito expõe**: em conferência pré-destrutiva, a evidência deve interrogar exatamente a propriedade cuja perda é irreversível — existência não substitui conteúdo.
3. **Imagens são compartilhadas** — `frappe/erpnext:v15.4.0`, `mariadb:10.6` e `redis:6.2-alpine` são usadas também pelo projeto `frappe`. **Nenhuma imagem foi removida** (salvaguarda: a T9 reusa as mesmas, e o disco é o recurso escasso).

Inventário integral do diretório destruído (39 arquivos), registrado para auditoria: `docker-compose.yaml`, `docker-compose.override.yml(.bak)`, `.env`, `run-locacao-automation-staging.{sh,log}`, `reference/` (3 arquivos) e `app-sync/locacao_automation/` (cópia defasada do app + `__pycache__`).

### 7.4 — AC-2 · Remoção executada e conferida

Seletores **explícitos** por label/nome do inventário. **Nenhum comando de varredura global** foi usado (`docker system prune`, `volume prune`, `container prune` estão proibidos — alcançariam `frappe` e `sysloc-react-1`).

```
$ docker rm $(docker ps -aq --filter "label=com.docker.compose.project=frappe_staging")
b423ea33cf50
7744c220f0c5
f06976a87dee
18b58f5ed203
3a9bd63cf1a5
49295d6b500f
1ea522a274a6
57ff00c43d7a
2460dce4c9a1
a9a2dbd49ad6
1db69c7df725

$ docker volume rm frappe_staging_db-data frappe_staging_logs frappe_staging_redis-cache-data \
                   frappe_staging_redis-queue-data frappe_staging_sites
frappe_staging_db-data
frappe_staging_logs
frappe_staging_redis-cache-data
frappe_staging_redis-queue-data
frappe_staging_sites

$ docker network rm frappe_staging_default
frappe_staging_default

$ docker run --rm -v /opt:/host-opt alpine sh -c 'test -d /host-opt/frappe-staging || exit 1; \
    test -d /host-opt/frappe || exit 1; ls -1 /host-opt; rm -rf /host-opt/frappe-staging; ls -1 /host-opt'
antes:  backups containerd frappe frappe-staging react
depois: backups containerd frappe react
```

Conferência de que **nada restou**:

```
$ docker ps -a --format '{{.Names}}' | grep -i staging   →  (nenhum)
$ docker volume ls --format '{{.Name}}' | grep -i staging →  (nenhum)
$ docker network ls --format '{{.Name}}' | grep -i staging →  (nenhuma)
$ ls -d /opt/frappe-staging
ls: cannot access '/opt/frappe-staging': No such file or directory

$ docker compose ls -a
NAME                STATUS                  CONFIG FILES
frappe              exited(2), running(9)   /opt/frappe/docker-compose.yaml,/opt/frappe/docker-compose.override.yml
```

O projeto `frappe_staging` **deixou de existir** para o Compose. ✅ AC-2

### 7.5 — AC-1 · Disco DEPOIS

```
$ df -h /
Filesystem                         Size  Used Avail Use% Mounted on
/dev/mapper/ubuntu--vg-ubuntu--lv   30G   22G  6.7G  77% /
```

```
$ docker system df
TYPE            TOTAL     ACTIVE    SIZE      RECLAIMABLE
Images          8         5         3.062GB   38.24MB (1%)
Containers      12        10        45.88MB   720.9kB (1%)
Local Volumes   27        8         888.1MB   380.5MB (42%)
Build Cache     21        0         627.8MB   81.04MB
```

**Delta: 5,9 GB → 6,7 GB livres (+0,8 GB); uso 80% → 77%.** ✅ AC-1

### 7.6 — AC-3 · A folga comporta dois ambientes

Pegada **real e medida** do ambiente que atende a operação (todos os volumes do projeto `frappe`):

```
$ docker run --rm -v frappe_db-data:/db -v frappe_sites:/sites -v frappe_logs:/logs \
    -v frappe_redis-cache-data:/rc -v frappe_redis-queue-data:/rq -v frappe_assets:/assets alpine du -sch /db /sites /logs /rc /rq /assets
451.7M	/db
8.6M	/sites
26.2M	/logs
120.0K	/rc
12.0K	/rq
20.0K	/assets
486.6M	total
```

Conclusão: **6,7 GB livres para uma pegada de 486,6 MB — folga de ~13,7×.** O segundo ambiente da T9 sobe com **banco vazio** (US-06), portanto sua pegada é *menor* que os 486,6 MB medidos, e as imagens já estão no host (`locacao-erpnext:v15.4.0-pdf`, `frappe/erpnext:v15.4.0`, `mariadb:10.6`, `redis:6.2-alpine`) — **nenhum download novo**, custo de imagem zero.

**A folga é suficiente. O fluxo alternativo do PRD §7.2 NÃO se aplica. A T9 está liberada.** ✅ AC-3

### 7.7 — AC-4 · Produção intacta

```
$ docker ps -a --filter "label=com.docker.compose.project=frappe" --format '{{.Names}}\t{{.Status}}'
frappe-scheduler-1	Up 5 days
frappe-queue-short-1	Up 5 days
frappe-queue-long-1	Up 5 days
frappe-backend-1	Up 18 hours
frappe-frontend-1	Up 5 days
frappe-websocket-1	Up 5 days
frappe-redis-cache-1	Up 5 days
frappe-redis-queue-1	Up 5 days
frappe-db-1	Up 5 days (healthy)
frappe-configurator-1	Exited (0) 13 days ago
frappe-create-site-1	Exited (1) 13 days ago

$ docker ps --filter "label=com.docker.compose.project=frappe" -q | wc -l
9
```

Os **9 contêineres seguem de pé** com o mesmo uptime de antes da remoção (`Up 5 days` / `Up 18 hours` — nenhum reiniciou). Volumes e rede preservados: `frappe_assets`, `frappe_db-data`, `frappe_logs`, `frappe_redis-cache-data`, `frappe_redis-queue-data`, `frappe_sites`, `frappe_default`.

O site responde:

```
$ docker compose exec -T backend bench --site frontend list-apps
frappe             15.4.0 UNVERSIONED
erpnext            15.4.0 UNVERSIONED
locacao_automation 0.0.1  UNVERSIONED

$ curl -H 'Host: frontend' http://localhost:8200/api/method/ping
{"message":"pong"}
```
> **Correção de rigor (D-002, cleanup v2-debits)**: o `curl` original não usava `-w`; "HTTP 200" era uma anotação de leitura misturada ao corpo da saída, não algo emitido pelo comando. Reconfirmado nesta correção (2026-07-29 22:28 -03) com `curl -s -o /dev/null -w 'HTTP %{http_code}\n' -H 'Host: frontend' http://localhost:8200/api/method/ping` → `HTTP 200`.

O `sysloc-react-1` (React na 8300, fora de ambos os projetos) também não foi tocado. ✅ AC-4

### 7.8 — IMPEDIMENTO: passo 3 da §3 (resíduos) não executado

O passo 3 manda remover «cópias `.bak-*` de arquivos de composição e o diretório de backups antigos de referência». **Os candidatos foram localizados e listados antes de qualquer remoção** (não presumidos) e **todos foram descartados** por motivo concreto. Nada foi apagado.

**(a) Cópias `.bak-*` de arquivos de composição — NÃO removidas: estão versionadas no Git.**

```
$ git ls-files | grep -i "docker-compose.*\.bak"
docker-compose.override.yml.bak-20260715-163428
docker-compose.override.yml.bak-assets-2026-04-06
docker-compose.override.yml.bak-locacao-automation
docker-compose.override.yml.bak-pdf-image-2026-04-08_10-27-21
docker-compose.override.yml.bak-sicoob-mtls-2026-04-28_10-09-28
docker-compose.override.yml.bak.2026-03-26_10-16-40
```

Dois bloqueios independentes:
- **Escopo (§5.2)**: a §5.2 declara «Arquivos a Modificar: Nenhum». Apagá-las produziria a exclusão de 6 arquivos **rastreados** no commit da task — desvio de escopo direto.
- **Dependência da T3 (ainda `A Fazer`)**: o `pre-refinement.md:332` usa exatamente esses arquivos como **prova** de uma decisão pendente — «`Configuracao Integracao Sicoob` | Referenciado apenas em arquivos `.bak-*`». Destruí-los agora eliminaria a base probatória de uma decisão que a T3 ainda não tomou.
- Ganho de disco: **24 KB**. Irrelevante para o AC-3.

> A cópia `.bak` que estava *dentro* do ambiente obsoleto (`/opt/frappe-staging/docker-compose.override.yml.bak`) **foi removida** junto com o diretório (§7.4).

**(b) «O diretório de backups antigos de referência» — não é identificável sem decisão do usuário.** Existem 3 candidatos, nenhum nomeado em nenhuma spec, e cada um trava por um motivo diferente:

| Candidato | Tamanho | Por que NÃO foi removido |
|---|---|---|
| `/opt/frappe/reference/backups_codex` | 5,7 MB | **Versionado no Git** (`git ls-files reference/` retorna seus arquivos) — mesma violação de escopo do item (a) |
| `/opt/frappe/reference/backups-tc001` | 21 MB | Criado há 1–5 dias pela TC-001 **recém-encerrada** (commit `a3560cd`) — não é «antigo»; retido deliberadamente com `.gitignore` próprio que declara conter a credencial do Administrator em texto claro |
| `/opt/backups/frappe/dumps-iniciais-2026-03-06` | 21,8 MB | Fica **dentro da árvore de backup ativa da produção** (`/opt/backups/frappe/daily` = 1,0 GB, cron diário 02:30, `latest` → `2026-07-29_02-30-01`). Confundi-lo com «backups antigos» seria catastrófico — a salvaguarda manda parar e perguntar |

**Ação tomada**: nenhum removido; impedimento registrado aqui. Resolver exige decisão explícita do usuário sobre *qual* diretório o passo 3 designava. Total não recuperado: **~48 MB — 0,7% da folga de 6,7 GB**, sem qualquer efeito sobre AC-1 a AC-4 nem sobre a liberação da T9.

### 7.9 — Fora do escopo desta task (observado, não tocado)

Recursos que **não pertencem** ao ambiente obsoleto e por isso não foram removidos — registrados para decisão futura:

- **Estado no momento da execução original desta task (2026-07-29, antes de a T9 subir a stack nova):** 5 volumes órfãos `erpnext_*` (`erpnext_db-data`, `erpnext_logs`, `erpnext_redis-cache-data`, `erpnext_redis-queue-data`, `erpnext_sites`) e 16 volumes anônimos, dos quais 14 sem contêiner associado (2 pertenciam a `frappe-create-site-1` e `frappe-configurator-1`). Somam a maior parte dos **380,5 MB** ainda marcados como `RECLAIMABLE` na §7.1. São resquício de um projeto Compose `erpnext` que já não existe — distinto do `frappe_staging`.
  > **Correção de contagem (D-001, cleanup v2-debits)**: a versão original desta seção afirmava *"15 volumes anônimos, todos sem contêiner associado"*. Estava errada em dois pontos — a contagem (eram 16) e o predicado (2 desses 16 tinham contêiner associado — não eram órfãos).
- **Remedição no momento desta correção (2026-07-29, 22:26 -03) — os números mudaram, porque a T9 da v1 subiu a stack nova no intervalo entre a execução da T8 e este cleanup:**
  ```
  $ docker volume ls -q | wc -l
  45
  $ docker volume ls -q --filter dangling=true | wc -l
  29
  $ docker volume ls -q | grep -E '^[0-9a-f]{64}$' | wc -l
  29
  $ docker volume ls -q --filter dangling=true | grep -E '^[0-9a-f]{64}$' | wc -l
  24
  $ docker volume ls -q | grep '^erpnext_'
  erpnext_db-data
  erpnext_logs
  erpnext_redis-cache-data
  erpnext_redis-queue-data
  erpnext_sites
  ```
  Estado atual: **5 volumes órfãos `erpnext_*`** (todos em `dangling=true`) e **29 volumes anônimos**, dos quais **24 sem contêiner associado** (órfãos reais) e **5 com contêiner associado** — 2 no projeto `frappe` (`frappe-create-site-1`, `frappe-configurator-1`, os mesmos de antes) e **3 novos no projeto `frappe-stack-nova`** (`frappe-stack-nova-configurator-1`, `frappe-stack-nova-create-site-1` e **`frappe-stack-nova-backend-1`, hoje `Up`**). Total de órfãos reais hoje: **29** (5 `erpnext_*` + 24 anônimos) — coincide, por definição, com a contagem de `dangling=true`.
  > **Divergência registrada**: os totais de hoje (29 anônimos / 24 órfãos / 5 em uso) **não** coincidem nem com os do momento da execução original da T8 (16 anônimos / 14 órfãos / 2 em uso) nem, por consequência, com os do enunciado deste débito (que replicava os números originais). A causa é o ambiente ter mudado desde então — a T9 subiu a stack nova depois da T8, criando 3 volumes anônimos adicionais hoje em uso, um deles de um contêiner ativo (`Up`). Isto não invalida a correção acima (que descreve o estado de 2026-07-29 antes da T9 subir); apenas confirma que a contagem é **um retrato do momento**, não uma constante do host.
- **Build cache**: 627,8 MB, sendo 81,04 MB recuperáveis (valor herdado da execução original — não remedido nesta correção, por não fazer parte do débito).

**Regra para a limpeza futura**: a task que for tratar estes recursos **deve filtrar por `docker volume ls --filter dangling=true`, nunca por "é anônimo"** — um seletor sobre a premissa antiga alcançaria volumes em uso (2 no momento da T8; **5** no momento desta correção, incluindo um contêiner hoje `Up`).

Recuperá-los exigiria comando de varredura (`docker volume prune`), **proibido** nesta task por alcançar recursos de `frappe` e do `sysloc-react-1`. Ficam como oportunidade de recursos adicionais, a ser tratada com seletor explícito (`--filter dangling=true`) em task própria.

---

### ADRs Aplicáveis nesta Task

- Nenhuma — esta task não toca código regido por ADR ativa.

---

## 8. Checklist Final

- [x] Implementação conforme a §3 e o tech_spec — passos 1, 2, 4 e 5 executados; **passo 3 (resíduos) bloqueado por impedimento registrado na §7.8**
- [x] Todos os critérios da §4 verificados — AC-1 a AC-4 com evidência literal na §7.A
- [x] Arquivos da §5 respeitados — **nenhum arquivo de código tocado**; alterado apenas o próprio `T8.md` (§4, §7, §8), exceção autorizada
- [x] Testes da §6 implementados e verdes — task operacional (§6.0 + tech_spec §19.6): evidência literal registrada, produção verificada intacta
- [x] ADRs da §7 obedecidas — nenhuma aplicável
