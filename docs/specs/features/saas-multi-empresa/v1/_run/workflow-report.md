
## Challenge Session — 2026-07-29 (artifact: tech_spec.md)

- Questões processadas: 4 (das 7 mapeadas; 3 resolvidas por leitura de código sem consultar o usuário)
- Conflitos de terminologia resolvidos: 0 — nenhum termo divergiu do glossário global
- Decisões implícitas explicitadas: 3
  - Ordem de migração dos endpoints: alias registrado ANTES de desativar o Server Script (`frappe/handler.py:62-72` — o override precede a busca por Server Script e usa o cmd já substituído)
  - Caminho de site novo vs site existente para os patches (`frappe/installer.py:307` — `set_all_patches_as_completed` marca sem executar)
  - Ordem obrigatória entre tasks: caracterização capturada com a regra original ainda ativa
- Premissas expostas: 1 — necessidade do patch de convergência não verificada; validação delegada à primeira task, em ambiente destrutível
- Lacunas de escopo corrigidas: 1 — os 19 Server Scripts desativados, levantados no tech-alignment (item C) e omitidos na spec, entraram no patch de descarte (RN-09)
- Termos canonizados no glossário: 0 — glossário não foi criado nem alterado (regra do não-vazio)
- Candidatos a ADR sinalizados: 1 parcial (ordem de migração de regra — falha C1 e C3)
- ADRs sugeridos para criação: 0

---

## Run de execução — 2026-07-29

- [run] executor resolvido: `__default__` (descoberta: `.claude/agents/` contém apenas os 3 agentes reservados aos gates; nenhum candidato executor)
- [run] executor_discipline injetado (fonte: references/executor-discipline.md)
- [run] decisão interativa: backup — usuário optou por seguir com o backup diário de 2026-07-29 02:30 (sem backup fresco) antes da T1 tocar a estrutura do site `frontend`
- [Fase 1] T1 removida do lote paralelo com T4: ambas operam sobre o MESMO banco Frappe do site `frontend` (T1 aplica estrutura de DocType via model sync; T4 executa a suíte criando `Imovel`/`Contrato`). Recurso externo compartilhado não-declarado nos paths → independência não provável → fallback determinístico para sequencial
- [Fase 1] fallback: sequencial T1 → T4
- [T1] base_sha=5a4e5197ee1d4e1cd262b6886c054ddf9a0da9b2
- [T1] ADRs injetadas no executor: ADR-0002, ADR-0003, ADR-0006 (fonte: task §7)
- [T1] Gate 1 (QA, opus — escalado por implicação de Custom DocPerm/ADR-0003): REJEITADO. 0 críticos, 2 altos (ALTO-001 data_handling, ALTO-002 tests), 0 médios, 3 baixos. criterios 4/5 (AC-4 PARCIAL). security_flags: [permission_grant_versioned_in_file]. tocou_area_critica=true. Suíte reexecutada pelo QA: 169/169 OK.
- [T1] attempt_count=1 → correção despachada ao executor (modelo mantido em opus; auto-escalate só se aplica a sonnet)
- [T1] Gate 1 (QA, opus) tentativa 2: REJEITADO. 0 críticos, 0 altos, 1 médio (MED-001 documentation — contradição interna §6 vs §7.1), 1 baixo. criterios 5/6 (AC-4 PARCIAL). Ambos os ALTOS da tentativa 1 verificados como genuinamente corrigidos; anti-gaming AP-24 sem sinal. Suíte reexecutada: 169/169 OK.
- [T1] attempt_count=2 → 3ª e última tentativa despachada (modelo mantido em opus)
- [T1] Gate 1 (QA, opus) tentativa 3: APROVADO. criterios 6/6, 0 problemas em todas as severidades. MED-001 e BAIXO-001 resolvidos e conferidos contra o texto literal de T2.md. Suspeita de transcrição de evidência (206.165s/206.166s) RETIRADA — o QA localizou os 3 logs originais no scratchpad e comparou byte a byte. executou_testes=false nesta rodada (código bit-idêntico ao já validado com suíte verde em 2 rodadas; md5sum de conjunto.json = 205cbf23... = migration_hash registrado).
- [T1] Gate 2 (Tech Review) despachado em opus (escalado: qa_security_flags não vazia + tocou_area_critica=true + retry_attempt>=1)
- [T1] Gate 2 (Tech Review, opus): **PARCIAL** — 0 CRITICO, 2 ALTO (P1 project_pattern, P2 security), 2 MEDIO (P3 testability, P4 adr_compliance), 0 BAIXO. TR não reexecutou a suíte (código bit-idêntico ao validado; nada em `tests/` tocado) — dentro do contrato.
- [T1] TR consultou: ADR-0002, ADR-0003, ADR-0006
- [T1] attempt_count=3 → **LIMITE DE 3 TENTATIVAS ESGOTADO** (execução inicial + 2 correções). Passo 10: task marcada `Bloqueado`, escalada ao usuário. Bloqueio propagado a T2 (dep. direta), T3 e T9 (transitivas).
- [run] rule_candidates: 1 sinal persistido em docs/specs/features/saas-multi-empresa/v1/_run/rule-candidates.md (qa=0, staff=1, orquestrador=0)

### T1 — retry classification (tentativa 4, concedida pelo usuário)
- attempt: 4 (limite padrão de 3 estendido por decisão interativa do usuário — Passo 10 escalou, usuário optou por "4ª tentativa focada em P1/P2/P3")
- problemas_por_categoria: { project_pattern: 1 (ALTO), security: 1 (ALTO), testability: 1 (MEDIO), adr_compliance: 1 (MEDIO) }
- overrides_ativos: [tocou_area_critica: true, task_risk: medium, qa_security_flags: ["permission_grant_versioned_in_file"], diff_stat_changed: n/a]
- requires_qa_revalidation: true
- decisao: após a correção, Gate 1 (QA) → Gate 2 (Tech Review)
- justificativa: "P2 é `security` e P3 é `testability`, ambas em revalidation_required; overrides tocou_area_critica e qa_security_flags também forçam true"
- escopo da correção: P1, P2, P3 (texto da §7.1 do T1.md — o contrato de entrada da T2). **P4 NÃO é corrigível na task** (emenda de ADR-0002) — sinalizado para `/agent-spec-adr-supersede`.
- [T1] bloqueio revertido: T1 → Em Progresso; T2, T3, T9 → A Fazer
- [T1] Gate 1 (QA, opus) tentativa 4: APROVADO_COM_OBSERVACOES. criterios 6/6, 0 críticos/altos/médios, 1 baixo (BAIXO-001 documentation — contagem "12" vs 11 na subseção Fidelidade de exportação). P1/P2/P3 verificados célula a célula contra o banco: tabela dos 14×12 confere, classificação dos 3 regimes de permissions confere, CT-012/CT-013 passam os 7 gates da doutrina. Anti-gaming: 511 inserções, 0 remoções. Suíte reexecutada pelo QA: 169/169 OK, exit 0.
- [T1] Gate 2 (Tech Review) re-despachado em opus (mesmos critérios de escalação)
- [T1] Gate 2 (Tech Review, opus) tentativa 4: **PARCIAL** — 1 ALTO (P1 architecture: `is_submittable` omitido da lista de fidelidade → `Contrato` perderia fluxo de submissão), 3 MEDIO (P2 project_pattern: existe exportador canônico `export_to_files`/`bench export-doc` que não depende de `developer_mode`, tornando o método enumerativo a causa-raiz do P1; P3 architecture: `tabCustom Field` com 3 registros fora do contrato; P4 security: regime A é condicional ao sombreamento, que não existe entre `install-app` e o 1º `bench migrate`).
- [T1] TR consultou: ADR-0002, ADR-0003, ADR-0006
- [T1] TR confirmou RESOLVIDOS: P2 da rodada anterior (três regimes de permissions — classificação confere, regime C correto, governança adequada) e P3 da rodada anterior (CT-012/CT-013 fecham o buraco do eixo permissions). Anti-gaming: sem sinal; correções substantivas, "faltou profundidade de método, não esforço".
- [T1] TR registrou verificações auxiliares (evitam retrabalho): `tabDocType Link/Action/State` vazios para os 14; `tabProperty Setter` vazio; `is_tree`/`index_web_pages_for_search`/`engine`/`sort_field`/`sort_order` de fato uniformes; ADR-0002 NÃO é violada pela conversão (a Decision cobre estrutura criada a partir da decisão e prevê conversão como iniciativa separada).
- [T1] 5ª tentativa NÃO iniciada — a 4ª foi a única autorizada pelo usuário. Escalado novamente.
- [run] rule_candidates: 2 sinais persistidos em docs/specs/features/saas-multi-empresa/v1/_run/rule-candidates.md (qa=0, staff=2, orquestrador=0)

### T1 — retry classification (tentativa 5, concedida pelo usuário)
- attempt: 5 (segunda extensão do limite por decisão interativa; usuário optou por "trocar o método por export_to_files")
- problemas_por_categoria: { architecture: 2 (P1 ALTO, P3 MEDIO), project_pattern: 1 (P2 MEDIO), security: 1 (P4 MEDIO) }
- overrides_ativos: [tocou_area_critica: true, task_risk: medium, qa_security_flags: ["permission_grant_versioned_in_file"], diff_stat_changed: SIM — o piloto será regerado pelo exportador]
- requires_qa_revalidation: true
- decisao: após a correção, Gate 1 (QA, com suíte) → Gate 2 (Tech Review)
- justificativa: "P1/P3 são `architecture` e P4 é `security`, todas em revalidation_required; além disso o artefato de código muda (conjunto.json regerado), o que por si só força re-QA com execução de suíte"
- escopo: troca do método de exportação (montagem manual → `export_to_files`/`bench export-doc`) + emendas P1 (`is_submittable`), P3 (`Custom Field`), P4 (condicionalidade do regime A)
- [T1] bloqueio revertido: T1 → Em Progresso
- [T1] Gate 1 (QA, opus) tentativa 5: APROVADO_COM_OBSERVACOES. criterios 6/6, 0 críticos/altos/médios, 2 baixos (BAIXO-001 terceiro campo `15` de VARREDURA_OK sem definição; BAIXO-002 regra 3 atribui "sem newline final" também aos 2 cadastros de referência, que têm). QA REPRODUZIU o pipeline do exportador em diretório temporário: diff contra o repo = 1 linha (`modified`). Omissão de falsy confirmada segura no banco (`editable_grid=0` após reimportar o gerado). `migration_hash` do banco = md5 do arquivo (98bafa06…) — o versionado é o aplicado. Suíte: 169/169 OK. security_flags += `permission_window_install_before_migrate`.
- [T1] Gate 2 (Tech Review) despachado em opus
- [T1] Gate 2 (Tech Review, opus) tentativa 5: **REJEITADO** — 1 CRITICO (P1 architecture), 1 ALTO (P2 testability), 2 MEDIO (P3 technical_requirement, P4 project_pattern), 1 BAIXO (P5 code_quality).
- [T1] **P1 CRÍTICO muda a conclusão da task**: `export_to_files` FALHA para os 13 restantes (`DoesNotExistError: Module Locação de imóveis not found` — reproduzido read-only para `Atraso`, `Contrato`, `Comodo`); e a convergência de `module` observada no AC-2 veio do valor escrito À MÃO no `.json`, não do model sync. Logo o AC-4 ("patch DESNECESSÁRIO") está parcialmente errado: a metade `custom` é desnecessária, a metade `module` permanece necessária no ambiente existente. O piloto nunca exercitou o caso — exportou DEPOIS da convergência, sobre o único cadastro cujo `module` já estava correto.
- [T1] TR consultou: ADR-0002, ADR-0003, ADR-0006
- [T1] 6ª tentativa NÃO iniciada — a 5ª foi a última autorizada. Escalado.
- [run] rule_candidates: 3 sinais persistidos (qa=0, staff=3, orquestrador=0)

### T1 — retry classification (tentativa 6, concedida pelo usuário)
- attempt: 6 (terceira extensão do limite; usuário optou por "reabrir o AC-4 nos dois eixos")
- problemas_por_categoria: { architecture: 1 (CRITICO), testability: 1 (ALTO), technical_requirement: 1 (MEDIO), project_pattern: 1 (MEDIO), code_quality: 1 (BAIXO) }
- overrides_ativos: [tocou_area_critica: true, task_risk: medium, qa_security_flags: 2 flags, diff_stat_changed: provável]
- requires_qa_revalidation: true
- decisao: após a correção, Gate 1 (QA) → Gate 2 (Tech Review)
- justificativa: "P1 é `architecture` CRITICO e P2 é `testability`, ambas em revalidation_required; a reclassificação do AC-4 é mudança de conclusão, não de redação"
- escopo: reclassificar o AC-4 separando os eixos `custom` (dispensável) e `module` (necessário); especificar — NÃO implementar — o patch mínimo de `module` para a T2; registrar a pré-condição de módulo do `export_to_files`; emendas P2 (exigência de ambiente do CT-013), P3 (custo real da decisão sobre Custom Field), P4 (forma canônica), P5 e os débitos D1/D2 do QA.
- [T1] bloqueio revertido: T1 → Em Progresso
- [T1] Gate 1 (QA, opus) tentativa 6: **REJEITADO** — 1 CRITICO (CRIT-001 logic), 1 ALTO (ALTO-001 tests), 2 BAIXO (citações de linha erradas). criterios 5/6 (AC-4 PARCIAL).
- [T1] **CRIT-001 é o espelho do CRÍTICO do TR no outro eixo**: `DocType.before_export` só remove atributos FALSY e `strip_default_fields` (`export_file.py:57-68`) só remove `migration_hash` e default fields de filhos — logo exportar um DocType com `custom=1` **emite `"custom": 1` no .json**. O piloto só omitiu a chave porque foi escrito à mão. Sob a ordem prescrita (patch só de `module` → exportar → migrar), os 13 seriam exportados `custom=1`, o model sync reimporia `custom=1`, e o AC-1 da T2 + o CT-001 reescrito falhariam. O contrato ficou internamente insatisfazível: exige ausência da chave `custom`, proíbe editar o gerado à mão, e proíbe o patch de tocar `custom`.
- [T1] QA confirmou como corretos: a reclassificação em dois eixos e a coerência entre as 5 seções (a incoerência da rodada 2 não reapareceu); a pré-condição de módulo (reproduziu o `DoesNotExistError` byte a byte, nada escrito em app-sync/); a spec do patch mínimo (guard conferido contra `migrar_configuracao_integracao_bancaria.py:43-44`, no-op da T9 conferido em `installer.py:301-308`); o inventário (3ª versão, correta sob a conclusão nova, conferido linha a linha contra T2.md); a dependência CT-013→T9 registrada em 3 lugares, com `sync.py:115` literal; o `DEFAULT 1`; a forma canônica. Anti-gaming: sem sinal — a conclusão mudou para MAIS rigor. Suíte reexecutada: 169/169 OK.
- [T1] 7ª tentativa NÃO iniciada — a 6ª foi a última autorizada. Escalado.

### T1 — retry classification (tentativa 7, concedida pelo usuário)
- attempt: 7 (quarta extensão; usuário optou por "fechar com o patch completo")
- problemas_por_categoria: { logic: 1 (CRITICO), tests: 1 (ALTO), documentation: 2 (BAIXO) }
- overrides_ativos: [tocou_area_critica: true, qa_security_flags: 1 flag]
- requires_qa_revalidation: true (rejeição veio do QA — sempre re-passa pelo QA)
- escopo: fechar o AC-4 como "patch NECESSÁRIO nos dois eixos, por razão diferente da que a spec dava"; reconciliar a ordem operacional (converger `custom`+`module` → exportar → revisar → migrar); asserção de `custom` no CT-012; 2 citações de linha.
- convergência: a superfície encolheu — não há mais nada a remover do patch. O tech_spec §5.1 chegava à conclusão certa (patch necessário) pelo mecanismo errado ("zerar custom é o que faz o arquivo virar fonte"); o mecanismo real é que o exportador copia `custom` do banco.
- [T1] bloqueio revertido: T1 → Em Progresso
- [T1] Gate 1 (QA, opus) tentativa 7: **REJEITADO** — 0 CRITICO, 1 ALTO (ALTO-001 tests, `T1.md:681`), 1 MEDIO (MED-001 logic, `T1.md:665`), 2 BAIXO. criterios 5/6 (AC-4 PARCIAL).
- [T1] **CRIT-001 da rodada 6 RESOLVIDO NA RAIZ** — QA confirmou que os 5 pontos concordam e a contradição tripla deixou de existir. Evidência do exportador reproduzida literalmente pelo QA (`Atraso custom=1 → chave no export True valor=1`; `Conjunto custom=0 → False`), com a cadeia causal conferida no código (`before_export` remove falsy antes do `if self.custom: return`; `strip_default_fields` não toca `custom`).
- [T1] Resquício: `T1.md:681` (item (e)/CT-003) ainda fala em "patch reduzido" e manda observar "só `module`" — se seguido, removeria a asserção `custom=1` do companheiro negativo declarado em `T2.md:126`, no eixo que motivou a rejeição anterior. MED-001: a razão (ii) da linha 665 atribui ao model sync uma necessidade que a linha 213 do próprio documento desmonta.
- [T1] QA confirmou: formulação do mecanismo precisa (`import_doc` faz delete+insert sem ramo condicionado a `custom`); regra invertida do checklist coerente com a regra 4; ALTO-001 da rodada 6 corrigido com redação específica; 2 baixos corrigidos; aviso de padrão bem posicionado (blockquote da Decisão, caminho de leitura obrigatório); anti-gaming sem sinal (rodada 5 "desnecessário" → 6 "só module" → 7 "dois eixos" = mais restritivo a cada vez).
- [T1] 8ª tentativa NÃO iniciada — a 7ª foi a última autorizada. Escalado.

### T1 — retry classification (tentativa 8, concedida pelo usuário)
- attempt: 8 (quinta extensão; usuário optou por "reconciliar os 2 pontos")
- problemas_por_categoria: { tests: 1 (ALTO), logic: 1 (MEDIO), documentation: 1 (BAIXO), tests: 1 (BAIXO) }
- overrides_ativos: [tocou_area_critica: true, qa_security_flags: 1 flag]
- requires_qa_revalidation: true (rejeição veio do QA — sempre re-passa pelo QA)
- escopo: 2 edições pontuais (T1.md:681 e :665) + 2 baixos. Núcleo do documento já validado pelo QA — sem reabertura de conclusão.
- [T1] bloqueio revertido: T1 → Em Progresso
- [T1] Gate 1 (QA, opus) tentativa 8: **APROVADO**. criterios 6/6, ZERO problemas em todas as severidades. ALTO-001 e MED-001 corrigidos na raiz (grep por "reduzido" = 0 ocorrências); os 2 baixos corrigidos com fortalecimento de asserção. QA fez varredura própria por 15 termos da conclusão superada: LIMPA (3 hits legítimos em texto pré-execução explicitamente anulado). Escopo respeitado (+3 linhas líquidas, nenhuma seção aprovada reaberta). Anti-gaming: a remoção da razão (ii) é correção de afirmação factualmente falsa, não diluição — o documento exige o patch em 3 pontos independentes. tocou_area_critica=false (diff 100% documental). security_flags: [] .
- [T1] QA validou o raciocínio do MED-001 contra o código: no caminho normal `import_doc` reinsere a partir do arquivo nas duas posições; no cenário do `migration_hash` o `import_file_by_path` retorna antes e a escrita do patch sobrevive em qualquer posição. Nota de reforço para a T2: `remove_orphan_doctypes()` lê `custom=0` e roda ao fim de `sync_all()` — em tese sensível à posição, na prática inalcançável pelas 2 razões que ele registrou; REFORÇA o item 3 da §7.1.
- [T1] Gate 2 (Tech Review) despachado em opus (escalação por retry_attempt>=1)
- [T1] Gate 2 (Tech Review, opus) tentativa 8: **REJEITADO** — 1 CRITICO (P1 architecture), 1 ALTO (P2 technical_requirement), 1 BAIXO (P3 project_pattern).
- [T1] **P1 CRÍTICO — a ordem operacional destruiria os 13 cadastros de produção.** Converger `custom`+`module` ANTES de exportar remove simultaneamente as duas proteções que os 13 têm hoje: (a) `custom=1` os mantém fora do filtro `frappe.get_all("DocType", {"custom": 0})` do `remove_orphan_doctypes`; (b) `import_controller` (`base_document.py:82-84`) devolve `Document` sem tocar no módulo enquanto `custom` for truthy. Com `custom=0` + `module='Locacao Automation'` e SEM o `.py` no app, `get_controller` levanta `ImportError` → `remove_orphan_doctypes` chama `frappe.delete_doc(force=True)` **sem `for_reload`** → apaga a linha de `tabDocType` e os filhos (DocField, DocPerm, DocType Link/Action/State). Dados de `tab<Cadastro>` sobrevivem; os cadastros deixam de existir para o framework.
- [T1] **Correção de localização**: `remove_orphan_doctypes()` NÃO roda ao fim de `sync_all()` (como o QA registrou) — roda em `migrate.py::SiteMigration.post_schema_updates`, DEPOIS de `[pre_model_sync]`, `sync_all()` e `[post_model_sync]`. É essa localização que torna a posição do patch irrelevante para esse mecanismo — conclusão certa do documento, pela razão que ninguém tinha escrito.
- [T1] **P2 ALTO**: o passo de escrever os 13 controllers `.py` não aparece em NENHUMA das 3 sequências operacionais do documento, e o exportador não os gera (`write_code_files` depende de `get_code_fields`, que `DocType` não implementa; `create_init=True` só cria `__init__.py`). Nenhum CT cobre a existência do controller.
- [T1] **Causa estrutural apontada pelo TR**: toda a base empírica da task exclui `bench migrate` — a §7.1 declara que usou `import_file_by_path` escopado e que "nenhum patch de nenhum app foi executado", logo `remove_orphan_doctypes` NUNCA foi exercitado nas 5 importações. As conclusões sobre comportamento em migrate são derivadas de leitura de código, não de observação.
- [T1] TR consultou: ADR-0002, ADR-0003, ADR-0006
- [T1] 9ª tentativa NÃO iniciada — a 8ª foi a última autorizada. Escalado.

### Decisão de replanejamento (2026-07-29) — antecipar o ambiente destrutível
- Usuário optou por "Antecipar a T9 e revalidar a T1 nela", após o CRÍTICO do TR expor que a base empírica da T1 exclui `bench migrate`.
- [T1] Status: Bloqueado (aguardando ambiente destrutível), 8 tentativas. NÃO conta como falha da task — é replanejamento por causa estrutural.
- **Impedimento levantado pelo orquestrador na leitura de T8/T9**:
  - T8 é **destrutiva e irreversível** sobre infraestrutura (remove contêineres, volumes e diretório do ambiente de homologação obsoleto; a §7 registra que ele não tem backup próprio). Exige confirmação explícita do usuário antes de executar.
  - T9 declara `Dependências: T8, T2, T3` e consome `Estrutura versionada ← T2, T3`. Os CTs dela (`CT-028`, `CT-029`) exigem que os 14 DocTypes preservados existam versionados — o que só a T2 entrega. Antecipar a T9 **integralmente é impossível** enquanto T2/T3 estiverem atrás da T1.
  - O que É alcançável: subir a stack nova (o artefato `deploy/compose/docker-compose.stack-nova.yaml` + site vazio) como **infraestrutura de verificação para a T1**, deixando a T9 formalmente incompleta (ACs 1/2/5 pendentes) até T2/T3 rodarem.
- [T8] base_sha=5a4e5197ee1d4e1cd262b6886c054ddf9a0da9b2
- [T8] executor: **opus** — DESVIO DELIBERADO do frontmatter (`model: sonnet`), logado para auditoria. Razão: a heurística do gerador classificou por sinais textuais e não pesou que a operação é **irreversível** sobre infraestrutura **compartilhada com o host de produção** (`frappe` e `frappe_staging` no mesmo Docker daemon). Um alvo errado em `docker volume rm` destrói produção sem recuperação. Custo de erro assimétrico justifica o modelo maior.
- [T8] gates: [qa] (declarado). ADRs injetadas no executor: nenhuma (a §7 declara "esta task não toca código regido por ADR ativa")
- [T8] alvo confirmado pelo orquestrador antes do despacho: projeto compose `frappe_staging` (12 contêineres, todos Exited, config em /opt/frappe-staging/) — distinto de `frappe` (produção, 9 running). Disco antes: 30G total, 23G usado, 5.9G livre, 80%.
- [T8] Gate 1 (QA, opus): **APROVADO_COM_OBSERVACOES**. criterios 4/4, 0 críticos/altos/médios, 3 baixos (todos de rigor documental). security_flags: [plaintext_credential_at_rest]. tocou_area_critica=true. executou_testes=false — decisão deliberada e endossada pelo QA (diff de código vazio + ADR-0006 veda suíte contra produção + tech_spec §19.6 alerta que CT-006..010/028/029 exigem remoção real de estrutura).
- [T8] QA verificou os 4 ACs contra o host de forma independente. **Contraprova POSITIVA de que nenhum `prune` global rodou**: permanecem 19 volumes dangling, 627.8MB de build cache e 4 imagens sem uso — um prune teria varrido exatamente esses. AC-4 com prova estrutural: uptimes 5d/18h anteriores à execução descartam reinício silencioso.
- [T8] QA endossou a decisão de PARAR no passo 3 (resíduos): confirmou que as 6 cópias `.bak-*` estão em `git ls-files`, que `backups_codex` é versionado e que `dumps-iniciais-2026-03-06` fica na árvore de backup ATIVA. A expressão "o diretório de backups antigos de referência" não identifica unicamente nenhum dos 3 candidatos.
- [T8] Tech Review PULADO (gates: [qa]) — task marcada concluída após o QA.
- [T8] staged: docs/specs/features/saas-multi-empresa/v1/tasks/T8.md
- [T8/pós] decisão interativa do usuário: remover `reference/backups-tc001/` (21 MB) — contém credencial do Administrator em texto claro (`site_config.json.bak-tc001-faseb`), apenas em disco. Confirmado antes que `git ls-files reference/backups-tc001/` = 0 arquivos (gitignored, nada versionado a perder). Artefato de TC já encerrada.
  - 1ª tentativa FALHOU: `rm -rf` como `sysloc` retornou `Permission denied` — o diretório pai `reference/` é `root:root` e remover uma entrada dele exige permissão de escrita no pai. O diretório permaneceu intacto. Correção do log: uma linha anterior deste relatório afirmou "REMOVIDO" antes da verificação e estava errada; substituída por este registro.
- [T8/pós] decisão interativa do usuário: passo 3 da §3 (demais resíduos, ~48 MB) encerrado como **resolvido-por-decisão** — os `.bak-*` e `reference/backups_codex` permanecem por estarem versionados e terem valor probatório para a T3; `/opt/backups/frappe/dumps-iniciais-2026-03-06` permanece por estar na árvore de backup ativa. Nenhum AC dependia disso.
  - 2ª tentativa OK: removido via contêiner efêmero com bind-mount escopado a `/opt/frappe/reference` (`docker run --rm -v /opt/frappe/reference:/host alpine ... rm -rf /host/backups-tc001`), com guard `test -d` antes e listagem antes/depois. Mesmo padrão que a T8 usou para o diretório do staging — sem `sudo`, sem varredura global, escopo mínimo. Confirmado: `backups-tc001` não existe mais; os outros 25 itens de `reference/` intactos; `git status reference/` limpo (era gitignored).

### T9 — execução PARCIAL autorizada (infraestrutura apenas)
- [T9] base_sha=5a4e5197ee1d4e1cd262b6886c054ddf9a0da9b2
- [T9] executor: opus (declarado no frontmatter) · gates: [qa, tech_review] (declarado) · risk: high
- [T9] ADRs injetadas no executor: ADR-0006, ADR-0002 (fonte: task §7)
- [T9] **Escopo delimitado por decisão do usuário**: entrega apenas a INFRAESTRUTURA (compose isolado + site vazio + app instalado + migrate). Os AC-1, AC-2 e AC-5 e os CTs CT-028/CT-029 **não podem fechar** — dependem dos 14 cadastros versionados, que só a T2 entrega, e a T2 está atrás da T1. AC-3 (isolamento) e AC-4 (operação intacta) são validáveis agora.
- [T9] Propósito imediato: dar à T1 o ambiente destrutível para exercitar `bench migrate` de verdade — incluindo o `remove_orphan_doctypes` que nunca rodou nas 5 importações e que o CRÍTICO do TR expôs.
- [T9] Disco disponível na largada: 6.7G livres (77%).
- [T9] Gate 1 (QA, opus): **REJEITADO** — 0 críticos, 0 altos, 2 médios (MED-001 security: o cabeçalho do compose afirma isolamento mais forte que o real e usa isso como justificativa da senha hardcoded; MED-002 documentation: censo da produção incompleto — 5 DocTypes `custom=0`, não 3). criterios 2/5 (AC-1/AC-2/AC-5 PARCIAIS **por escopo acordado**, não por falha). security_flags: [hardcoded_secret].
- [T9] **AC-3 e AC-4 CONFIRMADOS** pelo QA de forma independente: sem `ports`/`published`/`external`/`network_mode`; volumes e redes disjuntos; `sysloc-react-1` não alcança a stack nova (exit 143) e alcança produção (controle positivo — o teste não é vácuo); 9 contêineres de produção com uptime inalterado; `git status` limpo em `docker-compose*.yaml` e `deploy/nginx/`.
- [T9] **CONTRAPROVA DO `remove_orphan_doctypes` REPRODUZIDA DO ZERO pelo QA e CONFIRMADA**: criou `Qa Orfao Prova` com registro-sentinela, promoveu a `custom=0` + módulo `Locacao Automation` sem controller, rodou migrate → `Orphaned DocType(s) found`, definição apagada (0 DocField), **tabela e linha SOBREVIVERAM** com a sentinela intacta. A distinção "definição some / linhas ficam" está CORRETA. Artefato removido ao final; ambiente limpo com os 3 versionados.
- [T9] **REFINAMENTO CRÍTICO PARA A T2 (MED-002)**: o critério de órfão exige `ImportError` **especificamente**. Quando o Module Def não existe, `get_controller` levanta `DoesNotExistError` — que **não** é subclasse de `ImportError` (MRO: DoesNotExistError → ValidationError → Exception) — e cai no `except Exception: continue`, sendo **pulado**. Consequência: flipar os 14 para `custom=0` mantendo `module='Locação de imóveis'` NÃO os apagaria; movê-los para `Locacao Automation` SEM o controller os apagaria. O gatilho do risco é a tripla `custom=0` + Module Def existente + controller ausente.
- [T9] QA confirmou: `bench migrate` exit 0, `Conjunto` preservado com `modified` byte a byte igual ao `.json` (ADR-0002 satisfeita); correção do `PYTHONPATH` é causa-raiz, não contorno; o achado do `apps.txt` de produção procede mas o risco NÃO se materializa (o module map de runtime vem dos apps instalados no site — `get_all_apps` e `get_controller` funcionam normalmente); AC-5 com placar idêntico ao do executor (169/9/161/1), classificação auditada e procedente, incluindo as 2 failures "None is not true" que poderiam esconder defeito real e não escondem.
- [T9] Gate 1 (QA, opus) rodada 2: **APROVADO_COM_OBSERVACOES**. 0 críticos/altos/médios, 2 baixos (BAIXO-001: §7.A.12 item 1 diz que o migrate sozinho fecha o AC-1, sem apontar a ressalva do bind mount de §7.A.13; BAIXO-002: mesma linha diz "2 já versionados" onde a evidência do próprio doc diz 3). security_flags: [hardcoded_secret] — não é achado novo, é decisão deliberada agora documentada com justificativa válida e corolário de invalidação.
- [T9] MED-001 e MED-002 RESOLVIDOS na raiz. QA fez varredura própria por 6 padrões da premissa falsa: só restaram ocorrências que a NEGAM (compose:35, compose:66) e uma verdadeira e provada (T9.md:169).
- [T9] **QA CORRIGIU A PRÓPRIA FORMULAÇÃO da rodada 1**: o gatilho do órfão é «Module Def **resolvível** (presente num `modules.txt`)», não «Module Def existente». Verificado por 3 caminhos: `issubclass(DoesNotExistError, ImportError)` = False; `get_controller('INATIVO')` levanta `DoesNotExistError`; e `Locação de imóveis` **existe como Module Def no banco** (`app_name='erpnext'`) mas **não está no `modules.txt` de nenhum dos 3 apps**. A distinção importa para a T2 — "existente" seria falso.
- [T9] Censo confirmado contra produção: 18 `custom=1` em `Locação de imóveis`; 3 `custom=0` em `Locacao Automation`; 2 `custom=0` (`INATIVO`, `INATIVO_2`, 0 linhas) em `Locação de imóveis`. Total 5.
- [T9] Anti-gaming: crescimento aditivo (compose 164→189, T9.md 662→730), toda a evidência da rodada 1 intacta. Nota metodológica do QA: ambos os arquivos estão staged como adições novas, sem versão em HEAD para diffar — verificação feita por inspeção integral.
- [T9] Gate 2 (Tech Review) despachado em opus (task_risk=high + qa_security_flags não vazia + retry_attempt>=1)
- [T9] Gate 2 (Tech Review, opus): **PARCIAL** — 1 ALTO (P1 technical_requirement), 2 MEDIO (P2 project_pattern, P3 architecture), 3 BAIXO (P4 security, P5 architecture, P6 architecture).
- [T9] **P1 corrige a informação central desta execução**: o gatilho do órfão tem **4 caminhos de `ImportError`**, não 1. Além de (a) módulo ausente — o único que a T9 exercitou —, há (b) módulo com import interno que falha (`load_doctype_module` reembala qualquer ImportError; `ModuleNotFoundError` é subclasse), (c) **classe com nome divergente do derivado** `doctype.replace(' ','').replace('-','')` → `raise ImportError` em `base_document.py:99`, (d) classe que **não herda `BaseDocument`** → `raise ImportError` em `:106`. Há ainda um **5º fator**: `if doctype in class_overrides: continue`. O TR provou (c) e (d) empiricamente injetando módulos-stub na stack nova.
- [T9] **Armadilhas concretas nos nomes reais**: `Verificar scheduler` exige a classe `Verificarscheduler` (s minúsculo); `INATIVO_2` exige `INATIVO_2` (o `_` NÃO é removido); `Cobranca Integracao Sicoob` exige `CobrancaIntegracaoSicoob`. A orientação "entregar `.json` + `.py` no mesmo commit é seguro" é **falsa como enunciada**.
- [T9] TR descartou por experimento uma hipótese de raio maior: perder o `PYTHONPATH` NÃO torna todos os DocTypes órfãos — o migrate aborta antes (`Could not find app`), sem alcançar o `remove_orphan_doctypes`. Registrou que "estava a um passo de virar um CRÍTICO infundado — a diferença foi executar em vez de inferir".
- [T9] TR consultou: ADR-0006, ADR-0002, ADR-0005, ADR-0004
- [T9] Risco registrado para a T10 (fora do escopo da T9): a stack nova **não** estende a proteção da ADR-0006 à T10 — o instalador de rotinas escreve em `/etc/cron.d` e `/var/log` do host, alvo que nenhum contêiner desta stack hospeda.
- [T9] attempt_count=2 → 3ª tentativa despachada. requires_qa_revalidation=true (P1 `technical_requirement`, P3 `architecture`).
- [run] rule_candidates: 4 sinais persistidos (qa=0, staff=4, orquestrador=0)
- [T9] Gate 1 (QA, opus) rodada 3: **REJEITADO** — 1 ALTO (ALTO-001 tests), 1 MEDIO (MED-001 error_handling), 1 BAIXO. criterios 2/2 (AC-3 e AC-4 revalidados ✅ após a reconstrução).
- [T9] **ALTO-001 — o portão empírico repete o defeito que deveria eliminar**: ele seleciona alvos com `{"custom": 0, "module": "Locacao Automation"}`, mas o predicado real do `remove_orphan_doctypes` é `{"custom": 0}` **sem recorte de módulo** — o que a própria §7.A.7 afirma. Resultado: o portão **não enxerga `INATIVO`/`INATIVO_2`**, exatamente o caso que a §7.A.12 item 6 registra como risco iminente. O QA rodou o portão SEM o filtro: 739 DocTypes em 8,4s, 0 órfãos. O recorte não compra desempenho, só remove cobertura.
- [T9] **MED-001 — guarda de idempotência do `create-site` usa sentinela escrita cedo demais**: `site_config.json` é gravado por `install_db()` ANTES do laço que instala `erpnext` (~3m50s, a etapa mais longa e sujeita a falha). Se ela falhar, o `set -e` derruba o serviço (correto), mas a sentinela permanece no volume; o próximo `up -d` pula com "ja existe", o `backend` sobe por `service_completed_successfully`, e o ambiente fica meio-provisionado **sem sinal de erro** — placar de suíte inválido, o oposto do que a ADR-0006 pede.
- [T9] QA confirmou como CORRETOS: o predicado completo do órfão (conferiu `base_document.py:95/99/105-106` na fonte); **as 20 linhas da tabela de nomes derivados**, incluindo `Verificarscheduler`, `INATIVO`, `INATIVO_2`; o `create-site` (bloco literal `|` corrigido, idempotência confirmada por execução própria, `up -d` em 18,7s); P3 (decisão (b) vs (a) rejeitada, `APP_SRC` nos 3 serviços); P4/P5/P6 (senhas nos 4 pontos incl. healthcheck, `restart: "no"` nos 6 serviços, bloco de topologia). Anti-gaming: crescimento integralmente aditivo, §7.A.4 marcada como superada em vez de apagada.
- [T9] QA reexecutou a suíte após a reconstrução: **placar idêntico** (169 testes, 9 failures/161 errors/1 skipped) — confirma que o `create-site` reproduz o mesmo ambiente da montagem manual.
- [T9] attempt_count=3 → **LIMITE ESGOTADO**. Escalado ao usuário.

### T9 — retry classification (tentativa 4, concedida pelo usuário)
- attempt: 4 (extensão do limite por decisão interativa; usuário optou por "corrigir portão e sentinela")
- problemas_por_categoria: { tests: 1 (ALTO), error_handling: 1 (MEDIO), documentation: 1 (BAIXO) }
- overrides_ativos: [tocou_area_critica: true, task_risk: high]
- requires_qa_revalidation: true (rejeição veio do QA — sempre re-passa pelo QA)
- escopo: ALTO-001 (remover recorte de módulo do portão empírico) + MED-001 (sentinela de idempotência do `create-site`) + BAIXO-001 (evidência da imagem do configurator desatualizada)
- [T9] bloqueio revertido: T9 → Em Progresso (parcial)
- [T9] Gate 1 (QA, opus) rodada 4: **APROVADO_COM_OBSERVACOES**. 0 críticos/altos/médios, 1 baixo (BAIXO-001 documentation — IP de contêiner fixado no cabeçalho do compose inverte de resultado após rebuild). security_flags: [].
- [T9] **ALTO-001 resolvido no lugar que importa**: o QA rodou o portão amplo na PRODUÇÃO (somente leitura) e `INATIVO`/`INATIVO_2` **entram na amostra**, classificados corretamente como pulos por `DoesNotExistError` (`741 verificados, 0 seriam apagados`). O portão é executável onde a T2 vai precisar dele.
- [T9] **O QA fechou um buraco que seria o achado dele: provou o portão FALSIFICÁVEL.** Ele só havia sido observado verde. O QA plantou um órfão sintético na stack nova e o portão ficou **vermelho** corretamente (`740 verificados, 1 seria apagado -> [('Portao Teste Vermelho', ...)]`). Com isso os 4 ramos estão exercitados: detecção por `ImportError`, pulo por `override_doctype_class` (`Address`), pulo por `except Exception` (`INATIVO`/`INATIVO_2`) e verde limpo. Resíduo removido; portão de volta a 739/0.
- [T9] **5º fator auditado com precisão**: `Address` está em `class_overrides` (`{'Address': ['erpnext.accounts.custom.address.ERPNextAddress']}`) e o portão o classifica como pulado. O QA registrou uma precisão relevante: `get_controller("Address")` resolve limpo por si só, então o ramo é **exercitado mas não load-bearing** — a redação da §7.A.7 não exagera.
- [T9] **MED-001: prova dos 2 caminhos sólida.** O QA reproduziu a única parte que era inferência ("backend não promovido"): com marcador ausente, `create-site` Exited(1) com `Site verificacao already exists`, `up -d` EXIT REAL = 1, e o backend em estado **`Created`** — criado e nunca iniciado. Caminho de recuperação (`down -v`) documentado nos 2 arquivos. Escolha do marcador sobre `list-apps|grep` procede: a guarda por completude viraria verdadeira no penúltimo passo e pularia falha no `set-config allow_tests`, quebrando o AC-5.
- [T9] AC-3 e AC-4 revalidados após a 2ª reconstrução, com controle positivo. Anti-gaming: 100% adições, inventário íntegro (§7.A.0 a §7.A.15, migrate em 3 ocorrências, contraprova com sentinela, controle positivo, §7.A.4 marcada como superada). Determinismo: placar da suíte e contagem do portão reproduzidos exatamente.
- [T9] Gate 2 (Tech Review) despachado em opus (task_risk=high + tocou_area_critica=true + retry_attempt>=1)
- [T9] Gate 2 (Tech Review, opus) rodada 4: **REJEITADO** — 1 CRITICO (P1 testability), 3 MEDIO (P2 testability, P3 technical_requirement, P4 security), 1 BAIXO (P5 code_quality).
- [T9] **P1 CRÍTICO — o portão PUBLICADO é incapaz de ficar vermelho.** O bloco `python` da §7.A.7 **não tem nenhuma linha de import** (varredura confirmou: zero `from`/`import` em todo o T9.md fora de colagens de fonte). `get_controller` não está no namespace do `bench console`, e o último handler do laço é `except Exception: pass` — então cada `NameError` é contado como "pulado, não apaga". O TR rodou o bloco VERBATIM: `VERBATIM_ORFAOS: 0` / `VERBATIM_RAMO_EXCEPT_EXCEPTION: 738 {'NameError': 738}`. O portão reporta `739 verificados, 0 órfãos` **sem ter testado nenhum**, e a saída é **idêntica à linha de base verde** que a §7.A.12 item 5 manda a T2 comparar.
- [T9] **Isto inverte a conclusão do QA num ponto de método**: o teste de falsificabilidade dele (plantar órfão sintético → vermelho) validou o script **EXECUTADO**, não o **PUBLICADO**. A T2 herda uma cópia manual, e a cópia nunca foi falsificada. O risco do P1 da 1ª revisão não foi fechado — foi **transferido do texto da regra para o código do portão**.
- [T9] **P2** — o portão omite `clear_controller_cache()`, que o framework chama antes do laço. `get_controller` só reimporta quando `dev_server or in_migrate` (`base_document.py:56-71`); fora disso lê o cache. O portão roda FORA do migrate → é estruturalmente **menos estrito** que a operação que antecipa. Falso-negativo mediado por cache, na direção que não perdoa.
- [T9] **P3** — o procedimento publicado para fechar o AC-1 (`APP_SRC=<checkout> ... up -d`, **sem `down -v`**) é **no-op silencioso**: o marcador `.provisionado` vive no volume `sites`, que sobrevive ao `up -d`; o `create-site` reentra, acha o marcador e pula. O AC-1 fecharia contra o site nascido do working tree — exatamente o defeito que a §7.A.13 rejeitou por escrito.
- [T9] **P4** — direção **stack→produção aberta**, que ninguém havia testado (a §7.A.8d prova proxy→stack). IPs de contêiner da produção estão isolados (`exit=124`), mas `172.19.0.1:8200` (gateway da própria bridge) **alcança** o site `frontend`. O ambiente destrutível onde T2/T3 vão errar tem rota funcional para o que guarda os dados. TR sugere avaliar `internal: true`.
- [T9] **Achado do TR fora do diff, independente da T2**: o site `frontend` de produção **ainda tem `allow_tests: true`**. A ADR-0006 é uma proibição; a T9 entregou a **capacidade** de obedecê-la, não a obediência. `bench --site frontend set-config allow_tests false` custa um comando, não depende da T2, e o TR recomenda task própria em vez de ficar pendurado no AC-5.
- [T9] TR consultou: ADR-0002, ADR-0004, ADR-0005, ADR-0006
- [T9] TR confirmou 5 dos 6 problemas dele RESOLVIDOS NA RAIZ, "com qualidade acima do que eu havia pedido"; elogiou o compose como melhor que o de produção em vários pontos (`pull_policy: never`, `restart: "no"` com raciocínio, `PYTHONPATH` explicado). Registrou um brinde de isolamento: como o compose vive em `deploy/compose/`, um futuro `/opt/frappe/.env` NÃO seria lido por esta stack.
- [T9] Risco novo para a T2 registrado pelo TR: ela vai precisar de `developer_mode` para exportar os DocTypes, e ele é `0` no `frontend` e não é setado na stack nova — **nenhum dos dois ambientes está pronto para isso hoje**.
- [T9] attempt_count=4 → **LIMITE ESGOTADO NOVAMENTE**. Escalado ao usuário.
- [run] rule_candidates: 5 sinais persistidos (qa=0, staff=5, orquestrador=0)

### T9 — retry classification (tentativa 5, concedida pelo usuário)
- attempt: 5 (segunda extensão do limite da T9; usuário optou por "portão vira arquivo versionado")
- problemas_por_categoria: { testability: 2 (P1 CRITICO, P2 MEDIO), technical_requirement: 1 (P3 MEDIO), security: 1 (P4 MEDIO), code_quality: 1 (P5 BAIXO) }
- overrides_ativos: [tocou_area_critica: true, task_risk: high]
- requires_qa_revalidation: true
- escopo: P1 corrigido na CLASSE (portão sai da prosa e vira arquivo versionado executável, com imports, `clear_controller_cache()` e contador de exceções puladas) + P2 + P3 + P4 + P5
- **Ampliação de escopo autorizada**: a §5.1 da T9 declara só o compose. O usuário autorizou a criação de um arquivo de script versionado para o portão. Registro para o Tech Review não tratar como `scope_deviation`.
- [T9] bloqueio revertido: T9 → Em Progresso (parcial)
- [T9] verificação independente do orquestrador após a rodada 5: rede `frappe-stack-nova_default internal=true`; `ip route` no backend mostra **apenas** `172.19.0.0/16` (nenhuma rota default); `gateway:8200` → timeout exit=124; IP de contêiner da produção (`172.18.0.10:8000`) → **`Network is unreachable`** exit=1 (o kernel nem tenta — não há rota); `db:3306` interno → exit=0. P4 fechado como propriedade do arquivo, não como circunstância. Portão agora é `deploy/scripts/portao_orfaos.py` (199 linhas). Produção uptime 6 dias.
- [T9] Nota de método do orquestrador: a 1ª tentativa desta verificação usou `sh -c` com `/dev/tcp` (bashism) e falhou nos dois alvos com o MESMO erro (`Directory nonexistent`) — teste inválido que poderia ter sido lido como "rota fechada". Refeito com `bash -c`. Registro porque é a mesma classe de erro que o run vem encontrando: saída que parece confirmar a hipótese por motivo errado.
- [T9] Gate 1 (QA, opus) rodada 5: **REJEITADO** — 0 críticos, 0 altos, 1 médio (MED-001 tests), 3 baixos. criterios 2/2 (AC-3 e AC-4 revalidados ✅).
- [T9] **P1 FECHADO NO ARTEFATO QUE A T2 HERDA.** O QA replicou a falsificabilidade **sobre o arquivo** nos 3 estados, por execução própria: (1) saudável → VERDE exit 0, 739 alvos, 738 ok, 1 override pulado (`Address`), 0 por exceção; (2) órfão sintético plantado por ele → **VERMELHO exit 1** com o ImportError e o módulo faltante; (3) contador divergente → **RODADA_INVALIDA exit 2** (`PORTAO_ESPERADO_PULADOS=2, medido 0`). Bônus: sem `PORTAO_SITE` → RODADA_INVALIDA. Resíduo removido, verde restaurado. A execução por redirecionamento de stdin elimina o passo de cópia onde a divergência nascia.
- [T9] Portão contra produção (leitura) confirmado byte a byte: `PORTAO_PULADOS_POR_EXCECAO: 2 tipos={'DoesNotExistError': 2}`, `PORTAO_TOTAL_ALVOS: 741`, VERDE. A diferença de 2 alvos contra a stack nova é exatamente `INATIVO`/`INATIVO_2` — valida o refinamento do predicado contra o site real.
- [T9] **P2 (`in_migrate`) auditado e considerado SEGURO** — o QA grepou ~30 ocorrências do flag no framework: quase todas em caminhos de ESCRITA que o portão não alcança; os 2 ramos alcançáveis são inócuos (`log_touched_tables` é no-op sobre SELECT; os builders de cache retornam cedo, direção segura). Ordem correta: `clear_controller_cache()` ANTES de ligar o flag, e ele só faz `frappe.controllers.pop(site)` — dict em memória do processo, nada em redis, nada compartilhado com workers da produção. Com `frappe.destroy()` no finally e processo efêmero, sem vazamento.
- [T9] **P4 (rede interna) confirmado pelo QA com o cuidado de método pedido** (`bash -c`, nunca `sh -c`): `ip route` só com `172.19.0.0/16`, gateway derivado imprime `<nenhum: rede interna>`, `172.18.0.10:8000` → `Network is unreachable` exit=1 (sinal mais forte que timeout). **Custo medido: nenhum** — DNS interno funciona, provisionamento completo rodou sob rede interna. Ele avaliou T2 (exporta para arquivo, framework local) e T3 (banco local): nenhuma precisa de rede externa.
- [T9] **MED-001 — a invocação da suíte publicada no compose sai com exit 0 mesmo VERMELHA.** O QA mediu duas vezes: suíte completa (FAILED 9/161) → **exit 0**; execução escopada (FAILED 9/97) → **exit 0**. É a mesma classe de defeito que a rodada corrigiu no portão: o portão ganhou veredito por código de saída, e o comando irmão publicado 2 linhas acima no MESMO arquivo reporta sucesso enquanto falha. Causa é do `bench` (upstream), não de código da T9 — e a §7.A.10 lê o placar TEXTUALMENTE e conclui certo, por isso é médio e não alto. O risco é herdado por quem automatizar.
- [T9] Baixos: BAIXO-001 (falha antes do laço sai 1, mesmo código do VERMELHO — um upgrade do frappe que mova `get_controller` daria VERMELHO onde o certo é RODADA_INVALIDA); BAIXO-002 (nenhum piso para `len(alvos)` — o mecanismo de contagem esperada existe para os pulados e não para o total); BAIXO-003 (o "mapa atual" de IPs da §7.A.18 **já está errado** — db e redis-queue trocados; é a armadilha do P5 reintroduzida em prosa uma linha depois de ser demolida no compose).
- [T9] Red flag registrada pelo QA: `couples_to_framework_internals` — necessária (espelhar o predicado real é a função do portão), mas obriga revalidar em qualquer upgrade de frappe. Vale aviso no docstring.
- [T9] ADR-0006: o QA considerou e **descartou** violação pela execução do portão contra `frontend` — o portão não é a suíte e é estritamente leitura; censo pós-execução confirma zero alteração. ADR-0002: aderência por espírito forte (o defeito da rodada 4 foi o Context da ADR em miniatura), literal fora de escopo (a ADR trata de estrutura de dados).
- [T9] Ambiente devolvido saudável e conferido item por item após ser sujado 3× (órfão plantado/removido, APP_SRC trocado/restaurado, worktree removido). Produção nunca recebeu escrita.
- [T9] attempt_count=5 → **LIMITE ESGOTADO**. Escalado ao usuário.

### T9 — retry classification (tentativa 6, concedida pelo usuário)
- attempt: 6 (terceira extensão do limite da T9; usuário optou por "fechar o médio e os 3 baixos")
- problemas_por_categoria: { tests: 2 (MED-001 MEDIO, BAIXO-002), error_handling: 1 (BAIXO-001), documentation: 1 (BAIXO-003) }
- overrides_ativos: [tocou_area_critica: true, task_risk: high]
- requires_qa_revalidation: true (rejeição veio do QA)
- escopo: MED-001 (aviso/wrapper do exit 0 da suíte) + BAIXO-001 (exit 1 pré-laço → RODADA_INVALIDA) + BAIXO-002 (piso de amostra) + BAIXO-003 (mapa de IPs na prosa). Nenhuma mexe em comportamento do ambiente.
- [T9] bloqueio revertido: T9 → Em Progresso (parcial)
- [T9] verificação independente do orquestrador após a rodada 6: nenhum DocType com `Qa`/`Portao`/`Orfao`/`Teste` no nome; `custom=1` = **0**; módulo `Locacao Automation` com exatamente os 3 versionados (`Auditoria Configuracao Bancaria`, `Configuracao Integracao Bancaria`, `Conjunto`), todos `custom=0`. Produção uptime 6 dias. `portao_orfaos.py` NÃO está montado no contêiner — roda por redirecionamento de stdin do host (`bench console < arquivo`), que é o design declarado.
- [T9] **Ponto a esclarecer no Gate 1**: o executor relata ter encontrado e removido resíduo sintético do QA (`Qa Portao Orfao Check`, DocType e tabela). O QA da rodada 5 afirmou ter removido e **confirmado** (`DOCTYPE_EXISTE: False, TABELA_EXISTE: False, CUSTOM_1_TOTAL: 0`). O ambiente está limpo agora, mas as duas afirmações não podem ser ambas completas — provável remoção parcial (DocType sem tabela, ou registro em outra tabela de metadado). Vale o QA esclarecer o que exatamente sobrou, porque limpeza incompleta de artefato de teste em ambiente compartilhado é exatamente o tipo de resíduo que contamina rodada seguinte.
- [T9] Gate 1 (QA, opus) rodada 6: **REJEITADO** — 0 críticos, 1 ALTO (ALTO-001 tests), 3 MEDIO (MED-001 tests, MED-002/MED-003 documentation), 3 BAIXO.
- [T9] **ALTO-001 — MED-001 da rodada 5 NÃO foi corrigido; o wrapper publicado tem exit code CONSTANTE 1.** O `unittest` escreve o placar (`Ran N tests`, `OK`/`FAILED`) em **stderr**, e o stdout do `bench run-tests` sai **vazio** (medido: 0 linhas em stdout, 2 em stderr). Como o pipe só carrega stdout, o `grep -qE '^OK'` recebe fluxo vazio e **nunca casa** → sai 1 sempre, verde ou vermelho. O `tee /dev/stderr` é no-op. **O insight do QA**: a medição do executor foi feita só com a suíte vermelha — "o único lado em que 'funciona' e 'está travado' são indistinguíveis". Quando a T2 versionar os DocTypes e a suíte ficar verde, a invocação continuará dizendo exit 1 e o reflexo será remover o embrulho, voltando ao exit-0-sempre. Correção medida pelo QA: `2>&1 | tee /dev/stderr | grep -qE '^OK($| \()'` → exit 0 no verde, 1 no vermelho.
- [T9] **MED-001 — o piso de amostra está ausente das DUAS invocações publicadas no próprio portão** (`portao_orfaos.py:44` e `:55`); só o bloco USO do compose o inclui. Como a variável é opt-in, quem copiar do arquivo do portão perde a defesa recém-adicionada. O QA citou o princípio do próprio cabeçalho do compose que isso viola: "a promessa passa a ser propriedade do arquivo, nao disciplina de quem usa".
- [T9] **MED-003 — a §7.A.20 diz "Cinco variações" numa tabela de SEIS** (o QA auditou as 6 e todas são reais e corretas). Palavras dele: "o erro de não varrer o vizinho ocorrendo dentro do parágrafo sobre não varrer o vizinho". E a conclusão ("mudar a classe fechou o problema") está superdeclarada: **a própria rodada 6 produziu a sétima variação** — o embrulho corrigido no compose sem verificar o lado que importa (ALTO-001), e o piso adicionado ao compose sem varrer as invocações do portão (MED-001).
- [T9] **BAIXO-003 resolveu o mistério do resíduo.** As duas afirmações eram verdadeiras e ambas parciais: o que sobrou não era DocType nem tabela — eram **2 registros em `tabDeleted Document`** (4242 bytes de JSON arquivado cada), porque `frappe.delete_doc(force=True)` **arquiva antes de remover**. Os 3 checks da confirmação do QA na rodada 5 (`DOCTYPE_EXISTE`, `TABELA_EXISTE`, `CUSTOM_1_RESTANTES`) eram todos verdadeiros e **nenhum cobria o arquivo**. Dois registros do mesmo nome = dois ciclos plantar/apagar, então "removi e confirmei" e "encontrei e removi" coexistem. O QA deu a receita completa de limpeza (13 tabelas a varrer por nome) e a lição: **a varredura tem de ser a ÚLTIMA ação da rodada** — foi por ser intermediária que a confirmação envelheceu.
- [T9] Correção factual do QA sobre o orquestrador: o "uptime 6 dias" **não vale para todos os serviços** — db/frontend/queues/scheduler/websocket estão há 6 dias, mas o **backend está há 23h** (`StartedAt=2026-07-29T00:00:57Z`, `RestartCount=0`, sem recriação). Nada liga esse start ao trabalho da T9; a fonte não foi identificada. Registro para não repetir "6 dias" como se valesse para todos.
- [T9] BAIXO-001: o bootstrap foi protegido mas o **corpo não** — `try/finally` sem `except` (linhas 162-257); `PORTAO_ESPERADO_ALVOS_MIN=setecentos` → `ValueError` → **exit 1**, o código reservado ao VERMELHO. Mesma família do BAIXO-001 anterior, deslocada do bootstrap para o corpo.
- [T9] BAIXO-002: as frases "não há mais mapa publicado em lugar nenhum" (`T9.md:1188`) e "não há mapa publicado aqui de propósito" (`:613`) são **literalmente falsas** — desmentidas por blocos 2 e 10 linhas abaixo, respectivamente.
- [T9] QA confirmou: as 4 invalidações do portão funcionam (incluindo o cenário do interno renomeado → exit 2, não 1); portão determinístico em 8 execuções; AC-3 e AC-4 reafirmados; anti-gaming limpo (a única remoção é o mapa trocado, que era a correção pedida); ambiente deixado limpo e **provado** com varredura por nome em 13 tabelas.
- [T9] attempt_count=6 → **LIMITE ESGOTADO**. Escalado ao usuário.
- [run] rule_candidates: 6 sinais persistidos (qa=1, staff=5, orquestrador=0)

### T9 — retry classification (tentativa 7, concedida pelo usuário — ESCOPO CORTADO)
- attempt: 7 (quarta extensão da T9; usuário optou por "7ª rodada só nos 2 funcionais, resto vira débito")
- problemas_por_categoria: { tests: 2 (ALTO-001, MED-001) — ambos com consequência funcional }
- **CORTE DELIBERADO DE ESCOPO**: MED-002, MED-003, BAIXO-002 e BAIXO-003 (imprecisão de prosa, §7.A.20, mapas, referências de seção, receita de limpeza) **não** entram nesta rodada — vão para a §2 do `run-report.md` como débito. BAIXO-001 (exceção no corpo do portão) também vira débito.
- Razão do corte: as rodadas 5 e 6 foram gastas corrigindo documentação sobre documentação, e cada uma encontrou imprecisão nova na anterior — inclusive a §7.A.20, escrita para fechar o padrão, contendo o próprio padrão ("Cinco variações" numa tabela de seis). Rendimento decrescente com risco de loop. Só o que tem consequência funcional é corrigido.
- requires_qa_revalidation: true
- [T9] bloqueio revertido: T9 → Em Progresso (parcial)
- [T9] verificação independente do orquestrador (rodada 7) — wrapper testado nos DOIS lados com placar emulado em stderr: forma ANTIGA (sem `2>&1`) → vermelho exit 1, **verde exit 1** (travamento confirmado); forma CORRIGIDA (`2>&1 | tee /dev/stderr | grep -qE '^OK($| \()'`) → vermelho exit 1, **verde exit 0**, verde-com-skip (`OK (skipped=1)`) exit 0. O defeito era real e a correção discrimina. `PORTAO_ESPERADO_ALVOS_MIN` confirmada OBRIGATORIA no código (`portao_orfaos.py:154-157`) e presente nas duas invocações do docstring (`:45`, `:56`). Produção intacta.
- [T9] Gate 1 (QA, opus) rodada 7: **REJEITADO** — 2 ALTOS, ambos **consequência das correções desta rodada**, não débito pré-existente.
- [T9] **ALTO-001 — a correção do wrapper abriu o falso-verde que a versão travada não podia ter.** Ao ganhar a capacidade de reportar sucesso, o predicado `grep -qE '^OK($| \()'` passou a casar QUALQUER linha do fluxo mesclado por `2>&1`, sem âncora no placar. Dois escapes medidos: (1) **suíte com ZERO testes** — no Python do contêiner (3.11.6) um `TestSuite()` vazio imprime `Ran 0 tests` + `OK`, `wasSuccessful()==True`, e o wrapper sai **exit 0**: é verde por não ter rodado, exatamente a classe que motivou tornar `PORTAO_ESPERADO_ALVOS_MIN` obrigatória **duas linhas acima no mesmo bloco**; (2) **linha espúria** — suíte VERMELHA com qualquer linha isolada `OK` no fluxo (agora mesclado pelo próprio `2>&1` que a correção introduziu) sai **exit 0** com `FAILED (failures=9, errors=97)` no placar. As afirmações publicadas (`compose:143` e §7.A.19: "exit 0 so quando a suite passa") são falsas nos dois casos.
- [T9] Atenção de método do QA: ele mediu no Python do **contêiner** (3.11.6, onde o escape é real) depois de medir no host (3.12, que imprime `NO TESTS RAN` e sai 1). O ambiente que importa é o do contêiner.
- [T9] **ALTO-002 — tornar o piso obrigatório universalizou dois caminhos que antes eram acidentais.** (a) `PORTAO_ESPERADO_ALVOS_MIN=setecentos` → `ValueError` em `:256` → **exit 1**, o código que o próprio arquivo reserva ao VERMELHO, sem imprimir `PORTAO_VEREDITO`; as linhas 163-173 justificam os imports tardios exatamente para impedir isso — a defesa foi construída para um caminho e reintroduzida em outro pelo parse não protegido. (b) `=0` satisfaz a obrigatoriedade (`not "0"` é False) e **desliga o piso** (`len(alvos) < 0` nunca é verdade) — opt-out por valor. `PORTAO_ESPERADO_PULADOS` tem o mesmo defeito, com o agravante de só falhar **depois** da varredura (~12 s).
- [T9] QA confirmou CORRIGIDOS: o wrapper propaga nos dois lados (verde 0 / `OK (skipped=1)` 0 / `OK (expected failures=1)` 0 / vermelho 1 / bench abortando antes do placar 1 — fail-safe); o piso obrigatório com mensagem acionável e valor 700 defensável (739/741, folga ~5%; o modo de falha que o originou devolve 3-16 alvos). Portão reprovado nos 3 estados + 4 variantes de invalidação, com **precedência correta** (invalidação vence o VERMELHO). Limpeza aplicando a lição da rodada 6 (`VARREDURA_POR_NOME_TOTAL: 0`, varredura como ÚLTIMA ação).
- [T9] QA descartou por medição 4 regressões suspeitas, para o débito não herdar suspeita: placar múltiplo é impossível na forma publicada (`run_all_tests` usa um único `TextTestRunner` para `--app`); precedência invalidação×vermelho correta; `tee /dev/stderr` roda no shell do host; `frappe.destroy()` executa mesmo no traceback.
- [T9] **Observação do QA que nomeia o padrão**: "se aquela tabela [§7.A.20] for atualizada algum dia, o ALTO-001 desta rodada é a **oitava variação** do mesmo padrão — a defesa foi para o portão e não foi varrida para o comando irmão publicado duas linhas acima".
- [T9] red_flags: `green_signal_without_execution`, `assertion_not_anchored_to_scoreboard`.
- [T9] attempt_count=7 → **LIMITE ESGOTADO**. Escalado ao usuário.

### T9 — retry classification (tentativa 8, concedida pelo usuário)
- attempt: 8 (quinta extensão da T9; usuário optou por "ancorar o predicado no placar", mantendo o wrapper)
- problemas_por_categoria: { tests: 1 (ALTO-001), error_handling: 1 (ALTO-002) }
- overrides_ativos: [tocou_area_critica: true, task_risk: high]
- requires_qa_revalidation: true
- escopo: ALTO-001 (ancorar o predicado no placar: `^Ran [1-9]` E exatamente um `^OK` E nenhum `FAILED`/`NO TESTS RAN`) + ALTO-002 (proteger o parse dos contratos inteiros → RODADA_INVALIDA, e exigir piso > 0). Os 5 débitos da rodada 6 seguem fora de escopo; BAIXO-001 desta rodada (amostras sem o campo `piso em vigor`) entra no débito.
- nota do orquestrador: o usuário optou por manter o wrapper em vez de removê-lo. A alternativa apresentada era publicar o comando cru + aviso (menos superfície); a escolha foi fechar os escapes conhecidos com predicado ancorado. Registrado para rastreabilidade da decisão.
- [T9] bloqueio revertido: T9 → Em Progresso (parcial)
- [T9] verificação independente do orquestrador (rodada 8) — `deploy/scripts/veredito_suite.sh` testado nos 6 casos críticos com placar emulado em stderr: verde legítimo → 0; vermelho → 1; **zero testes (`Ran 0 tests` + `OK`) → 1**; **linha `OK` espúria com placar `FAILED` → 1**; `OK (skipped=1)` → 0; fluxo sem placar → 1. Os dois escapes que a rodada 7 abriu estão fechados. O wrapper virou arquivo executável, eliminando a duplicação em 3 lugares que gerou os dois defeitos.
- [T9] Gate 1 (QA, opus) rodada 8: **REJEITADO** — 0 críticos, 0 altos, 1 MEDIO (MED-001 documentation), 3 BAIXO.
- [T9] **Os 2 ALTOS da rodada 7 estão CORRIGIDOS**, verificados pelo QA em **20 casos sem nenhum exit 0 indevido**: verde→0; vermelho→1; **zero testes→1**; **linha `OK` espúria→1**; `OK (skipped=1)`→0; `OK (expected failures=2)`→0; `Ran 1 test` singular→0; sem placar→1; fluxo vazio→1; stdin fechado→1; `NO TESTS RAN`→1; `NO TESTS RAN (skipped=3)`→1; dois `OK`→1; `OK tudo certo`→1; `OKAY`→1; ` OK` indentado→1; `Ran` sem veredito→1; `OK` sem `Ran`→1; `FAILED` sem `Ran`→1; binário puro→1.
- [T9] ALTO-002 fechado: os 5 casos de contrato + 2 extras dão **exit 2 com linha de veredito**, e a falha rápida está confirmada **por cronômetro** — 0 s contra 11 s da varredura completa. A inversão que a rodada 7 abriu (código de VERMELHO para erro de contrato) está fechada.
- [T9] **MED-001 — a §5.1 e o checklist §8 não registram `deploy/scripts/veredito_suite.sh`**: a task cria 3 arquivos e declara 2. Não é artefato oculto (está documentado em §7.A.19 com justificativa), mas a §5.1 é a lista que a **Camada 0 de qualquer gate futuro** lê para cruzar declarado×entregue — sem linha lá, o script fica sem task de origem rastreável. O QA nomeou como o mesmo defeito de classe da §7.A.20: um fato entrou em §7.A.19 e os dois vizinhos que o registram não foram varridos.
- [T9] Baixos: BAIXO-001 (o cabeçalho justifica a promoção citando "arquivo temporário" que a implementação **não usa** — `grep mktemp|/tmp|trap` vazio; resíduo de versão descartada); BAIXO-002 (o `-a` do primeiro grep não foi replicado no grep que monta o motivo — com byte inválido o VERMELHO sai **sem motivo** e vaza `binary file matches` no stderr; o exit code está correto, o dano é de diagnóstico); BAIXO-003 (a condição (c) ancora `^FAILED` em início de linha — um prefixo escapa; o QA registrou como baixo porque **não conseguiu construir falso-verde plausível**: exigiria `^OK` limpo E `FAILED` escondido simultaneamente).
- [T9] **Achado de método do QA na limpeza**: a receita completa **não cobria `tabComment`** — a primeira varredura (683 tabelas, 5 nomes) encontrou 1 resíduo lá (`reference_doctype`/`reference_name`/`content`). Purgou e refez a varredura como última ação: `VARREDURA_ACHADOS: NENHUM`. Registrado como nota de método para quem repetir a receita, não como defeito do artefato.
- [T9] QA confirmou as colunas reais citadas pelo executor via `DESC`: `tabDeleted Document` tem `deleted_name`/`deleted_doctype`; `tabVersion` tem `docname`/`ref_doctype`.
- [T9] Falso-VERMELHO conservador identificado e **não** classificado como defeito (o cabeçalho declara "na dúvida, VERMELHO"): fluxo com CRLF → 1, porque `^OK($| \()` não casa `OK\r`. Não afeta a forma publicada (`exec -T`, sem TTY); afetaria quem trocasse por `-t`.
- [T9] Duplicação do predicado **de fato eliminada no que é executável**: existe em 1 lugar e é referenciado de 1 lugar; o compose explica sem repetir os regexes. Sincronização residual só em prosa (T9.md:1248).
- [T9] ADR-0005 verificada pelo QA nos 2 scripts: grep por `password|senha|secret|token|api_key` → **nenhuma ocorrência**.
- [T9] attempt_count=8 → **LIMITE ESGOTADO**. Escalado ao usuário.
- [run] rule_candidates: 7 sinais persistidos (qa=2, staff=5, orquestrador=0)
- [T9] **DECISÃO DO USUÁRIO: T9 aceita como PARCIAL com débito registrado.** Registro preciso do estado dos gates no momento da aceitação: Gate 1 (QA) rodada 8 = **REJEITADO** (0 críticos, 0 altos, 1 médio de escrituração, 3 baixos); Gate 2 (Tech Review) última execução = rodada 4, **REJEITADO**. **Nenhum gate aprovou a task.** A aceitação é decisão explícita do usuário após 8 rodadas, com os 10 achados abertos convertidos em débito na §2 do `run-report.md`. Isto NÃO é aprovação de gate e está registrado como tal para não contaminar o eval de pipeline.
- [T9] staged: deploy/compose/docker-compose.stack-nova.yaml, deploy/scripts/portao_orfaos.py, deploy/scripts/veredito_suite.sh, docs/specs/features/saas-multi-empresa/v1/tasks/T9.md

## agent-spec-debt-resolution — 2026-07-29

- Débitos coletados: 12 (3 `MEDIO` + 9 `BAIXO`)
- Agente especialista: `__default__` (`.claude/agents/` contém apenas os 3 agentes reservados aos gates — sem candidato executor; resolvido sem `AskUserQuestion` porque a opção única tornaria a pergunta degenerada)
- Recomendados pela LLM: 9 (D-001, D-002, D-003, D-004, D-005, D-006, D-008, D-010, D-011)
- Perfumaria: 3 (D-007, D-009, D-012 — todos classificados assim por **custo**, não por falta de valor)
- Selecionados pelo usuário: **12 de 12** (incluindo os 3 de perfumaria)
- **Granularidade: agrupamento por arquivo, a pedido explícito do usuário.** O padrão da skill é 1 task por débito; os 12 débitos estavam concentrados em 4 arquivos (7 no `T9.md`), o que tornaria as 12 tasks integralmente sequenciais e faria sete delas disputar o mesmo arquivo. Agrupadas em 4 tasks com arquivos disjuntos → lote paralelo de 4 (`MAX_PARALLEL=4` cabe exatamente).
- **Desvio deliberado do `gates: [qa]` default da skill**: T2 (`veredito_suite.sh`) e T3 (`portao_orfaos.py`) recebem `[qa, tech_review]`. Nenhum path bate com as categorias canônicas de Critical Paths, mas ambas mudam comportamento de **gate de segurança** — D-007 tem `risco_regressao: medio` e reestrutura o consumo de stdin num predicado de veredito; D-010 muda o significado do `exit 1` de um script que bloqueia `bench migrate` em produção. Aplicar `[qa]` aqui seria seguir a letra da regra contra a intenção dela.
- **Risco reintroduzido pelo agrupamento, registrado**: D-005, D-007 e D-012 têm lados gêmeos em arquivos de tasks distintas (script × prosa). O agrupamento por arquivo resolve o paralelismo e reintroduz o padrão dominante de defeito da v1 (*corrigir num lugar e não varrer o vizinho*, 8 ocorrências). Guardrail cruzado em `scope.md` §3.6, com T2 e T4 apontando uma para a outra.
- Output: `docs/specs/features/saas-multi-empresa/v2-debits/`
- Comando para executar: `/agent-spec-minispec-run-tasks docs/specs/features/saas-multi-empresa/v2-debits/task_plan.md`

## Run 2026-07-30 — reabertura da T1 + T4

- [run] executor resolvido: `__default__` (origem: descoberta interativa degenerada). `.claude/agents/` contém apenas os 3 agentes reservados aos gates (`agent-spec-qa-validator`, `agent-spec-staff-architecture-review`, `agent-spec-qa-test-generator`) — zero candidatos a executor. `AskUserQuestion` não disparada para este eixo porque a opção única a tornaria degenerada; mesmo critério já registrado no run da `agent-spec-debt-resolution`.
- [run] executor_discipline injetado (fonte: references/executor-discipline.md)
- [run] cleanup idempotente: `_run/tmp/T1.md` tem < 24h → preservado. `_run/tmp/` já consta no `.gitignore:33`.
- [run] **resume pós-interrupção detectado** — sinais: (a) `_run/tmp/T1.md` fresco de task não-`Concluído`; (b) diff da T1 vivo no working tree como intent-to-add (`app-sync/.../doctype/conjunto/{__init__.py,conjunto.json,conjunto.py}` + `tasks/T1.md`), nunca staged porque a task não passou no Gate 2.
- [run] **DECISÃO DO USUÁRIO (AskUserQuestion, 2026-07-30)**: (1) T1 → **reabrir com os dados novos**, retomando a partir do código existente (não reexecutar do zero); contador zerado. (2) Escopo do run → **só T4**, parando depois dela para avaliação.
- [run] paralelismo T1×T4 **descartado novamente** — mesmo motivo do run anterior: paths e símbolos disjuntos, mas ambas operam sobre o mesmo banco de produção (`frontend`); independência de recurso não demonstrável. Fallback determinístico para sequencial: T1 → T4. Reforço adicional nesta rodada: a T1 entra em **loop de correção** (executor edita o working tree) enquanto o QA da T4 rodaria suíte — o guard executor×QA da rule "Execução Paralela de Tasks" veda a concorrência de forma independente.
- [run] **divergência de memória detectada e corrigida**: `_run/tmp/T1.md` registrava `attempt_count: 3` (estado da 3ª tentativa, arquivo nunca atualizado após as extensões 4–8). O estado real, reconstruído da telemetria, é tentativa 8: Gate 1 **APROVADO** (6/6, zero problemas), Gate 2 **REJEITADO** (1 CRÍTICO architecture + 1 ALTO technical_requirement + 1 BAIXO project_pattern). Memória lazy reescrita com o estado real antes do despacho.

### T1 — reabertura (tentativa 1 do ciclo novo; 9 em termos absolutos)
- [T1] bloqueio revertido: T1 → Em Progresso. attempt_count zerado por decisão do usuário.
- [T1] base_sha=5a4e5197ee1d4e1cd262b6886c054ddf9a0da9b2 (HEAD inalterado desde o bloqueio — o diff da task continua íntegro e isolável)
- [T1] executor: **opus** (declarado no frontmatter) · gates: [qa, tech_review] (declarado)
- [T1] ADRs injetadas no executor: ADR-0002, ADR-0003, ADR-0006 (fonte: task §7 "ADRs Aplicáveis nesta Task")
- [T1] **o que mudou desde o bloqueio**: a causa estrutural apontada pelo TR ("toda a base empírica da task exclui `bench migrate`") deixou de valer. A T9 entregou o ambiente destrutível e o `bench migrate` foi observado — `remove_orphan_doctypes` apaga a definição e preserva tabela/linhas; o gatilho real foi refinado 4× (o predicado é `get_controller()` levantar `ImportError`, por 4 caminhos). O executor recebe esses dados como insumo primário da correção.
- [T1] Gate 1 (QA, opus) tentativa 1 do ciclo reaberto: **APROVADO_COM_OBSERVACOES**. criterios 7/7, 0 críticos/altos/médios, 3 baixos (todos `documentation`). `executou_testes: true` (SUITE_COMPLETA no site `verificacao`), `tocou_area_critica: true`, `security_flags: []`.
- [T1] **P1 (CRÍTICO herdado) FECHADO** — a ordem antiga `converger → exportar → revisar → migrar` foi varrida; as 5 ocorrências restantes a nomeiam para declará-la incorreta. A subseção "Ordem operacional que a T2 herda" (T1.md:371) é fonte única real — o QA procurou e não achou segunda cópia da sequência.
- [T1] **P2 (ALTO herdado) FECHADO** — o passo dos 13 controllers virou A4 na sequência, com derivação literal `classname = doctype.replace(" ","").replace("-","")`, tabela nominal dos 14, reforço em 4 pontos concordantes e cobertura no `CT-014` novo.
- [T1] **Anti-gaming no AC-5 (acrescentado pelo próprio executor) — julgado LEGÍTIMO pelo QA**: o AC-5 **estreita** o P1 em vez de afrouxar (acrescenta exigência de observação de `bench migrate` + saída literal por passo, que o P1 não pedia). O qualificador "durante uma migração" é tecnicamente exato — `remove_orphan_doctypes` só dispara em `post_schema_updates` — e a janela A2→A4 está declarada com 3 razões verificáveis, não escondida.
- [T1] **2 verificações independentes do QA (não é só leitura)**: (a) reexecutou `portao_orfaos.py` no `verificacao` e obteve saída **byte a byte idêntica** à colada nos passos 1 e 10 (`PORTAO_OK: 738`, `739 alvos`, `VERDE`, exit 0) — confirma que as saídas da Campanha 2 são genuínas E que a limpeza do experimento aconteceu; (b) reexecutou a suíte: `Ran 169 tests` / `FAILED (failures=9, errors=161, skipped=1)`, idêntico à linha de base da T9 §7.A.10 capturada ANTES desta task. Sem regressão — o vermelho é ausência da estrutura que a T2 entrega.
- [T1] md5 de `conjunto.json` conferido pelo QA: `98bafa06940d0db0fa28c0f7b3424e51`, inalterado. Delta desta rodada 100% em `T1.md` (737 → 1087 linhas).
- [T1] ADR-0006 **respeitada e não contornada**: impedimento declarado explicitamente em `T1.md:908`; nenhuma execução de suíte tocou o `frontend`; consultas a produção todas declaradas somente-leitura; `export_to_files` nos 13 demonstrado não-destrutivo (`DoesNotExistError` antes de qualquer escrita, `git status app-sync/` inalterado).
- [T1] **Limitação de ambiente registrada pelo QA (insumo para o Gate 2, não defeito da task)**: não existe hoje ambiente onde a suíte feche verde. O `frontend` fecha 169/169 mas está proibido pela ADR-0006; o `verificacao` é permitido mas não tem os 13 DocTypes, que são o entregável da T2. Enquanto a T2 não fechar, "verde" não é estado alcançável e a prova de não-regressão só pode ser identidade de placar com a linha de base.
- [T1] Baixos (débito anotado): BAIXO-001 (a conta `169 testes / 170 resultados` é afirmada sem ser derivada, e a soma descarta o `skipped=1`); BAIXO-002 (a segunda metade do fato F4 — `after_migrate` não roda — é dedução de fluxo dentro de lista que se apresenta como observada); BAIXO-003 (`CT-014` especificado depois do item (g) em vez de dentro do bloco (f) que anuncia os três casos — ocorrência do padrão dominante do run).
- [T1] `rule_candidates_emitidos`: nenhum.
- [T1] Gate 2 (Tech Review, opus) tentativa 1 do ciclo reaberto: **REJEITADO** — 1 CRITICO (P1 architecture), 1 ALTO (P2 technical_requirement), 1 MEDIO (P3 technical_requirement), 2 BAIXO (P4, P5 code_quality).
- [T1] TR consultou: ADR-0002, ADR-0003, ADR-0006
- [T1] **P2 da rodada anterior (ALTO) CONFIRMADO FECHADO** pelo TR: passo A4 existe, regra de derivação literal da classe, tabela nominal dos 14, `CT-014` especificado. Base empírica julgada genuína — campanha 2 com saída literal por passo, variantes A/B/C são desenho experimental correto (B é a contraprova que o P2 exigia, C a positiva). Fonte única da sequência **existe** (TR procurou e não achou segunda cópia). Anti-gaming: sem sinal — diff 100% documental, nenhum teste removido/enfraquecido, `conjunto.json` inalterado.
- [T1] **AC-5 autoral: TR concorda com o QA — LEGÍTIMO.** Acrescenta exigências que o P1 não pedia; o qualificador "durante uma migração" não abre exceção porque o invariante entregue é mais estrito que o AC. Citação do TR: "o problema não é o AC ter sido escrito frouxo — é o invariante, mesmo o estrito, cobrir só uma das duas metades do predicado".
- [T1] **P1 CRÍTICO — não foi fechado, foi DESLOCADO.** A correção fechou a metade de **banco** (a convergência saiu da produção para a cópia restaurada) e não varreu a metade de **arquivo**. O cabeçalho do bloco A (`T1.md:381`) afirma "A produção não é escrita em nenhum destes passos" — verdadeiro para o banco, **falso para a árvore de código**.
- [T1] **Fato confirmado independentemente pelo orquestrador antes de despachar a correção**: `docker-compose.override.yml:11,29,42,55` monta `/opt/frappe/app-sync/locacao_automation` → `apps/locacao_automation` **sem `:ro`** nos 4 serviços de produção (backend, scheduler, filas). `deploy/compose/docker-compose.stack-nova.yaml:261,345,363` usa `${APP_SRC:-../../app-sync/locacao_automation}` — **o mesmo diretório por default**. O documento cita o mount em `T1.md:719` e nunca conecta os dois fatos.
- [T1] **Consequência de ordem**: entre o A3 (exporta os 13 `.json`, com `create_init=True`) e o A4 (escreve os 13 `.py` à mão), a produção passa a ter 13 definições de DocType em disco sem controller importável. Um `bench migrate` de produção nessa janela importa os `.json` (que declaram `module` e omitem `custom`, gravando `custom=0`), `load_doctype_module` levanta `ImportError`, o migrate aborta com exit 1 deixando convergência parcial commitada (`sync.py:115` comita por arquivo) — ou, na variante com classe mal nomeada, completa com exit 0 e `remove_orphan_doctypes` apaga silenciosamente. Mesmo desfecho do P1 original, por outro vetor. Tensiona a ADR-0006: o fluxo de verificação escreve no artefato que a operação carrega.
- [T1] **P2 ALTO (novo)**: o critério de aceite do A7 ("exit 0 **e** ausência de `Orphaned DocType(s) found`") não foi varrido para o **B4**, que é o migrate da **produção** e não tem critério de aceite algum. O B5 não fecha a lacuna — após a deleção o cadastro sai de `tabDocType` e portanto do seletor `{"custom": 0}` do portão, que volta VERDE. É o padrão de defeito dominante do run aplicado ao passo de maior consequência do contrato.
- [T1] **P3 MEDIO (novo)**: o bloco A não tem caminho executável hoje. Além do A1 (já declarado como pendente), o **A3 também está bloqueado** — a stack nova monta o app `:ro` (`stack-nova.yaml:261,345,363`), e `export_to_files` grava em `get_module_path`. A alternativa registrada ("converger em transação + rollback") é infalsificável no único ambiente permitido. Pendência delegada à T2 sem critério de fechamento, e a T2 é task de patch/versionamento, não de infra.
- [T1] Baixos do TR (débito anotado): P4 (a razão (iii) da janela A2→A4 afirma que o portão do A6 sai VERMELHO se a janela for atravessada — sai **VERDE**, contradito pelo fato F5 do próprio documento); P5 (A6 e A8 não são executáveis sem reinterpretação — faltam `PORTAO_ESPERADO_ALVOS_MIN`/`PORTAO_ESPERADO_PULADOS` e o critério de aceite da suíte).
- [T1] `rule_candidates_emitidos`: nenhum (nem QA nem TR).

### T1 — retry classification (tentativa 1 do ciclo reaberto)
- attempt: 1
- problemas_por_categoria: { architecture: 1 (CRITICO), technical_requirement: 2 (1 ALTO + 1 MEDIO), code_quality: 2 (BAIXO) }
- overrides_ativos: [tocou_area_critica: true, task_risk: medium, qa_security_flags: [], diff_stat_changed: false]
- requires_qa_revalidation: **true**
- decisao: próxima rodada passa por Gate 1 (QA) e depois Gate 2 (Tech Review)
- justificativa: "P1 é `architecture` e P2/P3 são `technical_requirement` — as três em `revalidation_required`; override `tocou_area_critica: true` forçaria `true` de qualquer forma"
- escopo da correção: P1 (isolar a árvore de código do bloco A; estender invariante e corolário à metade de arquivo), P2 (replicar o critério do A7 no B4 e dar asserção positiva de presença dos 14 no B5), P3 (marcar A3 como bloqueado junto do A1; definir critério de fechamento observável e destinatário coerente). P4/P5 vão como observações opcionais.

### T1 — rodada 2 do ciclo reaberto (correção dos 3 bloqueantes + 5 baixos)
- [T1] executor: opus (declarado; sem auto-escalate — a regra só escala `sonnet`). Agente da rodada 1 retomado por `SendMessage` em vez de novo despacho: preserva o contexto do documento que ele acabou de escrever, e a Disciplina + bloco de ADRs já estavam no contexto dele.
- [T1] **verificação independente do orquestrador antes do despacho da correção**: o fato central do P1 confirmado com `grep` — `docker-compose.override.yml:11,29,42,55` monta `/opt/frappe/app-sync/locacao_automation` **sem `:ro`** nos 4 serviços de produção; `stack-nova.yaml:261,345,363` usa `${APP_SRC:-../../app-sync/locacao_automation}`, o mesmo diretório por default. O achado do TR é factual.
- [T1] executor declara: P1 atacado no predicado inteiro (invariante com as duas metades + **dois corolários** banco/arquivo; passo **A0** de isolamento da árvore; B2 como único ponto de chegada dos 14 diretórios à produção, com atomicidade do par `.json`+`.py` como critério falseável). P2: B4 passou a exigir `exit 0` **E** `grep -c 'Orphaned DocType(s) found' == 0`; B5 ganhou asserção nominal dos 14; remediação virou dois ramos. P3: bloqueio do A3 **medido** (`touch` no contêiner de verificação → `Read-only file system`, exit 1); tabela com coluna de status (A1/A3 bloqueados, A0 parcial); 3 critérios de fechamento observáveis; alternativa transacional registrada como **terceiro consumidor do mesmo pré-requisito**.
- [T1] os 5 baixos também foram tratados (razão (iii) → VERDE com o mecanismo real de detecção; A6/A8 com invocação literal e critério de aceite; conta 169/170 derivada com `_ErrorHolder` e `skipped=1`; F4 partido em metade medida e metade derivada; `CT-014` movido para dentro do bloco (f)).
- [T1] diff da rodada: `T1.md` 1087 → 1169 linhas, **100% documental**. Piloto byte-idêntico (md5 `98bafa06940d0db0fa28c0f7b3424e51` reconferido pelo orquestrador). `deploy/` e compose **intocados** — o executor registrou a exigência de mount gravável no contrato em vez de editar a infra, conforme instruído.
- [T1] **pendência que o executor escalou**: A1 e A3 sem caminho executável; fechá-los exige editar `deploy/compose/docker-compose.stack-nova.yaml` (mount `:ro` → gravável). Recomendação registrada no documento (task de INFRA dependente da T9 e anterior à T2); **task NÃO criada** — está fora do escopo do executor.
- [T1] **Gate 1 (QA) da rodada 2 interrompido sem completar** — o processo do Claude Code encerrou com o agente em background; nenhum arquivo de output foi produzido. O QA não escreve no working tree, logo não há estado parcial a limpar: `T1.md` em 1169 linhas e piloto com md5 `98bafa06940d0db0fa28c0f7b3424e51` conferidos íntegros após a interrupção. **Não conta como tentativa** — nenhum veredito foi emitido. Re-despachado do zero (agente novo; o transcript do anterior é de sessão encerrada).
