# T8 — Resolver D-008: filtro RN-02 em fonte única

## 1. Identificação

- **ID**: T8
- **Nome da Task**: Resolver D-008 — filtro RN-02 ("boleto em aberto") duplicado entre dois módulos
- **model**: sonnet
- **risk**: medium  # toca `rotina_pagamentos.py`, código de produção do fluxo de cobrança
- **gates**: [qa, tech_review]  # path em Critical Path (payments)
- **Status**: Concluído
- **Fase**: 3 (apuração de boletos — sequencial; **executar por último da fase**)
- **Dependências**: Nenhuma (colide em arquivo com T7 e T9 — ver §3.2)
- **Símbolos públicos criados**: constante compartilhada do filtro RN-02 (nome a definir na implementação)
- **Símbolos consumidos de outras tasks**: N/A
- **Critério de Conclusão**: filtro definido uma única vez e importado pelos dois módulos; semântica idêntica; suíte passa sem regressão.
- **Feature**: integracao-bancaria-configuravel
- **Versão**: v2-debits
- **Parent task**: T13 (de v1)
- **Débito origem**: D-008 (project_pattern, BAIXO) — classificação: `perfumaria` (incluído por decisão do usuário)
- **Source**: agent-spec-debt-resolution

---

## 2. Objetivo da Task

Resolver o débito técnico D-008 promovendo o filtro RN-02 a **fonte única** importada pelos dois módulos, eliminando o risco de drift silencioso da regra financeira — **sem alterar a semântica do filtro**.

---

## 3. Arquivos Impactados

### 3.1 Arquivos a Criar

_Nenhum_ (a constante deve viver num dos módulos existentes ou num módulo comum já existente — evite criar arquivo novo só para uma constante; decida como sênior e justifique).

### 3.2 Arquivos a Modificar

| Arquivo | Descrição |
|---------|-----------|
| `app-sync/locacao_automation/locacao_automation/integracao_bancaria_api/boletos_abertos.py` | Passar a consumir a constante compartilhada em vez de definir `FILTROS_BOLETO_ABERTO` localmente (linha ~36) |
| `app-sync/locacao_automation/locacao_automation/cobranca_sicoob/rotina_pagamentos.py` | Extrair o `filtros_elegiveis` local (linha ~34) para a constante compartilhada e consumi-la nos 2 pontos de uso (`filters` ~linha 59 e o retorno do dry_run ~linha 79) |

> ⚠️ **Arquivo compartilhado**: `boletos_abertos.py` também é tocado por **T7** e **T9**. Execute esta task **por último** na Fase 3.
> ⚠️ **Código de produção fora do módulo da feature**: `rotina_pagamentos.py` é a rotina de sincronização de pagamentos em produção.

### 3.3 Arquivos de Referência

- `docs/specs/features/integracao-bancaria-configuravel/v1/_run/run-report.md` — débito original (§2, linha 71).
- `docs/specs/features/integracao-bancaria-configuravel/v1/tasks/T13.md` — task que gerou o débito.

---

## 4. Detalhes de Implementação

### 4.1 Contexto do Débito

**Origem**: task `T13` da `v1` — registrado em `docs/specs/features/integracao-bancaria-configuravel/v1/_run/run-report.md` (linha 71).

**Descrição original**: `FILTROS_BOLETO_ABERTO` (`boletos_abertos.py:36`) é cópia literal de `filtros_elegiveis` (`rotina_pagamentos.py:34`) — uma **regra de negócio financeira** (o que conta como "boleto em aberto": `status_cobranca in ['Pendente','Vencida']`, `boleto_gerado=1`, `nosso_numero` preenchido) materializada em dois módulos. O executor de T13 não pôde importar a fonte de produção porque lá o filtro é **variável local** dentro de uma função (não exportada), o que exigiria tocar produção — fora do escopo daquela task.

**Mitigação existente**: o CT-019 cruza `listar_boletos_abertos` com o dry_run da rotina de produção no mesmo estado de banco e **falha se divergirem** — é a rede de proteção contra o drift enquanto a duplicação existe.

**Categoria**: `project_pattern` (BAIXO)

**Classificação do especialista**: `perfumaria`
**Justificativa**: "Custo: ~15min (criar constante compartilhada + atualizar import em 2 módulos + tocar rotina_pagamentos.py, código de produção do fluxo de cobrança). Risco: baixo mas não nulo — está mitigado hoje por CT-019 comparando com o dry_run real, então o custo de esperar não é alto; mudar o arquivo de produção agora por uma duplicação sem sintoma é troca desfavorável."
**Custo estimado**: ~15min
**Risco de regressão**: `baixo`

> **Incluído por decisão explícita do usuário** (cleanup completo), ciente de que o especialista o classificou como perfumaria.

### 4.2 Correção

- [x] Definir o filtro RN-02 **uma única vez** como constante de módulo, num local que ambos possam importar sem criar ciclo de import. Decida como sênior entre: (a) manter em `boletos_abertos.py` e importar em `rotina_pagamentos.py`; (b) manter em `rotina_pagamentos.py` (onde nasceu) e importar em `boletos_abertos.py`; (c) mover para um módulo comum já existente. **Justifique a escolha** no diff (comentário curto) e evite dependência circular.
- [x] `boletos_abertos.py` passa a consumir a constante compartilhada.
- [x] `rotina_pagamentos.py` passa a consumir a constante nos **dois** pontos de uso (o `filters` da query e o retorno do dry_run).
- [x] Semântica **idêntica**: as chaves e valores do filtro não mudam.

### 4.3 Guardrails de Execução

**DEVE**:

- Comparar os dois dicionários **antes** de unificar e confirmar que são literalmente equivalentes. Se houver qualquer diferença, **pare e reporte** — unificar filtros divergentes mudaria a apuração financeira.
- Verificar que a constante é usada nos **dois** pontos de `rotina_pagamentos.py` (query e retorno do dry_run) — deixar um deles com a cópia antiga recriaria o drift.
- Cuidar de **ciclo de import**: `integracao_bancaria_api` e `cobranca_sicoob` são pacotes distintos; escolha a direção que não crie import circular.
- Rodar a suíte completa antes de retornar a task. **CT-019 é o oráculo central** desta task.

**NÃO DEVE**:

- NÃO alterar as chaves/valores do filtro (`status_cobranca`, `boleto_gerado`, `nosso_numero`) — a semântica deve permanecer bit-a-bit.
- NÃO alterar outra lógica de `rotina_pagamentos.py` (é produção do fluxo de cobrança) — apenas a extração da constante.
- NÃO antecipar **D-007/T7** nem **D-009/T9** no mesmo `boletos_abertos.py`.
- NÃO adicionar testes novos.

---

## 5. Testes

**N/A — task é cleanup técnico**. Não cria testes novos.

O Gate 1 (QA) DEVE executar a **suíte completa** após a modificação:

```
docker compose exec -T backend bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend run-tests --app locacao_automation'
```

- ✅ Os **113 testes** continuam passando — em especial **CT-019**, que compara a apuração com o dry_run de `rotina_pagamentos` no mesmo estado de banco. Se a extração mudou semântica, este teste quebra.
- ❌ Qualquer teste regredindo → task rejeitada.

> Atenção do QA: esta task toca código de produção **fora** do módulo da feature. Verifique que `rotina_pagamentos.py` teve apenas a extração da constante — nenhuma outra mudança de lógica.

---

## 6. Notas / Observações

_Vazio. Tasks de débito não geram débito novo._

---

## 7. Checklist Final

- [x] Correção aplicada exatamente como descrita em §4.2 — sem expansão de escopo
- [x] Diff afeta APENAS `integracao_bancaria_api/boletos_abertos.py` e `cobranca_sicoob/rotina_pagamentos.py`
- [x] Filtro definido **uma única vez** e consumido nos 2 pontos de `rotina_pagamentos.py`
- [x] Semântica do filtro idêntica (chaves e valores inalterados); sem ciclo de import
- [x] Nenhuma antecipação de T7/T9 (mesmo arquivo, tasks próprias)
- [x] Suíte completa passa sem regressão (113 testes), com CT-019 verde
- [x] Gates aprovaram (QA + Tech Review)
- [x] Staged para commit (git add pelo orquestrador)
