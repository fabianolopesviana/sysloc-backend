{
  "name": "@sysloc/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Identidade do backend Sysloc — instância do arcabouço sobre o acesso restrito a `identidade`, política de senha, bloqueio por conta e trilha de tentativas",
  "license": "UNLICENSED",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "test": "tsc --build && tsc -p tsconfig.test.json && vitest run"
  },
  "dependencies": {
    "@sysloc/db": "workspace:*",
    "@sysloc/shared": "workspace:*",
    "better-auth": "1.6.25",
    "drizzle-orm": "0.45.2",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
