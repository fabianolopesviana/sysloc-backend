# T6 — Resolver D-006: remover teste que assere código-fonte

## 1. Identificação

- **ID**: T6
- **Nome da Task**: Resolver D-006 — teste assere estrutura do código-fonte via `inspect.getsource`
- **model**: sonnet
- **risk**: low
- **gates**: [qa]  # toca exclusivamente arquivo de teste — fora de Critical Path de produção
- **Status**: Concluído
- **Fase**: 1 (cleanup isolado — paralelizável)
- **Dependências**: Nenhuma
- **Símbolos públicos criados**: N/A
- **Símbolos consumidos de outras tasks**: N/A
- **Critério de Conclusão**: teste frágil removido; a cobertura comportamental da serialização permanece intacta; suíte passa (113 testes).
- **Feature**: integracao-bancaria-configuravel
- **Versão**: v2-debits
- **Parent task**: T10 (de v1)
- **Débito origem**: D-006 (testability, BAIXO) — classificação: `recomendado_corrigir`
- **Source**: agent-spec-debt-resolution

---

## 2. Objetivo da Task

Resolver o débito técnico D-006 eliminando o teste que assere o **texto do código-fonte**, preservando integralmente a prova comportamental da serialização do contador.

---

## 3. Arquivos Impactados

### 3.1 Arquivos a Criar

_Nenhum._

### 3.2 Arquivos a Modificar

| Arquivo | Descrição |
|---------|-----------|
| `app-sync/locacao_automation/locacao_automation/tests/test_emissao_sequencial.py` | Remover `test_ct014_for_update_presente_no_sql` (assere `inspect.getsource`) |

> Arquivo **disjunto** de todas as outras tasks — esta é uma das duas tasks paralelizáveis (Fase 1).

### 3.3 Arquivos de Referência

- `docs/specs/features/integracao-bancaria-configuravel/v1/_run/run-report.md` — débito original (§2, linha 59).
- `docs/specs/features/integracao-bancaria-configuravel/v1/tasks/T10.md` — task que gerou o débito (CT-014).

---

## 4. Detalhes de Implementação

### 4.1 Contexto do Débito

**Origem**: task `T10` da `v1` — registrado em `docs/specs/features/integracao-bancaria-configuravel/v1/_run/run-report.md` (linha 59). Apontado por **QA e Tech Review** independentemente.

**Descrição original**: `test_ct014_for_update_presente_no_sql` usa `inspect.getsource(sequencial._obter_configuracao_ativa_for_update)` e assere as substrings `'for update'` e `'tabConfiguracao Integracao Bancaria'` no **texto** do código — asserção sobre a implementação, não sobre comportamento observável (smell `testing_internal_structure`). Quebra sob refactor cosmético do SQL (constante, mudança de case/whitespace, query builder) sem que o invariante mude.

**Por que é seguro remover**: o invariante real de RN-03 (serialização do contador sob concorrência) já é provado **empiricamente** por `test_ct014_concorrencia_real_serializa` — 2 threads com conexões de banco reais + `threading.Barrier`, assertando sufixos `000000000011`/`000000000012` e contador final `000000000012`. Se o `FOR UPDATE` for removido, esse teste falha com sequencial duplicado.

**Categoria**: `testability` (BAIXO)

**Classificação do especialista**: `recomendado_corrigir`
**Justificativa**: "Custo: 2min (remover um método de teste). Risco: nenhum — só arquivo de teste, e a serialização real já é provada empiricamente por test_ct014_concorrencia_real_serializa. Valor: elimina teste frágil que quebraria em qualquer refactor cosmético do SQL."
**Custo estimado**: ~2min
**Risco de regressão**: `nenhum`

### 4.2 Correção

- [ ] Remover o método `test_ct014_for_update_presente_no_sql` de `test_emissao_sequencial.py`.
- [ ] Se o import de `inspect` ficar órfão após a remoção, remova-o também (verifique com grep que é o único uso).

> **Alternativa aceitável** (se preferir manter um guard barato do lock): substituir a inspeção de fonte por uma verificação **comportamental** leve. Na dúvida, prefira a remoção — a cobertura já existe.

### 4.3 Guardrails de Execução

**DEVE**:

- Confirmar por leitura que `test_ct014_concorrencia_real_serializa` permanece no arquivo e **intacto** — é ele que passa a ser a única prova de RN-03.
- Remover apenas o método citado (e o import órfão, se aplicável).
- Rodar a suíte completa antes de retornar a task.

**NÃO DEVE**:

- NÃO remover nem enfraquecer nenhum outro teste — em especial `test_ct014_concorrencia_real_serializa`, `test_ct015_*`, `test_ct027_*`, `test_ct024_*`, `test_ct028_*`, `test_ct030_*`.
- NÃO tocar código de produção (`sequencial.py`, `emissao.py`).
- NÃO adicionar testes novos.

---

## 5. Testes

**N/A — task é cleanup técnico**. Não cria testes novos.

O Gate 1 (QA) DEVE executar a **suíte completa** após a modificação:

```
docker compose exec -T backend bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend run-tests --app locacao_automation'
```

- ✅ **113 testes** passando — a contagem cai de 114 para 113 **por design**, pois esta task remove exatamente 1 teste redundante. Isso **não** é regressão.
- ❌ Qualquer OUTRO teste sumindo ou falhando → task rejeitada.

> Atenção do QA: verifique que a redução é de exatamente **1** teste e que o removido é o `test_ct014_for_update_presente_no_sql` — nenhum outro.

---

## 6. Notas / Observações

_Vazio. Tasks de débito não geram débito novo._

---

## 7. Checklist Final

- [ ] Correção aplicada exatamente como descrita em §4.2 — sem expansão de escopo
- [ ] Diff afeta APENAS `tests/test_emissao_sequencial.py`
- [ ] `test_ct014_concorrencia_real_serializa` permanece intacto (única prova de RN-03)
- [ ] Suíte completa passa com **113** testes (redução de exatamente 1, por design)
- [ ] Gate 1 (QA) aprovou
- [ ] Staged para commit (git add pelo orquestrador)
