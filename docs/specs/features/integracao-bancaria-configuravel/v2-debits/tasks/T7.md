# T7 — Resolver D-007: enxugar retorno de `listar_boletos_abertos`

## 1. Identificação

- **ID**: T7
- **Nome da Task**: Resolver D-007 — `listar_boletos_abertos` retorna chave `boletos` sem consumidor
- **model**: sonnet
- **risk**: low
- **gates**: [qa, tech_review]  # path em Critical Path (payments — apuração da carteira)
- **Status**: Concluído
- **Fase**: 3 (apuração de boletos — sequencial)
- **Dependências**: Nenhuma (colide em arquivo com T8 e T9 — ver §3.2)
- **Símbolos públicos criados**: N/A
- **Símbolos consumidos de outras tasks**: N/A
- **Critério de Conclusão**: retorno reduzido ao contrato declarado; nenhum consumidor quebrado; suíte passa sem regressão.
- **Feature**: integracao-bancaria-configuravel
- **Versão**: v2-debits
- **Parent task**: T13 (de v1)
- **Débito origem**: D-007 (speculative_complexity, BAIXO) — classificação: `recomendado_corrigir`
- **Source**: agent-spec-debt-resolution

---

## 2. Objetivo da Task

Resolver o débito técnico D-007 removendo do retorno de `listar_boletos_abertos` a chave e os campos que nenhum consumidor lê, **sem alterar a apuração** (filtro RN-02) nem o contrato efetivamente usado.

---

## 3. Arquivos Impactados

### 3.1 Arquivos a Criar

_Nenhum._

### 3.2 Arquivos a Modificar

| Arquivo | Descrição |
|---------|-----------|
| `app-sync/locacao_automation/locacao_automation/integracao_bancaria_api/boletos_abertos.py` | Remover a chave `boletos` do retorno de `listar_boletos_abertos` e os campos `status_cobranca`/`nosso_numero` do `fields` do `get_all` |

> ⚠️ **Arquivo compartilhado**: `boletos_abertos.py` também é tocado por **T9** (bufferizar páginas) e **T8** (filtro em fonte única). São tasks separadas — não antecipe.

### 3.3 Arquivos de Referência

- `docs/specs/features/integracao-bancaria-configuravel/v1/_run/run-report.md` — débito original (§2, linha 65).
- `docs/specs/features/integracao-bancaria-configuravel/v1/tasks/T13.md` — task que gerou o débito.

---

## 4. Detalhes de Implementação

### 4.1 Contexto do Débito

**Origem**: task `T13` da `v1` — registrado em `docs/specs/features/integracao-bancaria-configuravel/v1/_run/run-report.md` (linha 65).

**Descrição original**: `listar_boletos_abertos` devolve `{total, identificadores, boletos}` e busca os campos `status_cobranca` e `nosso_numero` além de `name`/`data_vencimento`. O único consumidor real (`montar_pdf_consolidado`) usa apenas `identificadores`; `total` é repassado. A chave `boletos` e os campos extras **não são lidos em nenhum ponto** do código nem dos testes. A docstring justifica com um caso de uso ("para quem precisar... ex.: montagem do consolidado") que é **factualmente incorreto** — o consolidado usa `identificadores`.

O contrato declarado na §3 da task original é "Devolve contagem e identificadores" — o payload extra é retorno especulativo.

**Categoria**: `speculative_complexity` (BAIXO)

**Classificação do especialista**: `recomendado_corrigir`
**Justificativa**: "Custo: 3min (remover chave 'boletos' e 2 campos do fields do get_all). Risco: nenhum — grep confirma que nenhum consumidor (nem o único caller montar_pdf_consolidado, nem os testes) lê essa chave/campos. Valor: reduz superfície de query e afasta leitura enganosa da docstring."
**Custo estimado**: ~3min
**Risco de regressão**: `nenhum`

### 4.2 Correção

- [x] Remover a chave `boletos` do dict retornado por `listar_boletos_abertos` (mantendo `total` e `identificadores`).
- [x] Remover `status_cobranca` e `nosso_numero` da lista `fields` do `get_all` (manter `name` e `data_vencimento` — este último é usado pelo `order_by`).
- [x] Ajustar a docstring que justificava o payload extra, para refletir o contrato real ("contagem e identificadores").

### 4.3 Guardrails de Execução

**DEVE**:

- **Antes de remover**, confirmar por grep que nenhum consumidor lê `["boletos"]`/`.get("boletos")` nem os campos `status_cobranca`/`nosso_numero` do retorno desta função (inclua testes na varredura). Se encontrar consumidor, **pare e reporte**.
- Preservar **intocado** o filtro `FILTROS_BOLETO_ABERTO` (RN-02) — é a regra de negócio da apuração e é comparada com o dry_run de produção pelo CT-019. (A extração dele para fonte única é a **T8**.)
- Manter `data_vencimento` no `fields` se o `order_by` depender dele.
- Rodar a suíte completa antes de retornar a task.

**NÃO DEVE**:

- NÃO alterar o filtro RN-02 nem a lógica de apuração.
- NÃO antecipar **D-008/T8** (filtro em fonte única) nem **D-009/T9** (bufferizar páginas) no mesmo arquivo — cada um na sua task.
- NÃO tocar `montar_pdf_consolidado` nem `baixar_consolidado_boletos_abertos`.
- NÃO adicionar testes novos.

---

## 5. Testes

**N/A — task é cleanup técnico**. Não cria testes novos.

O Gate 1 (QA) DEVE executar a **suíte completa** após a modificação:

```
docker compose exec -T backend bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend run-tests --app locacao_automation'
```

- ✅ Os **113 testes** continuam passando — em especial CT-019 (contagem e identificadores do filtro RN-02) e CT-020 (consolidado + ausentes).
- ❌ Qualquer teste regredindo → sinal de que algum consumidor lia o payload removido → task rejeitada.

---

## 6. Notas / Observações

_Vazio. Tasks de débito não geram débito novo._

---

## 7. Checklist Final

- [x] Correção aplicada exatamente como descrita em §4.2 — sem expansão de escopo
- [x] Diff afeta APENAS `integracao_bancaria_api/boletos_abertos.py`
- [x] Filtro `FILTROS_BOLETO_ABERTO` (RN-02) permanece intocado
- [x] Nenhuma antecipação de T8/T9 (mesmo arquivo, tasks próprias)
- [x] Suíte completa passa sem regressão (113 testes)
- [x] Gates aprovaram (QA + Tech Review)
- [x] Staged para commit (git add pelo orquestrador)
