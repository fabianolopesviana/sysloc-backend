## Challenge Session — 2026-09-01 (artifact: tech_spec.md)

- Questões processadas: 4 escaladas ao usuário + 3 resolvidas por medição sem perguntar
- Conflitos de terminologia resolvidos: 2 (*origem* × *restrição* de impedimento; *exclusão* lógica × física)
- Decisões implícitas explicitadas: 4
  1. **Completude da integridade referencial** — o critério D1-b pressupunha propriedade não asserida. Medido: 23 tabelas em `negocio`, 16 com FK direta a `identidade.empresa`, 7 transitivas por colunas `NOT NULL`. → guarda de cobertura CT-1242/CT-1243.
  2. **Chave de `IMPEDIMENTOS_DE_EXCLUSAO`** — `constraint_name`, sobre as **25** restrições `no action` (8 usuário + 17 empresa). A `acesso_usuario_app` carrega **duas**, e o PostgreSQL não garante ordem de disparo.
  3. **ADR-0016** — justificativa da spec refutada por medição em 3 pontos; o controlador **novo** passa a derivar o JSON-Schema por `esquemaPublicado(...)`, como 15 dos 22 controladores. Divergência do arquivo existente contida como débito.
  4. **Congelamento** — o `CLAUDE.md` afirma os números em **5** lugares, não 1. Quatro normativas emendadas + `CT-1196` estendido a elas; a quinta (L405) declarada histórica.
- Achados corrigidos por medição, sem pergunta: `SIMBOLOS_ESPERADOS` **220 → 233** era falso nas duas pontas (medido **216**; delta declarado de 12 símbolos ⇒ **216 → 228**) · *"7 controladores usam `@Put(':id')`"* (medido: **5** ocorrências, **9** controladores com algum `@Put`, `@Patch` = 0) · a §7 do `handoff-master-frontend.md` perde 3 marcadores **mais** o aviso aninhado *"Guarde o `usuarioId`"*, e a §8 já está falsa pelo fecho do `D23 · F1/T8`
- Achados verificados e **confirmados** (sem ajuste): `paresDoMaster()` tem 6 pares · `IMPORTADORES_ESPERADOS` = 17 · `PARES_PUBLICOS_DA_SUPERFICIE` = 20 não se move (as 7 rotas exigem sessão) · `admitirAdministrador` **não** cria vínculo de acesso, logo o estado "admin virgem" da CA-18 é alcançável · `catalogo.spec.ts` usa instância dedicada por `describe`, então CT-1215/1216/1242/1243 não contaminam as guardas de RLS
- Termos canonizados no glossário: 4, todos no nível **FEATURE** (`Impedimento de exclusão`, `Caminho bloqueante`, `Prévia de elegibilidade`, `Superfície do operador do SaaS`) — nenhum no global, que já cobre o vocabulário de negócio
- Candidatos a ADR sinalizados: 1 **parcial** (guarda de completude referencial — C1 e C2 batem, C3/C4/C5 falham; 2 de 5)
- ADRs sugeridos para criação: 0

---

## Run de execução — iniciado 2026-09-01

- `[run] executor resolvido: sysloc-backend-implementer (origem: argumento explícito)`
- `[run] executor_discipline injetado (fonte: references/executor-discipline.md)` — bloco de 89 linhas, marcadores ausentes (sanity check OK)
- `[run] git: repositório verificado · HEAD=1fcb33c01eb9c4f1898e0950a22217af9ae86d94`
- `[run] cleanup idempotente: _run/tmp/ inexistente (nada a limpar); `docs/specs/**/_run/tmp/` já em .gitignore (linha 48)`
- `[run] resume pós-interrupção: NENHUM sinal (0 tasks Em Progresso, 0 memórias lazy, 0 diffs em paths declarados) → execução do zero`
- `[run] guarda de continuidade ARMADA`
- `[run] reconciliação de dependências: task_plan.md × T{N}.md §1 — as 8 batem exatamente; nenhuma união necessária`
- `[run] decisão auto-resolvida (A1): inlining integral do conteúdo da task no prompt do executor × leitura pelo próprio executor → adotada a recomendada: leitura pelo executor, com §1/§5/§6-CTs/§7-ADRs inlinados como navegação e ordem de leitura integral obrigatória como PRIMEIRA ação · razão: o executor tem Read e o path; inlinar ~200 KB de task duplicaria o custo (leitura no orquestrador + escrita no prompt) e inflaria o contexto do orquestrador em TODA turn subsequente, sem entregar ao executor um byte a mais. Os dois blocos que ele não obtém por convenção — Disciplina e ADRs — seguem injetados verbatim.`
- `[T1] base_sha=1fcb33c01eb9c4f1898e0950a22217af9ae86d94`
- `[T1] executor: opus (declarado no frontmatter) · gates: [qa, tech_review] (declarado) · risk=high ⇒ qa_model=opus, tech_model=opus`
- `[T1] ADRs injetadas no executor: ADR-0008, ADR-0009, ADR-0013, ADR-0024, ADR-0026, ADR-0030, ADR-0038 (fonte: task §7)`
- `[T1] QA (rodada 1) = APROVADO_COM_OBSERVACOES` · critérios 11/11 · CTs 14/14 sem lacuna · bloqueantes 0 · anotáveis 2 (BAIXO/documentation, BAIXO/tests+happy_path_only) · `security_flags: []` · `escopo_testes: PARCIAL` (6 dos 9 pacotes — os 3 restantes não importam `@sysloc/db`) · `tocou_area_critica: true`
- `[T1] antipadroes_verificados: 3/3 arquivos de teste declarados (completo)`
- `[T1] observação não-bloqueante: o QA declara AP-16 em `detectados[]` de `administrador-do-master.spec.ts` sem emitir item correspondente em `problemas.*`. Registro como instrumentação inconsistente, não defeito — não rejeita a task (Passo 4.1).`
- `[T1] ledger: memória lazy NÃO criada — rodada 1 aprovada sem rejeição; sem achado bloqueante a rastrear (regra do "Nascimento do ledger")`
- `[T1] rule_candidates: 2 sinais do QA (repeated_fixture, repeated_assertion_shape) persistidos`
- `[T1] Tech Review (rodada 1) = PARCIAL` · bloqueantes 1 (`P1` MEDIO/architecture — categoria bloqueante pela partição) · anotáveis 1 (`P2` BAIXO/project_pattern) · `adrs_consultadas: ADR-0008, ADR-0009, ADR-0013, ADR-0038`
- `[T1] convergência: NÃO se aplica — rodada 1. A regra só vale a partir da rodada 3.`
- `[T1] TR observações registradas: (i) suíte NÃO re-executada apesar de a condição (2) do contrato disparar — julgamento declarado e VERIFICADO, não aceito por declaração: contracts/documentos/regua não declaram nem importam @sysloc/db; (ii) garantia removida = nenhuma, varredura independente da declaração do executor — o diff inteiro em packages/ tem UMA linha removida, a constante 38→39; (iii) empresa.ts sem linha "-" alguma, exigência de "estender sem alterar" cumprida; (iv) nenhum marcador DECISÃO FECHADA preexistente alterado; (v) as duas pontas do DÉBITO COM GATILHO D1 · F7/T1 fecham; (vi) os 3 arquivos fora da §5.2 NÃO são scope_deviation — compelidos por barreira executável do próprio projeto; (vii) risco operacional registrado sem virar achado: a sonda cascateia em conta/dois_fatores/sessao e retém bloqueio sobre as sessões vivas de até 200 empresas até o fim da transação — decisão da Fase 2 com o número na mão; (viii) resíduo transferido: 23503 desconhecido chega à Fase 2 como exceção crua e o detail cai no diário, território do gatilho do D53 · F4/T16.`

### T1 — retry classification
- attempt: 1
- problemas_por_categoria: { architecture: 1, project_pattern: 1 }
- bloqueantes: [P1 · MEDIO · architecture]
- overrides_ativos: [tocou_area_critica: true, task_risk: high, qa_security_flags: [], diff_stat_changed: false]
- requires_qa_revalidation: **true**
- decisao: RE-QA obrigatório (próxima rodada passa por Gate 1 antes do Gate 2)
- justificativa: "o único bloqueante está em `architecture`, que a rule classifica como `revalidation_required`; e os overrides `task_risk == high` e `tocou_area_critica: true` forçariam `true` de qualquer modo"
- `[T1] attempt_sha (rodada 1)=b34c18a1d8eebfec3a82f7a5a4d06eddff92f5e4`
- `[T1] executor de correção (rodada 2): opus · sem auto-escalonamento (attempt_count=1, last_severity=MEDIO — o modelo declarado já é opus)`
- `[T1] QA (rodada 2, scan_scope=DELTA) = APROVADO` · 0 problemas em todas as severidades · `db` 290→291 (o `CT-1206 (b)`, bloco aditivo; ZERO linhas `-` no arquivo de teste) · as outras 5 unidades inalteradas · AP-24 não detectado · `determinismo_observado: ok`, sem flake
- `[T1] ledger: TR-P1 → corrigido · TR-P2 → corrigido · QA-BAIXO-001 → corrigido · QA-BAIXO-002 → corrigido (todos rodada_ultima_verificacao=2, rodada_origem preservada em 1)`
- `[T1] débitos D2 e D3 REMOVIDOS da §2 do run-report — fechados dentro do ciclo de correção, com o fecho verificado pelo QA por grep (os três CTs citados de fato não existem na árvore, e a rede de `lerAdministrador` virou as duas últimas asserções do `CT-1206 (b)`)`
- `[T1] QA rodada 2 não caiu para FULL: raio de impacto determinado por arquivo (granularidade 2) e declarado fechado — 4 consumidores nomeados, nenhum com símbolo renomeado`
- `[T1] Tech Review (rodada 2, scan_scope=DELTA) = APROVADO` · `problems: []` · `adrs_consultadas: ADR-0008, ADR-0009, ADR-0013, ADR-0038` · raio de impacto determinado com confiança, sem queda para FULL · suíte não re-executada, com o julgamento da rodada 1 mantido e declarado
- `[T1] TR rodada 2 — as três verificações que pesaram: (i) o marcador DECISÃO FECHADA NÃO foi violado — o REVERTER EXIGE gateia o ABANDONO da decisão, não o endurecimento dela, e POR QUÊ/REVERTER EXIGE saíram byte a byte idênticos (o hunk tem exatamente 2 linhas "-", ambas do campo O QUÊ); a rede que o próprio marcador nomeia (CT-1206) está intacta e segue verde, o que é prova comportamental de que o USING identidade.usuario não barra o caso legítimo; (ii) a claim estrutural do docblock ("o USING não põe política alguma no caminho") foi MEDIDA, não aceita — identidade não declara RLS, registrado como decisão em esquema/identidade.ts:19 e confirmado por varredura das migrações; se fosse falsa, a correção reproduziria o modo de falha silenciosa que o POR QUÊ do marcador descreve; (iii) anti-gaming limpo: 34 linhas "+" e ZERO "-" no arquivo de teste.`
- `[T1] TR rodada 2 — ADR-0009 e ADR-0013 REFORÇADAS pelo delta (não apenas não-violadas): o USING mantém a função inteiramente do lado da identidade, enquanto o molde imitado atravessa para negocio.acesso_usuario_app; e o recorte estreita a escrita ao longo do mesmo eixo que a Decision da 0013 restringe.`
- `[T1] discrepância menor registrada pelo TR sem virar achado: a contagem real de linhas "-" é 12, não as 11 que o sumário do QA reportou — a extra é prosa de docblock, e nenhuma das 12 é garantia.`
- `[T1] ledger: 4 achados totais | 0 originados em rodada >1 | 0 suspeitos de incompletude da rodada 1`
- `[T1] staged: packages/db/src/administrador-do-master.ts, packages/db/test/administrador-do-master.spec.ts, packages/db/src/empresa.ts, packages/db/src/index.ts, packages/db/test/unidade-de-trabalho.spec.ts, packages/shared/test/protocolo-antirregressao.spec.ts, CLAUDE.md, tasks/T1.md, _run/{run-report,workflow-report,rule-candidates,sdd_state}`
- `[T1] CONCLUÍDA — 2 rodadas, QA APROVADO_COM_OBSERVACOES → APROVADO, TR PARCIAL → APROVADO`
- `[T1] → despachando T2 (restam 7 de 8)`
- `[T2] base_sha=1fcb33c01eb9c4f1898e0950a22217af9ae86d94` (o stage da T1 NÃO move o HEAD; o filtro de resíduo subtrai os 12 paths staged por ela — nenhum deles é `packages/db/test/catalogo.spec.ts`, então a lista da T2 sai limpa)
- `[T2] executor: opus (declarado no frontmatter) · gates: [qa, tech_review] (declarado) · risk=medium ⇒ a heurística do framework indicaria sonnet nos gates; o CLAUDE.md proíbe Sonnet e Haiku neste projeto, então qa_model=opus e tech_model=opus`
- `[T2] ADRs injetadas no executor: ADR-0006, ADR-0009, ADR-0031, ADR-0038 (fonte: task §7)`
- `[T2] QA (rodada 1) = REJEITADO` · bloqueantes 1 (`ALTO-001` ALTO/tests, `smell: tautological_assertion` — AP-29 no `CT-1242`) · anotáveis 1 (`BAIXO-001` BAIXO/documentation, a §5.2 não declara os 2 arquivos fora do escopo) · `criterios 7/7` · `rastreabilidade_cts 4/4` · `security_flags: []`
- `[T2] antipadroes_verificados: 1/1 arquivo de teste tocado, sweep sobre o arquivo INTEIRO (2338 linhas) como a §5.2 exige — AP-29 detectado`
- `[T2] convergência: NÃO se aplica — rodada 1, e o bloqueante é ALTO (nunca converge, em rodada nenhuma)`
- `[T2] o QA VERIFICOU E SUSTENTOU três decisões do executor, que NÃO se reabrem: (i) a divergência do CT-1215 (filtro por modo de remoção ['a','r'] em vez de só ['a']) — conjunto medido = 25 e schema com ZERO restrict, medido por grep nas duas fontes; divergência mais abrangente, nunca mais frouxa; (ii) as DUAS pernas do CT-1243 — a 2ª é a única que exercita o ramo da ligação anulável, e apagar a guarda de attnotnull deixaria a 1ª ainda vermelha e aquele vetor verde; (iii) CT-1216 × CT-1243 não é AP-26 — coincidem em 1 campo só, o molde CREATE→medir→DROP do P4.`
- `[T2] rule_candidates: 2 sinais do QA (repeated_assertion_shape ×2) persistidos`
- `[T2] attempt_sha (rodada 1)=bf1498845a0471cc502c9d26b3e79705678ad0c2`
- `[T2] executor de correção (rodada 2): opus · saída (a) adotada — a asserção passou a confrontar TRANSITIVAS_ATE_A_EMPRESA contra o complemento do alcance de UM SALTO, computado por consulta nova e independente ao catálogo`
- `[T2] QA (rodada 2, scan_scope=DELTA) = APROVADO` · 0 problemas · `db` 296→296, `api` 410→410 (suítes rodadas UMA DE CADA VEZ; a contenção da rodada 1 não reapareceu) · AP-24 descartado por varredura das 16 linhas "-", todas do bloco tautológico substituído
- `[T2] QA rodada 2 — a armadilha que o orquestrador apontou NÃO se materializou: `tabelasQueAlcancamAEmpresaEmUmSalto()` consulta `pg_constraint`/`pg_attribute` de forma independente e não deriva nada de `verificarCoberturaDoCriterioDeExclusao` nem de `cobertura.examinadas`. Os dois estados de falsificação afirmados pelo executor foram confirmados por leitura, e o controle antivácuo é bidirecional.`
- `[T2] ledger: QA-ALTO-001 → corrigido · QA-BAIXO-001 → corrigido (ambos rodada_ultima_verificacao=2, rodada_origem preservada em 1)`
- `[T2] débito D2 REMOVIDO da §2 — a §5.2 da task passou a declarar os três arquivos, com a razão de cada um`
- `[T2] decisão auto-resolvida (A1): scan_scope a passar ao Gate 2 → adotada a recomendada: FULL · razão: o Tech Review NUNCA revisou a T2 — a rodada 2 é a primeira invocação dele nesta task. DELTA restringiria a revisão à correção de uma asserção e deixaria os dois `describe` novos inteiros sem passagem de arquitetura, que é o oposto do que o escopo incremental existe para fazer. O `scan_scope` é relativo ao que AQUELE gate já viu, não ao número da rodada da task.`
- `[T2] Tech Review (rodada 2, scan_scope=FULL — primeira passagem deste gate) = APROVADO_COM_OBSERVACOES` · bloqueantes 0 · anotáveis 2 (`P1` BAIXO/code_quality, `P2` BAIXO/project_pattern) · `adrs_consultadas: ADR-0006, ADR-0009, ADR-0031, ADR-0038` · suíte não re-executada (nenhuma das 3 condições acionada)
- `[T2] TR — parecer pedido sobre `TRANSITIVAS_ATE_A_EMPRESA` (oráculo de 7 nomes à mão): julgado o desenho CERTO, por três razões medidas. (i) NÃO é circular — o lado esquerdo é `TABELAS_COM_LINHA_PROPRIA` menos o retorno da consulta de um salto, que não tem fechamento transitivo nenhum; a medida nunca se compara consigo mesma. (ii) O envelhecimento dela é a FUNÇÃO, não o defeito: se uma das sete ganhar FK direta, ou se uma das dezesseis diretas passar a chegar por corrente, ela reprova SOZINHA com `examinadas` e `excecoes` intactas. (iii) É a disciplina que o arquivo já pratica desde o `CT-009` com `TABELAS_LEGITIMAS`, e que a `ancoras-de-superficie.md` cobra. Derivar as sete do próprio ponto fixo tornaria a asserção tautológica — foi corretamente recusado.`
- `[T2] TR — segurança da fronteira de privilégio conferida: o DDL real usa `executarPrivilegiado(doMigrador, …)` sobre `conexaoDeMigracao(banco)`, e a limpeza é garantida em TRÊS camadas (o `CREATE` fora do `try`, o `DROP` no `finally`, e a instância dedicada derrubada em `afterAll`) — de modo que nem um `DROP` que falhasse contamina o `CT-300`/`CT-009`. Sem injeção: interpolações passam pelo template etiquetado do postgres.js, e as strings de DDL são concatenação de constantes literais.`
- `[T2] TR — ADR-0009 checada no ponto que merecia: a Decision exige cobertura como "propriedade consultada no catálogo do sistema, nunca lista de exceções mantida à mão". Conforme — `TABELAS_COM_LINHA_PROPRIA` e `TRANSITIVAS_ATE_A_EMPRESA` vivem no lado da ASSERÇÃO, não da consulta; nenhuma tabela é dispensada do exame por estar numa lista.`
- `[T2] TR — anti-gaming: `git diff <base_sha>` do arquivo de teste tem ZERO linhas removidas; as 16 deleções que o QA reportou são internas ao par de rodadas e não sobrevivem ao diff contra a base. `O QUE ESTA MUDANÇA REMOVE: nenhuma` confirmado por varredura própria.`
- `[T2] ledger: 4 achados totais | 2 originados em rodada >1 | 0 suspeitos de incompletude da rodada 1` — os 2 de rodada >1 são `TR-P1` e `TR-P2`, e NÃO indicam varredura incompleta: o Tech Review não foi invocado na rodada 1 (o QA rejeitou antes), então a rodada 2 é a primeira passagem dele. A métrica `{C}` é 0 por essa razão, não por acaso.
- `[T2] CONCLUÍDA — 2 rodadas, QA REJEITADO → APROVADO, TR APROVADO_COM_OBSERVACOES na primeira passagem`
- `[T2] → despachando T3 (restam 6 de 8)` — Fase 1 fechada (T1, T2); entra a Fase 2
- `[T3] base_sha=1fcb33c01eb9c4f1898e0950a22217af9ae86d94` (o stage de T1/T2 não move o HEAD; o filtro de resíduo subtrai os 14 paths já staged)
- `[T3] executor: opus (declarado) · gates: [qa, tech_review] (declarado — tipo=refactor_cross_module, 7 arquivos) · risk=medium ⇒ heurística indicaria sonnet nos gates; o CLAUDE.md proíbe, então opus`
- `[T3] ADRs injetadas no executor: NENHUMA (fonte: task §7 — "a task move acessório de teste e não toca decisão arquitetural registrada")`
- `[T3] QA (rodada 1) = APROVADO_COM_OBSERVACOES` · critérios 7/7 · bloqueantes 0 · anotáveis 3 (BAIXO/documentation ×3) · `escopo_testes: SUITE_COMPLETA` · `tocou_area_critica: true` (infraestrutura de teste COMPARTILHADA no caminho de arranjo de autenticação com segundo fator)
- `[T3] antipadroes_verificados: 7/7 arquivos de teste tocados. AP-09 detectado em recusa-indistinguivel.e2e.spec.ts é PRÉ-EXISTENTE, fora do delta e semanticamente legítimo — declarado por honestidade do sweep, sem virar problema.`
- `[T3] DIVERGÊNCIA (A) — o marcador NÃO removido, contra os itens 4 e 5 do Aceite: JULGADA CORRETA pelo gate, com os cinco pontos verificados um a um. O marcador reduzido conserva os 4 campos canônicos mais o ÍNDICE; continua sendo DÉBITO COM GATILHO (a linha que o distingue de DECISÃO FECHADA foi preservada byte a byte); as duas pontas da §3-B casam (39 linhas de índice, e a do D32 aponta para marcador VIVO); a metade paga está escriturada nos DOIS lugares com a reposição da cópia proibida em texto; e o total narrado e LINHAS_DO_INDICE_NO_FECHO_DA_FATIA continuam em 39 — não tocá-la foi o certo, com PROVA EXECUTÁVEL: o shared saiu 295/295 VERDE com o CLAUDE.md já editado, ou seja, o CT-1198 aceitou a reescrita. Itens 4 e 5 ATENDIDOS PELO ESPÍRITO.`
- `[T3] DIVERGÊNCIA (B) — não escalar o gatilho de parada da §7: JULGADA CORRETA. (a) a divergência de COMPORTAMENTO era só a de entrega-da-noticia, confirmado por leitura dos seis diffs; (b) o ramo tolerante era inalcançável, mas a prova do executor é o instrumento ERRADO — o que discrimina é a suíte sair 15/15 PASSED, porque credencialDeSessao levanta no beforeAll (virou o débito D4); (c) a substituição é ESTRITAMENTE mais forte: a antiga tolerava ausência e casava por substring sobre o Set-Cookie BRUTO (casaria um cookie cujo VALOR contivesse a agulha), a nova levanta e confere o NOME por sufixo; (d) a direção está certa — subir a tolerante REMOVERIA o levantamento diagnóstico de cinco suítes (proibição 3 da §4 do Protocolo), subir a estrita apenas endurece uma.`
- `[T3] AP-24 varrido linha a linha nos seis diffs: NENHUMA asserção removida ou afrouxada. Todo bloco removido é declaração de acessório privado mais docblock; as guardas reaparecem TODAS dentro da função compartilhada, uma a uma. Zero `it(`/`describe(` acrescentado ou removido — é a razão ESTRUTURAL de a contagem por arquivo não poder ter se movido.`
- `[T3] equivalência de base conferida suíte a suíte (o vetor real de regressão silenciosa, porque a assinatura passou a receber `base` por parâmetro): o caso delicado é cobertura-de-autorizacao, cujo `pedir` local resolvia `opcoes.base ?? baseMutante` — a cópia antiga operava sobre baseMutante por DEFAULT, e o call site novo passa baseMutante EXPLICITAMENTE. Passar a base primária ali teria emitido a credencial numa montagem e usado noutra.`
- `[T3] ledger: memória lazy NÃO criada — rodada 1 aprovada sem rejeição`
- `[T3] rule_candidates: 0 — o QA declarou a supressão deliberada e a razão é boa: o sinal `repeated_fixture` existe e é medível, mas a convenção que ele proporia JÁ EXISTE escrita no CLAUDE.md, e o limite de conversão está declarado por decisão no docblock de acessorios-de-borda.ts. Propor regra existente contra decisão registrada é ruído, não sinal.`
- `[T3] Tech Review (rodada 1, scan_scope=FULL) = APROVADO_COM_OBSERVACOES` · bloqueantes 0 · anotáveis 1 (`P1` BAIXO/project_pattern) · `adrs_consultadas: ADR-0006, ADR-0032, ADR-0039` · suíte não re-executada (nenhuma das 3 condições acionada)
- `[T3] decisão auto-resolvida (A1) — REGISTRO EXIGIDO PELA §A1 DA autonomia-do-run.md, e o Tech Review cobrou a sua ausência: as seis cópias de `entrarComSegundoFatorCumprido` divergiam, e a §7 da task declarava gatilho de parada ("PARE e escale"). Alternativas concorrentes: (a) subir a forma ESTRITA, vigente em 5 das 6; (b) subir a forma TOLERANTE de `entrega-da-noticia`. → adotada a recomendada: (a), a estrita · razão: (b) REMOVERIA de cinco suítes o levantamento diagnóstico de `credencialDeSessao`, que é a proibição 3 da §4 do Protocolo (remover guarda que não se introduziu); (a) apenas ENDURECE uma. Entre duas mudanças em suíte alheia, só a que endurece é admissível — é a opção conservadora que a §3 da autonomia-do-run.md exige. Os dois gates confirmaram a leitura de forma independente.`
- `[T3] TR — por que a forma estrita é a conservadora, medido pelo gate: a antiga era mais fraca em DOIS eixos independentes. (i) `cookies.find(bruto => bruto.includes('session_token'))` casa a agulha em qualquer posição do Set-Cookie BRUTO, inclusive dentro do VALOR de um cookie de outro nome; a nova parte em ";" e em "=" e confere o NOME por `endsWith(SUFIXO_DO_COOKIE_DE_SESSAO)`. (ii) O `reemitido === undefined ? cookie : …` devolvia silenciosamente a credencial PRÉ-segundo-fator — justamente a restrita cujo 403 o acessório existe para evitar.`
- `[T3] TR — verificações independentes: (i) as três constantes da âncora de superfície NÃO foram tocadas (106/91/20, ausentes do diff em linhas "+" e "-"); (ii) marcadores DECISÃO FECHADA contados antes e depois nos oito arquivos — 0/1/1/3/4/2/2, nenhum removido, esvaziado, movido ou com natureza trocada; (iii) AP-24 por varredura mecânica das linhas "-" por `expect(`/`it(`/`it.each`/`describe(`/`.skip`/`.only`/`todo(` — ZERO ocorrências nos sete arquivos; (iv) equivalência de `base` nos SEIS call sites, com o caso delicado de cobertura-de-autorizacao confirmado (passa `baseMutante` explicitamente, e NÃO `baseReal`).`
- `[T3] TR — efeito colateral favorável, medido e registrado sem virar achado: as escritas de `codigoDoSegundoFator` caíram de QUATRO para DUAS (a compartilhada privada + `sessao-restrita.e2e.spec.ts`, fora da §5.2). O Limiar de Três deixou de estar disparado para ela.`
- `[T3] ledger: memória lazy NÃO criada — rodada 1 aprovada nos dois gates sem rejeição`
- `[T3] CONCLUÍDA — 1 rodada, QA e TR ambos APROVADO_COM_OBSERVACOES`
- `[T3] → despachando T4 (restam 5 de 8)`
- `[T4] base_sha=1fcb33c01eb9c4f1898e0950a22217af9ae86d94`
- `[T4] executor: opus (declarado) · gates: [qa, tech_review] (declarado — tipo=service_complexo + rota governada por perfil) · risk=medium; o path `apps/api/src/autenticacao/`-adjacente e a guarda de perfil colocam a task em área sensível ⇒ qa_model=opus, tech_model=opus (o CLAUDE.md já proíbe sonnet de todo modo)`
- `[T4] ADRs injetadas no executor: ADR-0011, ADR-0016, ADR-0017, ADR-0018, ADR-0021, ADR-0026, ADR-0039 (fonte: task §7)`
- `[T4] executor concluído · api 410→417 (403 verdes + 14 VERMELHOS esperados), db 296→296, shared 295→295 · build e lint limpos`
- `[T4] ⚠️ ESTADO VERMELHO DELIBERADO E PREVISTO PELO TASK PLAN: a superfície medida foi de 106/91 para 109/94 com as três rotas novas, e as três âncoras executáveis de `cobertura-de-autorizacao.e2e.spec.ts` seguem em 106/91/20 até a T7, que as move de uma vez para 113/98/20. É o desenho da §4.0 do task_plan — T4/T5/T6 escrevem nos mesmos arquivos, então a âncora foi deslocada para a Fase 3. Conferido pelo orquestrador: as três constantes NÃO aparecem no diff, nem em linha "+" nem em "-".`
- `[T4] as três pontas do D8 concordam, conferidas pelo orquestrador: índice do CLAUDE.md com 40 pares (grep -c '^| \*\*D'), prosa "São **40**", e LINHAS_DO_INDICE_NO_FECHO_DA_FATIA = 40`
- `[T4] bloco ### D8 acrescentado à §2 do run-report pelo ORQUESTRADOR — o executor o sinalizou como pendência porque o snapshot é regenerado aqui, não por ele. Correto.`
- `[T4] QA (rodada 1) = APROVADO_COM_OBSERVACOES` · critérios 10/10 · CTs 6/6 sem lacuna · bloqueantes 0 · anotáveis 2 (MED-001 MEDIO/code_quality — categoria ANOTÁVEL pela partição; BAIXO-001 BAIXO/code_quality) · `escopo_testes: SUITE_COMPLETA` · `tocou_area_critica: true`
- `[T4] ESTADO VERMELHO — as duas condições CONFIRMADAS POR MEDIÇÃO, com a suíte executada DUAS vezes e resultado byte a byte idêntico (portanto sem flakiness). (a) `Test Files 1 failed | 44 passed (45)` — UM único arquivo falhou, e os 14 vermelhos são CT-213, CT-213 (b), CT-318, CT-355, CT-427, CT-533, CT-635, CT-732, CT-836, CT-937, CT-972, CT-1004, CT-1038 e CT-1095, TODOS em cobertura-de-autorizacao.e2e.spec.ts. A causa é única: 89 observadas contra 86 esperadas, com as três rotas novas como ÚNICA diferença e NENHUMA entrada anterior ausente. (b) 403 verdes = 396 dos 410 da base + os 7 novos — o número fecha exatamente, o que PROVA que todo caso que era verde e ficou vermelho é um dos 14 da superfície.`
- `[T4] os quatro riscos nomeados foram verificados um a um: (a) ADR-0016 — `grep 'schema:'` no controlador novo dá 15 ocorrências e as 15 DERIVAM (3 por esquemaPublicado, 12 por esquemaDoErro, o ponto único de comum/esquema-de-erro.ts usado por 19 controladores); ZERO JSON-Schema à mão, e o empresa.controller.ts NÃO foi tocado. (b) ponto único da conversão — `grep '.ativo'` no diff de produção dá UMA ocorrência, em paraContratoDoAdministrador; os literais `estado: 'SUSPENSO'`/`'ATIVO'` das transições NÃO são derivação, são `z.literal(...)` do esquema de saída. (c) transação — encerramento INCONDICIONAL dentro do mesmo emUnidadeDeTrabalho, sem `if (mudouDeEstado)`; o CT-1224 é o discriminador e passa, com a segunda suspensão devolvendo 200 e `sessoesEncerradas: 0` MEDIDO. (d) reativação — grep por fila/enfileirar só acha COMENTÁRIO dizendo que nada é enfileirado; o serviço nem recebe o produtor, e `imports` do módulo ficou inalterado.`
- `[T4] as DUAS âncoras fora da §5.2 são CRESCIMENTO LEGÍTIMO, julgadas contra o discriminador: ROTAS_PROTEGIDAS_ACEITAS ganhou 3 caminhos e NENHUMA saiu, com o conjunto PÚBLICO intacto; ABRIDORES_LEGITIMOS ganhou 1 e nenhuma saiu. As duas seguem afirmando por IGUALDADE de conjunto — nenhum toContain nasceu — e trazem SUT_IS_CORRECT_BECAUSE fundamentado. Nenhum AP-24: as QUATRO âncoras SOBEM.`
- `[T4] RN-06 conferida no código: o envelope é `422 CAMPO_INVALIDO` com `campo: 'id'` e `detalhes: {perfilExigido, perfilDoAlvo}`, NUNCA 404 — a decisão da T1 de deixar lerAdministrador sem recorte por perfil TEM consumidor. O CT-1220 (b) ainda acrescenta a asserção que discrimina o defeito de ORDEM: a sessão do alvo recusado CONTINUA servindo, o que reprova uma implementação que encerrasse antes de recusar.`
- `[T4] escrituração do D8 conferida pelo QA nas CINCO pontas e íntegra; NENHUMA linha de DECISÃO FECHADA foi adicionada, alterada ou removida em todo o diff (grep sobre o git diff)`
- `[T4] rule_candidates: 3 sinais do QA (repeated_fixture ×2, repeated_assertion_shape ×1) persistidos`
- `[T4] Tech Review (rodada 1, scan_scope=FULL) = PARCIAL` · bloqueantes 3 (`P1` ALTO/adr_compliance, `P2` ALTO/performance, `P3` MEDIO/testability — a categoria é bloqueante pela partição) · anotáveis 2 (`P4` MEDIO/project_pattern, `P5` BAIXO/project_pattern) · `adrs_consultadas: ADR-0011, ADR-0016, ADR-0021, ADR-0039`
- `[T4] convergência: NÃO se aplica — rodada 1, e dois dos três bloqueantes são ALTO (nunca convergem)`
- `[T4] TR aprovou explicitamente o deslocamento da âncora de superfície para a T7: a alternativa produziria TRÊS valores intermediários (109/94 → 111/96 → 113/98), cada um exigindo a escrituração normativa das quatro ocorrências do CLAUDE.md no mesmo diff, e dois deles seriam falsos em menos de uma task. A janela de risco é real mas estreita e observável — a causa do vermelho é única e medida, e ROTAS_PROTEGIDAS_ACEITAS, que é âncora de CONJUNTO e não de tamanho, SUBIU nesta task e continua discriminando por caminho. Recomendação do TR à T7: afirmar os três eixos contra a MEDIÇÃO, nunca contra a aritmética 106+7.`
- `[T4] ⚠️ COLISÃO IDENTIFICADA PELO ORQUESTRADOR entre a saída (a) sugerida pelo TR para o P2 e o marcador DECISÃO FECHADA da T1. O TR sugere "derivar a prévia da listagem por LEITURA — contar as dependências das mesmas tabelas que IMPEDIMENTOS_DE_EXCLUSAO já mapeia". O campo `exclusao` da listagem É a prévia de elegibilidade, e trocá-lo por contagem colide com (i) o REVERTER EXIGE do marcador da T1 ("demonstrar que uma contagem sobre negocio a partir do Master enxerga linha alguma"), (ii) a Decision da ADR-0038 (o critério é integridade referencial, NUNCA contagem) e (iii) o CT-1204, que MEDE que a contagem devolve ZERO para uma empresa CHEIA sob contexto de Master, por causa da RLS FORCE. A saída (a) reproduziria a falha SILENCIOSA que a fatia foi desenhada para impedir. As saídas (b) e (c) permanecem válidas; o executor foi instruído a medir antes de escolher, e a §3 da autonomia-do-run.md manda que a recomendada seja a CONSERVADORA — preservar o marcador e resolver por outro caminho.`

### T4 — retry classification
- attempt: 1
- problemas_por_categoria: { adr_compliance: 1, performance: 1, testability: 1, project_pattern: 2 }
- bloqueantes: [P1 · ALTO · adr_compliance, P2 · ALTO · performance, P3 · MEDIO · testability]
- overrides_ativos: [tocou_area_critica: true, task_risk: medium, qa_security_flags: [], diff_stat_changed: false]
- requires_qa_revalidation: **true**
- decisao: RE-QA obrigatório (próxima rodada passa por Gate 1 antes do Gate 2)
- justificativa: "os três bloqueantes estão em categorias `revalidation_required` (adr_compliance, performance, testability); o override `tocou_area_critica: true` forçaria `true` de qualquer modo"
- `[T4] attempt_sha (rodada 1)=55a82d34e898ece9c739551a497d8fd52a6c7363`
- `[T4] executor de correção (rodada 2): opus · P1 fechado com z.infer em administrador.contrato.ts (arquivo NOVO); P2 fechado com (b)+(c), sendo (c) CORRIGIDA pela medição; P3 fechado com CT-1244/CT-1245; P4 e P5 também fechados`
- `[T4] ⚠️ ACHADO DE MEDIÇÃO QUE REFUTA O PRÓPRIO GATE: o impacto declarado no TR-P2 — "a listagem estagna a entrada dos administradores listados" — é FALSO. O executor mediu com a unidade da listagem AINDA ABERTA e 203 bloqueios de relação retidos: a entrada de um administrador da própria página (INSERT em identidade.sessao, que toma FOR KEY SHARE na linha) ATRAVESSOU EM 8 ms. O controle positivo discrimina — contra um DELETE VIVO na mesma linha, o mesmo INSERT esperou o teto e foi recusado com 55P03 após 5014 ms. Conclusão: o ROLLBACK TO SAVEPOINT LIBERA o bloqueio de LINHA; ficam os de RELAÇÃO, que não conflitam com a entrada. O QA re-julgou a medição e a sustentou, notando que ela é coerente com a semântica real do PostgreSQL (o xmax da tupla aponta para um subxact abortado, enquanto o ROW EXCLUSIVE de relação é do nível da transação). É o SEXTO caso do precedente "prescrição de gate é hipótese, não ordem".`
- `[T4] custo medido da sonda por item (o que a §12.3 do tech spec mandava medir UMA vez e nunca fora feito): 674–885 ms para 200 itens, 167–181 ms para 50, 92–141 ms para 25, 35–39 ms para 10 — linear em ~3,4 ms/item. O teto foi a 50 e o padrão a 25, com a tabela no docblock de MAIOR_PAGINA_DE_ADMINISTRADORES.`
- `[T4] a saída (a) do TR foi CONFIRMADA REFUTADA pelo executor nos três eixos, e ele não parou aí: a única variante privilegiada que tornaria a contagem correta precisa de parâmetro de empresa e cai no discriminador da emenda de 2026-08-13 da ADR-0024. A colisão que o orquestrador identificou estava certa.`
- `[T4] QA (rodada 2, scan_scope=DELTA) = APROVADO_COM_OBSERVACOES` · critérios 14/14 · CTs 9/9 · bloqueantes 0 · anotáveis 1 (BAIXO/documentation — a §6.6 com 6 cards para 9 casos) · `api` 417→419, `db` 296→296, `shared` 295→295
- `[T4] QA rodada 2 cruzou as DUAS pontas do índice POR SCRIPT: 42 marcadores DÉBITO COM GATILHO únicos no código contra 41 linhas de índice, e a ÚNICA diferença é `D99 · F7/T3`, um literal SINTÉTICO dentro de protocolo-antirregressao.spec.ts:540 (exemplo de mutante), não um marcador vivo. Zero órfão nas duas direções.`
- `[T4] ledger: TR-P1, TR-P2, TR-P3, TR-P4 e TR-P5 → corrigido (rodada_ultima_verificacao=2, rodada_origem preservada em 1). QA-MED-001 e QA-BAIXO-001 seguem aceito_como_debito — nenhuma evidência nova eleva a severidade.`
- `[T4] débito D11 REMOVIDO da §2 (P4 fechado). ⚠️ D12 PERMANECE e MUDOU DE NATUREZA: deixou de ser anotável a pagar e passou a ser o DESTINO do campo ÍNDICE do marcador DÉBITO COM GATILHO — D12 · F7/T4 que o executor emitiu. Removê-lo deixaria o marcador órfão.`
- `[T4] D13 e D14 escriturados a partir de achados do executor: a frase de ensaiarExclusao em packages/db é mais larga que a medição sustenta (gatilho: a primeira task autorizada a abrir aquele arquivo), e o nome do it do CT-357 diz "seis importadores" desde quando já eram nove (hoje dez).`
- `[T4] Tech Review (rodada 2, scan_scope=DELTA) = APROVADO_COM_OBSERVACOES` · bloqueantes 0 · anotáveis 1 (`P6` BAIXO/code_quality — o `Readonly<>` raso) · `adrs_consultadas: ADR-0016, ADR-0024, ADR-0038, ADR-0039`
- `[T4] ⚠️ O TECH REVIEW RECONHECEU O PRÓPRIO ERRO, e o fez reexaminando a semântica em vez de aceitar o número: "o impacto que eu declarei na rodada 1 estava ERRADO, e eu o reconheço. Bloqueio de LINHA é gravado no xmax da tupla com o subxid; abortado o subxact, quem espera lê o status como abortado e SEGUE. Bloqueio de RELAÇÃO é heavyweight e, no aborto do subxact, é TRANSFERIDO AO PAI, ficando retido até o commit. É exatamente o par que a medição relata, e o ROW EXCLUSIVE de relação não conflita com o FOR KEY SHARE que a entrada toma na linha. A minha premissa foi HERDADA do docblock de ensaiarExclusao e é mais larga que a realidade." É o SEXTO caso do precedente, e o primeiro em que o próprio gate escritura a refutação.`
- `[T4] TR rodada 2 — julgamento sobre a escalabilidade do molde, verificado e não presumido: o módulo novo já hospeda esquema de ENTRADA (ESQUEMA_DA_JANELA) ao lado dos de saída, de modo que os corpos do PUT parcial e da exclusão têm domicílio precedente. ⚠️ RESSALVA REPASSADA À T5: o comentário do controlador (linhas 117-123) diz "esquemas de SAÍDA vivem em administrador.contrato.ts"; a janela prova que a regra é mais larga (todo esquema da rota, entrada inclusive, menos o corpo vazio compartilhado) — se a frase induzir a T5 a declarar corpo no controlador, o P1 volta pela porta de trás.`
- `[T4] TR rodada 2 — AP-24: CINCO linhas removidas nos três arquivos de teste do delta, todas inócuas (uma de rastreabilidade em docblock substituída por versão ampliada, três de comentário em validacao.spec.ts que a rodada 2 tornou falsas, e a constante 40→41 acompanhando o marcador real). NENHUMA asserção removida ou afrouxada, zero skip/only/todo.`
- `[T4] TR rodada 2 — DECISÃO FECHADA: o marcador aparece em DUAS ocorrências no delta e as duas são CITAÇÃO, não edição. Nenhum código sob marcador alterado, movido ou removido.`
- `[T4] TR rodada 2 — achado de varredura FORA do delta, registrado e descartado: cobertura-de-autorizacao.e2e.spec.ts mostra 7 inserções / 67 remoções contra base_sha mas diff VAZIO contra attempt_sha_anterior. O TR o abriu assim mesmo para descartar AP-24 — é a extração da T3 para a casa compartilhada, mudança de domicílio, nenhuma asserção e nenhum caso saiu.`
- `[T4] TR rodada 2 — aritmética do CLAUDE.md conferida pelo gate: 455+419+295+296+180+159+95+114+30 = 2043, igual ao total escriturado`
- `[T4] ledger: 9 achados totais | 2 originados em rodada >1 | 0 suspeitos de incompletude da rodada 1` — os 2 de rodada >1 (`TR-P6` e o BAIXO da §6.6) nasceram **da própria correção**: o `Readonly<>` raso é consequência do `z.infer` que a rodada 2 introduziu, e a lacuna da §6.6 é dos dois CTs que a rodada 2 acrescentou. Nenhum deles existia na rodada 1.
- `[T4] CONCLUÍDA — 2 rodadas, QA APROVADO_COM_OBSERVACOES → APROVADO_COM_OBSERVACOES, TR PARCIAL → APROVADO_COM_OBSERVACOES`
- `[T4] → despachando T5 (restam 4 de 8)`
- `[T5] base_sha=1fcb33c01eb9c4f1898e0950a22217af9ae86d94`
- `[T5] executor: opus (declarado) · gates: [qa, tech_review] (declarado — risk=high: publica verbo destrutivo irreversível) ⇒ qa_model=opus, tech_model=opus`
- `[T5] ADRs injetadas no executor: ADR-0011, ADR-0013, ADR-0014, ADR-0016, ADR-0017, ADR-0021, ADR-0030, ADR-0038, ADR-0039 (fonte: task §7)`
- `[T5] ⚠️ DOIS critérios da §4 da T5 JÁ FORAM ENTREGUES PELA T4, e o orquestrador os declarou ao executor para que ele não os refizesse: (i) "exclusao por item na R1, vindo da sonda" — a T4 já o publica, e o CT-1220 o afirma por igualdade com `exclusao: { disponivel: true, impedimentos: [] }`; (ii) "latência da sonda no teto de página medida uma vez e registrada" — a rodada 2 da T4 a mediu (674–885 ms para 200, 167–181 para 50, 92–141 para 25, 35–39 para 10; linear em ~3,4 ms/item) e a registrou no docblock de MAIOR_PAGINA_DE_ADMINISTRADORES, com o teto reduzido a 50 e o padrão a 25 por causa dela. O que falta à T5 é levar os números ao _run/run-report.md, que é o que a §4 pede literalmente.`
- `[T5] QA (rodada 1) = REJEITADO` · critérios 11/11 · CTs 6/6 · bloqueantes 1 (`ALTO-001` ALTO/tests, `smell: happy_path_only` — o cenário "e-mail em uso" da §6.4, CA-11, sem teste algum) · anotáveis 2 · `escopo_testes: SUITE_COMPLETA` · `tocou_area_critica: true`
- `[T5] convergência: NÃO se aplica — rodada 1, e o bloqueante é ALTO (nunca converge)`
- `[T5] o QA VERIFICOU E SUSTENTOU os cinco pontos de risco um a um: (a) esquemas em administrador.contrato.ts com tipos por z.infer — o P1 da T4 NÃO voltou pela porta de trás, e os dois únicos strictObject são as duas ENTRADAS; (b) dupla barreira da RN-06 nas duas rotas, com o NAO_ALCANCADO de cada escrita LIDO e virando 404; (c) as TRÊS asserções de ausência de efeito do CT-1226 discriminam e nenhuma é tautológica — a (i) compara a linha crua INTEIRA e não é implicada pelo 422, a (ii) pega a suspensão que encerrasse antes de conferir e vive em OUTRA tabela, e a (iii) é a única que observa do lado do CLIENTE, pegando a sessão invalidada sem apagar a linha; (d) a recusa nomeia CLASSE na resposta E no diário, com grep '.detail' no serviço devolvendo ZERO; (e) o docblock do @Delete declara a exceção da ADR-0038 à ADR-0014, comparado com o molde do único outro @Delete da base.`
- `[T5] divergência de escopo do CT-1227/CT-1230 julgada CORRETA pelo gate: implementá-los sobre a rota de Empresa exigiria publicar rota fora do escopo, e contra rota inexistente daria 404 — verde por motivo errado. O CT-1227 troca a igualdade de instante de suspensa_em (coluna que identidade.usuario não tem) pela linha crua INTEIRA, que é o análogo MAIS FORTE.`
- `[T5] ⚠️ ACHADO QUE NÃO É DESTA TASK E QUE O ORQUESTRADOR VAI CARREGAR PARA O DESPACHO DA T6 (débito D17): a §6 da T6 NÃO tem CT de corpo fechado. O critério de aceite existe (T6 §4, linha 69) e não tem CT que o cubra; as duas chaves proibidas do corpo de Empresa ficam ÓRFÃS — a T5 não pode cobri-las e a T6 não as declara. Medido pelo gate na própria T6.md.`
- `[T5] divergência factual levada ao Gate 2 pelo QA, sem ser achado: a frase larga de ensaiarExclusao aparece TAMBÉM nos "Cons" da ADR-0038 ("a verificação prévia toma bloqueios que o retorno ao ponto de salvamento não libera antes do fim da transação"). NÃO é violação da Decision — é um "Cons" factualmente mais largo que a medição da T4. O TR decide se a ADR merece emenda.`
- `[T5] rule_candidates: 1 sinal do QA (repeated_fixture — as mensagens canônicas de erro redeclaradas em 22 arquivos)`
- `[T5] attempt_sha (rodada 1)=477659e334d14944c626d108b1cef724e7b5f0a1`
- `[T5] executor de correção (rodada 2): opus · CT-1246 acrescentado, produção INTOCADA (git diff sobre apps/api/src volta vazio)`
- `[T5] QA (rodada 2, scan_scope=DELTA) = APROVADO_COM_OBSERVACOES` · critérios 11/11 · CTs 7/7 · bloqueantes 0 · anotáveis 1 (BAIXO/tests, `smell: magic_strings`) · `api` 432→433, `shared` 295→295
- `[T5] QA-ALTO-001 SANADO, e a LACUNA ESTRUTURAL também — este é o ponto que mais importa. A causa-raiz não era "esqueci um caso": era que a §6.4 (Cenários de Erro) NÃO gerava CT na §6.3, de modo que cenário declarado podia ficar sem prova sem que a rastreabilidade acusasse (já havia acontecido na T4, onde o CT-1220 (b) nasceu pela mesma razão). A correção fechou nas QUATRO pontas: §6.3 com a linha do CT-1246, §6.4 com COLUNA `CT` NOVA e nota normativa em blockquote nomeando o achado como origem, §6.5 com a linha CA-11, e §6.6 com o card completo. ⚠️ A T6 CONTINUA SEM essa coluna — carregado para o despacho dela junto do D17.`
- `[T5] QA rodada 2 — o controle positivo da RN-15 discrimina: `varrerAgulhas` é UMA função (linha 1991) e as TRÊS aplicações usam a MESMA função com a MESMA constante `agulhas` (declarada uma vez na linha 1421). A terceira planta as duas na forma exata do vazamento e afirma `toEqual(agulhas)` — a lista INTEIRA, por igualdade e por ordem. As agulhas foram conferidas POR MEDIÇÃO contra o que o `detail` do 23505 de fato carrega, não por leitura do teste.`
- `[T5] QA rodada 2 — o antivácuo do diário é específico: as linhas novas são filtradas por `caminho === PADRAO_DA_ROTA_DE_UMA_PESSOA && metodo === 'PUT'` com `toHaveLength(1)` — um filtro que não casasse nada REPROVA ali mesmo, e é isso que impede as duas asserções de ausência de serem vácuas.`
- `[T5] QA rodada 2 — a segunda montagem instrumentada NÃO é seam (Iron Law #6 satisfeita): `aplicacao-instrumentada.ts` é PRÉ-EXISTENTE e commitado (6c5baae, fecho do D57/D7), intocado no diff; `TOKEN_LOGGER` é token de DI de PRODUÇÃO publicado por app.module.ts; e a opção `destino` de `criarLogger` é de produção e commitada (packages/shared/src/log.ts:132), consumida por ~10 suítes. A troca é por `overrideProvider` sobre token que a composição já publica — nunca por ramo condicional nem variável de ambiente em produção.`
- `[T5] ledger: QA-ALTO-001 → corrigido · QA-BAIXO-002 → corrigido (por CRESCIMENTO: o `not.toBe(200)` foi PRESERVADO e ganhou a comparação byte a byte ao lado) · QA-BAIXO-001 segue aceito_como_debito (é da spec da T6)`
- `[T5] débito D18 SUBSTITUÍDO: o antigo (a asserção vaga do CT-1237) foi fechado na rodada 2; o número foi reusado para o achado novo da mesma rodada (a agulha literal), preservando a sequência.`
- `[T5] Tech Review (rodada 1 dele, scan_scope=FULL) = APROVADO_COM_OBSERVACOES` · bloqueantes 0 · anotáveis 3 (`P1` MEDIO/project_pattern — categoria ANOTÁVEL pela partição; `P2` BAIXO/scope_deviation; `P3` BAIXO/adr_compliance) · `adrs_consultadas: 0038, 0014, 0013, 0016, 0017, 0021, 0030, 0039, 0011`
- `[T5] TR abriu a Decision INTEGRAL das TRÊS que decidem o verbo destrutivo (0038, 0014, 0013) e declarou que as outras seis foram consultadas pelo INDEX mais as citações literais do diff, com a conformidade decidível sem a abertura — honestidade de método, registrada.`
- `[T5] TR — segurança, o eixo da task, verificada ESTRUTURALMENTE e não só pelo teste: (i) escalação e auto-alvo FECHADOS — a checagem é por DESIGUALDADE (perfil !== 'ADMIN_EMPRESA'), não por enumeração, de modo que SYSLOC_MASTER e USUARIO_EMPRESA caem pelo mesmo ramo; (ii) RN-16 honrada pelo SCHEMA, não por convenção de código — `grep references(() => usuario.id` em packages/db/src/esquema/ devolve EXATAMENTE três `onDelete: cascade` (conta, sessao, dois_fatores), e `tentativa_login.usuarioId` referencia SEM cascata, logo é RESTRICT e IMPEDE a remoção; (iii) o `detail` do driver nunca é lido nem propagado, e a falha é FECHADA — `semDeixarEfeitoNaRecusa` RELANÇA quando a classe não é reconhecida, em vez de degradar para "elegível".`
- `[T5] TR — a irreversibilidade SEM confirmação prévia foi julgada e NÃO é achado: a contenção é a que a ADR-0038 declara ("o operador ganha verbo destrutivo cuja contenção é a chave estrangeira, não a autorização"), e ela é forte na prática — só é removível quem nunca tentou entrar, nunca teve vínculo e nunca assinou registro. A pessoa removível é, por construção, aquela cujo histórico é VAZIO. Confirmação de intenção é decisão de TELA, e tela não se faz neste repositório.`
- `[T5] TR — acoplamento registrado por completude, sem achado: `alterar` recompõe a prévia APÓS a escrita, então um 23503 que o vocabulário não conhecesse transformaria uma correção cadastral válida em 500. O risco é CONTIDO fora desta task — as guardas CT-1215/CT-1216 da T2 acusam por nome qualquer dependência nova antes que ela chegue à produção. É a T2 pagando dividendo.`
- `[T5] TR — convention_drift NÃO emitido para o P1, com razão declarada: a convenção violada (Limiar de Três) ESTÁ escrita, e no lugar de maior alcance possível — CLAUDE.md → Convenções, que carrega no system-prompt de TODO agente, com razão e gatilho explícitos. É problema de APLICAÇÃO, não de ausência de regra. Sweep dirigido feito em .claude/rules/ e docs/adr/ pelos termos "Limiar"/"terceiro consumidor".`
- `[T5] ledger: memória lazy tinha 3 achados de QA (1 alto corrigido, 2 baixos — um corrigido e um substituído) + 3 do TR (todos anotáveis). Métrica: 6 achados totais | 4 originados em rodada >1 | 0 suspeitos de incompletude da rodada 1 — os 4 são os 3 do TR (que só entrou na rodada 2) e o BAIXO da agulha literal (que nasceu DO caso que a rodada 2 acrescentou).`
- `[T5] CONCLUÍDA — 2 rodadas, QA REJEITADO → APROVADO_COM_OBSERVACOES, TR APROVADO_COM_OBSERVACOES na primeira passagem`
- `[T5] → despachando T6 (restam 3 de 8) — última da Fase 2`
- `[T6] base_sha=1fcb33c01eb9c4f1898e0950a22217af9ae86d94`
- `[T6] executor: opus (declarado) · gates: [qa, tech_review] (declarado — risk=high: verbo destrutivo que remove TENANT INTEIRO) ⇒ qa_model=opus, tech_model=opus`
- `[T6] ADRs injetadas no executor: ADR-0011, ADR-0014, ADR-0016, ADR-0017, ADR-0021, ADR-0031, ADR-0032, ADR-0038, ADR-0039 (fonte: task §7)`
- `[T6] ⚠️ DUAS LACUNAS DE SPEC ACHADAS PELOS GATES DA T5 E CARREGADAS PELO ORQUESTRADOR PARA ESTE DESPACHO: (i) o débito D17 — a §6 da T6 NÃO tem CT de corpo fechado, embora a §4 (linha 69) exija "Corpo do PUT em z.strictObject, sem estado/suspensaEm"; as duas chaves proibidas ficariam ÓRFÃS, porque a T5 não podia cobri-las e a T6 não as declara; (ii) a §6.4 da T6 NÃO tem coluna `CT`, que é a lacuna ESTRUTURAL que a T5 fechou na própria spec depois de ela ter produzido um achado ALTO — cenário declarado na §6.4 sem CT na §6.3 fica sem prova e a rastreabilidade não acusa. Já aconteceu DUAS vezes nesta fatia (o CT-1220 (b) da T4 e o CT-1246 da T5).`
- `[T6] attempt_sha (rodada 1)=b4ff6567daba9ada2e74b9bfbbf64797296c16ab`
- `[T6] QA (rodada 1) = REJEITADO` · bloqueantes 1 (`ALTO-001` ALTO/logic — a R6 PUT nunca respondia 200; TRÊS mutantes sobreviviam à suíte de 443) · `CA-09` PARCIAL · anotáveis 3
- `[T6] o orquestrador REFUTOU POR MEDIÇÃO a pendência (xii) do executor antes de despachar o gate: ele alegou que a contagem do CLAUDE.md "já estava defasada antes desta task", e 455+433+295+296+180+159+95+114+30 = 2057 é EXATAMENTE o valor correto após a T5. Não estava defasado — a T6 é que o defasou. O QA confirmou a refutação de forma independente.`
- `[T6] executor de correção (rodada 2): opus · CT-1248 acrescentado; NENHUM dos três mutantes era real, e o SUT ficou INTOCADO (o savepoint da sonda é aberto DENTRO de ensaiarExclusao, depois do UPDATE, e o ROLLBACK TO SAVEPOINT não o alcança)`
- `[T6] QA (rodada 2, scan_scope=DELTA) = APROVADO` · critérios 9/9 (o `CA-09` saiu de PARCIAL) · CTs 10/10 · ZERO problemas em todas as severidades · `api` 443→444, `shared` 295→295
- `[T6] QA rodada 2 — as três asserções do CT-1248 discriminam, julgadas por raciocínio: (a) é `toEqual` e NÃO `toMatchObject`, de modo que devolver paraContrato(alterada) faz sumir a chave `exclusao` e a igualdade de objeto inteiro reprova; (b) mede a GRAVAÇÃO de suspensa_em sobre a linha CRUA, com a empresa suspensa ANTES pela rota publicada e `toBeInstanceOf(Date)` como CONTROLE ANTIVÁCUO — dois undefined comparados entre si passariam por vacuidade; (c) afirma a linha crua INTEIRA e reprova o savepoint aberto antes do UPDATE.`
- `[T6] QA rodada 2 — CONFIRMADO que empresa.service.ts mudou SÓ docblock: o delta tem 16 linhas, todas dentro de um bloco /** */, e a única linha "-" é o docblock de uma linha que ele substitui, com a frase original PRESERVADA como primeira linha do bloco novo. Nenhuma linha de código tocada.`
- `[T6] QA rodada 2 — a RETRATAÇÃO foi feita e escriturada: o CLAUDE.md declara literalmente que "2057 era o valor CORRETO depois da T5, e não um número defasado — não o reponha, e não o leia como erro da T5". A alegação falsa não foi reposta em lugar nenhum.`
- `[T6] ⚠️ TERCEIRA lacuna estrutural da mesma família, registrada pelo executor na T6.md §6.5: a rastreabilidade confere `CT → caso`, NUNCA `caso → verbo` — critério com verbo de escrita passa a exigir na coluna ao menos um caso de SUCESSO daquela escrita. É a terceira desta fatia: a §6.4 sem CT (T4 e T5) e agora a §6.5 apontando para um caso que não exercita o verbo.`
- `[T6] ledger: QA-ALTO-001 → corrigido · QA-BAIXO-001 → corrigido · QA-BAIXO-002 → corrigido (por docblock, sem marcador, LINHAS_DO_INDICE em 42 intacto) · QA-BAIXO-003 segue aceito_como_debito (nada a mudar em código)`
- `[T6] attempt_sha (rodada 2)=b8db9a01bf93e7bc5a90f28ab77c24ac4551d5be`
- `[T6] Tech Review (rodada 2, primeira passagem dele, scan_scope=FULL) = PARCIAL` · bloqueantes 1 (`P1` MEDIO/testability — o 404 das duas rotas novas sem caso algum) · anotáveis 3 · `adrs_consultadas: 12 ADRs`
- `[T6] convergência: NÃO se aplicou na interpretação daquele veredito — attempt_count era 1, logo a rodada era 2. A regra vale a partir da rodada 3.`
- `[T6] TR — segurança profunda SEM ACHADO, e verificada no fonte e não herdada: atomicidade confirmada (um commit, dois DELETE, dentro do mesmo semDeixarEfeitoNaRecusa); trilha sobrevive (tentativa_login em IMPEDIMENTOS_DE_EXCLUSAO); ⚠️ plataforma.notificacao_bancaria NÃO é alcançada por razão ESTRUTURAL — a tabela não tem empresa_id NEM chave estrangeira alguma para identidade, o que é ADR-0031 pura, de modo que não há nem bloqueio nem órfão; o detail não vaza; a falha é FECHADA; e as duas linhas de logger.info de sucesso são emitidas FORA da unidade, DEPOIS do commit — nada é registrado como feito antes de ter sido feito.`
- `[T6] TR — anti-gaming com atenção máxima na âncora partida: verificou NO ARQUIVO, não aceitou a alegação. CHAVES_DA_EMPRESA_PUBLICADA CONTINUA sendo consumida por igualdade na criação (ciclo-de-acesso:447), CHAVES_DO_ITEM_DA_LISTAGEM é DERIVADA por spread, e a passagem ainda GANHOU o toMatchObject da prévia INDISPONÍVEL da empresa A — que é o que impede `exclusao` de ser objeto constante na página.`
- `[T6] QA (rodada 3, scan_scope=DELTA) = APROVADO_COM_OBSERVACOES` · critérios 9/9 · CTs 11/11 · bloqueantes 0 · anotáveis 1 (`MED-001` MEDIO/code_quality — categoria ANOTÁVEL pela partição) · `api` 444→446, `shared` 295→295
- `[T6] ⚠️ O QA REFUTOU POR MEDIÇÃO DO FONTE o "bônus" que o executor declarava no docblock do CT-1249: o 404 do ARCABOUÇO sai pelo MESMO envelope canônico (CODIGO_POR_STATUS[404] → doNossoCodigo → MENSAGEM_POR_CODIGO, sem campo e sem detalhes), de modo que a igualdade NÃO distingue "rota ausente" de "recurso ausente". Removida a rota, o caso fica VERDE. ⚠️ Isso NÃO invalida o caso — os três mutantes do TR-P1 seguem reprovados, e a existência da rota é coberta pela âncora de superfície. O defeito é a FRASE. Virou o D26.`
- `[T6] QA rodada 3 — os três mutantes do TR-P1 reprovados, com a asserção nomeada para cada um: (i) toBe(404) contra o 422; (ii) toEqual de objeto INTEIRO contra o campo acrescentado — CONFIRMADO que é toEqual e que o esperado é exatamente {codigo, mensagem}, de modo que toMatchObject o aprovaria; (iii) o mesmo toBe(404) contra o 200 {removida:true}.`
- `[T6] QA rodada 3 — a desestruturação do P4 TOCA produção de rota entregue e foi varrida: mudança PURAMENTE de nome de variável, as quatro chaves de PaginaDeEmpresas com os mesmos valores e a mesma ordem de composição. Prova empírica: ciclo-de-acesso e campos-fechados, que exercitam GET /v1/master/empresas, estão entre os 44 verdes.`
- `[T6] QA rodada 3 — o marcador D12 foi EMENDADO, nunca removido nem esvaziado: natureza preservada, os quatro campos mais o ÍNDICE presentes, e o QUANDO FECHA GANHOU o bloco "JÁ DISPAROU (F7/T6)" sem perder uma linha do texto anterior.`
- `[T6] o QA achou um lapso de aritmética NA MEMÓRIA LAZY DO ORQUESTRADOR (a linha dizia "429 verdes + 14" para 446, e 429+14=443). Corrigido para 432 verdes. É lapso do orquestrador, não da suíte.`
- `[T6] ledger: TR-P1, TR-P2, TR-P3 e TR-P4 → corrigido (rodada_ultima_verificacao=3, rodada_origem preservada em 2) · débitos D26/D27/D28 REMOVIDOS da §2 (fechados na rodada 3) e o número D26 REUSADO para o achado novo da mesma rodada`
- `[T6] Tech Review (rodada 3, scan_scope=DELTA) = APROVADO_COM_OBSERVACOES` · bloqueantes 0 · anotáveis 2 (`P1` e `P2`, ambos BAIXO/code_quality) · `adrs_consultadas: ADR-0017, ADR-0038`
- `[T6] TR rodada 3 — julgamento pedido sobre o MED-001: a análise do QA SE SUSTENTA, verificada de forma INDEPENDENTE no fonte (NotFoundException → traduzir → recusaDeOutrem(404) → CODIGO_POR_STATUS[404] → doNossoCodigo → paraCorpo(), que OMITE opcional não informado). ⚠️ "A frase é falsa PORQUE o filtro global cumpre bem a sua razão de ser" — o docblock dele diz literalmente que o formato do arcabouço "é o que a decisão existe para eliminar".`
- `[T6] TR rodada 3 — e o gate RECUSOU escalar a severidade, com quatro razões medidas e um argumento de método que vale registrar: "(i) nenhuma cobertura se perde — a existência das rotas é afirmada pela âncora de conjunto, que é o mecanismo que o repositório designou para isso; (ii) os três mutantes seguem reprovados; (iii) a frase falsa NÃO é a declaração de discriminação que o P4 exige — essa é sobre o defeito perseguido, está correta e foi verificada; a falsa é um adendo rotulado "também", fora do eixo do caso; (iv) por isso a categoria é code_quality e não testability: o objeto do defeito é o TEXTO, não a prova. ⚠️ Reclassificar para testability mudaria o DESTINO do achado sem mudar o achado — seria escolher a categoria pelo efeito de bloqueio, que é precisamente o que meu contrato proíbe."`
- `[T6] TR rodada 3 — dois complementos que o QA não tinha: (1) a frase falsa tem DUAS pontas — entrou também na §6.3 do CARD (tasks/T6.md), e o card é o artefato que uma fatia futura lê para derivar CTs; o gate AMPLIOU o D26 em vez de abrir Dnn novo, "porque um número a mais para o mesmo defeito polui o índice"; (2) "a indistinguibilidade que a ADR-0017 exige" ATRIBUI À DECISION DELA algo que o texto não diz — ela fixa a FORMA, e `campo?` é opcional e PERMITIDO; um 404 com campo:'id' seria CONFORME. A indistinguibilidade é doutrina do produto (20+ pontos), ancorada em RN e decisões de fatia, não naquela ADR. Virou o D27.`
- `[T6] TR rodada 3 — o gate NÃO duplicou o MED-001 em problems[], declarando a razão: "ele já está escriturado como D26 na §2, e reportá-lo de novo o contaria duas vezes; meus P1 e P2 são COMPLEMENTOS dele, com evidência que o QA não tinha".`
- `[T6] convergência (rodada 3): NÃO houve item a converter — os dois achados do TR são BAIXO, e BAIXO nunca entrou no bloqueio. Nenhum MEDIO de categoria convergível apareceu.`
- `[T6] ledger: 11 achados totais | 6 originados em rodada >1 | 0 suspeitos de incompletude da rodada 1` — os 6 são os 4 do TR (que só entrou na rodada 2) mais os 2 achados que nasceram DA PRÓPRIA correção (o "bônus" falso e a atribuição de ADR, ambos escritos na rodada 3). **Nenhum existia na rodada 1.**
- `[T6] CONCLUÍDA — 3 rodadas. QA: REJEITADO → APROVADO → APROVADO_COM_OBSERVACOES. TR: PARCIAL → APROVADO_COM_OBSERVACOES.`
- `[T6] FASE 2 FECHADA (T3, T4, T5, T6). Resta só o lote paralelo T7 ∥ T8 da Fase 3.`

---

## ⏸️ PAUSA CONTROLADA — 2026-09-02, a pedido do usuário

- `[run] pausa solicitada pelo usuário no meio do turno, com a T6 recém-aprovada nos dois gates`
- `[run] fronteira escolhida: FIM DA FASE 2. É a fronteira limpa mais próxima — 6/8 concluídas, zero tasks em ciclo de correção, _run/tmp/ vazio, e o que resta é o lote PARALELO T7 ∥ T8, que a §4.0 do task_plan derivou como o único da fatia.`
- `[run] guarda de continuidade DESARMADA para a pausa. ⚠️ Isto NÃO declara o run encerrado — a pré-condição de entrada de "Após TODAS as tasks concluídas" NÃO foi satisfeita (tasks_completed=6, tasks_total=8, zero bloqueadas). O desarme é para que o gancho de Stop não bloqueie o encerramento do turno numa pausa legítima. A skill rearma na Inicialização ao retomar.`
- `[run] sdd_state.yaml permanece em `execution: in_progress` — o run NÃO foi marcado como completed.`
- `[run] instruções de retomada em _run/RETOMADA.md`
- `[run] HEAD não se moveu: 1fcb33c01eb9c4f1898e0950a22217af9ae86d94. Nada commitado; 46 caminhos staged, à espera da decisão do usuário.`

---

## ▶️ RETOMADA — 2026-09-02, Fase 3 (lote paralelo T7 ∥ T8)

- `[run] retomada a partir da pausa controlada. Sinais de resume conferidos: NENHUMA task em `Em Progresso`, `_run/tmp/` VAZIO, HEAD imóvel em 1fcb33c. Nenhuma das três condições do Passo 4.0.1 disparou ⇒ não há resume de task interrompida a resolver; o run simplesmente continua na próxima fase.`
- `[run] guarda de continuidade REARMADA (`_run/.run-ativo`).`
- `[run] executor resolvido: sysloc-backend-implementer (origem: argumento explícito)`
- `[run] executor_discipline injetado (fonte: references/executor-discipline.md)`
- `[run] cleanup idempotente: `_run/tmp/` já vazio; `docs/specs/**/_run/tmp/` consta no .gitignore (linha 48)`
- `[Fase 3] lote paralelo: T7, T8 — guards RE-VERIFICADOS um a um, não herdados da coluna:`
  - `(1) mesma fase (3) ✓`
  - `(2) independência no DAG: T7.deps = T8.deps = {T4,T5,T6}; nenhuma é ancestral/descendente da outra ✓`
  - `(3) disjunção de símbolo: T7 cria `paresDoCicloDeVidaDoMaster` (privado da suíte) e T8 consome N/A; interseção vazia nos dois sentidos ✓`
  - `(4) paths disjuntos: T7 = {apps/api/test/cobertura-de-autorizacao.e2e.spec.ts, CLAUDE.md, packages/shared/test/protocolo-antirregressao.spec.ts} · T8 = {docs/adr/0014-*.md, docs/plano-backend-novo/handoff-master-frontend.md} ✓ (o CLAUDE.md é REFERÊNCIA de leitura da T8 — a §4 dela exige literalmente que ele NÃO seja tocado)`
  - `(5) alta contenção: nenhuma toca DI/router/barril/manifest/migrations ✓`
  - `(6) MAX_PARALLEL=4, lote de 2 ✓`
- `[Fase 3] ⚠️ acoplamento POR SUÍTE medido e REFUTADO: `protocolo-antirregressao.spec.ts` não lê NADA sob `docs/` (grep `docs/` no arquivo = 0 ocorrências), de modo que a emenda de ADR da T8 não pode mover caso algum do `shared` que a T7 mede.`
- `[Fase 3] base_sha=1fcb33c01eb9c4f1898e0950a22217af9ae86d94 (o mesmo do run — HEAD não se moveu desde o início da fatia)`
- `[T7] base_sha=1fcb33c01eb9c4f1898e0950a22217af9ae86d94`
- `[T8] base_sha=1fcb33c01eb9c4f1898e0950a22217af9ae86d94`
- `[T7] executor: opus (declarado no frontmatter) · gates: [qa, tech_review] (declarado) · risk: medium · diff_touches_critical_path=TRUE (cobertura-de-autorizacao ⇒ categoria security/authorization) ⇒ qa_model=opus, tech_model=opus`
- `[T8] executor: opus (declarado no frontmatter) · gates: [qa] (declarado) · risk: low · diff_touches_critical_path=FALSE (só docs) ⇒ qa_model=opus por decisão do CLAUDE.md ("onde a regra mandar sonnet, leia opus")`
- `[T7] ADRs injetadas no executor: ADR-0011, ADR-0018, ADR-0039 (fonte: task §7)`
- `[T8] ADRs injetadas no executor: ADR-0014, ADR-0038, ADR-0017 (fonte: task §7)`
- `[Fase 3] decisão auto-resolvida (A1): os QAs do lote rodam em PARALELO ou serializados? → adotada a recomendada: SERIALIZADOS (T7 depois T8) · razão: o guard formal da rule não dispara (só a T7 tem §6.3 não-vazia), mas o precedente MEDIDO deste repositório é contrário — o `db` já saiu 289 em vez de 290 numa execução que corria em paralelo com outra suíte no mesmo host, e o custo de serializar o QA de uma task de DOCUMENTO é desprezível diante de queimar tentativa por flake.`
- `[Fase 3] dispatch_parallel: 2 executores em paralelo (T7 e T8), numa única mensagem. T7 e T8 marcadas `Em Progresso` no task_plan.md e na §1 de cada card ANTES do despacho — é o marcador que permite detectar execução interrompida num resume.`
- `[T8] executor: opus · retornou primeiro (760s). 3 arquivos modificados, 0 criados. Rotas sob /v1/master MEDIDAS: 13 (administrador.controller.ts 5 + empresa.controller.ts 8), conferidas por igualdade de conjunto contra a tabela da §4 do handoff.`
- `[T7] executor: opus · retornou depois (2040s — a baseline P1/P5 dos 9 pacotes é o custo). 4 arquivos modificados, 0 criados. P1 total 2070 → P5 total 2093, os 9 pacotes verdes, skipped/todo em zero.`
- `[T7] ⚠️ TRÊS divergências DECLARADAS E MEDIDAS pelo executor, que o gate lê como declaração e não como desvio:`
  - `(1) uma QUARTA âncora se moveu, além das três da §3.2: ROTAS_PUBLICADAS_NO_MUTANTE 100 → 107 (é a montagem do CT-213 (b), que carrega o AppModule inteiro). Sem ela o CT-213 (b) seguiria vermelho. Não acrescenta caso.`
  - `(2) entrarComSegundoFatorCumprido NÃO foi usado no CT-1241, contra a letra da §6.6 — medido: segundoFatorExigido (packages/auth/src/admissao.ts) é `perfil === 'SYSLOC_MASTER' && !doisFatoresAtivo`, e NÃO alcança ADMIN_EMPRESA. Aplicá-lo ATIVARIA o segundo fator numa pessoa da carga compartilhada para obter uma sessão que a entrada simples já dá plena. Usou a forma do análogo que o próprio card nomeia (ciclo-de-acesso CT-221) e do CT-837 do mesmo arquivo, com a plenitude AFIRMADA por GET /v1/sessao (senhaProvisoria:false, segundoFatorPendente:false) em cada perna.`
  - `(3) sem campanha de mutantes para CT-1240/CT-1241 — nenhum dos dois inspeciona o TEXTO do código, logo a prova de falsificação por execução não se aplica (fronteira do P4 da nao-regressao.md). A falsificação do CT-1240 é o DEFEITO REAL medido na baseline: 14 casos reprovando com `expected 113 to be 106`, `expected 98 to be 91`, `expected 107 to be 100` e o CT-318 nomeando os sete pares.`
- `[Fase 3] contabilidade pós-executor: git status --porcelain SEM untracked novo; NENHUM arquivo criado fora do escopo declarado pelas duas tasks. Verificado por medição direta: as 3 âncoras em 113/98/20, a 4ª em 107, o describe count de protocolo-antirregressao.spec.ts INTACTO em 13 (CT-902 sobrevive) e paresDoCicloDeVidaDoMaster presente.`
- `[Fase 3] ⚠️ três arquivos aparecem no diff não-staged sem serem de T7 nem de T8 — `deploy/scripts/instalacao/criar-sysloc-master.mjs`, `.sh` e `docs/adr/INDEX.md`. Eles JÁ ESTAVAM modificados quando este run começou (constam no snapshot de git status da abertura da sessão, com marca ` M`). Registrados como mudança prévia não relacionada; NÃO bloqueiam e NÃO entram na lista `arquivos` de gate algum.`
- `[T7] Gate 1 (QA) despachado · model=opus (diff_touches_critical_path) · scan_scope=FULL (rodada 1) · lista `arquivos` = o diff REAL apurado pelo orquestrador, com os 3 arquivos de mudança prévia e os 3 da T8 EXPLICITAMENTE excluídos`
- `[T7] as TRÊS divergências declaradas foram repassadas ao QA como matéria de JULGAMENTO, não como fato aceito — com o ponteiro para o fonte a conferir em cada uma (packages/auth/src/admissao.ts na (2); a fronteira do P4 da nao-regressao.md na (3))`
- `[T7] instrução extra ao QA: AP-24 na DIREÇÃO INVERSA. Esta task fecha 14 vermelhos MUDANDO CONSTANTES — o gate tem de conferir que 113/98/20/107 são MEDIÇÃO e não número escolhido para a suíte passar, e que os dois eixos do CT-1240 são de fato INDEPENDENTES (se derivarem da mesma varredura, a independência é aparente e é achado).`
- `[T8] Gate 1 (QA) NÃO despachado ainda — decisão A1 da abertura desta fase: os QAs do lote correm serializados, e o da T7 é o que roda suíte pesada.`
- `[T7] QA (rodada 1, scan_scope=FULL) = APROVADO_COM_OBSERVACOES` · critérios **9/9** · CTs **3/3** · bloqueantes **0** (zero críticos, zero altos, zero médios) · anotáveis **1** (`BAIXO-001` baixo/tests, `smell: semantically_duplicated_test`) · `security_flags: []` · `escopo_testes: SUITE_COMPLETA` · `tocou_area_critica: true`
- `[T7] SUÍTE: os 9 pacotes medidos um a um pelo script de cada um, TODOS VERDES, skipped/todo em zero — contracts 455 · api 455 · shared 309 · db 296 · worker 180 · documentos 159 · auth 95 · cobranca-bancaria 114 · regua 30 = **2093**, idêntico ao P5 do executor e ao que o CLAUDE.md agora narra. ⚠️ Os 14 vermelhos da baseline FECHARAM, todos, e nenhum caso verde ficou vermelho em pacote algum.`
- `[T7] ⚠️ AP-24 NA DIREÇÃO INVERSA — a checagem central desta task, e o QA a fez ESTRUTURALMENTE, não por confiança: os dois eixos do CT-1240 são genuinamente independentes — o primeiro lê `printRoutes()` do adaptador (`verificarCoberturaDeAutorizacao`), o segundo varre decoradores por `DiscoveryService` + `MetadataScanner` + `Reflect.getMetadata(METHOD_METADATA)` (`manipuladoresDe`) —, nenhum consome o outro, e a igualdade ENTRE eles é afirmada à parte e ANTES da comparação com a constante. Mais: o tamanho não é a única âncora — `cobertura.comExigencia` é comparado por igualdade contra `ROTAS_COM_EXIGENCIA` e `cobertura.publicas` contra `PARES_PUBLICOS_ACEITOS`, de modo que a IDENTIDADE de cada rota está ancorada, não só a contagem.`
- `[T7] as TRÊS divergências declaradas foram SUSTENTADAS, uma a uma, com conferência independente no fonte:`
  - `(1) quarta âncora — o CT-213 (b) afirma ROTAS_PUBLICADAS_NO_MUTANTE em valor exato e sem o movimento seguiria vermelho. O valor 107 é coerente com a medição: a montagem mutante importa o AppModule inteiro (ganha os 7) e não registra os 9 de ROTAS_FORA_DO_ARCABOUCO → 113−9=104, mais os 3 controladores de verificação = 107. O SUT_IS_CORRECT_BECAUSE está escrito no ponto e declara «Medido, e não derivado». PUBLICAS_LEGITIMAS_NO_MUTANTE corretamente NÃO recebeu nenhum dos 7.`
  - `(2) entrarComSegundoFatorCumprido — conferido em packages/auth/src/admissao.ts:171-173: `segundoFatorExigido` é `perfil === 'SYSLOC_MASTER' && !doisFatoresAtivo` e NÃO alcança ADMIN_EMPRESA. Aplicá-lo teria EFEITO COLATERAL em outras suítes (ativaria o segundo fator numa pessoa da carga compartilhada). O caminho usado é o legítimo e a plenitude é AFIRMADA em cada perna por GET /v1/sessao — é o que separa o 403 da guarda de perfil do 403 da restrição de sessão.`
  - `(3) sem campanha de mutantes — os dois CTs são COMPORTAMENTAIS pela fronteira do P4 (o CT-1240 introspecta uma aplicação Nest MONTADA; o CT-1241 atravessa HTTP real), e a falsificação do CT-1240 não é argumento: foi MEDIDA na linha de base, com os 14 reprovando por `expected 113 to be 106` / `expected 98 to be 91` / `expected 107 to be 100`. ⚠️ Já o CT-1196 estendido É ESTÁTICO e carrega falsificação EM SUÍTE: 10 pernas de mutante, uma por par (ocorrência, eixo), com substituição pela posição exata do grupo capturado e igualdade sobre a lista inteira de divergências — o que também prova que os quatro moldes são DISJUNTOS.`
- `[T7] ⚠️ A QUINTA lacuna da família §6.4 NÃO ACONTECEU. O docblock do CT-1241 nomeia os dois cenários literalmente («§6.4, cenário 1» para o 403 com `detalhes: { exigido: 'PERFIL:SYSLOC_MASTER' }`; «§6.4, cenário 2» para o 401 SEM `detalhes`), e as duas pernas existem dentro da mesma it.each, com o corpo INTEIRO por igualdade nas duas e a remedição das três contagens depois. A intervenção do orquestrador na abertura da fase — declarar ao executor a coluna de CT que a §6.4 não tem — fechou a classe que custou quatro rodadas nesta fatia.`
- `[T7] ADR-0018 atendida com o eixo que a ADR pede: RETRATO_DEVIDO_POR_MANIPULADOR_DO_MASTER é escrito à mão, independente da aplicação, e afirma não só O QUE cada um dos 7 manipuladores exige mas a ORIGEM (`classe`, nunca `metodo`) — a diferença que `getAllAndOverride` torna invisível por comportamento. `exigenciaEfetivaDoManipulador` LEVANTA quando o rótulo não existe, fechando a comparação de ausência com ausência. E a asserção de que PERFIL:SYSLOC_MASTER não é nenhuma das 17 chaves do catálogo fecha a saída que a ADR-0011 rejeita por escrito.`
- `[T7] ADR-0039 — a Decision foi ABERTA e conferida contra as citações (o CLAUDE.md e o INDEX.md são paráfrases). As emendas reproduzem fielmente o alcance e a Cons que esta task materializa. Nenhuma rota da superfície da imobiliária foi tocada.`
- `[T7] as 4 ocorrências normativas conferidas UMA A UMA no arquivo real (L83 prosa da F5, L92 bullet Superfície, L526 marco item 2, L790 bloco de débitos), todas com o texto original PRESERVADO em bloco de emenda datado. ⚠️ A HISTÓRICA (L566, auditoria de drift do handoff) NÃO foi reescrita — segue dizendo `91 manipuladores` — e ganhou a marca «Os números desta frase são a MEDIÇÃO DE 2026-08-24 e NÃO se reescrevem». O docblock de OCORRENCIAS_NORMATIVAS ancora cada molde na FRASE INTEIRA que o cerca, justamente para não casar a menção histórica.`
- `[T7] antivácuo e CT-902: o CT-1196 afirma MEDICOES_NORMATIVAS_DA_SUPERFICIE = 10 e a igualdade dos 4 nomes de ocorrência ANTES das pernas, de modo que declaração esvaziada REPROVA em vez de sumir. Entrou como pernas em describe EXISTENTE — o arquivo continua com 13 describe e o CT-902 saiu verde.`
- `[T7] antipadroes_verificados: 2/2 arquivos de teste declarados (cobertura-de-autorizacao.e2e.spec.ts e protocolo-antirregressao.spec.ts) — sweep COMPLETO, nada a observar no Passo 4.1.`
- `[T7] Ledger de Achados: NÃO criado. A rodada 1 aprovou sem rejeição alguma, e a rule é explícita — sem rejeição não há achado bloqueante a rastrear, e o anotável já vai para a §2. Não é lacuna. Pela mesma razão, a métrica do Passo 4.3 NÃO é logada para esta task.`
- `[T7] débito anotado: D28 · baixo · tests · QA (smell semantically_duplicated_test / AP-26) escriturado na §2 do run-report.md com arquivo:linha e correcao_sugerida preservados. ⚠️ O QA foi explícito em que NÃO é duplicata de teste inteiro e que é convenção PRÉ-EXISTENTE da rule ancoras-de-superficie.md, não defeito introduzido pela T7 — fica anotado porque a próxima fatia que publicar rota acrescenta a 15ª cópia.`
- `[run] rule_candidates: +1 sinal (RC-001, repeated_assertion_shape, origem agent-spec-qa-validator)`
- `[T7] Gate 2 (Tech Review) despachado · model=opus (diff_touches_critical_path + qa aprovado) · scan_scope=FULL (1ª passagem dele) · retry_attempt=0`
- `[T7] ⚠️ INSTRUÇÃO CRÍTICA DE DIFF passada ao TR, e ela é específica desta fatia: `git diff <base_sha> -- <path>` NÃO é o delta da T7, porque as SEIS tasks anteriores estão staged sem commit e o HEAD não se moveu — para cobertura-de-autorizacao.e2e.spec.ts ele traria também T4, T5 e T6. O delta EXATO é `git diff -- <path>` (working tree contra o índice), justamente porque tudo das anteriores já está staged. Sem esta instrução o gate revisaria do zero código que ele mesmo já aprovou em três rodadas.`
- `[T8] Gate 1 (QA) despachado · model=opus (decisão do CLAUDE.md) · scan_scope=FULL (rodada 1) · EM PARALELO com o Gate 2 da T7, e isso NÃO viola a decisão A1 de serialização: o que ela serializa são os QAs que rodam SUÍTE, e o Tech Review não executa suíte (o contrato dele só a re-executa em 3 condições, nenhuma satisfeita aqui — o QA da T7 saiu com executou_testes:true e escopo SUITE_COMPLETA).`
- `[T8] instrução central ao QA: o critério 7 (cada uma das 13 rotas confere com o código) é o coração da task e o mais fácil de aprovar por leitura superficial — este handoff é o documento a partir do qual OUTRO agente, em OUTRA máquina, implementa o frontend, e rota errada aqui é defeito que EMBARCA sem que suíte alguma deste repositório o pegue. Mandado conferir rota a rota no fonte: verbo e caminho do decorador, corpo contra o esquema Zod (com a rule contrato-publicado.md em mão), forma da resposta e envelope da ADR-0017.`
- `[T8] instrução extra: julgar se a correção do bloco de abertura, §2, §4.2, §5 e §6 do handoff — além da §4, §7 e §8 nominalmente autorizadas — é conclusão dos critérios 5 e 7 ou alargamento de escopo.`
- `[T7] Tech Review (rodada 1, scan_scope=FULL) = APROVADO_COM_OBSERVACOES` · bloqueantes **0** · anotáveis **1** (`P1` BAIXO/project_pattern) · `adrs_consultadas: ADR-0011, ADR-0018, ADR-0039`
- `[T7] TR — o gate VERIFICOU NO ARQUIVO, não pela alegação, e a tabela de conferência dele merece registro: `paresDoMaster()` extraído de HEAD e comparado contra a árvore = **IDÊNTICO, 13 linhas**; os **10 moldes do CT-1196 rodados contra o CLAUDE.md REAL**, os dez casando e devolvendo 113/98/20; `ls docs/adr/0*.md` = 39 e `grep '^status:'` = 32 accepted; `grep -c '^describe('` = 13; e `git diff | grep -E '^-.*(DECISÃO FECHADA|DÉBITO COM GATILHO)'` = **VAZIO**.`
- `[T7] TR — garantia removida: as 9 linhas `-` do spec e as 19 do CLAUDE.md lidas UMA A UMA. Todas são redeclaração de constante com docblock novo, extensão de cadeia de filtro, ou prosa reemitida com emenda. ⚠️ O único comentário retirado — «É a **última** subtração que esta conta recebe» — foi SUBSTITUÍDO por outro que declara por que a afirmação anterior caiu («ela o era sob a leitura do congelamento sem a qualificação da ADR-0039»). A contagem de advertências ⚠️ no CLAUDE.md SUBIU de 124 para 138: nenhum fato medido foi apagado.`
- `[T7] TR — anti-gaming no eixo de maior risco (14 vermelhos fechados movendo constantes): nenhuma asserção afrouxada. ⚠️ A subtração acrescentada em EXIGENCIA_ANTERIOR_A_FATIA (`!PARES_DO_CICLO_DE_VIDA_DO_MASTER.includes(par)`) vem acompanhada da asserção POSITIVA compensatória, que afirma os sete por igualdade de arranjo — sem ela, um par que sumisse escaparia pelo filtro. A conta de ROTAS_PUBLICADAS_ANTES_DA_FATIA continua comparando contra o MESMO valor (33).`
- `[T7] TR — as três divergências julgadas de forma INDEPENDENTE e as três sustentadas. Sobre a (1), o gate acrescentou o que o QA não tinha: ela NÃO é scope_deviation porque a rule ancoras-de-superficie.md fixa a granularidade da declaração no ARQUIVO, não em cada constante — «a lacuna é da §3.2 do card, que enumerou três constantes onde o arquivo tem quatro, não do executor». Sobre a (2): «o caso troca o auxiliar por uma garantia MAIS FORTE, e não mais fraca».`
- `[T7] TR — Lei do seam conferida: EMPRESA_B e esquemaIdentidade JÁ eram exportados pelo barril de @sysloc/db em HEAD, com dezenas de consumidores pré-existentes. Nenhum símbolo de produção nasceu para servir a esta suíte.`
- `[T7] TR — antivácuo do retrato de exigências: `exigenciaEfetivaDoManipulador` LANÇA quando o rótulo não é encontrado na varredura da aplicação real, e lança de novo quando nem método nem classe declaram — logo os nomes de classe de RETRATO_DEVIDO_POR_MANIPULADOR_DO_MASTER são cruzados contra o app montado, e a igualdade do CT-1241 não pode passar por vacuidade.`
- `[T7] débito anotado: D29 · BAIXO · project_pattern · Tech Review, escriturado na §2 com arquivo:linha e suggested_fix preservados.`
- `[T7] Ledger de Achados: NÃO criado — os dois gates aprovaram na rodada 1, sem rejeição alguma. Métrica do Passo 4.3 NÃO logada, pela mesma razão.`
- `[T7] staged: CLAUDE.md, apps/api/test/cobertura-de-autorizacao.e2e.spec.ts, packages/shared/test/protocolo-antirregressao.spec.ts, docs/specs/features/painel-master-administradores/v1/tasks/T7.md`
- `[T7] CONCLUÍDA — 1 rodada, os dois gates aprovando de primeira. QA: APROVADO_COM_OBSERVACOES. TR: APROVADO_COM_OBSERVACOES.`
- `[run] rule_candidates: +1 sinal (RC-001 do staff, convention_drift — tema distinto do RC-001 do QA, sem dedupe aplicável)`
- `[T8] QA (rodada 1, scan_scope=FULL) = REJEITADO` · critérios **6/7** (`CA-07` PARCIAL) · bloqueantes **1** (`ALTO-001` ALTO/logic) · anotáveis **0** · `security_flags: []` · `escopo_testes: PARCIAL` (shared 309/309, decisão declarada) · `tocou_area_critica: false`
- `[T8] ⚠️ O ACHADO É REAL, e o orquestrador o CONFERIU NO FONTE antes de despachar a correção — não o aceitou pela alegação: `validar()` (apps/api/src/comum/validacao.ts:57-62) é `campo: caminho.length > 0 ? caminho.join('.') : campoPadrao`; `CAMPO_DA_CONSULTA = 'limite'` (administrador.controller.ts:132); e o `CT-1245` (master-administradores.e2e.spec.ts:1050-1057) pede `?deslocamento=-1` e afirma `campo: 'deslocamento'` por igualdade de corpo. A linha 819 do handoff diz `campo: "limite"` para os TRÊS casos agrupados. Duas conferem; a do meio não.`
- `[T8] ⚠️ POR QUE É ALTO E NÃO COSMÉTICO, e a razão vale registrar: este handoff é o documento a partir do qual OUTRO agente, em OUTRA máquina, implementa o Painel Master. Um frontend que roteie a mensagem de erro para o input pelo `campo` do envelope destacará o CONTROLE ERRADO — e nenhuma suíte deste repositório pega a divergência, o que o próprio gate MEDIU (grep por `handoff-master-frontend` sobre *.spec.ts em apps e packages, e sobre deploy/scripts, retorna vazio nas duas pontas).`
- `[T8] o gate SUSTENTOU o resto, e a correção NÃO deve reabrir: ADR-0014 com 22 inserções e ZERO remoções (critério 1, por diff dirigido); a emenda não contradiz a Decision e a citação do Consequences→Neutros da 0038 foi conferida palavra por palavra (critério 2); CLAUDE.md não tocado (critério 6); 12 das 13 rotas e 26 das 27 afirmações de `campo` conferem (critério 7); e o ALARGAMENTO DE ESCOPO NÃO EXISTIU — a correção da §2, da abertura, da §4.2, da §4.6, da §5 e da §6 é conclusão dos critérios 4, 5 e 7, e nenhum OUTRO documento foi tocado, que é o que a §3.2 do card proíbe.`
- `[T8] o gate JULGOU a justificativa de N/A da §6 e a sustentou (AP-23), mas registrou por honestidade uma ressalva que vira matéria do Gate 2 desta task... exceto que a T8 é `gates: [qa]` e não TEM Gate 2. ⚠️ O orquestrador a recolhe: «o raciocínio ‘duplicaria a cobertura’ cobre a prova da SUPERFÍCIE, não a da CORRESPONDÊNCIA entre o documento e ela — e é exatamente nessa lacuna que o ALTO-001 mora». Vira débito na §2 se sobreviver à rodada 2.`
- `[T8] ### T8 — retry classification`
  - `attempt: 1`
  - `problemas_por_categoria: { logic: 1 }`
  - `requires_qa_revalidation: true`
  - `decisao: RE-QA (a rejeição veio do Gate 1; rejeição de QA SEMPRE re-passa pelo QA — o algoritmo de skip só se aplica a rejeição do Gate 2)`
- `[T8] attempt_sha (rodada 2)=eb8894e8ee38ef14b4956d0cba2a45326227f4fd` — capturado pelo índice temporário (GIT_INDEX_FILE + write-tree + commit-tree, com `git rev-parse --git-path index`), NUNCA `git stash create`. Working tree e índice do usuário intactos.
- `[T8] modelo do executor de correção: opus. ⚠️ Divergência de leitura DECLARADA: a regra de auto-escalate condiciona a `resolved_model == "sonnet"`, e o CLAUDE.md manda ler `opus` onde a regra do framework disser `sonnet` — sob essa leitura, `last_severity: ALTO` dispararia `opus[xhigh]`. A ferramenta `Agent` não aceita `opus[xhigh]` como valor de `model`; o efeito de raciocínio estendido foi pedido NO PROMPT, e o modelo permanece `opus`.`
- `[T8] convergência (Passo 4.0): NÃO se aplica — attempt_count era 0 na interpretação deste veredito, logo a rodada era 1. A regra vale a partir da rodada 3. E o único bloqueante é ALTO, que não converge em rodada alguma.`
- `[T8] executor de correção (rodada 2): opus · diff de 26 linhas no handoff (4 pontos) + 8 no card. ADR-0014, CLAUDE.md e docs/adr/INDEX.md INTOCADOS.`
- `[T8] ⚠️ A correção ACHOU UMA SEGUNDA DIVERGÊNCIA que o gate não pegou, e ela é da mesma classe: a §3.4 afirmava `campo: "code"` para código do segundo fator mal formado, e o servidor responde `"codigo"` — `CAMPO_DO_CODIGO_NO_ERRO = 'codigo'` (packages/auth/src/autenticacao.ts:502,1326), contraditado por asserção executável em sessao-restrita.e2e.spec.ts:504-509 (corpo inteiro, 422). Ampliação declarada do CA-07, e o orquestrador a CONFERIU no fonte antes de despachar a revalidação.`
- `[T8] ⚠️ E o executor corrigiu um erro DELE MESMO, da mesma classe do P1, antes de fechar: a primeira redação da regra de campo padrão GENERALIZAVA para `"corpo"`, quando ela é declarada POR ROTA — `'corpo'` nas rotas do Master com corpo, `'limite'` nas duas listagens e `'senha'` na troca (senha.controller.ts:182,291). Registrado porque é exatamente o modo de falha que o achado original nomeia: agrupar por status o que difere por campo.`
- `[T8] Gate 1 (QA) rodada 2 despachado · model=opus · scan_scope=**DELTA** · attempt_sha_anterior=eb8894e8ee38ef14b4956d0cba2a45326227f4fd · delta_arquivos=[handoff-master-frontend.md, tasks/T8.md] · memória lazy passada (contém o Ledger)`
- `[T8] ⚠️ o RAIO DE IMPACTO desta task tem forma peculiar e foi enunciado ao gate: o entregável é um documento que DESCREVE CÓDIGO, logo o raio é o FONTE que as afirmações novas citam (validacao.ts, administrador.controller/contrato.ts, empresa.controller.ts, autenticacao.ts, senha.controller.ts). Conferir a afirmação nova contra o fonte É a validação, não um extra.`
- `[T8] instrução de risco passada ao gate: «uma regra enunciada errada é PIOR que a linha errada que ela substitui, porque ela se aplica a toda a superfície» — o campo padrão por rota é a armadilha desta rodada, e o gate foi mandado verificar a lista no fonte em vez de aceitar a declaração.`
- `[T8] QA (rodada 2, scan_scope=DELTA) = APROVADO_COM_OBSERVACOES` · critérios **7/7** (o `CA-07` saiu de PARCIAL) · bloqueantes **0** · anotáveis **1** (`MED-001` MEDIO/documentation — categoria **ANOTÁVEL** pela partição) · `security_flags: []`
- `[T8] QA rodada 2 — o QA-ALTO-001 foi SANADO e verificado NO FONTE, não pela alegação: a linha agrupada virou TRÊS (handoff:839-841), e `ESQUEMA_DA_JANELA` (administrador.contrato.ts:130-138) declara `limite` com `.int().min(1).max(...)` e `deslocamento` com `.int().min(0)` — os dois produzem issue com path próprio, e só a chave desconhecida (unrecognized_keys, caminho vazio) cai no CAMPO_DA_CONSULTA.`
- `[T8] ⚠️ QA rodada 2 — A ARMADILHA QUE O ORQUESTRADOR SINALIZOU FOI CONFERIDA POR INVENTÁRIO EXAUSTIVO, e é o achado de método desta rodada: o gate leu os **19 pontos de chamada de `validar()`** em `apps/api/src/master/` e enumerou os três valores de campo padrão que existem — `administrador.controller.ts`: 'id' (181,219,253,287,350), 'limite' (185), 'corpo' (216,250,291); `empresa.controller.ts`: 'corpo' (398,446,527), 'limite' (413), 'id' (442,497,523,551,570,596). «Não há um quarto valor.» E a exceção confere: a troca de senha usa CAMPO_PADRAO_DA_TROCA = 'senha' (senha.controller.ts:182,291) e NÃO 'corpo' — «a generalização que o executor diz ter corrigido antes de fechar teria sido falsa, e ela de fato não está no texto entregue».`
- `[T8] QA rodada 2 — a ampliação da §3.4 julgada LEGÍTIMA e não alargamento: mesma classe de defeito, mesmo arquivo entregável, divergência declarada e medida, nenhum outro documento tocado. CAMPO_DO_CODIGO_NO_ERRO = 'codigo' (autenticacao.ts:502,1326), contraditando a redação antiga, com asserção executável em sessao-restrita.e2e.spec.ts:504-509.`
- `[T8] QA rodada 2 — AP-24 na direção inversa: o delta tem EXATAMENTE DUAS remoções, e as duas eram falsas. O conteúdo verdadeiro da linha agrupada («`limite` acima de 50») foi PRESERVADO e AMPLIADO com precisão nova («abaixo de 1, ou não inteiro»), conferida contra `.int().min(1)`. As 27 demais afirmações de `campo` permanecem intactas.`
- `[T8] ⚠️ QA rodada 2 — o gate REFUTOU POR MEDIÇÃO uma falsa suspeita de alargamento que o próprio mecanismo de captura produz: o `git diff --name-only <attempt_sha>` lista sete arquivos além dos dois do delta, mas isso é ARTEFATO do `git add -A -- <task_paths>` do snapshot (arquivo fora dos paths aparece por diferir do HEAD, não por ter sido tocado). Os mtimes refutam: criar-sysloc-master.{mjs,sh} são de 2026-08-31 e docs/adr/INDEX.md de 2026-09-01, todos ANTERIORES à rodada. ⚠️ Vale registrar como propriedade do mecanismo, para a próxima fatia não a redescobrir.`
- `[T8] ledger: QA-ALTO-001 → **corrigido** (rodada_ultima_verificacao=2, rodada_origem preservada em 1) · MED-001 → **aceito_como_debito** (rodada_origem=2)`
- `[T8] ledger: 2 achados totais | 1 originado em rodada >1 | 0 suspeitos de incompletude da rodada 1` — o único de rodada >1 é o `MED-001`, e o `fingerprint` dele aponta para linhas que **a própria correção criou** (handoff:519 e 841, as duas remissões novas). Não existia na rodada 1.
- `[T8] convergência (Passo 4.0): NÃO se aplicou — a rodada era 2, e a regra vale a partir da 3. O único achado é MEDIO de categoria ANOTÁVEL, que não bloqueia em rodada alguma.`
- `[T8] Gate 2: NÃO se aplica — `gates: [qa]` declarado no frontmatter.`
- `[T8] staged: docs/adr/0014-exclusao-logica-para-entidade-de-cadastro-do-dominio.md, docs/plano-backend-novo/handoff-master-frontend.md, docs/specs/features/painel-master-administradores/v1/tasks/T8.md`
- `[T8] memória lazy DELETADA após a métrica do ledger ser registrada — nessa ordem, porque a métrica lê o arquivo que o cleanup apaga.`
- `[T8] CONCLUÍDA — 2 rodadas. QA: REJEITADO → APROVADO_COM_OBSERVACOES. Sem Gate 2 (gates=[qa]).`
- `[Fase 3] FECHADA. 8/8 tasks concluídas, ZERO bloqueadas.`
- `[run] §7 do task_plan verificada POR MEDIÇÃO, item a item, no fecho — 10 dos 11 critérios marcados.`
- `[run] ⚠️ UM CRITÉRIO DA §7 NÃO FOI MARCADO, E A RAZÃO É QUE ELE ESTAVA ERRADO AO SER ESCRITO: «O débito D32 · F5/T7 fechado — marcador E linha do índice removidos, duas pontas conferidas». A T3 pagou UMA das TRÊS metades (entrarComSegundoFatorCumprido, 6 cópias → 1); as outras duas (envelhecerOVigente e montarEmpresaComAdmin) seguem abertas no MESMO bloco, com o Limiar de Três ainda por disparar. Medido no fecho: o marcador segue vivo em acessorios-de-borda.ts, rotinas-agendadas.e2e.spec.ts, percurso-do-cliente-novo.e2e.spec.ts e entrega-da-noticia.e2e.spec.ts. ⚠️ Fechar pelo NÚMERO levaria as outras duas junto, que é precisamente o que a §3-B da nao-regressao.md adverte. O critério foi reescrito para dizer o que é verdade, em vez de ser marcado.`
- `[run] ⚠️ DOIS números da §7 divergiram do previsto, PARA MAIS, e os dois foram remedidos: os CTs são 46 (CT-1204 a CT-1249) e não 40 — os 6 extras nasceram de rodadas de gate; e os marcadores DECISÃO FECHADA são 12 linhas acrescentadas e não 2 — o previsto era o MÍNIMO, e os demais nasceram de fechos de gate, que é o gatilho que a §3 da nao-regressao.md nomeia.`
- `[run] índice de débito conferido nas TRÊS pontas no fecho: tabela do CLAUDE.md = 42 linhas · prosa «São **42**» (L760) · LINHAS_DO_INDICE_NO_FECHO_DA_FATIA = 42 (protocolo-antirregressao.spec.ts:1078). As três concordam, e o `shared` verde é a prova.`
- `[run] nenhuma migração criada nem alterada — `git status packages/db/migracoes/` vazio.`
- `[run] rule_candidates: 13 sinais persistidos em _run/rule-candidates.md (11 das sessões anteriores + 2 desta: qa=1, staff=1)`
- `[run] RUN ENCERRADO — 8/8, zero bloqueadas. sdd_state.yaml em `execution: completed`. Guarda de continuidade DESARMADA.`
