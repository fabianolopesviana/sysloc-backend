# Relatório do Run — regua-de-cobranca/v1

> Relatório para revisão humana. Telemetria de pipeline (base_sha, retries, paralelismo, rule mining) vive em `_run/workflow-report.md`.

## 1. Resumo do Run

> ▶️ **RUN RETOMADO em 2026-08-11**, pelo caminho **(a) Retomar nos gates**: a T3 foi direto ao Gate 2, sem reexecutar executor nem Gate 1. O Gate 2 aprovou, e a fase 2 do plano avança para a T4. Duas autorizações explícitas do usuário valem por todo este run: **nenhuma pausa para pergunta** (a resposta é sempre a recomendada) e **o limite de 3 tentativas está suspenso** — nenhuma task vai a `Bloqueado` por esgotamento.

Status: ✅ **12/12 tasks concluídas e aprovadas nos dois gates** · suíte TypeScript **1002 casos verdes**, medida **por pacote** (shared 218 · contracts 267 · auth 89 · db 147 · api 203 · worker 48 · regua 30) · frente shell **11 casos** no `verificar-golden.sh`, **10 aprovados** e o `CT-013` reprovando por causa **pré-existente** · `pnpm build` e `pnpm lint` verdes · **nenhuma task bloqueada**

> ▶️ **RUN RETOMADO em 2026-08-12**, de novo pelo caminho **(a) Retomar nos gates**: a T11 foi direto ao Gate 2, sem reexecutar executor nem Gate 1. O Gate 2 aprovou com dois baixos, e a fase 6 seguiu para a T12, que fechou a fatia.

**Crescimento da suíte ao longo do run: 835 → 1002 casos (+167), monotônico** — nenhum pacote encolheu em nenhuma rodada de nenhuma task.

| Task | Nome | Modelo | Arquivos | QA | Tech Review |
|------|------|--------|----------|-----|-------------|
| T1 | Extrair do sistema antigo o fonte do Server Script `PDF contrato` e provar o determinismo | opus | 3 criados, 6 mod | ✅ APROVADO_COM_OBSERVACOES (rodada 2) | ✅ APROVADO_COM_OBSERVACOES (rodada 2) |
| T2 | O contrato da automação de cobrança em `@sysloc/contracts` | opus | 1 criado, 3 mod | ✅ APROVADO_COM_OBSERVACOES (rodada 1) | ✅ APROVADO_COM_OBSERVACOES (rodada 1) |
| T3 | Schema e migrações da régua — 3 enums, 2 tabelas, RLS forçada e FK composta | opus | 3 criados, 8 mod | ✅ **APROVADO** (rodada 1, zero problemas) | ✅ APROVADO_COM_OBSERVACOES (rodada 1) |
| T4 | O pacote `@sysloc/regua` — o domínio declara as portas, a janela e a mensagem | opus | 11 criados, 2 mod | ✅ APROVADO_COM_OBSERVACOES (rodada 1) | ✅ APROVADO_COM_OBSERVACOES (rodada 1) |
| T5 | As portas de dados da régua — a política, o predicado no banco, o registro e o histórico | opus | 4 criados, 7 mod | ✅ APROVADO_COM_OBSERVACOES (rodada 1) | ✅ APROVADO_COM_OBSERVACOES (rodada 1) |
| T6 | A execução da régua e o adaptador de produção — a barreira que falha fechado | opus | 6 criados, 5 mod | ✅ APROVADO_COM_OBSERVACOES (**rodada 7**) | ✅ APROVADO_COM_OBSERVACOES (rodada 2) |
| T7 | O contrato da fila desce para `@sysloc/shared` — **fecha o D32 (F0/T6)** | opus | 1 criado, 9 mod | ✅ APROVADO_COM_OBSERVACOES (rodada 1) | ✅ APROVADO_COM_OBSERVACOES (rodada 1) |
| T8 | A borda do job no `worker` — o contexto vem da carga, e a partida falha fechado | opus | 2 criados, 16 mod | ✅ APROVADO_COM_OBSERVACOES (**rodada 3**) | ✅ APROVADO_COM_OBSERVACOES (rodada 2) |
| T9 | As duas rotas da política de aviso — `GET` e `PUT /v1/automacao-de-cobranca` | opus | 4 criados, 5 mod | ✅ APROVADO_COM_OBSERVACOES (rodada 1) | ✅ APROVADO_COM_OBSERVACOES (rodada 2) |
| T10 | O disparo manual e o histórico — a conjunção de exigências e a barreira na `api` | opus | 3 criados, 20 mod | ✅ APROVADO_COM_OBSERVACOES (rodada 1) | ✅ APROVADO_COM_OBSERVACOES (rodada 1) |
| T11 | A equivalência com o oráculo — dez vereditos escritos antes da execução | opus | 1 criado, 0 mod | ✅ APROVADO_COM_OBSERVACOES (rodada 2) | ✅ APROVADO_COM_OBSERVACOES (rodada 1) |
| T12 | A fronteira de autorização e o fecho da superfície em **86/71** | opus | 0 criados, 2 mod | ✅ APROVADO_COM_OBSERVACOES (rodada 2) | ✅ APROVADO_COM_OBSERVACOES (rodada 2) |

**Baseline do run**, medida antes da primeira edição (P1 do Protocolo Antirregressão), **por pacote** — `turbo run test` aborta os pacotes irmãos quando um falha e a contagem agregada não é confiável:

| Pacote | Casos |
|---|---|
| `@sysloc/shared` | 212 |
| `@sysloc/contracts` | 227 |
| `@sysloc/auth` | 89 |
| `@sysloc/db` | 115 |
| `@sysloc/api` | 176 |
| `@sysloc/worker` | 16 |
| **total** | **835** |

Bate exatamente com o número declarado no `task_plan.md`. Nenhum pacote encolheu em nenhuma rodada.

---

## 2. Débitos Técnicos Não Resolvidos

> Anotados pela política débito-controlado com bloqueio seletivo por categoria: baixos de qualquer categoria e médios de categoria anotável não bloqueiam. Resolva tudo de uma vez com `/agent-spec-debt-resolution docs/specs/features/regua-de-cobranca/v1/`.

### D1 · BAIXO · testability · T1 · Tech Review
- **Onde:** `deploy/scripts/caracterizacao/verificar-captura.sh` (`ct_603`, e a linha dele na lista de casos do `main`)
- **Problema:** o `CT-603` foi acrescentado ao `main()` de `verificar-captura.sh`, mas o `main` daquele arquivo aborta com `exit 1` antes do primeiro caso quando `contar_sites_efemeros() != 1` — o site efêmero precisa estar de pé. O `CT-603` **não tem dependência nenhuma** do site efêmero: ele lê a produção, roda o extrator duas vezes e compara `sha256sum`. Executor e QA o exercitaram por harness que carrega o arquivo sem a última linha (`main "$@"`) e refaz o setup de identidade; a linha `ct_603` dentro do `main` e a ordenação dela em relação a `ct_003` e `ct_012` são o único trecho da task sem execução pelo ponto de entrada real.
- **Impacto:** o caso mais sensível a prazo da fatia — o único que confere o determinismo enquanto a origem existe — depende, para rodar, de um preparo pesado e alheio a ele. Há contrapartida real: rodando dentro da janela `[t0, t1]`, a execução do extrator entra no escopo auditado pelo `CT-012` (atribuição de toda linha tocada em produção) e as saídas `extrair-*.out`/`.err` entram na varredura de credencial do `CT-013`. Mover o caso perderia essas duas coberturas cruzadas.
- **O que fazer:** **não mover o caso.** Registrar no cabeçalho do `CT-603` (no bloco "ONDE ELE RODA, E POR QUÊ") que a execução exige `preparar-site-efemero.sh criar` mesmo o caso não usando o site efêmero, e por qual razão; e, quando o site efêmero estiver de pé numa próxima janela, rodar `verificar-captura.sh` inteiro uma vez para exercitar a integração do `ct_603` no `main`.

### D2 · BAIXO · project_pattern · T1 · Tech Review
- **Onde:** índice do git — `docs/specs/features/caracterizacao-regras-legadas/v1/golden/contrato-pdf-fonte.py` e `.gitattributes`
- **Problema:** o executor fez `git add` real (stage, sem commit) na rodada 1. A regra é explícita: `.claude/rules/agent-spec-workflow-rules.md` → *"Stage real sequencial: após TODOS os gates aplicáveis do lote aprovarem, faça `git add`"*. A justificativa (o `CT-010`/`CT-433`/`CT-501`/`CT-601` afirmam `git ls-files` sobre `golden/`) é factualmente correta quanto à necessidade de visibilidade, mas o `git add -N` que o orquestrador já roda após cada executor torna o arquivo novo visível a `git ls-files` **sem consumar o stage**.
- **Impacto:** nenhum dano observável — o orquestrador encena os mesmos caminhos ao fim, e o mecanismo de `attempt_sha` usa índice temporário próprio (`GIT_INDEX_FILE`), imune a isso. O que se perde é a propriedade de que o índice reflete apenas o que já passou pelos gates: se o parecer resultasse em reversão, o rollback teria de desfazer também o índice.
- **O que fazer:** nada a reverter agora. Em tasks futuras, limitar-se a `git add -N` quando um caso precisar enxergar arquivo novo em `git ls-files`.

### D3 · BAIXO · code_quality · T1 · Tech Review
- **Onde:** `deploy/scripts/caracterizacao/extrair-fonte-do-pdf.sh` (`consultar_o_legado`) — **tem marcador `DÉBITO COM GATILHO` e linha no índice do `CLAUDE.md`**
- **Problema:** terceira cópia do caminho de leitura autenticada do legado. `extrair-fonte-do-pdf.sh` replica `compose()`, `no_container()`, o par `SERVICO`/`SERVICO_BANCO`/`DIR_BENCH`, o consultador autenticado (`consultar_o_legado` ≡ `consultar_banco_de_producao` de `verificar-captura.sh`) e a cadeia de identidade `default_site` → `db_name` → `db_password`. O `preparar-site-efemero.sh` já trazia a terceira cópia de `compose()`/`no_container()`. A réplica **não é literal**: a única divergência funcional é o `--raw` acrescentado ao `mysql`, necessário porque o `TO_BASE64` do MariaDB insere quebra a cada 76 caracteres, que o modo de lote escaparia como `\n` literal.
- **Impacto:** endurecer uma cópia deixa a outra para trás. O caminho tem três propriedades de segurança que precisam valer **juntas** — autenticar como usuário da base e nunca `root`, credencial por `stdin` e nunca em `argv`, e nunca ecoada — e elas agora vivem em dois lugares que ninguém obriga a se manterem iguais. É a mesma forma do `D1` e do `D26` já indexados no `CLAUDE.md`, cujo gatilho declarado é o terceiro consumidor.
- **O que fazer:** não refatorar dentro desta fatia. O marcador está emitido com `QUANDO FECHA: o quarto consumidor do caminho de leitura autenticada do legado, ou a primeira alteração das garantias de transporte da credencial`. Não existe biblioteca shell compartilhada neste projeto e criar uma seria ela própria um desvio; a `.claude/rules/testing-stack.md` documenta a replicação do vocabulário deste diretório como convenção aceita.

### D4 · médio · code_quality · T1 · QA
- **Onde:** `deploy/scripts/caracterizacao/verificar-golden.sh:1654` (metade 1 do `CT-640`)
- **Problema:** a metade 1 do `CT-640` carrega `capturar.py` por caminho (`spec_from_file_location` + `exec_module`). O `SourceFileLoader` grava o bytecode ao lado do fonte, de modo que **toda execução de `verificar-golden.sh`** cria `deploy/scripts/caracterizacao/__pycache__/capturar.cpython-312.pyc` (105 KB, binário) dentro do diretório versionado. Medido: removido o diretório e reexecutado o verificador, ele reaparece; `git check-ignore` confirma que **não** está coberto pelo `.gitignore` (que não tem `__pycache__` nem `*.pyc`), então ele aparece como `??` em todo `git status` posterior.
- **Impacto:** sem contaminação de asserção — reexecutado com o `__pycache__` presente, o `CT-013` mede as mesmas 72 ocorrências e o `CT-012` segue verde. O risco é de higiene e de **contrato**: o cabeçalho do próprio `verificar-golden.sh` declara *"lê apenas o que está versionado"*, e a `.claude/rules/testing-stack.md` manda o shell operar *"sempre em sandbox descartável — nunca na árvore de trabalho quando houver sandbox equivalente"*; a metade 2 do `CT-640` respeita isso exemplarmente e a metade 1 não. Um `git add` de diretório versionaria bytecode compilado de um script que lê a credencial do banco de operação. O Staff **avaliou e não escalou**: o `.pyc` é bytecode de um fonte já versionado em texto claro, não carrega credencial nem dado de runtime.
- **O que fazer:** uma linha resolve, no próprio heredoc: `sys.dont_write_bytecode = True` antes do `exec_module` (ou invocar o `python3` do `medir_autoria_do_manifesto` com `PYTHONDONTWRITEBYTECODE=1`). Alternativa equivalente: acrescentar `rm -rf "${DIR_SCRIPTS}/__pycache__"` ao `limpar()` do `trap EXIT`. **Ignorar por `.gitignore` fecha só a ponta do versionamento** e deixa o verificador escrevendo na árvore que ele existe para conferir — prefira a supressão na origem.

### D5 · BAIXO · testability · T1 · Tech Review (rodada 2)
- **Onde:** `deploy/scripts/caracterizacao/verificar-golden.sh:1699` (asserção de ponto único do `CT-640`)
- **Problema:** a medição do ponto único é `re.findall(chr(34) + 'PROCEDENCIA[.]md' + chr(34), fonte)` — `chr(34)` é a aspa **dupla**, e a expressão não alcança `'PROCEDENCIA.md'`. Considere o mutante **aditivo**: alguém acrescenta, em qualquer ponto de `capturar.py`, `(DIR_GOLDEN / 'PROCEDENCIA.md').write_text(...)` mantendo `gravar_procedencia` intacta. As nove asserções do `CT-640` passam todas — `literal_do_manifesto` = 1, `usos_da_constante` = 1, `uso_fora_do_ponto_unico` = vazio, e as seis comportamentais passam porque `medir_autoria_do_manifesto` chama `gravar_procedencia` diretamente e ela continua correta. O manifesto volta a ter dois autores com o `CT-640` inteiramente verde.
- **Impacto:** baixo e contido — mesmo com o `CT-640` verde, o efeito do segundo autor faria o `CT-601` reprovar na próxima execução do `verificar-captura.sh` (a §5 sumiria da árvore versionada). O que se perde não é a **detecção** do defeito, é a **atribuição** — que é precisamente o que tornava o defeito original silencioso e o que o `CT-640` existe para restituir. `capturar.py` tem 104 KB em aspas duplas e apenas 9 aspas simples no total, o que torna o escape improvável; mas a asserção declara *"nomeia o manifesto em um único ponto"*, e ela não prova isso.
- **O que fazer:** trocar o padrão por uma classe de aspas, mantendo o resto: `re.findall("['\"]PROCEDENCIA[.]md['\"]", fonte)` (ou `chr(34)+chr(39)` numa classe, para preservar o estilo `chr()` que o caso já usa e evitar colisão com o heredoc `<<'PY'`). O contrato do caso e a mensagem da asserção não mudam. A prova de falsificação exigida pela `.claude/rules/testing-stack.md` custa **um quarto mutante**: o aditivo de aspas simples, que hoje sobrevive e passaria a reprovar.

### D6 · BAIXO · project_pattern · T2 · Tech Review
- **Onde:** `docs/specs/features/regua-de-cobranca/v1/tasks/T2.md`, §6.5 linhas 161 e 163 — **as duas já corrigidas pelo orquestrador no fechamento da task**
- **Problema:** a tabela de rastreabilidade creditava **dois** mecanismos de prova inexistentes. A linha 161 (critério 7, *"saída aberta"*) apontava para uma varredura `ESQUEMAS_DE_SAIDA` — `grep -rn` sobre `packages/` e `apps/` devolve vazio, e o símbolo **nunca existiu em nenhuma fatia**. A linha 163 (critério 9, *"publicação símbolo a símbolo"*) apontava para `superficie-publica.spec.ts`, que **existe mas audita outro pacote**: ele vive em `packages/shared/test/` e confere a superfície do `@sysloc/shared`, não a do `contracts`. O QA pegou o primeiro; o segundo só apareceu no Gate 2.
- **Impacto:** rastreabilidade falsa em artefato de spec — quem auditar a fatia depois lê que dois critérios têm barreira automatizada quando não têm. O risco de propagação foi **medido e afastado**: `grep` pelas duas cadeias nas 12 tasks e no `tech_spec.md` devolve ocorrência **só na T2.md**, então as 10 tasks restantes não herdaram os fantasmas.
- **O que fazer:** ✅ **já feito** — a linha 161 passou a apontar para o `CT-604 (b)` (que é o que efetivamente prova o critério) e a 163 para a conferência de diff no Gate 2, declarando que **não há barreira executável** para `export *` neste pacote. **O que permanece aberto** é o débito de fundo, e ele é **PRÉ-EXISTENTE, anterior a esta fatia em três**: o lado da **entrada** tem barreira universal com âncora exata (`ESQUEMAS_DE_ENTRADA`, CT-336/CT-337, hoje em 15), e o lado da **saída não tem nenhuma**. Um esquema de saída declarado `strictObject` por engano numa fatia futura emitiria `additionalProperties: false` no documento da ADR-0016 e faria clientes gerados recusarem campo acrescentado naquelas rotas, **sem que nada na suíte acusasse**. Fechar isso é task própria — construir `ESQUEMAS_DE_SAIDA` nos moldes da irmã, com descoberta por símbolo exportado e âncora de contagem literal —, **não drive-by**: alcançaria todos os esquemas de saída do pacote, o que a proibição 5 do Protocolo Antirregressão veda dentro de uma task alheia.

### D7 · BAIXO · code_quality · T2 · Tech Review
- **Onde:** `packages/contracts/test/esquemas.spec.ts:2486` (e a menção da linha 205)
- **Problema:** a aritmética do docblock que narra o crescimento da tabela de recusas não fecha. Ele diz que a tabela *"cresceu de 14 para 20"* acrescentando *"os **cinco** moldes de hora que o aceite §4 enumera, a janela invertida e o inteiro fracionário"* — sete itens, e 14 + 7 = 21, não 20. A causa: `24:00` **já constava** do card (§6.6 do CT-605 o lista entre as faixas), então dos cinco moldes apenas **quatro** são novos, e 14 + 4 + 1 + 1 = 20, que é a contagem correta e verificada linha a linha. Secundariamente, a linha 205 chama a mesma tabela de *"as 22 recusas"*, número que só fecha somando os 2 casos de chave desconhecida que o próprio docblock declara estarem *"fora da tabela acima"*.
- **Impacto:** baixo e contido ao docblock — nenhuma asserção é afetada, e o crescimento de prova é real e correto. Mas o `CLAUDE.md` deste projeto registra **três** casos de número errado que circulou por artefatos (a superfície 75 × 77, a largura 4 × 5 do código de contrato, o D23 com cinco cópias e não duas), e o antídoto declarado é medir em vez de estimar. **Âncora que não fecha convida a próxima rodada a "corrigir" a contagem certa pela errada.**
- **O que fazer:** na linha 2486, trocar por *"os quatro moldes de hora que faltavam dos cinco que o aceite §4 enumera — `8:00`, `08:0`, `08:60` e `08:00:00`, já que `24:00` constava do card —, a janela invertida e o inteiro fracionário"*. Na linha 205, qualificar: *"as 22 recusas do CT-605 (20 na tabela e 2 de chave desconhecida)"*.

### D8 · BAIXO · project_pattern · T3 · Tech Review
- **Onde:** `deploy/scripts/instalacao/verificar-migracao.sh:186-192` (bloco de comentário datado de 2026-08-11, imediatamente acima de `TABELAS_DE_NEGOCIO_ESPERADAS`)
- **Problema:** o comentário novo afirma duas coisas que o repositório contradiz. **(a)** *"Esta lista é a MESMA `TABELAS_DE_NEGOCIO_ESPERADAS` que `packages/db/test/papel-de-conexao.spec.ts` declara"* — ela não é: a do shell tem **15** entradas, qualificadas por schema e incluindo `negocio.cobranca_derivada`; a do Vitest tem **14**, com nomes nus e **sem** a visão. O parágrafo seguinte do mesmo arquivo (*"A VISÃO ENTRA AQUI, e a sequência continua fora"*) existe justamente para explicar a diferença, de modo que a frase nova contradiz o vizinho imediato. **(b)** *"Esquecer uma delas faz a outra reprovar sozinha, que é a rede funcionando"* — as duas verificações são independentes e nenhuma observa a outra: esquecer a do Vitest reprova o Vitest; esquecer a do shell não reprova nada em `pnpm test`, porque `verificar-migracao.sh` não é invocado por `turbo run test` nem por `pnpm lint` (que sobre shell só roda `shellcheck`) — e nesta própria task ele não chegou a ser executado, por exigir `sudo` interativo.
- **Impacto:** um agente futuro que leia *"esquecer uma delas faz a outra reprovar sozinha"* conclui que existe detecção cruzada e deixa de conferir a lista do shell ao criar a próxima tabela em `negocio`. A lista deriva em silêncio e a divergência só aparece na próxima instalação do zero — caminho que o **D39 (F1/fechamento)** já mostra ser raramente exercitado. A frase (a) tem custo próprio: quem a levar ao pé da letra pode "alinhar" as duas listas removendo `negocio.cobranca_derivada` do shell, o que faria a asserção (b) do `CT-031` reprovar um schema íntegro — exatamente contra o que o parágrafo seguinte adverte.
- **O que fazer:** reescrever as duas últimas frases do bloco datado, mantendo o histórico intacto: *"Esta lista tem o MESMO NOME que a `TABELAS_DE_NEGOCIO_ESPERADAS` de `packages/db/test/papel-de-conexao.spec.ts`, e conteúdo deliberadamente diferente — a visão `negocio.cobranca_derivada` entra aqui e não lá, pela razão do parágrafo abaixo. As duas frentes se mantêm em paralelo à mão: nenhuma observa a outra, e esta aqui só reprova quando `verificar-migracao.sh` é executado, o que `pnpm test` não faz. O cabeçalho de `packages/db/src/esquema/negocio.ts` nomeia as duas e traz o comando que as encontra."*

### D9 · BAIXO · code_quality · T4 · QA
- **Onde:** `packages/regua/src/index.ts:35`
- **Problema:** `EmailCapturado` é publicado além dos **dez** símbolos que a §1 da T4 declara — o `index.ts` publica **onze**. A justificativa do executor (*"`capturas` precisa de nome no `.d.ts`"*) foi **refutada por medição**: o QA removeu o símbolo do barrel, apagou `dist/` e `tsconfig.tsbuildinfo`, rodou `tsc --build --force` e o build **passou**, com `dist/porta-de-email.d.ts` declarando o tipo de todo modo. A exportação é conveniência real, não exigência do compilador.
- **Impacto:** nenhum em runtime, e o Gate 2 julgou a escolha **correta** — publicar o elemento de um array já público evita o consumidor escrever `CapturadorDeEmail['capturas'][number]`. O que fica é **drift de artefato**: o campo `Símbolos públicos criados` da §1 alimenta a disjunção de símbolo do algoritmo de paralelismo, e um **superconjunto** no código é inofensivo (nunca produz falso-paralelo), mas a divergência ensina a próxima leitura a desconfiar do campo.
- **O que fazer:** acrescentar `EmailCapturado` à lista de símbolos públicos da §1 do `T4.md`, registrando que a razão é **ergonomia da superfície**, não necessidade de emissão. Não retirar do `index.ts`.

### D10 · BAIXO · code_quality · T4 · QA + Tech Review
- **Onde:** `packages/regua/src/mensagem.ts:82-88` (docblock de `MOTIVO_DE_ESTADO_NAO_AVISAVEL`)
- **Problema:** os **dois gates chegaram ao mesmo achado**, por caminhos independentes. O docblock justifica a constante dizendo que a cadeia é *"conferida por asserção (o companheiro negativo do CT-614) e **citada pela borda que traduz a recusa em `422`**. Três escritas do mesmo dizer ficariam livres para divergir."* Nenhuma das duas afirmações se sustenta: a constante **não é exportada** do módulo nem do `index.ts`, de modo que nem o teste nem a borda a alcançam. O CT-614 (b) redigita o literal em `test/mensagem.spec.ts:138` — o que ali é **correto e deliberado**, porque asserção que importa do SUT concorda consigo mesma —, e a borda, quando chegar, teria de redigitá-lo pela terceira vez ou discriminar um `Error` genérico por substring.
- **Impacto:** nenhum em runtime — o `throw` é a última linha de defesa de um caminho que o teste de admissão da T6 nunca deve alcançar. O custo é diferido: a **T6/T10 herdam duas representações possíveis da mesma recusa** (o `Error` genérico daqui e o `ErroDeEstadoNaoAvisavel` que nasce lá), e a tradução para `422` na borda passa a depender de qual delas chegou. Um docblock que afirma garantia inexistente também ensina a próxima rodada a confiar nela.
- **O que fazer:** **na T6**, quando `ErroDeEstadoNaoAvisavel` nascer em `packages/regua/src/regua.ts`, fazer o compositor levantá-lo — é **import interno do mesmo pacote**, sem dependência nova, o que dissolve inteiramente a razão que sustentou a decisão de usar `Error` genérico (`@sysloc/shared` não é dependência deste pacote). Enquanto isso não acontece, corrigir o docblock para o que a visibilidade sustenta: a constante existe para que o `throw` e um consumidor futuro **do mesmo pacote** saiam da mesma declaração, e a borda ainda não a alcança.

### D11 · BAIXO · project_pattern · T4 · Tech Review
- **Onde:** `docs/specs/features/regua-de-cobranca/v1/tech_spec.md:224` (mapa de arquivos da §3)
- **Problema:** a linha descreve `packages/regua/src/janela.ts` como *"A janela de horário, **e a normalização `HH:MM` do oráculo**"*. O arquivo entregue não tem `normalize_hhmm` nem equivalente, e **a ausência está correta**: o legado **coagia** a entrada malformada (`''`→`09:00`, `'7:5'`→`'07:05'`, `'2026-01-15T14:30:00'`→`'14:30'`, hora fora de faixa→fallback), e o produto novo **recusa**, em duas pontas independentes — a expressão ancorada `^([01]\d|2[0-3]):[0-5]\d$` de `packages/contracts/src/automacao-de-cobranca.ts` e a `politica_de_aviso_janela_chk` de `packages/db/migracoes/0011_dominio_regua.sql`. Quem ficou obsoleta foi a linha da spec, não o código. Nem a T4 (§3.4) nem a T6 (§3.1) repetem a atribuição; ela sobrevive só naquele mapa.
- **Impacto:** baixo e diferido, mas concreto: o mapa da §3 é material de leitura declarado das tasks vizinhas, e `janela.ts` aparece nos arquivos de referência da **T6** e da **T8**. Um executor que o tome como especificação pode **reintroduzir a coerção `HH:MM`** — exatamente o comportamento do legado que a fatia decidiu não portar —, e **nenhum CT desta fatia reprovaria**, justamente porque não há caso sobre normalização.
- **O que fazer:** emendar a linha 224 para `| packages/regua/src/janela.ts | A janela de horário — pura, com o instante por parâmetro (ADR-0026) | Domínio |`, e registrar numa frase que a normalização do oráculo **não é portada**: a recusa acontece na entrada (expressão ancorada do contrato + `CHECK` do banco), não na comparação.

### D12 · BAIXO · best_practices · T4 · Tech Review
- **Onde:** `packages/regua/src/mensagem.ts` (a desestruturação de `ESTADOS_DA_COBRANCA`) — **tem marcador `DÉBITO COM GATILHO` e linha no índice do `CLAUDE.md`**
- **Problema:** a escolha do molde depende da **ordem** de `ESTADOS_DA_COBRANCA`, um array declarado em outro pacote, e não de um nome publicado por ele. O Gate 2 examinou as três frentes: **(1)** a alternativa idiomática — literal do rótulo — está **proibida** pelo CT-612 (T8), que varre `packages/regua/src/**`; **(2)** a ordem **já é carga estrutural independente desta task**, porque `packages/db/src/esquema/negocio.ts` deriva `negocio.enum('status_cobranca', ESTADOS_DA_COBRANCA)` e a `0009_dominio_cobranca.sql` já gravou o tipo nessa ordem — reordenar hoje já é incompatível com migração aplicada; **(3)** a rede existe e foi medida pelo QA (reordenar dá `3 failed | 11 passed` no CT-614). A terceira forma melhor **não está disponível no escopo**: `ESTADOS_EM_ABERTO` é semanticamente o mesmo par, mas está tipado `readonly EstadoDaCobranca[]` e não como tupla `as const`, de modo que desestruturá-lo devolveria `EstadoDaCobranca | undefined` e **perderia** o estreitamento de união fechada que o compilador hoje confere.
- **Impacto:** baixo e coberto. A inserção de um estado novo em posição 0 ou 1 trocaria os dois moldes de lugar, e o CT-614 (a) reprovaria — e a mesma inserção já quebraria o enum do PostgreSQL antes disso. O risco residual é de **legibilidade e de precedente**: o pacote passa a exibir uma leitura por posição que a fatia seguinte pode imitar num ponto onde não haja CT equivalente.
- **O que fazer:** ao abrir `packages/contracts/src/cobranca.ts` por outra razão, publicar `export const ESTADOS_AVISAVEIS = Object.freeze(['A_VENCER', 'VENCIDA'] as const)` — tupla `as const`, no molde exato de `ESTADOS_DA_COBRANCA` e **não** no de `ESTADOS_EM_ABERTO` — e trocar a desestruturação de `mensagem.ts` para ela, preservando o estreitamento e eliminando a dependência de posição. **Não abrir a edição de `@sysloc/contracts` fora dessa ocasião**: o custo de escopo não se justifica pelo risco medido.

### D13 · MEDIO · code_quality · T5 · QA + Tech Review
- **Onde:** `packages/db/test/banco-efemero.ts` (`semearPoliticaDeAviso`) — **tem marcador `DÉBITO COM GATILHO` e linha no índice do `CLAUDE.md`**
- **Problema:** os **dois gates** chegaram ao mesmo achado. O auxiliar nasce exportado e **sem consumidor algum** — `grep -rn 'semearPoliticaDeAviso' packages` devolve só a própria declaração. Os dois casos que precisavam de política (CT-610 e CT-611) preferiram a porta real `gravarPoliticaDeAviso`, que é a **escolha certa** (Iron Law #4: sistema real no caminho crítico). O irmão `semearLocatarioSemContato` **tem** consumidor real, o que torna a assimetria nítida. Três consequências se somam: **(a)** o cabeçalho do bloco afirmava por escrito que os auxiliares moram ali porque têm *"mais de um consumidor nesta fatia: a suíte do predicado (T5), …"* — metade falsa; **(b)** o corpo escreve `ON CONFLICT ON CONSTRAINT politica_de_aviso_empresa_key` enquanto a produção escreve `ON CONFLICT (empresa_id)` — duas grafias do mesmo alvo, livres para divergir; **(c)** sem consumidor, **esse SQL nunca executou**, então a validade da cláusula é conclusão por leitura de catálogo, não fato medido.
- **Impacto:** baixo hoje — é acessório de teste, zero superfície de produção. O custo é de rodada futura: a T7/T8 que for montar a precondição da janela encontra um auxiliar que nunca rodou, com cláusula escrita de outro jeito que a produção, e gasta tempo decidindo se a divergência é intencional.
- **O que fazer:** ✅ **manter, não remover** — remover contraria a §5.2 da T5, que o declara entregável, e o consumidor está a uma task de distância. Já feito pelo orquestrador no fecho: o marcador `DÉBITO COM GATILHO — D13` foi emitido sobre a função, a linha entrou no índice do `CLAUDE.md`, e a frase falsa do cabeçalho do bloco foi corrigida para dizer que o consumidor é **previsto**, não existente. **O que resta**: ao ganhar o primeiro consumidor, alinhar a cláusula com a da produção (`ON CONFLICT (empresa_id)`) ou declarar no docblock por que a grafia por restrição é a certa ali — e remover o marcador no mesmo commit.

### D14 · BAIXO · architecture · T5 · Tech Review
- **Onde:** `packages/db/src/envio-de-cobranca.ts` (`FUSO_DA_OPERACAO`) — **tem marcador `DÉBITO COM GATILHO` e linha no índice do `CLAUDE.md`**
- **Problema:** o fuso da operação ganha uma **segunda declaração executável**. A primeira vive no corpo de `negocio.data_corrente_da_operacao()` (`migracoes/0010_seguranca_cobranca.sql`), e **nada amarra as duas** — o próprio docblock reconhece a obrigação sem instalar rede (*"a coincidência é obrigatória: a janela de horário e a classificação da cobrança precisam concordar sobre que dia e que hora são agora"*). Verificado: o **CT-612** (T8) varre `new Date(` em `packages/regua/src/**` e `apps/worker/src/tarefas/**` — **não alcança `packages/db/src/**`** e não compara os dois literais; e a §Cons da **ADR-0026** declara textualmente que *"nenhum caso comportamental pega a violação enquanto o host estiver no mesmo fuso da função"*.
- **Impacto:** latente e de gatilho remoto — a `0010` está sob `DECISÃO FECHADA` mais o `DÉBITO COM GATILHO — D20`, e vira imutável na primeira aplicação a banco durável, de modo que a divergência só nasceria de uma migração nova que redefinisse a função. Se nascer, o efeito é o pior caso que a ADR-0026 descreve: a janela do job e a classificação da cobrança discordam nas horas de virada, **em silêncio**, sem uma linha vermelha em lugar nenhum. É a irmã do risco que a §8 do `task_plan.md` registra como *"o relógio do processo entrar em silêncio"*, e está **fora** da rede do CT-612.
- **O que fazer:** ✅ marcador emitido pelo orquestrador no fecho. **O que resta**, quando o gatilho chegar: asserção barata em `packages/db/test/coerencia-de-migracoes.spec.ts`, que já é o lar desta classe — ler `pg_get_functiondef('negocio.data_corrente_da_operacao'::regproc)` e afirmar que a definição contém o mesmo literal de `FUSO_DA_OPERACAO`, com prova de falsificação (trocar o literal deixa o caso vermelho). É o molde que o CT-608 já usa para a projeção `HH24:MI`. ⚠️ **Não** trocar a expressão por função nova no banco: migração está fora do escopo.

### D15 · BAIXO · tests · T5 · QA
- **Onde:** `packages/db/test/envio-de-cobranca.spec.ts:816` (`admitirEmpresaNova`) · `smell`: `non_deterministic_input`
- **Problema:** o documento da empresa é montado como `${String(Date.now()).slice(-8)}${String(sequenciaDoCenario).padStart(6,'0')}` — a **única** leitura de relógio do **processo** em posição executável nos quatro arquivos de teste tocados. Ela não quebra o critério 10 (que alcança `politica-de-aviso.ts` e `envio-de-cobranca.ts`, e ali a varredura deu **zero**) nem a ADR-0026 (que governa leitura de tempo que **decide** comportamento de negócio, e esta não decide nada), e não tem vetor de flake alcançável: nenhuma asserção lê o valor, e o sufixo `sequenciaDoCenario` — já distinto nas cinco admissões do arquivo, sobre banco efêmero recém-migrado — sozinho garante a unicidade. **O prefixo do relógio é entropia que não compra nada.**
- **Impacto:** nenhum hoje. O débito é a **assimetria**: o arquivo irmão da mesma task (`politica-de-aviso.spec.ts:210`) resolve o mesmo problema de forma determinística (`9900000000${marca}`), e duas formas convivendo ensinam a próxima a copiar a pior.
- **O que fazer:** trocar por documento inteiramente derivado do contador, no molde de `politica-de-aviso.spec.ts:210` — ex.: `9910000000${String(sequenciaDoCenario).padStart(4, '0')}`. Uma linha, sem efeito sobre asserção alguma.

### D16 · BAIXO · testability · T5 · QA + Tech Review
- **Onde:** `packages/db/src/envio-de-cobranca.ts` (`registrarEnvioDeCobranca`, o `throw` do contexto que não alcança a cobrança)
- **Problema:** os **dois gates** convergiram. A porta levanta `'a tentativa de envio não foi gravada: o contexto não alcança a cobrança'`, e o próprio docblock a declara **ALCANÇÁVEL** — ao contrário das guardas gêmeas de `lerHoraCorrenteDaOperacao` e `gravarPoliticaDeAviso`, documentadas como inalcançáveis. O Gate 2 confirmou o alcance por leitura do SQL: código inexistente esvazia a CTE `alvo`, o `INSERT … SELECT FROM alvo` grava zero linhas, `gravado` sai vazio e o `SELECT` final não devolve nada. **Nenhum caso a exercita**: `COBRANCA_INEXISTENTE` é usado apenas contra as duas portas de **leitura**, no CT-609 (c) — o arranjo idêntico já está montado ao lado, do outro lado da fronteira.
- **Impacto:** a garantia é forte — impedir que uma tentativa não registrada atravesse como se tivesse sido gravada, sendo o registro o que impede o reenvio na passagem seguinte — e hoje é afirmação de docblock sem prova. Trocar o `throw` por um `as` ou por um retorno silencioso, exatamente o que o comentário do ponto existe para desencorajar, **passaria verde**.
- **O que fazer:** fechar na **T8**, junto do **CT-621**, que já é o dono do alcance cross-tenant do envio: acrescentar a asserção de que `registrarEnvioDeCobranca` com código inexistente **rejeita** — `rejects.toThrow(/o contexto não alcança a cobrança/)`, pela mensagem/sentinela específica e **nunca** `rejects.toThrow()` genérico —, mais a contagem crua de `negocio.envio_de_cobranca` intacta depois da recusa. É o mesmo molde de controle que o CT-609 (b) já usa. **Não bloqueia a T5**: a §6.4 não pede o cenário e a §6.5 defere o alcance cross-tenant à T8.

### D17 · BAIXO · code_quality · T5 · Tech Review
- **Onde:** `packages/db/src/envio-de-cobranca.ts` (docblock de `selecionarCandidatasAoAviso`)
- **Problema:** o docblock enumera **cinco** propriedades da consulta — a junção quádrupla, o recorte de dias, o par de `IS NULL`, o `destinatario` vazio e a ordem — e **omite justamente a que está sob `DECISÃO FECHADA`**: a trava do intervalo. O marcador está sobre `DESFECHO_QUE_TRAVA`, e o uso na consulta fica a ~150 linhas dele. Há três ponteiros para o marcador (o docblock da constante, o cabeçalho de `src/index.ts` e o cabeçalho do CT-611), e **o único lugar sem ponteiro é o docblock que se lê ao editar a consulta**.
- **Impacto:** não é violação do marcador — ele existe, tem os quatro campos, e o `REVERTER EXIGE` é falsificável e ancorado em fato verificável do oráculo. O que se perde é margem numa decisão que a §8 do `task_plan.md` e a §20 do tech spec classificam como risco de **alta probabilidade** de ser "consertada" de volta.
- **O que fazer:** acrescentar ao docblock de `selecionarCandidatasAoAviso` uma sexta seção de duas linhas — *"A TRAVA DO INTERVALO CONTA APENAS `ENVIADA` (RD-05 emendada)"* — remetendo ao marcador sobre `DESFECHO_QUE_TRAVA`, **sem recopiar o texto dele**. Cópia de justificativa apodrece; ponteiro, não.

### D18 · BAIXO · project_pattern · T5 · Tech Review
- **Onde:** `docs/specs/features/regua-de-cobranca/v1/tasks/T5.md`, §3.2, o card do CT-610 na §6.6, e a §5.2
- **Problema:** o código divergiu do artefato em **três** lugares, e nos três **o código está certo e a spec é que precisa ser emendada**. **(1)** A §3.2 escreve `WHERE cd.status IN ('A_VENCER', 'VENCIDA')` — literais de `negocio.status_cobranca` em posição executável em `packages/db/src/**`, que é **exatamente o que o CT-510 proíbe** por varredura; o código escreve `cd.status = ANY(...)` a partir de `ESTADOS_EM_ABERTO`, que o QA verificou ser `Object.freeze(['A_VENCER','VENCIDA'])` — semanticamente idêntico. **(2)** O card do CT-610 enumera a saída como `[L1, L3, L5]`, que é a ordem de **leitura do arranjo**, enquanto o `ORDER BY data_vencimento, codigo` devolve `[L5(−30), L3(−12), L1(+5)]`. **(3)** O desempate `ec.id DESC` do histórico não é mencionado, e é correção real — `now()` é o instante do **início da transação**, logo duas tentativas na mesma unidade carregam carimbo idêntico e paginariam mal sem ele. Some-se a §5.2, que lista **três** arquivos a modificar quando **seis** foram tocados.
- **Impacto:** o item (1) é o que morde: um leitor futuro que tome a §3.2 como autoritativa e "convirja" o código para o `IN` literal **derruba o CT-510**. A rede existe e é executável, então o defeito é caro em rodada, não em produção. Os itens (2) e (3) são ruído de leitura.
- **O que fazer:** emendar a §3.2 nos três pontos — trocar o `IN` ilustrativo por `= ANY(ESTADOS_EM_ABERTO::negocio.status_cobranca[])` **com nota de que o literal é proibido pelo CT-510**; corrigir a ordem esperada do card do CT-610 para `[L5, L3, L1]` nomeando o `ORDER BY` como fonte; e acrescentar o desempate `id DESC` à descrição de `lerEnviosDaCobranca`, com a razão do carimbo idêntico. Acrescentar à §5.2 os três arquivos consequentes (`tsconfig.json`, `test/unidade-de-trabalho.spec.ts`, `pnpm-lock.yaml`).

### D19 · BAIXO · architecture · T6 · Tech Review
- **Onde:** `packages/regua/src/index.ts` (a publicação de `criarCapturadorDeEmail`) — **dono nomeado: T8/T10**
- **Problema:** o modo de falha **INVERSO** da CA-17 não tem âncora. `criarCapturadorDeEmail` é publicado pelo barrel e, a partir da T8, alcançável pela **composição de produção** pela mesma fronteira e com a mesma interface do adaptador real — a régua, por desenho, **não sabe qual dos dois recebeu**. Toda a prova da T6 corre numa direção só: o CT-626 afirma que teste nenhum alcança o adaptador de produção; **nada afirma que composição de produção nenhuma alcança o capturador**.
- **Impacto:** uma composição que fiasse o capturador em produção **engoliria todo aviso em silêncio** — a régua gravaria `ENVIADA` com `causa: null` para cada candidata, a trava de intervalo prenderia cada cobrança, e nenhuma mensagem sairia. **É pior de detectar que o da CA-17**: não há caixa alheia acusando, não há linha `FALHOU`, e o histórico que o operador consulta **afirmaria positivamente que o aviso saiu**. O risco foi levantado no Gate 2 da **T4** e re-reportado no da T6.
- **O que fazer:** na **T8** (ou na T10, junto do crescimento já previsto de `ARQUIVOS_QUE_LIGAM_O_ADAPTADOR`), acrescentar a **âncora simétrica** em `barreira-de-envio.spec.ts`, reusando `caminharPeloRepositorio` e `semComentarios`: *"a lista dos arquivos sob `apps/*/src/**` que ligam `criarCapturadorDeEmail` é, por igualdade, `[]`"*. É **mais barata** que a existente (o conjunto é um prefixo, não a árvore inteira) e o mutante é uma linha em memória. **Não é caso de despublicar o capturador** — os quatro diretórios de teste dependem dele legitimamente.

### D20 · MEDIO · documentation · T6 · QA
- **Onde:** `packages/db/test/barreira-de-envio.spec.ts` (a afirmação de exaustividade do cabeçalho e o docblock de `classeDoCaminho`)
- **Problema:** a afirmação *"não sobra posição: toda classe está declarada carregável, declarada não-carregável, ou reprova"* é **literalmente falsa para nome sem extensão JÁ DECLARADO**. **Medido**: uma ponte em `packages/db/src/Makefile` (CJS, versionada, consumida por `createRequire` de um `.spec.ts` de diretório varrido) atravessa com a suíte **138/138 VERDE** — `classeDoCaminho('Makefile')` devolve `Makefile`, que **está** em `CLASSES_NAO_CARREGAVEIS`. O QA **não** classificou como ALTO por decisão fundamentada: batizar uma ponte de `Makefile` é **colisão deliberada** (ao contrário do `ponte-sem-extensao` da rodada 4, cujo nome era plausível), e em substância é a mesma evasão que o próprio arquivo já declara fora do modelo (*"JavaScript dentro de um `.md`"*).
- **Impacto:** nenhum hoje. **O defeito é da PROSA**, que promete cobertura categórica onde há cobertura declarada — e é a **quinta** afirmação de exaustividade desta task; as quatro anteriores foram todas falsificadas por medição, e cada falsificação custou uma rodada.
- **O que fazer:** corrigir a fronteira escrita, **sem varrer menos e sem mexer nas âncoras**. Substituir *"não sobra posição intermediária"* por: o complemento fecha toda classe **NOVA**; para as **já declaradas** — inclusive `Makefile`, a única sem extensão — a inércia é **por anúncio**, e escrever JavaScript dentro de qualquer uma é a evasão deliberada já nomeada. No docblock de `classeDoCaminho`, trocar *"cada nome novo sem extensão reprova aqui"* por *"cada nome sem extensão **NÃO DECLARADO** reprova aqui"*. Se um dia se quiser fechar também esta posição, o mecanismo é declarar as classes sem extensão **por caminho inteiro** (`deploy/Makefile`) em vez de por basename.

### D21 · BAIXO · tests · T6 · QA
- **Onde:** `packages/db/test/barreira-de-envio.spec.ts` (`CLASSES_NAO_CARREGAVEIS`, 16 classes declaradas) · `smell`: `brittle_selector`
- **Problema:** **medido** — versionar `docs/diagrama-da-regua.png` reprova o `CT-626 (d)` na âncora 1-C (*"classe versionada FORA do conjunto varrido e não declarada não-carregável: .png"*). O conjunto é acoplado ao **conteúdo incidental da árvore**: um `.png`, um `.csv`, um `.lock`, um `.ico` ou um `.editorconfig` acrescentado por qualquer fatia futura derruba uma **âncora de segurança** por motivo documental — e é exatamente a situação em que se afrouxa a asserção errada.
- **Impacto:** contido e **guiado**, não uma armadilha silenciosa: a mensagem de falha nomeia a classe **e** um arquivo de exemplo, e o docblock já escreve o procedimento correto (*"classe nova legítima é uma linha aqui mais a linha da tabela"*) e **proíbe as três saídas erradas**. Atenuantes medidos: `.env.example` cai na classe `.example`, já declarada; e a T8/T10 acrescenta bordas `.ts`, que não abrem classe.
- **O que fazer:** **manter como está** — a igualdade é o mecanismo, e trocá-la por contenção apagaria a âncora. Registrar como débito para a T8/T10: se a manutenção incomodar, o caminho é derivar o conjunto do `.gitattributes` (que já distingue binário de texto) em vez de lista manual, **preservando a igualdade**.

### D22 · BAIXO · code_quality · T6 · QA
- **Onde:** `packages/db/test/{execucao-da-regua,barreira-de-envio,envio-de-cobranca}.spec.ts`
- **Problema:** seis acessórios de arranjo (`emUnidade`, `admitirEmpresaNova`, `semearCenario`, `pessoaDe`, `lancar`, `dataDeslocada`) replicados quase idênticos em **três** arquivos. Parte é pré-existente à T6 (vem da T5), mas os dois arquivos novos elevaram a contagem de **duas para três** cópias, que é o limiar em que a extração costuma compensar.
- **Impacto:** de manutenção. O risco é o que `varredura-de-fontes.ts` já registra no próprio cabeçalho para o varredor: *"cópias de um varredor divergem em silêncio"* — e arranjo que divergiu faz o caso provar coisa diferente da que ele afirma provar.
- **O que fazer:** promover o conjunto a um acessório compartilhado ao lado de `banco-efemero.ts` e `varredura-de-fontes.ts` — o precedente do próprio pacote —, deixando em cada arquivo só o que é específico do cenário. ⚠️ **Não fazer em rodada de correção**: o próprio QA advertiu que *"mover arranjo durante correção de gate é superfície de regressão de graça"*.

### D23 · BAIXO · code_quality · T6 · QA + Tech Review
- **Onde:** `packages/db/test/barreira-de-envio.spec.ts` (`ARQUIVOS_QUE_LIGAM_O_ADAPTADOR`) — a exposição dos artefatos de `_run/`
- **Problema:** dos **284** arquivos do conjunto varrido, **18 estão sob `docs/`** e todos são `.json` — entre eles `docs/specs/features/regua-de-cobranca/v1/_run/test-cases.json` (122 KB), **gerado pelo pipeline e que descreve justamente o CT-626**. Hoje nenhum nomeia o símbolo, e por isso a suíte está verde.
- **Impacto:** uma **regeneração** que escreva `criarAdaptadorSmtp` ou `adaptador-smtp` na descrição de um caso faria a igualdade da ligação **reprovar nomeando um artefato de spec** — âncora de segurança vermelha por motivo documental.
- **O que fazer:** ✅ **já registrado no docblock** pelo executor. O conserto, quando ocorrer, é **corrigir o artefato de spec** para não citar o símbolo literalmente (o mesmo idioma de concatenação que o detector usa), **nunca** filtrar o culpado nem varrer menos. ⚠️ **Declarar `_run/` fora do conjunto está PROIBIDO** — abriria em diretório versionado exatamente a posição que as rodadas 3 e 4 fecharam; os dois gates concordaram nisso.

### D24 · BAIXO · security · T6 · QA
- **Onde:** `packages/regua/src/coordenadas-do-transporte.ts` (o objeto devolvido) e `packages/regua/src/adaptador-smtp.ts` (o ponto de chamada)
- **Problema:** `logger: false` e `debug: false` **saíram** na rodada 6. A remoção é **legítima e medida**: no caminho antigo elas eram descartadas junto com todo o objeto do chamador, logo o efeito era **nulo**. Mas no caminho **novo** elas passariam a ter efeito — removê-las é **escolha**, não consequência —, e a mudez do registrador passou a repousar **inteiramente no padrão do `nodemailer`**, sem asserção alguma.
- **Impacto:** **nulo hoje**, e medido em três vias pelo QA: `shared.getLogger({})` e `shared.getLogger({ logger: false })` devolvem o **mesmo** objeto (seis chaves, corpo `() => false`); com uma `SMTP_URL` contendo `SENHA_SECRETA`, exercitar `info/debug/error` produz **0 bytes** em stdout+stderr. O risco é contido porque o manifesto **pina `"nodemailer": "7.0.13"` em versão exata, sem `^`**. O residual é estreito: **se o padrão do `getLogger` mudar num bump, nada na suíte acusa**.
- **O que fazer:** ⚠️ **NÃO restaurar no ponto de chamada** — está **medido** que qualquer literal de objeto ali **reprova o `CT-641 (c)`**, que é a única barreira contra o defeito do TR-P1. O caminho certo, quando a T8/T10 abrir aquele módulo por outra razão: acrescentar `readonly logger: false` e `readonly debug: false` a `CoordenadasDoTransporte`, devolvê-las no objeto, e estendê-las nas duas igualdades estritas do `CT-641 (a)`/`(b)`. O QA **se retratou** explicitamente da premissa de "custo zero" que dera na rodada 6.

### D25 · BAIXO · dead_code · T6 · QA + Tech Review
- **Onde:** `packages/regua/src/coordenadas-do-transporte.ts` (`MOTIVO_DE_TRANSPORTE_INUTILIZAVEL`, e em menor grau `CredencialDoTransporte`)
- **Problema:** a correção da rodada 7 removeu o **único** import externo da constante (o teste passou a escrever o literal). `grep -rn` em `apps` e `packages` devolve **duas** ocorrências, ambas dentro do próprio módulo, e o `index.ts` não publica nada dali. O `export` virou **superfície sem consumidor**.
- **Impacto:** nenhum hoje. O ponto é outro: a `DECISÃO FECHADA` de `coordenadas-do-transporte.spec.ts` existe justamente para **proibir** que o oráculo do texto venha desta constante, e o `export` mantém aberta, **no nível da linguagem**, a porta que o marcador fecha **só em prosa**.
- **O que fazer:** retirar o `export` de `MOTIVO_DE_TRANSPORTE_INUTILIZAVEL` e de `CredencialDoTransporte` (este último continua alcançável por `CoordenadasDoTransporte['auth']`), citando o marcador no docblock. **Converte a proibição em impossibilidade estrutural.** ⚠️ Cuidado: o `CT-641 (a)`/`(b)` usa `satisfies CoordenadasDoTransporte`, que **não** depende de nenhum dos dois.

### D26 · BAIXO · security · T6 · Tech Review
- **Onde:** `packages/regua/test/coordenadas-do-transporte.spec.ts:74` — `new URL(\`../src/adaptador${'-'}smtp.ts\`, import.meta.url)`
- **Problema:** o **idioma de concatenação que evade o detector da CA-17** ganhou o seu **segundo** uso na árvore — e o **primeiro fora do próprio detector**. Verificado contra as duas expressões reais (`NOMEIA_O_SIMBOLO = /\bcriarAdaptadorSmtp\b/` e `NOMEIA_O_MODULO = /['"`][^'"`]*adaptador-smtp/`): como a cadeia `adaptador-smtp` não existe inteira no fonte, **nenhuma** das âncoras do CT-626 enxerga este arquivo. Até esta rodada o idioma só existia no **próprio detector**, onde é **intrínseco** (sem ele a varredura acusaria a si mesma).
- **Impacto:** o risco **não é o arquivo de hoje** — ele só faz `readFile`, nunca importa, e a suíte está verde. É o **precedente**: copiado o mesmo `${'-'}` para dentro de um `await import(...)`, uma verificação futura da T8/T10 constrói o adaptador de **produção** com o CT-626 verde nas duas âncoras. O detector é textual por construção e **não tem como distinguir a evasão legítima da ilegítima**.
- **O que fazer:** na T8/T10, em ordem de preferência — **(a)** mover a asserção do ponto de chamada para `barreira-de-envio.spec.ts`, reusando `MODULO_DO_ADAPTADOR`, de modo que o caminho tenha **definição única e auditada**; **(b)** manter aqui, mas **derivar o caminho por varredura** de `../src/` (o arquivo que contém a chamada ao construtor), de modo que o fonte deste teste não contenha o nome do módulo **em forma nenhuma, nem partida**. Em qualquer caso, registrar no docblock do detector que **o idioma é privativo dele e da leitura estática de fonte — nunca de carga de módulo**.

### D27 · BAIXO · project_pattern · T6 · Tech Review
- **Onde:** `.claude/rules/testing-stack.md` (a convenção ausente)
- **Problema:** o idioma da concatenação **não tem regra escrita** delimitando quando é legítimo. O sweep em `.claude/rules/*` e `docs/adr/*` por `concatenaç`, `partir as duas cadeias` e `acusar o próprio` **retorna vazio nos dois destinos**. A única explicação viva mora no docblock de `barreira-de-envio.spec.ts`, e descreve a razão **daquele** arquivo. Com o segundo uso aparecendo por razão **diferente**, o critério de legitimidade passou a ser inferido caso a caso.
- **Impacto:** cada arquivo novo redecide, e a diferença entre *"partir a cadeia para o detector não se autoacusar"* e *"partir a cadeia para o detector não me acusar"* fica dependendo de quem lê o docblock certo. **O mesmo truque aplicado a um `import()` em vez de a um `readFile` faz um envio real escapar com a suíte verde.**
- **O que fazer:** registrar a convenção em `.claude/rules/testing-stack.md`, ao lado da prova de falsificação, com o discriminador que os dois usos já sugerem: **partir a cadeia é legítimo quando o arquivo apenas LÊ TEXTO** (leitura estática de fonte, ou o próprio detector), **e é violação quando participa de CARGA DE MÓDULO** (`import`, `import()`, `createRequire`). Emitido também como candidato a regra (`convention_drift`) em `_run/rule-candidates.md`.

### D28 · BAIXO · documentation · T6 · QA
- **Onde:** `packages/db/test/barreira-de-envio.spec.ts` (o `CT-626 (b)`, na altura da substituição da rodada 5)
- **Problema:** a asserção `expect(comAPonte).not.toEqual(semAPonte)` foi **removida** e substituída por uma discriminante mais forte, **sem a linha literal `SUT_IS_CORRECT_BECAUSE:`** que o docblock do próprio arquivo manda escrever — a justificativa ficou em prosa nas linhas vizinhas.
- **Impacto:** nenhum. A remoção é **legítima e foi verificada**: a asserção era comprovadamente tautológica (implicada pela igualdade que a precede), e a substituta **foi medida disparando**. E o rigor do protocolo não incide estritamente — ele exige a linha para teste que **reprovou** a mudança, e esta **nunca reprovou nada**, por ser infalsificável.
- **O que fazer:** acrescentar a linha junto ao comentário existente: `SUT_IS_CORRECT_BECAUSE: a asserção removida era implicada pela igualdade seguinte e não podia falhar por estado nenhum do detector; a substituta discrimina e foi medida disparando.`

### D29 · BAIXO · documentation · T7 · QA
- **Onde:** `CLAUDE.md:39` e `packages/shared/test/protocolo-antirregressao.spec.ts:2` (mais a §6.0 e a §6.6 da `T7.md`)
- **Problema:** a barreira executável do Protocolo é descrita em três lugares como **`CT-501 a CT-510`**, e o arquivo a numera **`CT-901` a `CT-910`** desde uma renumeração **pré-existente a esta fatia** (colisão com a sequência de domínio da `cobranca-e-mora`, registrada no próprio cabeçalho do arquivo). Some-se que o título do arquivo declara *"CT-901 a CT-910"* e ele passou a carregar também o **CT-638**, cuja exceção só aparece 26 linhas abaixo.
- **Impacto:** escrituração vencida, **não defeito** — nenhuma asserção depende desses números (o CT-903 confere o caminho da rule e a contagem dos 4 itens do resumo, não a faixa). Mas o `CLAUDE.md` entra no contexto de **todo agente**, e a linha 39 é a que descreve a barreira.
- **O que fazer:** trocar `(CT-501 a CT-510)` por `(CT-901 a CT-910)` em `CLAUDE.md:39`; ajustar o título do arquivo de barreira para refletir as duas faixas; e alinhar as duas menções na `T7.md`. ⚠️ **Não renumerar o CT-638** — os artefatos da fatia o rastreiam por esse número, e a faixa 9xx nasceu para fugir da sequência de domínio, que não reutiliza número já reservado.

### D30 · BAIXO · scope_deviation · T7 · Tech Review
- **Onde:** `docs/specs/features/regua-de-cobranca/v1/tasks/T7.md`, §5.2
- **Problema:** a §5.2 declara **quatro** arquivos a modificar e o diff alcança **outros quatro** — `apps/worker/src/{main.ts,tarefas/eco.ts}`, `apps/worker/test/eco.spec.ts` e `packages/db/test/cobranca.spec.ts`. Os quatro são **consequência forçada do próprio desenho da task**: a §1 fixa `FILA_DO_ECO` e `CargaDoEco` entre os símbolos criados, e o critério 3 proíbe redefinição sobrevivente no `worker` — os três consumidores tinham de mudar de qualquer forma, ao menos na origem do import. **O desvio é do ARTEFATO, não da disciplina do executor**, que declarou o arraste.
- **Impacto:** baixo — nenhuma mudança de comportamento, nenhuma asserção enfraquecida. O custo é de auditoria: o Gate 2 teve de reconstruir por leitura o que a §5.2 deveria declarar, e um arraste que atravessa a suíte de uma **fatia FECHADA** (`eco.spec.ts`, 16/16 da F0) é exatamente onde uma edição não declarada passaria despercebida.
- **O que fazer:** completar a §5.2 com os quatro e a razão de cada um. **Para tasks futuras de `refactor_cross_module`**: quando a §1 renomeia símbolo público existente, a §5.2 lista **todos** os consumidores dele — o `grep` do nome antigo é a checagem, e custa um comando. Emitido também como candidato a regra.

### D31 · BAIXO · best_practices · T7 · Tech Review
- **Onde:** `packages/shared/src/index.ts` e `packages/shared/src/fila.ts` (`TENTATIVAS_POR_TAREFA`, `ESPERA_ENTRE_TENTATIVAS_MS`, `TAREFAS_CONCLUIDAS_RETIDAS`, `TAREFAS_FALHAS_RETIDAS`)
- **Problema:** das nove exportações novas, **quatro não têm consumidor algum** na árvore — verificado por varredura: fora do módulo onde nascem, do barrel e da lista de dados do CT-638, só aparecem no artefato da task. O próprio **CT-638 confirma pela outra ponta**: a asserção de consumo pela fronteira exige do `worker` exatamente **cinco** símbolos, e as quatro não estão entre eles.
- **Impacto:** API pública morta no pacote que **todos** importam — e, pior, um **segundo caminho, mais fraco**, para a política de repetição: publicá-las oferece as peças para um produtor futuro montar `{ attempts: …, backoff: … }` à mão, que é **exatamente a divergência que o D32 existe para fechar**. Retirar depois o que se publicou é mudança incompatível.
- **O que fazer:** rebaixar as quatro para `const` **privado** de `packages/shared/src/fila.ts` e removê-las do barrel, mantendo `OPCOES_PADRAO_DA_TAREFA` como o **único** caminho publicado. ⚠️ **O CT-638 não muda**: `definicaoDe()` casa `(?:export\s+)?(?:const|…)`, de modo que a definição privada continua encontrada e a unicidade continua provada. Se a decisão for manter publicadas, **a razão vai no docblock** — hoje ele justifica o objeto, não as peças.

### D32 · BAIXO · code_quality · T7 · Tech Review
> ⚠️ **Este `D32` é da `regua-de-cobranca` e NÃO é o D32 (F0/T6) que esta mesma task acabou de fechar.** A sequência corre dentro da §2 da fatia (§3-B da `nao-regressao.md`), e o identificador é o par — `D32 · F0/T6` era o contrato da fila duplicado, e está **fechado**; `D32 · F3/T7` é o de baixo. A coincidência é infeliz e legítima.
- **Onde:** `apps/worker/src/fila.ts` (`devolverGraciosamente`, o registro do estouro do prazo) — **dono nomeado: T8**
- **Problema:** a devolução graciosa passou a fechar **duas** filas, e o docblock novo declara a regra (*"todo produtor construído aqui é fechado aqui — inclusive o que ainda não tem consumidor"*). Mas o registro de erro do estouro continua `logger.error({ limiteMs, fila: FILA_DO_ECO }, 'o encerramento excedeu o limite — devolvendo a conexão sem esperar o restante')`. **O campo `fila` deixou de identificar o alcance**: a mensagem fala de *"o restante"* e o campo nomeia **uma** fila entre as duas devolvidas. É a linha que o próprio comentário acima dela declara ser *"a única pista de que uma tarefa em voo pode ter ficado para trás"*.
- **Impacto:** nulo hoje (a fila da régua não tem consumidor). **A partir da T8**, que registra o processador, uma tarefa da régua abandonada no desligamento produzirá uma linha que nomeia `eco`.
- **O que fazer:** na **T8**, remover o campo `fila` desta linha (o `limiteMs` já a identifica) ou trocá-lo pela **lista** de filas devolvidas. ⚠️ A alteração alcança `apps/worker/test/eco.spec.ts`, **suíte de fatia fechada**: exige a linha `SUT_IS_CORRECT_BECAUSE:` junto, no molde do que a T7 fez no CT-512 (b). **Fora do escopo da T7** — o critério 10 dela proíbe tocar a suíte do `eco`.

### D33 · BAIXO · project_pattern · T7 · Tech Review
- **Onde:** `packages/shared/package.json` (o campo `description`)
- **Problema:** o manifesto descreve o pacote como *"Contratos transversais do backend Sysloc — corpo de erro da API (**ADR-0007**) e registro estruturado de eventos"*. A T7 acrescentou uma **terceira** responsabilidade — o contrato das filas — e a descrição não a menciona. Na mesma linha, a citação da **ADR-0007** está **vencida**: o `INDEX.md` a marca `superseded-by:0012`, e a 0012 por sua vez `superseded-by:0017`. A citação vencida é **pré-existente** (o `package.json` não aparece no diff); o que a task acrescentou foi a defasagem da descrição.
- **Impacto:** escrituração. O manifesto é a primeira leitura de quem chega ao pacote transversal — que todos importam — e hoje descreve **dois terços** do conteúdo e aponta para uma ADR aposentada.
- **O que fazer:** nomear as três responsabilidades e trocar `ADR-0007` por **`ADR-0017`**. Uma linha, sem efeito de build. ⚠️ O Gate 2 **não** emitiu `convention_drift` para isto, e a razão importa: a convenção violada — *"nas duas cadeias de supersede só a última se cita"* — **está escrita** no item 6 da leitura obrigatória do `CLAUDE.md`. É problema de **aplicação**, não de ausência de regra.

### D34 · BAIXO · testability · T8 · QA + Tech Review
- **Onde:** `packages/regua/src/adaptador-smtp.ts` (`exigirDeclarada`) — **tem marcador `DÉBITO COM GATILHO` e linha no índice do `CLAUDE.md`** · **dono: T10**
- **Problema:** a guarda que recusa `SMTP_URL`/`EMAIL_REMETENTE` vazias **na construção do adaptador** é a última barreira contra transporte não declarado, e **nunca executou sob asserção nenhuma** (`AP-28`, `untestable_fail_fast`). A razão é **estrutural, não esquecimento**: alcançá-la exige que um arquivo de teste nomeie `criarAdaptadorSmtp` ou o caminho do módulo — **exatamente o que o CT-626 proíbe por igualdade** sobre os quatro diretórios de teste. **Não existe posição legítima de onde chamá-la.**
- **Impacto:** o degrau que a antecede tem prova (o `CT-625`/`CT-643` do `worker`). **Na T10 a `api` constrói o mesmo adaptador**, e se a validação de ambiente de lá divergir — for menos exigente, ou aceitar cadeia de espaços —, `exigirDeclarada` é o que sobra.
- **O que fazer:** ✅ marcador e linha do índice emitidos pelo orquestrador no fecho. **O fecho é da T10**: extrair `exigirDeclarada` para um módulo cujo nome **não** seja `adaptador-smtp`, o que a torna testável **sem tropeçar em nenhum dos dois fatos do detector**. A T10 já vai abrir a fiação do adaptador por outra razão.

### D35 · MEDIO · tests · T8 · QA
- **Onde:** `apps/worker/test/ambiente.spec.ts:297` · `smell`: `semantically_duplicated_test`
- **Problema:** o `CT-625` repete **três linhas** do `CT-006` no mesmo arquivo. Para `SMTP_URL`, `EMAIL_REMETENTE` e `DATABASE_URL` ausentes, a tupla coincide nos **quatro** campos; a única diferença é a asserção extra `not.toContain(SENHA_NA_CADEIA)`. A justificativa é visível (a convenção `CA-xx → CT-xxx` pede caso nomeado por CA, e são CAs distintas — CA-15 e CA-17), **mas uma mudança na mensagem de ausência passa a quebrar em dois lugares sem ganho de detecção**.
- **O que fazer:** consolidar numa tabela só, acrescentando à linha o CT que ela serve (`{ nome, ct, exigeNaoVazamento }`) e **preservando os dois IDs no nome do `it`**. Alternativa mínima: manter o CT-625 só com `SMTP_URL vazia` e a asserção de não-vazamento, citando o CT-006 no docblock.

### D36 · BAIXO · project_pattern · T8 · QA + Tech Review
- **Onde:** `docs/specs/features/regua-de-cobranca/v1/tasks/T8.md` — §3.5, §5.2, §4 (CA #9) e o card do CT-624 na §6.6
- **Problema:** **quatro** afirmações do artefato que a medição refutou. **(1)** o card do CT-624 diz que `app.empresa_id` aparece em posição executável em **exatamente 1** arquivo — são **três** legítimos (`unidade-de-trabalho.ts` fixa, `contexto-de-escrita.ts` lê, `semente.ts` semeia); **(2)** diz que `identidade.empresa` *"passa só por `abrirAcessoAIdentidade`"* — é emitida por **dois** arquivos (`empresa.ts`, `semente.ts`), nenhum via aquele acesso; **(3)** a §3.5/§5.2/CA #9 mandam **estender `varredura-de-fontes.ts`**, e o arquivo **não guarda lista de alvos** — a extensão cabe em `DIRETORIOS_VARRIDOS` de `fonte-unica-do-estado.spec.ts` (3→5), que é onde entrou; **(4)** a §5.2 declara **oito** arquivos e o diff alcança **sete além deles**, entre eles `apps/worker/src/fila.ts`, que carrega **a maior mudança de produção da task**.
- **Impacto:** o card é **molde** de qualquer varredura futura da mesma classe, e um `exatamente 1` que a árvore não sustenta **ensina a escrever a asserção errada** — que reprovaria de saída e convidaria a afrouxar para `toContain`. E a §5.2 é o **oráculo da Camada 0**: a tabela incompleta produziu apuração enganosa **nos dois sentidos** neste run.
- **O que fazer:** emendar os quatro pontos com os números medidos; acrescentar à §5.2 os sete arquivos com a modificação de cada um; e mover `packages/db/src/contexto.ts` da §5.3 para a §5.2, preservando a nota que o autoriza. **Nenhuma mudança de código.**

### D37 · BAIXO · tests · T8 · QA
- **Onde:** `apps/worker/test/regua.spec.ts` (o construtor de cenário e o import relativo profundo) · `smell`: `magic_strings`
- **Problema:** **duas** coisas no mesmo arquivo. **(a)** `emUnidade`, `admitirEmpresaNova`, `pessoaDe`, `semearCenario`, `lancar`, `dataDeslocada` e `TERMOS_DO_CONTRATO` ganharam uma **terceira** cópia, agora em **outro pacote** — a constante sozinha alcança **oito** arquivos de dois pacotes. **(b)** o arquivo importa `../../../packages/db/test/banco-efemero.ts`, que é **o gatilho do `D28 · F0/T5`** (já disparado), e **não traz o marcador** — enquanto os 18 arquivos de `apps/api/test/` que fazem o mesmo trazem.
- **Impacto:** (a) é manutenção — o protocolo de ativação de contrato está redigitado, e uma mudança nele obriga a editar N arquivos. (b) é escrituração, **com uma ressalva que muda a leitura**: o irmão `apps/worker/test/eco.spec.ts` (F0/T6, aprovado nos dois gates) **também** importa assim e **também** não traz marcador — **o padrão é do pacote, não uma regressão desta task**.
- **O que fazer:** (a) **quando o D28 for fechado**, promover o construtor a um object mother único (`packages/db/test/cenario-de-regua.ts`). ⚠️ **Não consolidar antes** — acrescentaria um quarto consumidor do caminho relativo profundo. (b) **decidir uma vez para o pacote inteiro**: ou marcar os **dois** arquivos de `apps/worker/test/`, ou registrar que o pacote está deliberadamente fora da convenção. **Duas metades divergentes é o que apodrece.**

### D38 · BAIXO · logic · T8 · QA
- **Onde:** `deploy/scripts/instalacao/provisionar-base.sh` (`REMETENTE_PADRAO_DO_AVISO`)
- **Problema:** o valor padrão `avisos@sysloc.invalid` foi **julgado e aprovado no mérito** pelos dois gates (inócuo na instalação, porque a `SMTP_URL` provisionada aponta para o capturador local; o domínio é reservado pela RFC 6761; e a alternativa — não gravar chave nenhuma — **é literalmente o modo de falha do D39**). Fica o resíduo: **nada acopla as duas**. Na virada da F7 o operador troca ambas à mão, e se trocar **só a `SMTP_URL`**, nada no provisionamento acusa que o remetente continua o substituto.
- **Impacto:** a falha é **RUIDOSA** — o relay recusa remetente em domínio que não resolve, a tentativa nasce `FALHOU` em `negocio.envio_de_cobranca` e `houveFalha` sobe. É **atrito, não silêncio**. O comentário gravado no próprio arquivo instrui a troca, o que é a mitigação certa hoje.
- **O que fazer:** quando a **F7** abrir, considerar um caso **do verificador** (não do provisionamento, que não deve abortar por conteúdo de negócio) cobrando a implicação: `SMTP_URL` divergente do capturador local ⇒ `EMAIL_REMETENTE` diferente do padrão.

### D39 · BAIXO · logic · T8 · QA
> ⚠️ **Este `D39` é da `regua-de-cobranca` e NÃO é o `D39 (F1/fechamento)` do índice do `CLAUDE.md`** — a sequência corre dentro da §2 da fatia (§3-B), e o identificador é o par. Ironicamente, os dois são sobre o **mesmo arquivo**.
- **Onde:** `deploy/scripts/instalacao/provisionar-base.sh` (`conferir_coordenadas_do_ambiente`) — **PRÉ-EXISTENTE, fora do delta**
- **Problema:** o laço decide ausência pelo **VALOR** (`sed -n "s|^${chave}=||p"`), de modo que um `SMTP_URL=` ou `REDIS_URL=` **esvaziado pelo operador** entra em `CHAVES_AUSENTES`, o passo P06 acrescenta uma **segunda** atribuição da mesma chave, e **a execução seguinte aborta por ambiguidade** em `extrair_credencial_db`.
- **Impacto:** não é regressão da T8 — o `git diff --numstat` sobre o provisionador é **74/0**, todas em código novo. E a função nova `garantir_chaves_de_conteudo` faz o **contrário**, ancorando em `grep -q '^EMAIL_REMETENTE='` (existência da **linha**), o que o QA mediu no cenário (s4).
- **O que fazer:** trocar o critério de ausência do laço para **existência de linha**, alinhando-o ao que a função nova já adota, e acrescentar ao bloco (l) de `verificar-provisionamento.sh` um cenário com **chave presente e vazia**. ⚠️ **Fora do escopo** — o conserto passa pela função que o bloco (l) do verificador **sonda por igualdade**.

### D40 · BAIXO · error_handling · T8 · Tech Review
- **Onde:** `deploy/scripts/instalacao/provisionar-base.sh:719-721` (`garantir_chaves_de_conteudo`) e, pela mesma classe, as linhas 1329-1330
- **Problema:** a semeadura faz `printf 'EMAIL_REMETENTE=%s\n' … >>"${arquivo}"` **sem conferir se o arquivo preexistente termina em `\n`**. Se a última linha não tiver quebra final (arquivo editado à mão por editor que não a acrescenta), o acréscimo **se cola nela**: `SMTP_URL=smtp://127.0.0.1:1025EMAIL_REMETENTE=avisos@sysloc.invalid`.
- **Impacto:** **duplamente ruim** — `EMAIL_REMETENTE` continua ausente (a partida segue recusada, que é o defeito que o TR-P1 existia para fechar) **e** a `SMTP_URL` fica corrompida, de modo que a execução seguinte aborta acusando divergência de `SMTP_URL` e **manda o operador corrigir uma linha cuja causa foi a execução anterior**. O `shellcheck --severity=error` não pega, e o script **não foi executado** (exige `sudo`). ⚠️ A classe é **PRÉ-EXISTENTE** — os acréscimos de 1329-1330 têm o mesmo desenho —, e o que a torna reportável é que **a instância nova dispara no caminho de ATUALIZAÇÃO**, que é justamente aquele para o qual o TR-P1 foi aberto.
- **O que fazer:** antes do `printf`: `[[ -s "${arquivo}" && -n "$(tail -c1 "${arquivo}")" ]] && printf '\n' >>"${arquivo}"`. **Preferir fechar a CLASSE**: extrair `acrescentar_linha_ao_ambiente()` e fazer os **três** pontos (720, 1329, 1330) passarem por ela.

### D41 · BAIXO · project_pattern · T8 · Tech Review
- **Onde:** `apps/worker/test/ambiente.spec.ts` (toda a maquinaria do `CT-643`, ~150 linhas) — **dono: T10**
- **Problema:** `variaveisConsultadasPor`, `variaveisExigidasPor`, `chavesEmitidasPor`, `chavesDeclaradasNaUnidade`, `destinoDaEmissao` e `semCaminhoDeProvisionamento` vivem **dentro do arquivo de teste do worker** e derivam o conjunto exigido de **UMA** composição raiz. **A T10 fará `apps/api/src/configuracao/ambiente.ts` exigir `SMTP_URL` e `EMAIL_REMETENTE`**, e o teste da API **não alcança essa maquinaria** sem duplicá-la ou sem import relativo entre apps (**que é a forma do `D28`**).
- **Impacto:** **nada acusa a omissão** — se a T10 acrescentar as duas à `VARIAVEIS_EXIGIDAS` da API sem construir o equivalente, **a suíte fica verde**. E a própria testemunha negativa do CT-643 (`BETTER_AUTH_SECRET`, exigido pela API e provisionado por ninguém) **é a medição de que o lado da API já hoje não tem essa guarda**. O contraste é do mesmo run: `barreira-de-envio.spec.ts` declara `COMPOSICOES_QUE_CONSTROEM_O_ADAPTADOR` com a nota literal *"⚠️ Ela cresce na T10 … Deixar de acrescentar não é neutro"*. **O CT-643 não tem parágrafo equivalente.**
- **O que fazer:** na **T10**, promover a maquinaria para local compartilhado (`packages/shared/test/`) e derivar de uma **lista** de composições raiz. Enquanto isso, acrescentar ao docblock do `describe('CT-643 …')` a mesma nota de extensão que a irmã já carrega.

### D42 · BAIXO · testability · T8 · Tech Review + QA
- **Onde:** `apps/worker/test/ambiente.spec.ts` (o detector do `CT-643`) e o comentário das linhas 421-427
- **Problema:** **duas coisas.** **(a)** o `CT-643` casa pelo destino **literal** `${ARQ_AMBIENTE}` e por isso **não enxerga a semeadura**, que escreve em `${arquivo}` — de modo que **metade da correção do TR-P1 não tem rede permanente**: removendo só a semeadura, a suíte fica **48/48 verde** (medido). **(b)** o comentário afirma que *"nenhuma redação futura escapa"*, e o QA mediu que **a exigência CONDICIONAL escapa** — `if (fonte.NODE_ENV === 'production' && …)` deixa a suíte **48/48 verde** com variável que recusa a partida em produção e que ninguém provisiona, porque o ramo nunca abre e **as duas âncoras ficam mudas**.
- **Impacto:** (a) a direção do erro é **ruidosa** e o caminho que fecha o modo de falha do TR-P1 — a **instalação de máquina nova** — é a **criação**, que continua contada (medido nos dois sentidos). (b) o resíduo é **BAIXO por medição**, não por conveniência: o QA verificou que **a composição raiz irmã NÃO pratica exigência condicional** (o `selecionar()` dela itera lista de módulo **incondicional** e o esquema Zod é fixo), de modo que **o autor da T10, espelhando a irmã, cai na via COBERTA**.
- **O que fazer:** (a) a rede é barata e **não exige `sudo`** — `garantir_chaves_de_conteudo` só faz `grep` e `printf` sobre um path recebido por parâmetro; um caso que a carregue por `source` sobre arquivo temporário torna permanentes os **quatro** cenários que o QA já mediu ad hoc. (b) emendar **somente o comentário** das linhas 421-427, nomeando o resíduo. ⚠️ **Não emendar o `REVERTER EXIGE` do marcador**: o Gate 2 foi explícito — *"os três campos falam de reverter PARA o detector textual, e condicionalidade não é redação; emendar um `DECISÃO FECHADA` para registrar limitação que ele não reivindica é editar decisão fechada sem ganho"*.

### D43 · MEDIO · testability · T9 · Tech Review
- **Onde:** ⚠️ **17 ocorrências em 17 arquivos** — mas o que a T9 corrigiu foram só os **dois** do arquivo que ela criou (`apps/api/test/automacao-de-cobranca.e2e.spec.ts`)
- **Problema:** a afirmação *"a recusa de um esquema de SAÍDA não produz `422` — ela levanta na serialização e derruba a rota"* é **FALSA**, e circulou pelo projeto desde o Gate 2 da **T2**, ecoada pela T3, repetida pela T5 e inscrita no controlador da T9. **Refutada por medição** (com a projeção crua, a rota respondeu **`200` com `'08:00:00'`**) e **confirmada por leitura do mecanismo**: `apps/api/src/comum/esquema-publicado.ts` só traduz para JSON Schema (`z.toJSONSchema`), e `app.module.ts` registra **um único** `APP_INTERCEPTOR` (`GuardaDeContexto`). **Nenhum esquema de saída deste produto é aplicado em runtime.** A **ADR-0016 está correta e não promete** validação de saída — *"quem inventou o efeito foi a prosa"*.
- **Impacto:** era **R2 pela via da prosa**: *"é justamente o texto que um agente futuro lê para decidir se o `toMatch` é redundante; se ele acreditar que o `200` já discrimina, removerá a única asserção que mede a forma"*. **Consequência de produto que a refutação expõe**: campo a mais devolvido pelo serviço **chega ao cliente sem que nada barre** — a única rede é o `toStrictEqual` do teste da rota.
- **O que fazer:** ✅ **os dois pontos do arquivo novo foram corrigidos na rodada 2** (razão trocada pela medida, mais o fato de produto no cabeçalho), o que **corta a propagação para a T10**. **O que resta, em duas camadas**: **(a)** os 10 pontos em `packages/*` fora de marcador e os 5 em artefatos de spec — **intervenção dirigida fora do pipeline**, no molde das de 2026-08-09 e 2026-08-10; **(b)** ⚠️ **`packages/contracts/src/comum.ts:256`, que EXIGE ESCALADA AO USUÁRIO**: a premissa é a **razão (1)** do marcador `DECISÃO FECHADA` de `ESCALA_DA_METRAGEM`, cujo `REVERTER EXIGE` cobra literalmente *"provar que a recusa de um esquema de saída tem caminho que a converta em resposta de erro deliberada"* — **nenhum agente resolve sozinho** (§3 item 3). **Insumo para a escalada**: a **DECISÃO permanece CORRETA**, porque a razão (2) — `metragemTotal` é soma de ponto flutuante que sai da escala em ~1% dos casos — é **medida e independente**; o que caiu foi o **efeito** que as duas razões supõem.

### D44 · MEDIO · project_pattern · T9 · Tech Review
- **Onde:** `apps/api/src/automacao/automacao.controller.ts:148` — e mais **sete** iguais
- **Problema:** `const CAMPO_DO_CORPO = 'corpo'` nasce pela **oitava** vez em `apps/api/src` (as outras em `comodo`, `conjunto`, `imovel`, `mora`, `cobranca`, `superficie-de-cadastro`, `contrato`). **Todas as oito** são o mesmo valor passado ao mesmo parâmetro da mesma função — `validar(esquema, corpo, CAMPO_DO_CORPO)` —, cuja assinatura exige `campoPadrao: string` **sem valor padrão**.
- **Impacto:** baixo hoje. O custo é de **coerência**: o projeto **já pagou este exato débito três vezes** e sempre pela mesma correção — `validar()` (D38), `esquemaDoErro()` (D40) e `sobContextoDaSessao()` (D12) subiram para `apps/api/src/comum/` depois de N cópias byte a byte —, e os débitos D1 e D26 agendam a promoção **já ao terceiro** consumidor. Aqui são **oito**, e a T10 acrescentaria a nona **no mesmo arquivo**.
- **O que fazer:** exportar `CAMPO_PADRAO_DO_CORPO` de `apps/api/src/comum/validacao.ts` (ou dar valor padrão a `campoPadrao`) e importar nas oito bordas. ⚠️ Alcança `validacao.spec.ts` (CT-343), cujas três âncoras são igualdade de conjunto — **confira as três pontas** antes de declarar pronto.

### D45 · BAIXO · scope_deviation · T9 · QA + Tech Review
- **Onde:** `docs/specs/features/regua-de-cobranca/v1/tasks/T9.md`, §5.2 — e a §5.2 das tasks futuras que publicam rota
- **Problema:** `apps/api/test/contexto.e2e.spec.ts` e `apps/api/test/validacao.spec.ts` foram alterados **sem constar na §5.2**, e as duas edições eram **obrigatórias por construção** (as âncoras são **igualdade de conjunto** e reprovariam sem elas — que é o que existem para fazer). É a **NONA** reincidência do **D26 (F2/T6)**, cuja recomendação literal é declará-las.
- **Impacto:** nenhum risco técnico. **A reincidência é o desenho funcionando** — o que não funciona é o **planejamento**: *"o executor gasta parágrafos justificando a mesma divergência a cada rodada, e o Gate 2 gasta a auditoria confirmando que ela é benigna — nove vezes"*.
- **O que fazer:** fechar o D26 **do lado do planejamento**: a §5.2 das tasks que publicam rota passa a listar os arquivos de âncora obrigatórios — `contexto.e2e.spec.ts`, `cobertura-de-autorizacao.e2e.spec.ts` e, quando a borda importa `validar()`, `validacao.spec.ts`. ⚠️ **As três candidatas imediatas são a T10, a T11 e a T12 desta mesma fatia.**

### D46 · BAIXO · testability · T9 · Tech Review
- **Onde:** `apps/api/src/automacao/automacao.controller.ts` (a trilha) — e as outras **três** rotas do produto que gravam trilha
- **Problema:** o critério do log está **conforme** (dois campos, os seis valores da política de fora, só após o sucesso, e a leitura não registra — conferido por diff pelo Gate 2, a quem a §6.5 item 9 o atribuiu **nominalmente**). **O que não existe é prova**: nenhum caso da suíte assere conteúdo de trilha em rota alguma. *"Uma edição futura que acrescentasse `entrada` ao objeto de log publicaria os seis valores da política no registro estruturado, e nada acusaria — nem compilador, nem suíte, nem Gate 1."*
- **Impacto:** **R2 latente**. O vão é **do produto inteiro**, não desta task — mas ela acrescenta a **quarta** rota que grava trilha (`contrato`, `cobranca`, `configuracao_de_mora`, `politica_de_aviso`) **sem nenhuma delas ter rede**.
- **O que fazer:** dono natural é uma task de fatia com escopo de **observabilidade**. Forma sugerida: um caso único que monte o logger sobre destino capturável e afirme **por igualdade de chaves** que a linha carrega exatamente `{ empresaId, entidade }`. ⚠️ **Asserção por igualdade fecha a classe**; `not.toContain('janelaInicio')` fecharia um caminho e deixaria cinco abertos.
- **Nota do Gate 2, sem virar achado:** a linha de trilha roda **dentro** da unidade, **antes do commit** — se o commit falhar, a trilha registra ato que não aconteceu. É o que `mora.controller.ts` e `contrato.controller.ts` já fazem: **padrão estabelecido**, não regressão desta task.

### D47 · BAIXO · code_quality · T9 · Tech Review
- **Onde:** `apps/api/test/automacao-de-cobranca.e2e.spec.ts` (o parágrafo de propagação, ~l. 74-77) e a §5.3 da T11 e da T12
- **Problema:** o parágrafo novo fecha com *"nos casos que **T10, T11 e T12** acrescentarem a este arquivo"*, e **isso vale para uma das três**: a §5.2 da **T10** lista o arquivo (CT-629..CT-632); a **T11** declara *"§5.2 — Nenhum"* e cria arquivo próprio; e a **T12** modifica outras duas suítes, **sem mencionar este em nenhuma** subseção da §5.
- **Impacto:** baixo e **conservador nas duas frentes** — para a T10 a advertência está no lugar certo; para T11 e T12 ela **não alcança**, porque abrem arquivos onde este texto não é lido. **Ninguém é induzido ao erro** (aplicar `toStrictEqual` seria correto em qualquer um); o que a frase faz é **declarar coberta uma propagação que cobre um terço**.
- **O que fazer:** restringir a frase ao alcance real (*"nos casos que a T10 acrescentar"*); e, se a doutrina do `toStrictEqual` precisar alcançar T11 e T12, **a rede é a §5.3 delas** — acrescentar o arquivo à §5.3 da T12 (hoje ausente) e emendar o *"Motivo da Consulta"* da §5.3 da T11.

### D48 · BAIXO · testability · T9 · Tech Review
- **Onde:** `apps/api/test/automacao-de-cobranca.e2e.spec.ts`, l. 58-64 — o fato de produto no cabeçalho
- **Problema:** o texto declara, num arquivo de teste de fatia, um fato sobre **o produto inteiro**: *"nenhum esquema de saída deste produto é aplicado em tempo de execução"*. O Gate 2 **conferiu e ele procede em três pontos** (o `z.toJSONSchema` sem conferência; o `APP_INTERCEPTOR` único; e um grep por `validateResponses|ZodSerializer|useGlobalInterceptors|\.parse(` que devolve só dois `JSON.parse` de corpo alheio). **O que falta é rede**: nada reprova se o produto adquirir serialização validante — e a **própria ADR-0016 admite** *"uma camada de contrato adicional… desde que derive do esquema"*, que é o caminho por onde essa mudança chegaria.
- **Impacto:** **modo de falha conservador**, e é por isso que é BAIXO: se um dia houver validação de saída, o texto passa a mentir dizendo que as asserções são a única rede — **o leitor mantém asserção forte a mais, nunca a menos**. O custo é o comentário **envelhecer em silêncio**, que é a mesma classe do D43 com o dano invertido.
- **O que fazer:** **datar a medição** no próprio texto (o *"isto foi MEDIDO"* já está lá; falta o **quando**), ou promover o parágrafo a `DECISÃO FECHADA` com `REVERTER EXIGE: provar que nenhum interceptor ou camada derivada confere a saída em tempo de execução` — o que também **dá ao fato um gancho grepável** para quem registrar o interceptor futuro.

### D49 · BAIXO · project_pattern · T10 · QA + Tech Review
- **Onde:** `apps/worker/test/ambiente.spec.ts` (o bloco privado, ~l. 415-520) × `packages/shared/test/exigencia-de-ambiente.ts` (o lar canônico novo)
- **Problema:** a maquinaria de exigência de ambiente — `fonteDeclarando`, `variaveisConsultadasPor`, `variaveisExigidasPor`, `chavesEmitidasPor`, `chavesDeclaradasNaUnidade` e `semCaminhoDeProvisionamento` — passa a existir em **duas implementações independentes do mesmo contrato**: a privada do worker (`CT-643`) e a canônica que a `api` consome desde o `CT-639`. **A divergência não produz vermelho em processo nenhum** — cada suíte fica verde sem a outra.
- **Impacto:** o **D41 prescrevia a promoção**, e ela ficou **pela metade por razão legítima e verificada pelos dois gates**: o código a mover está sob `DECISÃO FECHADA — T8 / Gate 1 rodada 2` (`ambiente.spec.ts:444`), cujo `REVERTER EXIGE` — *"provar que nenhuma exigência de partida pode ser escrita por uma forma de leitura ou uma redação de recusa que o detector textual não reconheça"* — **não está satisfeito**, e a §3 item 2 da `nao-regressao.md` proíbe **nominalmente mover** código protegido. O Gate 2 confirmou por diff que a T10 **não tocou o arquivo do worker**: marcador intacto, código sob ele intacto. **Não houve violação.**
- **O que fazer:** ✅ **a escrituração foi feita pelo orquestrador ao fechar a task** — marcador `DÉBITO COM GATILHO — D49 · F3/T10` no docblock do `CT-643`, mais a linha no índice do `CLAUDE.md` e a nota apontando o lar canônico (as três edições **fora** do código sob marcador). **O que resta é a escalada**, e o Gate 2 deixou o argumento pronto: *"a promoção PRESERVA o mecanismo protegido — a derivação por observação continua idêntica no lar canônico — e muda apenas o ENDEREÇO dele"*, o que ele lê como **argumento forte para o usuário autorizar sem que a decisão reabra**. Dono: quem atacar o D49.

### D50 · BAIXO · code_quality · T10 · QA + Tech Review
- **Onde:** `packages/db/test/unidade-de-trabalho.spec.ts:847-848`
- **Problema:** a linha `SUT_IS_CORRECT_BECAUSE:` afirma *"Os dois fragmentos que a igualdade produziu — `colunasDaCandidata` e `origemDaCandidata` — não saem do pacote"*. **`origemDaCandidata` nunca existiu**: `grep -rn` sobre a árvore versionada devolve **só essa menção**. A extração do `FROM` e das quatro junções foi tentada e **revertida**, porque cegava o `CT-612 (c)`.
- **Impacto:** é a **forma barata da R3**. O texto está dentro de um `SUT_IS_CORRECT_BECAUSE:` — *"exatamente o lugar onde a próxima rodada vai buscar a razão da mudança"*. Quem o ler vai procurar o símbolo, não achar, e pode **"restaurar" a extração**, reabrindo a regressão de prova que a reversão fechou.
- **O que fazer:** trocar por *"O fragmento que a igualdade produziu — `colunasDaCandidata` — não sai do pacote"*, e remeter à explicação da reversão que já está no docblock de `localizarCandidataAoAviso` e no de `SIMBOLOS_COM_ESTADO_DA_COBRANCA`.

### D51 · BAIXO · architecture · T10 · Tech Review
- **Onde:** `apps/api/src/automacao/automacao.service.ts:131` (o construtor novo)
- **Problema:** até a T10, `AutomacaoDeCobrancaService` **não tinha construtor**, e o docblock chamava essa ausência de *"o mecanismo"*. Agora tem, e o enunciado foi reescrito para *"a ausência de `AcessoAoBanco` no construtor é o mecanismo"*. O Gate 2 julgou a reescrita **conforme e mais precisa, não enfraquecimento**: `PortaDeEnvioDeEmail` **não abre transação, não alcança tabela e não conhece empresa**, os quatro métodos seguem recebendo `tx` de quem abriu a unidade, e nenhum chama `emUnidadeDeTrabalho`.
- **Impacto:** o que mudou é a **verificabilidade**. `mora.service.ts` e `cobranca.service.ts` seguem sem construtor algum, este vira a exceção, e **não existe asserção no repositório** que reprove `@Inject(TOKEN_ACESSO_AO_NEGOCIO)` num `*.service.ts` — a barreira é prosa em três docblocks mais o marcador de `unidade-de-trabalho.ts`. *"Acrescentar `@Inject(TOKEN_ACESSO_AO_NEGOCIO)` a esse construtor passa a ser uma edição de uma linha que nenhum gate automático pega — antes, criar um construtor era, por si, um evento visível na revisão."*
- **O que fazer:** caso por varredura na suíte da `api` afirmando **por igualdade** que nenhum `apps/api/src/**/*.service.ts` nomeia `TOKEN_ACESSO_AO_NEGOCIO` nem `AcessoAoBanco` na lista de parâmetros do construtor, no idioma de `validacao.spec.ts` (definição única + lista fechada), com prova de falsificação sobre cópia em memória.

### D52 · BAIXO · scope_deviation · T10 · Tech Review
- **Onde:** `packages/db/src/envio-de-cobranca.ts:265` e `packages/db/src/index.ts:470` — e a §1 e a §5.2 da T10
- **Problema:** `localizarCandidataAoAviso` é **símbolo público novo de `@sysloc/db`** e a task não o declara em lugar nenhum: a §1 lista só os dois manipuladores e `TOKEN_PORTA_DE_EMAIL`, a §5.1 declara zero arquivos de produção a criar, e a §5.2 não lista nenhum dos dois arquivos. **A construção é necessária e bem resolvida** — nenhuma função existente produzia uma `CandidataAoAviso` a partir do código, e escrever a consulta na borda deixaria a projeção do manual livre para divergir da do automático, que é **o defeito de origem da fatia**; a §3.2 da própria task prescreve o comportamento. **Faltou a declaração, não o trabalho.**
- **Impacto:** a §1 é o que o **guard de disjunção de símbolo** consome. Símbolo público não declarado ali pode ser criado por uma task e consumido por outra **do mesmo lote** sem que o guard perceba. O Gate 2 reconferiu os outros 11 arquivos fora da §5.2 e **todos** são consequência mecânica de trabalho declarado — este é o único resíduo.
- **O que fazer:** acrescentar `localizarCandidataAoAviso` à §1 da T10 e os dois arquivos à §5.2. **Nenhuma mudança de código.**

### D53 · BAIXO · adr_compliance · T10 · Tech Review
- **Onde:** `apps/api/src/automacao/automacao.controller.ts:63-67` e `automacao.service.ts:96-99`
- **Problema:** os dois docblocks afirmam que *"a ADR-0021 o alcança pela **cláusula de ato de negócio governado pela natureza**, não pela de transição"*. Abrindo a `Decision` literal da `0021`, o sujeito da decisão inteira é *"Toda transição de estado de entidade de negócio"*, e o `Context` confirma o alcance. **Não existe cláusula autônoma** para ato sensível que não seja transição. ⚠️ **O código NÃO contradiz a ADR** — o disparo de fato não move `status`, e a governança escolhida (rota própria + chave de ação, conjunção inteira) é exatamente o que o princípio dela prescreveria. **O defeito é de citação, não de conformidade** — e a citação errada veio da **task**, que a escreve na §7.
- **Impacto:** é o **mesmo modo de falha que já custou a emenda de 2026-08-10** àquela ADR: um gate futuro que abrir a `Decision` literal lê divergência onde houve decisão, e a rodada se gasta reabrindo a classificação. O `CLAUDE.md` já registra que citar ADR pela paráfrase **divergiu do texto real** neste projeto.
- **O que fazer:** reformular as duas citações para *"o disparo não é transição de estado, e a ADR-0021 não o alcança pelo texto; a governança escolhida segue o mesmo princípio dela — a natureza do ato — e nada nela é contrariado"*. Se o usuário preferir vinculação formal, o caminho é emenda declarando o alcance, no molde da de 2026-08-10. ⚠️ **Corrigir a §7 da T10 junto**, senão a citação volta.

### D54 · BAIXO · project_pattern · T11 · Tech Review
- **Onde:** `apps/api/test/equivalencia-com-o-oraculo.spec.ts` (`CODIGO_NO_ASSUNTO`), com as irmãs em `packages/db/test/execucao-da-regua.spec.ts:257` e `apps/worker/test/regua.spec.ts:183`
- **Problema:** `CODIGO_NO_ASSUNTO = /COB-\d{4}-\d{7}/` é a **terceira cópia** do mesmo símbolo, com o mesmo nome e o mesmo propósito, em três pacotes. ⚠️ **A cópia nova JÁ diverge das outras duas** — só ela carrega a flag `u`. A largura **sete** é decisão medida e protegida por `DECISÃO FECHADA` em `packages/contracts/src/cobranca.ts`, e as três cópias a redigitam sem ponteiro para ela. **O que faltava era a ESCRITURAÇÃO, não a correção**: o executor acertou em não refatorar (§4.5 da `nao-regressao.md` proíbe o refactor oportunista), e o marcador é justamente o que substitui o refactor adiado.
- **Impacto:** diferido, sem efeito em asserção nenhuma hoje. O custo é de rodada futura: endurecer uma cópia (ancorar com `^...$`, alargar o ano, mudar a largura) deixa as outras duas para trás sem que nada acuse — que é o modo de falha que esta mesma §2 registra em três entradas separadas, e que o projeto já pagou três vezes (`validar()` no D38, `esquemaDoErro()` no D40, `sobContextoDaSessao()` no D12, todos promovidos só depois de N cópias byte a byte).
- **O que fazer:** ✅ **A escrituração JÁ FOI EXECUTADA pelo orquestrador ao fechar a task** — marcador `DÉBITO COM GATILHO — D54 · F3/T11` no ponto da cópia (5 campos, no molde do D1/D26) mais a linha no índice do `CLAUDE.md`, com as duas pontas conferidas pelo `CT-907` (`@sysloc/shared` **218/218** verde após a edição). **O que resta é o débito em si**: promover o molde a um módulo de acessório de teste compartilhado entre `apps/` e `packages/` quando o **quarto consumidor** chegar, ou na primeira alteração da forma do código de cobrança. É vizinho do **D28 (F0/T5)** e provavelmente fecha junto com ele.

### D55 · BAIXO · best_practices · T11 · QA (rodada 2) + Tech Review
- **Onde:** `apps/api/test/equivalencia-com-o-oraculo.spec.ts:816` (a guarda) e `:589` (o `let` de módulo `tabelaDoProduto`)
- **Problema:** ⚠️ **Os dois gates convergiram no mesmo achado, por caminhos independentes** — registrado uma vez só, e não duas. O CT-637 consome a `tabelaDoProduto` que o CT-636 publica: a dependência é **ordinal** (a ordem de declaração dos `it` no arquivo), quando um desenho **estrutural** equivalente estava ao alcance. Nada disso é defeito: a dependência é declarada, guardada por `throw` nomeado, e determinística sob a configuração real (`apps/api/vitest.config.ts` não declara `sequence.shuffle` nem `concurrent`). A ordem fina também está certa — a publicação vem **antes** do `toStrictEqual`, e é o que devolve ao CT-637 o poder de **nomear** o par divergente em vez de morrer na guarda.
- **Impacto:** contido à ergonomia de execução, sem risco de falso verde (a guarda levanta com mensagem nomeada em vez de comparar contra `undefined`, e a suíte completa executa os dois na ordem certa). O custo é diagnóstico: `pnpm --filter @sysloc/api test -- -t "CT-637"` — forma documentada na `testing-stack.md` — **não roda o CT-637; ela levanta**. Quem investigar uma divergência nova paga os 300 s do `LIMITE_CASO_MS` do CT-636 a cada tentativa.
- **O que fazer:** reunir os dois `it` sob um `describe` e mover a medição (passos 1 a 6 do CT-636) para um `beforeAll` desse `describe`, que publica a tabela como `const` de escopo — os dois casos passam a rodar isolados por `-t`, **sem semear os dez cenários duas vezes** e sem perder a rastreabilidade `CA-09 → CT-636`/`CT-637`. Alternativa barata, se mexer num arquivo já aprovado nos dois gates não compensar: uma linha no docblock do CT-637 declarando que ele não roda por `-t` isolado.

### D56 · BAIXO · documentation · T11 · QA (rodada 2)
- **Onde:** `apps/api/test/equivalencia-com-o-oraculo.spec.ts:863` (o comentário do incremento de `coincidentes`)
- **Problema:** o comentário generaliza a partir de **um** cenário. Ele diz que o contador sobe *"uma vez por par que coincide"*, o que é verdade, mas a redação sugere que suprimir uma célula qualquer seria pego **por esta asserção** — e não é assim para a célula **divergente**: suprimir o REG-08/manual deixaria `coincidentes` em 19 do mesmo jeito, com a reprovação vindo da asserção **anterior** (o conjunto de divergências deixaria de ter o único elemento).
- **Impacto:** nenhum sobre o comportamento — a rede existe e está correta, e o par de asserções cobre as duas classes. O risco é de leitura: um agente futuro que confie no comentário pode julgar a asserção anterior redundante e removê-la, abrindo o vão que o comentário diz não existir.
- **O que fazer:** reescrever o comentário nomeando a divisão de trabalho entre as duas asserções — `expect(divergencias)` cobre a célula divergente, `expect(coincidentes).toBe(19)` cobre a travessia das outras dezenove. É edição de comentário, sem tocar em asserção.

### D57 · BAIXO · code_quality · T12 · Tech Review
- **Onde:** `apps/api/test/autorizacao-do-dominio.e2e.spec.ts` (o bloco da montagem instrumentada), com a cópia de origem em `apps/api/test/automacao-de-cobranca.e2e.spec.ts`
- **Problema:** a montagem instrumentada é a **segunda cópia literal** do mesmo bloco (`Test.createTestingModule({ imports: [AppModule] }).overrideProvider(TOKEN_PORTA_DE_EMAIL)` → `createNestApplication(new FastifyAdapter())` → `setGlobalPrefix` → `listen`, mais a mutação de `process.env.PORT`). Ela **não deriva de `criarAplicacao()`** e por isso omite cinco coisas que a montagem de produção faz: `logger: false`, `abortOnError: false`, o `exclude: ROTAS_FORA_DO_PREFIXO` do prefixo global, `publicarContrato()` e `enableShutdownHooks()`. ⚠️ **O padrão do projeto foi seguido com fidelidade, não inventado** — o executor copiou o bloco que já existia; o que o débito registra é a consequência de agora existirem **duas** cópias.
- **Impacto:** **nenhum hoje**, e isso foi verificado em vez de presumido: nenhuma das cinco omissões alcança o que os casos medem — guarda, filtro de erro (o envelope da ADR-0017) e interceptador de contexto são registrados por `APP_GUARD`/`APP_FILTER`/`APP_INTERCEPTOR` **dentro do `AppModule`**, e as quatro rotas sob prova vivem sob o prefixo. A omissão do `exclude` está inclusive comentada no ponto, com a razão. O risco é **futuro e silencioso**: um global que venha a ser registrado **fora** do `AppModule`, dentro de `criarAplicacao()`, não alcançaria nenhuma das duas montagens instrumentadas, e a suíte seguiria verde afirmando exercitar *"a aplicação de produção montada por inteiro"*. **A divergência não reprova caso algum — a asserção que a acusaria não existe.**
- **O que fazer:** ✅ **A escrituração JÁ FOI EXECUTADA pelo orquestrador ao fechar a task** — marcador `DÉBITO COM GATILHO — D57 · F3/T12` no ponto (5 campos) mais a linha no índice do `CLAUDE.md`, com as duas pontas conferidas pelo `CT-907` (`@sysloc/shared` **218/218** verde após a edição). **O débito em si**: derivar a montagem instrumentada da mesma composição que atende. ⚠️ **O Gate 2 desaconselha extrair AGORA** — a convenção deste repositório agenda a promoção de símbolo duplicado no **terceiro** consumidor (é a forma literal do D1 · F3/T2 e do D26 · F3/T8), e aqui são dois. Fecha na terceira suíte que precisar da montagem, ou na primeira vez que `criarAplicacao()` registrar um global fora do `AppModule` — o que vier antes.

---

## 3. Tasks Bloqueadas

✅ Nenhuma task bloqueada.

---

## 4. Notas para Revisão Humana

### ✅ O run TERMINOU em 2026-08-12 — a fatia está fechada e staged

A pausa controlada foi retomada pelo caminho **(a) Retomar nos gates**: a T11 foi direto ao Gate 2 e
a T12 rodou inteira. **Nada foi commitado** — o pipeline nunca commita, e isso é por desenho.

### 4.0 O mesmo antipadrão reprovou as duas últimas tasks, e a segunda vez mostra o pipeline funcionando

Vale ler junto, porque é o achado mais transferível deste run. Nas **duas** provas de fecho — as duas
tasks que **não acrescentam código de produção** — a única rejeição foi por **AP-29**
(`tautological_assertion`): uma asserção que **não podia falhar**, apresentada como garantia.

- **T11 (Gate 1)**: `expect(CENARIOS.length * CAMINHOS.length - divergencias.length).toBe(19)` era
  aritmética de constantes — a linha anterior já fixava o comprimento em 1.
- **T12 (Gate 2)**: `expect(status).not.toBe(404)` vinha **depois** de `expect(status).toBe(403)`;
  como o `expect` lança ao reprovar, a negativa só era avaliada quando o status já era `403`.

⚠️ **Na T12 o Gate 1 classificou como `BAIXO` e o Gate 2 elevou para `ALTO` — e o interessante é que
o Gate 1 declarou a divergência de rubrica em vez de escondê-la**, registrando o achado com arquivo,
linha e correção *"justamente para que essa decisão seja possível sem re-auditoria"*. O Gate 2 julgou
pela letra, com três razões, e registrou que a leitura substantiva do Gate 1 estava **correta no que
afirma**: *"o critério é a infalibilidade, não a perda de detecção. O que se perde não é cobertura, e
sim a garantia NOMEADA."* Nos dois casos o QA **reproduziu o mutante com o CONTROLE** — a versão
antiga e a nova sob o mesmo mutante —, e é o par que prova o ponto: na T12, a rodada 1 reprova pela
mensagem genérica e a rodada 2 pela mensagem nomeada.

**A lição operacional**: as duas ocorrências nasceram do mesmo instinto — escrever a asserção que
*parece* provar a propriedade que o caso anuncia. É por isso que a T12 fechou a ordem sob uma
`DECISÃO FECHADA`: a ordem de duas asserções é invisível a compilador, suíte e QA, e é exatamente o
que uma passagem de arrumação restaura para a forma idiomática — **R3 em forma pura**.

### 4.0.1 Duas notas operacionais medidas, que valem para os próximos runs

1. **`git diff <sha> -- <arquivo untracked>` reporta o arquivo como `deleted file mode`** (na T11,
   com **−1415** linhas). É artefato: `git diff <sha> -- path` só considera caminhos presentes no
   índice. O contorno é o `git add -N` do orquestrador — sem ele, *"quem repetir a auditoria com o
   comando do prompt vai concluir, erradamente, que a task apagou a própria suíte"*.
2. **O `attempt_sha` é um snapshot do ÍNDICE, não da árvore.** Consequência que o Gate 2 da T12
   levantou: em `DELTA`, toda edição **não-staged anterior** à rodada aflora no
   `git diff <attempt_sha>`. Ali foram nove arquivos de `docs/`, todos conferidos um a um —
   **nenhum remove marcador ou linha do índice de débito**.

### 4.0.2 A T11 custou **uma rodada de correção**, e o achado que a causou vale ser lido por quem retomar:
a asserção `expect(CENARIOS.length * CAMINHOS.length - divergencias.length).toBe(19)` **não podia
falhar** — a linha anterior já fixava o comprimento em 1, e os dois `.length` são constantes
literais. O que a tornou achado **ALTO**, e não ruído, foi o **comentário**, que a apresentava como
rede contra *"o laço não ter percorrido tudo"*. A correção conta as células que **coincidiram dentro
do laço**, e o QA **reproduziu o par de mutantes com o controle**: o mesmo mutante **sobrevive** na
versão antiga e **reprova** na nova. É a demonstração mais limpa deste run de que a diferença entre
prova e decoração é medível.

### 4.1 O `CT-013` do `verificar-golden.sh` reprova, e **não é regressão desta fatia**

O `verificar-golden.sh` termina com **saída não-zero** pelo `CT-013` (*"a credencial aparece na árvore versionada"*). É **herança medida**, provada idêntica num worktree limpo em `fb93915` e já registrada na §4 do `_run/run-report.md` da fatia `cobranca-e-mora`. Causa provável, não confirmada: colisão de agulha — a credencial do ambiente legado é palavra de dicionário de 5 caracteres e o casamento é por token sobre a árvore inteira.

**Medido nas duas rodadas da T1**: as **mesmas 72 ocorrências** antes e depois, e nenhum dos arquivos desta task contribui com uma sequer (conferido por filtro sobre a lista completa). Os arquivos apontados são `apps/api/**`, `packages/auth/**`, `packages/db/**` e documentos de spec antigos.

> **A comparação que vale é CASO A CASO, nunca o código de saída do script.** Uma rodada futura que leia o `exit 1` como reprovação vai "corrigir" o que o CA-9 da própria task manda **registrar e não corrigir**.

### 4.2 O bloqueante da rodada 1 fechou a **classe**, não o caminho

O Gate 2 reprovou a rodada 1 com um `ALTO`/`architecture`: o `PROCEDENCIA.md` tinha **dois geradores** — `capturar.py` reescrevia o arquivo inteiro e apagava a §5 (`## 5. Inventário dos artefatos`) que o extrator grava. Duas consequências verificadas: **(a)** `verificar-captura.sh` executa `capturar.py` dentro do próprio `main`, de modo que rodar aquele verificador deixava o `CT-601` vermelho **em silêncio**; **(b)** depois da F7 a §5 ficaria **irrecuperável**, porque o caminho que a regravava estava atrás de `exigir_legado_de_pe` por mera posição de chamada.

A correção instalou uma **fronteira de autoria posicional**: `capturar.py` passa a gravar por ponto único (`gravar_procedencia`, sob `DECISÃO FECHADA`) que recorta e reanexa tudo a partir de `\n## 5. ` **sem interpretar** — uma §6 ou §7 alheia futura é preservada de graça; e o extrator ganhou `--so-manifesto`, que sai de `main()` **antes** de qualquer pré-condição do legado e regrava a §5 a partir do `INVENTARIO` declarado e do `modified` já carimbado no artefato versionado.

O Staff confirmou que a fonte **secundária** de vermelho que ele apontara na rodada 1 foi eliminada: o `exit 1` do `verificar-golden.sh` volta a ter causa única e conhecida (o `CT-013`).

### 4.3 O `CT-640` prova uma invariante **topológica** por asserção estática — e isso é deliberado

Vale registrar para que uma rodada futura não o "conserte". No mutante **substitutivo** (a forma mais fiel ao defeito original, com um só `write`), quem detecta é a asserção **estática de ponto único**, não a comportamental — porque `medir_autoria_do_manifesto` chama `gravar_procedencia` diretamente e essa função continua correta no mutante.

Os dois gates convergiram, independentemente, em que o desenho é o certo, e o Staff foi além: para esse mutante a estática **não é fallback, é o único mecanismo que pode funcionar**. Três razões: **(a)** o `main` de `capturar.py` exige o legado de pé, e alcançá-lo comportamentalmente obrigaria a pôr o caso no `verificar-captura.sh` — justamente o que a correção rejeita, porque o guarda morreria no dia em que passa a importar (F7); **(b)** a §5 da `nao-regressao.md` prescreve literalmente *"ataque a topologia, não a ocorrência"* — um teste comportamental provaria que o caminho de escrita **atual** está certo, o estático prova que **não existe um segundo caminho**; **(c)** o par exigido pela `testing-stack.md` está do lado certo — o difícil ganhou asserção (junção byte a byte, o negativo que discrimina *"preservou tudo"* de *"não escreveu nada"*, o arquivo virgem, a topologia), em vez do fácil.

O resíduo é a **largura do padrão**, e está registrado como **D5**.

### 4.4 A prova de falsificação foi **executada**, e o QA acrescentou um mutante próprio

A `.claude/rules/testing-stack.md` exige prova de falsificação para asserção estática. Ela não foi descrita — foi rodada, com `sha256sum` conferido contra a baseline na reversão de cada mutante:

| Mutante | Autor | Falhas |
|---|---|---|
| preservação removida de `compor_manifesto` | executor | 2 |
| gravação de volta fora do ponto único (o defeito original, forma aditiva) | executor | 2 |
| `--so-manifesto` atrás de `exigir_legado_de_pe` | executor | 4 |
| gravação vácua — `compor_manifesto` devolve `anterior` | **QA** | 3 |

O quarto é o que fecha o raciocínio: ele prova que o negativo discriminante funciona — *"preservou tudo"* **não** é indistinguível de *"não escreveu nada"*. O QA também reproduziu o defeito original na forma **substitutiva** (1 falha, pela asserção estática de ponto único).

### 4.5 Duas premissas do planejamento foram **confirmadas por medição**, e uma foi refinada

- **A premissa do `sudo` é falsa, como o task plan afirmava.** `grep -rn sudo deploy/scripts/caracterizacao/` devolve vazio; o acesso é por `docker compose exec -T`. A task de prazo **não travou** — ao contrário da fatia anterior, onde essa premissa herdada custou a paralisação da única task cuja janela não reabre.
- **As 752 linhas eram número medido, e bateram.** ⚠️ **O arquivo tem 759 linhas** — 7 de cabeçalho de procedência + **752 de fonte**. As asserções medem o **corpo**, não o arquivo. **Não "corrija" o 752 para 759.**
- **A §3.1 da task mandava usar `bench --site frontend`, e o executor não seguiu — deliberadamente, e o Staff julgou conformidade SUPERIOR.** O `CT-012` pré-existente varre `deploy/scripts/caracterizacao/` por esse literal e reprova toda invocação sem `backup` como subcomando; o molde literal reprovaria dois casos preexistentes **e executaria Python dentro do site de produção** para ler um campo de texto. A leitura é `SELECT` no serviço de banco, autenticada como o **usuário da base** (nunca `root`), com credencial por `stdin`. A §3.1 descreve o **molde** do acesso (sem `sudo`, sem escrita, por `docker compose exec -T`), não o mecanismo obrigatório.

### 4.6 `.gitattributes` nasceu fora do escopo declarado, e o desvio foi julgado justificado

O host tem `core.autocrlf=input` e o campo `script` do legado guarda **CRLF**. Sem a declaração `-text`, o git normalizaria o artefato no índice e **um clone novo divergiria do que o extrator produz** — o critério de aceite 6 (três `sha256sum` iguais) seria falso fora desta máquina. Medido nas duas direções (blob `610a620a` sem a declaração, `610d0a620d0a` com ela), e o `CT-601` afirma a declaração por `git check-attr`.

O Staff registrou como `scope_deviation` **BAIXO** e **não pediu reversão**: a declaração é a mínima possível — um caminho, um atributo, nenhum glob — e é condição de verdade de um critério de aceite da própria task. O arquivo foi **declarado na §5.1** na rodada 2, junto com `capturar.py` na §5.2.

> **Sinal para o refinamento das próximas fatias**: está registrado em `_run/rule-candidates.md` como candidato a regra — *"arquivo de configuração do repositório exigido pela corretude de um entregável entra na lista de Arquivos a Criar da task, e não como descoberta do executor"*. Antecipar isso para a spec custa uma linha; descobrir no gate custou uma rodada.

### 4.7 Lacunas registradas que **não** viraram achado

Nenhuma das duas foi classificada como problema; ficam escritas para quem estender os casos.

- **Cobertura de falsificação do `CT-601`**: o `CT-602` exercita **dois dos ~14 pontos estáticos** (a contagem de linhas e a bijeção com o manifesto), mais o controle. Os demais — `git check-attr`, cabeçalho canônico, delimitador, cruzamento do `modified` artefato × manifesto, terminador CRLF — não têm mutante. A escolha recai sobre as duas propriedades de maior carga, e o método é o canônico.
- **Três caminhos de erro de `ler_modificado_do_artefato`** (artefato ausente, cabeçalho sem a linha da versão, versão vazia) não têm caso que os exercite. São alcançáveis por entrada externa, estão guardados por `[[ -n ]]` explícito, e nenhum CA da task os exige.

### 4.8 Nota operacional para as 11 tasks restantes

- **Disco do host em 94%**, com 2,0 GB livres. Rode `rm -rf /tmp/sysloc-banco-*` entre execuções: `No space left on device` **se disfarça de teste vermelho** e queima tentativa.
- **Meça a suíte POR PACOTE** (`pnpm --filter @sysloc/<pacote> test`). O `turbo run test` aborta os pacotes irmãos quando um falha, e a saída agregada não carrega contagem confiável dos interrompidos.
- **O `CT-907` é flaky pré-existente**: falha por **timeout** (5000 ms sob disputa de CPU) é o flake; falha por **asserção** é achado. Ele **não se manifestou** em nenhuma das execuções da T1.

### 4.9 ✅ A pausa controlada foi retomada, e a T3 fechou

**Registro histórico.** O run pausou em 2026-08-11 a pedido do usuário, com a T3 no ponto exato entre os dois gates: Gate 1 (QA) `APROVADO` com os quatro baldes vazios, Gate 2 pendente. Não houve falha, bloqueio nem esgotamento de tentativas.

A retomada, na mesma data, escolheu o caminho **(a) Retomar nos gates**: a T3 foi direto ao Gate 2, **sem reexecutar o executor nem o Gate 1** — teria sido queimar trabalho já validado e subir instância efêmera com migração completa num host com disco em 94%. O Gate 2 devolveu `APROVADO_COM_OBSERVACOES` na rodada 1, com **zero bloqueantes** e um único `BAIXO`/`project_pattern` (o **D8** da §2). Os 12 caminhos da task foram staged e a T3 está `Concluído`.

**Os quatro julgamentos que a pausa deixou encomendados foram todos entregues pelo Gate 2:**

1. **`scope_deviation` sobre os dois arquivos fora da §5.2** — **não é desvio**, e por razão mais forte que *"a suíte ficaria vermelha"*: o cabeçalho **pré-existente** de `packages/db/src/esquema/negocio.ts` (linhas 57-80, fora de todo hunk desta task) é o inventário canônico das listas-espelho e **nomeia os seis alvos por extenso**, incluindo `SIMBOLOS_ESPERADOS` em `test/unidade-de-trabalho.spec.ts` e o homônimo em `deploy/scripts/instalacao/verificar-migracao.sh`, mais o `grep -rln` que não envelhece. O executor seguiu o inventário do repositório; foi a **§5.2 da task que declarou 4 dos 6**. Conferido no diff: as duas mudanças são **adição pura** — `unidade-de-trabalho.spec.ts` tem zero linhas removidas, e `verificar-migracao.sh` troca a linha da lista por um **superconjunto estrito** (13 → 15, nada removido).
2. **As três ocorrências de `DÉBITO COM GATILHO`** na `0011` (l. 26-27), na `0012` (l. 24) e em `negocio.ts` (l. 40) são **prosa**, confirmado pelo texto: nenhuma tem a forma canônica da §3-B (faltam `O QUÊ`/`QUANDO FECHA`/`POR QUE NÃO AGORA`/`ÍNDICE`), as três aparecem em parágrafo corrido e as três **referenciam o D20 vivo na `0010`**. Nenhum marcador novo foi emitido; nenhuma linha nova no índice do `CLAUDE.md` é devida. ⚠️ **Efeito colateral a registrar**: o `grep -rl --exclude-dir=dist "DÉBITO COM GATILHO" apps packages deploy` da §3-B passa a devolver **três arquivos sem marcador**. Não é falso órfão (as menções apontam para débito real, vivo e com §2 correspondente) e não muda o veredito do comando, mas quem fizer a conferência de fecho no sentido *marcador → registro* terá três arquivos a abrir e descartar.
3. **`Garantias removidas: nenhuma` está CONFIRMADA**, conferida linha `-` a linha `-` nos oito diffs. As remoções se resumem a prosa de docblock reescrita (`quatro`→`cinco`, `treze`→`quinze`, `doze`→`quatorze`), um `import` do Vitest substituído por bloco maior, e `toHaveLength(12)`→`(14)`. As **cinco listas auditadas por igualdade** foram conferidas no estado final: `TABELAS_LEGITIMAS` em 15 preservando `VISAO_DA_COBRANCA_DERIVADA` na posição alfabética; `TABELAS_DE_NEGOCIO_ESPERADAS` em 14 (Vitest) e 15 (shell); `SIMBOLOS_ESPERADOS` só cresceu. **Nenhuma igualdade virou contenção** — ao contrário, `papel-de-conexao.spec.ts` ganhou a frase que proíbe a troca por `toContain`.
4. **A obrigação da projeção `HH24:MI` atravessa para a T5** — permanece aberta e está na §4.10 abaixo, que **não** foi fechada por esta task.

**A `0010` está byte a byte intocada** — `git diff 6753e5b -- packages/db/migracoes/0010_seguranca_cobranca.sql` devolve vazio; a `DECISÃO FECHADA` e o marcador do D20 seguem íntegros, e nenhum outro `DECISÃO FECHADA` do repositório foi alterado, movido ou removido.

⚠️ **O `CLAUDE.md` continua NÃO staged**, e ele carrega a linha do índice do **D3 (F3/T1)**. **O commit que levar o marcador de `consultar_o_legado` precisa levar o `CLAUDE.md` junto**, senão o `CT-907` fica vermelho (marcador vivo sem linha no índice). O arquivo também tem modificações de documentação anteriores a este run — o agrupamento é decisão do usuário.

### 4.10 ⚠️ Obrigação que atravessa para a T5 — não a perca na retomada

O Gate 2 da **T2** levantou, e a **T3** fixou: `esquemaDaPoliticaDeAviso` (o esquema de **saída**) mantém o regex `HH:MM` nos dois horários.

> **Se a projeção da coluna `time` não for `to_char(coluna, 'HH24:MI')`, o driver devolve `'09:00:00'`, o esquema de saída recusa, e isso NÃO produz `422` — levanta na serialização e DERRUBA a rota de leitura da política.**

A T3 fixou a obrigação em três pontos (docblock de `politicaDeAviso` em `negocio.ts`, cabeçalho da `0011`, e **prova executável** no passo 7 do CT-608, que assere `{ cru: '09:00:00', projetado: '09:00' }` e roda o `safeParse` sobre os dois). **O que nenhuma prova alcança é a consulta que a T5 vai escrever.**

- A **T5** precisa obedecê-la em `packages/db/src/automacao-de-cobranca.ts` **e ter asserção própria sobre a linha lida**. O CT-607 já lê pela projeção correta e serve de molde.
- A **T9** precisa de caso de integração que leia a política gravada **de ponta a ponta**.
