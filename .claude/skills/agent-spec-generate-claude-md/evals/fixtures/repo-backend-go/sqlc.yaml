version: "2"
sql:
  - engine: "postgresql"
    queries: "db/queries"
    schema: "db/migrations"
    gen:
      go:
        package: "users"
        out: "internal/users"
        sql_package: "pgx/v5"
