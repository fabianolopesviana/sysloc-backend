# Workflow report — documentos-e-confirmacao/v1

> Telemetria de pipeline (append-only). O relatório humano é o `_run/run-report.md`.

## Challenge Session — 2026-08-12 (artifact: tech_spec.md)

- Questões processadas: 7 (5 respondidas pelo usuário, 2 fechadas por medição no código)
- Conflitos de terminologia resolvidos: 1 (`carimbo` do legado × **Carimbo** financeiro do glossário → **Marca de cancelamento**)
- Contradições internas do artefato corrigidas: 3
  - carga da fila: `portadorId` (§3.1, §3.6) × `segredo` (§4.3, §5.1-B.1, CT-734) → `segredo`
  - RD-07 / §5.1-B.1 disparavam em todo `PUT` × CT-718 exige o contrário → condição `IS DISTINCT FROM` no `UPDATE`
  - `esquemaDaPessoa` declarado com 14 campos; medido: **15**
- Divergências spec × ADR encontradas: 2 (a spec afirmava "nenhum conflito")
  - ADR-0028: `Decision` diz que a rota de bytes não declara forma do corpo de sucesso; §4.1 prescreve `schema: {type:'string', format:'binary'}` → resolvido por **leitura conjunta registrada** (`format: binary` é o idioma OpenAPI para opacidade, não descrição de forma)
  - ADR-0027: `Decision` exige portador "de uso único"; RD-10 manda o consumido continuar resolvendo → resolvido por **leitura conjunta registrada** (uso único é o EFEITO, imposto por `UPDATE … WHERE consumido_em IS NULL RETURNING`; a alternativa de filtrar `consumido_em` na função quebraria a RN-10 e foi proibida por escrito)
- Escopo subdeclarado corrigido: **12 arquivos** que o `DROP COLUMN` quebra e a §3.4/§3.6 não listavam (medidos por `grep -rl`, 22 no total contra 10 declarados). Nova §3.6.1
- Decisões de desenho cravadas antes da execução: 3
  - `colunaLivre` da entidade `contrato` em `isolamento.spec.ts` → `valor_total_contrato` + `valorDaEscritaCruzada: '9999.99'`, pelo precedente de `contrato_fiador`
  - condição do disparo no `UPDATE` por `RETURNING OLD./NEW.` (PG 18; `psql 18.4` e `embedded-postgres 18.4.0-beta.17` conferidos)
  - gatilho como 5º parâmetro opcional do construtor de `SuperficieDeCadastro`, suprido só pelo `LocatarioController` — sem ramo por papel na classe compartilhada
  - `202` do reenvio mantido e justificado por escrito (é o 1º do produto; os 20 `@HttpCode` existentes são todos `200`)
  - CT-725 fechado nas duas camadas: hexadecimal → `422` (esquema), `digest('base64url')` de 43 caracteres → `404` (derivação)
- Termos canonizados no glossário: **6** — GLOBAL: Documento do contrato, Marca de cancelamento, Portador de confirmação, Veredito de divergência · FEATURE (arquivo criado): Derivado, Representação textual
- Candidatos a ADR sinalizados: 1 (5/5) · **ADRs confirmados para criação: 1** — *"artefato derivado de dado gravado é composto sob demanda, nunca armazenado"* (`architecture`, `data`), **pré-requisito da geração do task_plan**

---

## Run de execução — 2026-08-12

- `[run] executor resolvido: sysloc-backend-implementer (origem: argumento explícito)`
- `[run] executor_discipline injetado (fonte: references/executor-discipline.md)`
- `[run] git verificado; _run/tmp/ coberto por .gitignore (linha 45: docs/specs/**/_run/tmp/)`
- `[run] cleanup idempotente: nenhum arquivo em _run/tmp/ (diretório inexistente — memória lazy nasce só em rejeição)`
- `[run] resume pós-interrupção: NÃO se aplica — 12/12 tasks em "A Fazer", nenhum _run/tmp/T*.md, nenhum diff em paths declarados`
- `[run] reconciliação de dependências: 12/12 tasks sem divergência entre a tabela do task_plan.md e a seção 1 de cada TN.md`
- `[run] paralelismo: lote vazio em todas as 5 fases — as 12 tasks têm flag "Não" derivado (§4.2 do task_plan). Execução 100% sequencial em ordem topológica`
- `[run] AUTORIZAÇÃO EXPLÍCITA DO USUÁRIO (2026-08-12, meio do run): (a) NENHUMA pausa por AskUserQuestion do orquestrador — toda decisão assume a opção recomendada e o run segue; (b) o limite de 3 tentativas por task fica SUSPENSO neste run — correção prossegue quantas rodadas forem necessárias até não haver bloqueante. O Passo 10 (escalar/bloquear) só se aplica se o usuário reverter a autorização`
- `[run] disco do host em 95% (1,5 GB livres) — risco medido de "No space left on device" disfarçado de teste vermelho; `rm -rf /tmp/sysloc-banco-*` entre execuções (0 resíduos no início)`
[T1] base_sha=700d5461ccd513527fbf4a6529a028d8c62315cc
[T1] ADRs injetadas no executor: ADR-0006 (fonte: task §7)
- `[T1] arquivos tocados NÃO declarados: deploy/scripts/caracterizacao/extrair-fonte-do-pdf.sh (+2/-1 — 1 linha no array INVENTARIO + ajuste de prosa "O 11º artefato" → "Um único artefato"). Era §5.3 (referência). Justificativa do executor: a bijeção diretório↔inventário do CT-601 reprova sem a linha. Encaminhado ao Gate 2 como candidato a scope_deviation`
- `[T1] filtro de resíduo: CLAUDE.md, docs/adr/INDEX.md, docs/plano-backend-novo/roadmap.md e docs/specs/domain-glossary.md aparecem no diff mas são modificações PRÉ-EXISTENTES ao run (constavam do git status inicial) — subtraídos da lista de "tocados pela task"`
- `[T1] desfechos dos 3 eixos: contrato_com_fiador = AUSÊNCIA MEDIDA (0 de 1 contratos reais; 0 vínculos e 0 fiadores no legado) · locatario_pessoa_juridica = CAPTURADO (contrato real CTR-2026-00020) · parte_sem_documento_identidade = CAPTURADO (mesmo artefato — o único contrato real exercita os dois ramos)`
[T1] attempt_sha (rodada 1)=5499c01f647342a9b54d1ff5e7ac1cdeb1c6e641
- `[T1] QA rodada 1: REJEITADO (1 alto security bloqueante + 1 medio tests anotável + 2 baixos)`
- `[T1] QA rodada 2 (scan_scope=DELTA): APROVADO_COM_OBSERVACOES — 4/4 achados da rodada 1 sanados e verificados pelo gate por reprodução independente (recálculo dos dígitos verificadores; reexecução do M5 contra a asserção antiga, que o vê sobreviver). 1 baixo novo (tests, sem smell): a 2ª direção da bijeção nasceu sem mutante próprio`
- `[T1] Gate 2 invocado em scan_scope=FULL — é a PRIMEIRA revisão técnica desta task (o TR nunca a viu); DELTA faria ele revisar só a correção e nunca o corpo da task`
- `[T1] TR consultou: ADR-0006, ADR-0030`
- `[T1] Tech Review (scan_scope=FULL): APROVADO_COM_OBSERVACOES — 1 BAIXO code_quality. Veredito sobre o arquivo não declarado: NÃO é scope_deviation (a entrada no INVENTARIO é forçada pela bijeção pré-existente do CT-601; é dependente direto da criação do artefato — Regra 3). Registrado como imprecisão de AUTORIA DE TASK: a §5 o declarou em §5.3 quando o CT-601 o tornava obrigatoriamente §5.2`
- `[T1] TR observações: marcadores D3 e DECISÃO FECHADA íntegros por varredura do diff (grep por linhas +/- tocando marcador volta vazia); AP-24 ausente (as únicas linhas "-" acompanham a adição do CT-701: 10/10 → 11/11); barreira de dado pessoal julgada fail-closed e estrutural, não best-effort`
- `[T1] ledger: 6 achados totais | 2 originados em rodada >1 | 0 suspeitos de incompletude da rodada 1`
- `[T1] staged: capturar.py, verificar-golden.sh, extrair-fonte-do-pdf.sh, PROCEDENCIA.md, contrato-pdf-pessoa-juridica.txt`
- `[T2] base_sha=700d5461ccd513527fbf4a6529a028d8c62315cc`
- `[T2] ADRs injetadas no executor: ADR-0016, ADR-0017, ADR-0022 (fonte: task §7)`
- `[T2] critical_path: SIM — packages/contracts/ casa a categoria api_contracts. Gates em opus (já era, por decisão do projeto)`
- `[T2] QA rodada 1: APROVADO_COM_OBSERVACOES — 9/9 critérios, 1 medio tests (smell semantically_duplicated_test = AP-26, conjunto de manutenibilidade ⇒ ANOTÁVEL, não bloqueia). Suíte 1004 → 1030, contracts 267 → 293, nenhum pacote encolheu. tocou_area_critica: true (api_contracts)`
- `[T2] QA registrou divergência para o Gate 2 julgar: o Aceite Técnico §4 manda strictObject nos TRÊS esquemas; o tech_spec §4.2 só o declara para esquemaDaApresentacaoDoPortador e cala sobre as duas respostas; a convenção do pacote para saída é aberta. O executor seguiu a task e registrou a razão no cabeçalho do arquivo`
- `[T2] rule_candidates: +1 (repeated_assertion_shape, origem agent-spec-qa-validator)`
- `[T2] TR consultou: ADR-0016, ADR-0017, ADR-0022, ADR-0027, ADR-0029`
- `[T2] Tech Review: APROVADO_COM_OBSERVACOES — 1 MEDIO project_pattern (anotável) + 1 BAIXO code_quality. Nenhum bloqueante; NENHUMA rodada de correção aberta pela política`
- `[T2] TR julgou o ponto delegado pelo QA: seguir o Aceite Técnico §4 literal (strictObject nas duas RESPOSTAS) foi o CERTO. Razão medida: esquemas de saída são consumidos só por esquemaPublicado(...,'output') na geração do OpenAPI, nunca analisados na serialização — logo strictObject de saída não pode derrubar rota neste projeto`
- `[T2] rodada de ESCRITURAÇÃO (não de correção): o orquestrador optou por FECHAR os dois anotáveis do TR em vez de anotá-los, porque o P1 é classe R3 do Protocolo Antirregressão (texto de decisão que mente sobre o código) e a correção é edição de comentário. Provado mecanicamente que zero linhas de código executável mudaram nos 2 arquivos pré-existentes; suíte idêntica (293/293)`
- `[T2] staged: confirmacao-de-email.ts, pessoa.ts, index.ts, esquemas.spec.ts, configuracao-de-mora.ts, automacao-de-cobranca.ts`
- `[T2] ledger: não criado — a rodada 1 aprovou nos dois gates, sem rejeição`
- `[T2] rule_candidates: +1 (convention_drift, origem staff-review)`
- `[T3] base_sha=700d5461ccd513527fbf4a6529a028d8c62315cc`
- `[T3] ADRs injetadas no executor: ADR-0008, ADR-0009, ADR-0016, ADR-0022, ADR-0024, ADR-0026, ADR-0030 (fonte: task §7)`
- `[T3] critical_path: SIM — db_migrations (packages/db/migracoes/) + security (RLS forçada, SECURITY DEFINER) + api_contracts (packages/contracts/). risk: high. É a task de MAIOR RAIO da fatia: 22 arquivos editáveis`
- `[T3] QA rodada 1: APROVADO_COM_OBSERVACOES — 12/12 critérios, 1 MEDIO code_quality (anotável) + 1 BAIXO documentation. Suíte 1030 → 1037 (contracts 293→297, db 148→151), nenhum pacote encolheu. tocou_area_critica: true`
- `[T3] QA auditou a prova de falsificação do CT-712 por TRÊS verificações independentes e a validou: (a) banco-efemero.ts aplica migrações por readdir em tempo de execução, logo o mutante alcança o que executa; (b) a asserção lê pg_attribute com NOT attisdropped; (c) 1 failed | 150 passed é internamente consistente com 151`
- `[T3] QA confirmou por LEITURA do cabeçalho de packages/db/src/esquema/negocio.ts:66-88 que os 4 arquivos fora do escopo declarado são as QUATRO listas-espelho que o próprio cabeçalho nomeia — dependentes diretos da criação da tabela. Julgamento de scope_deviation delegado ao Gate 2`
- `[T3] QA validou a DECISÃO FECHADA nova da 0014 contra a §3 da nao-regressao.md: 4 campos presentes, gatilho aplicável por dois caminhos (forma menos óbvia que a idiomática + debate registrado na §21.3 do tech spec)`
- `[T3] rule_candidates: +2 (repeated_fixture, repeated_assertion_shape — origem agent-spec-qa-validator)`
- `[T3] TR consultou: ADR-0008, ADR-0009, ADR-0016, ADR-0022, ADR-0024, ADR-0026, ADR-0027, ADR-0030`
- `[T3] Tech Review rodada 1: REJEITADO — 1 CRITICO architecture + 1 MEDIO testability (bloqueante). O CRITICO foi VERIFICADO POR EXECUÇÃO contra PostgreSQL 18.4 real, não por leitura: SECURITY DEFINER não atravessa FORCE RLS quando o dono da função é o dono da tabela. A função devolve [] no único cenário que existe para atender. Origem: §7.3 e §3 do tech_spec — defeito de SPEC, não do executor`
- `[T3] TR julgou os 4 arquivos fora do escopo: NÃO são scope_deviation (Regra 3 — dependentes diretos). O que ficou incompleto foi a §5.2 da task, não o diff`
- `[T3] DECISÃO DO ORQUESTRADOR (usuário autorizou assumir o recomendado sem pausar): correção pela via (a) PAPEL DEDICADO sysloc_resolucao, não pela (b) política escopada ao dono. Razão: preserva a invariante "nenhum papel enxerga sem contexto, nem o dono" e confina a exceção a um papel NOLOGIN de propósito único; a (b) criaria precedente copiável de leitura irrestrita para o dono`
- `[T3] attempt_sha (rodada 1)=473fd7307023035ae01073a0b6e1782234cbddbc`
- `[T3] requires_qa_revalidation=true (P1 architecture ∈ revalidation_required; overrides: tocou_area_critica + task_risk high)`
- `[T3] QA rodada 2 (scan_scope=DELTA): APROVADO_COM_OBSERVACOES — TR-P1 e TR-P2 SANADOS e verificados POR EXECUÇÃO (não por declaração). O ponto de maior risco da correção (política USING(true) sob FORCE) foi auditado em DOIS eixos independentes: CT-735 pernas 1/2 e isolamento.spec.ts CT-302/303/304 — o isolamento NÃO regrediu. Suíte 1037 → 1038 (db 151 → 152). 1 BAIXO novo (documentation)`
- `[T3] QA aceitou a justificativa do executor para NÃO fechar o P3 e acrescentou razão mais forte: aquele `it` é o COMPANHEIRO POSITIVO da recusa — removê-lo faria a asserção de unrecognized_keys ficar verde também sobre um esquema que recusasse TUDO. Recomendação do QA: marcar no_change_needed em vez de débito pendente`
- `[T3] QA security_flags: ["rls_nominal_bypass_policy"] — emitida porque o delta ALTERA o mecanismo de isolamento, não porque a auditoria tenha encontrado regressão`
- `[T3] Tech Review rodada 2: PARCIAL — 1 MEDIO testability (BLOQUEANTE) + 1 MEDIO project_pattern (anotável) + 1 BAIXO architecture. O TR-P1 e o TR-P2 da rodada 1 estão SANADOS e verificados contra o texto literal`
- `[T3] TR julgou a membership WITH INHERIT FALSE, SET TRUE como NECESSÁRIA, MÍNIMA e SUFICIENTE, com razão mais forte que a do QA: sysloc_migracao JÁ É dono da tabela e sempre pôde ALTER TABLE ... NO FORCE — a leitura irrestrita já lhe era alcançável por um comando, e a forma escolhida não amplia a superfície dele`
- `[T3] TR reavaliou ADR-0008/0009 contra a Decision integral e CONFIRMOU a leitura do QA: a travessia aqui É a RLS (uma política declarada), não um contorno dela — o NOBYPASSRLS explícito é o que a separa da alternativa que a 0008 rejeita por escrito`
- `[T3] TR elevou o QA-BAIXO-002 de documentation/BAIXO para testability/MEDIO com evidência nova: a lacuna não é de prosa, é dos laços de asserção (b) e (c) do CT-030, e é SILENCIOSA porque a alínea (a) filtra por IN (...) e continua verde`
- `[T3] attempt_sha (rodada 2)=3682a484716bdf3112f87835cf69365e145d76c1`
- `[T3] QA rodada 3 (DELTA): APROVADO_COM_OBSERVACOES — contagem IDÊNTICA (1038), 1 BAIXO documentation novo`
- `[T3] Tech Review rodada 3 (DELTA): APROVADO — zero problemas. TR-P1/P2/P3 da rodada 2 sanados e verificados contra o código. O TR confirmou os DOIS cenários indetectáveis agora detectáveis, e achou um TERCEIRO que a própria sugestão dele não previa: provisionar-base.sh:2077 guarda o GRANT por pg_has_role(...) != 't', logo membership preexistente com INHERIT errado NÃO é corrigida pelo provisionamento — a leitura direta de pg_auth_members.inherit_option é o único ponto do produto que acusa isso`
- `[T3] TR declarou explicitamente: "não resta NADA que impeça o fecho"`
- `[T3] ledger: 8 achados totais | 4 originados em rodada >1 | 0 suspeitos de incompletude da rodada 1`
- `[T3] staged: 3 migrações criadas + 4 de produção + 18 de teste + 6 fora da §5.2 (todos julgados legítimos pelos dois gates) + tech_spec.md §3/§7.3`
- `[T3] task_plan.md corrigido: a faixa de CTs passou de CT-701..CT-734 para CT-701..CT-735 (o CT-735 nasceu na rodada 3)`
- `[T4] base_sha=700d5461ccd513527fbf4a6529a028d8c62315cc`
- `[run] AJUSTE DE MÉTODO a partir da T4: o orquestrador deixa de COLAR o corpo da task no prompt do executor e passa a mandá-lo LER o arquivo integral (`tasks/TN.md`), colando apenas a identificação, os avisos críticos e o que não está no arquivo. Razão: fidelidade maior (sem risco de truncar a §6.6) e contexto preservado para as 9 tasks restantes. A Disciplina do Executor e o bloco de ADRs continuam colados VERBATIM, como a rule exige`
- `[T4] QA: APROVADO (sem nenhum problema). Ele RECONDUZIU a prova de falsificação com DEZ mutantes próprios sobre o SUT, todos revertidos com diff conferido, e demonstrou por medição que NENHUM dos 12 casos é decorativo (os mutantes de colapso e de toLowerCase derrubam os 12)`
- `[T4] QA mediu que a ORDEM das três operações não é discriminável por teste — as três permutações produzem saída idêntica; o mutante de ordem é EQUIVALENTE e nenhuma suíte pode matá-lo`
- `[T4] TR consultou: ADR-0025, ADR-0016`
- `[T4] Tech Review: APROVADO_COM_OBSERVACOES — 2 MEDIO code_quality + 3 BAIXO, TODOS anotáveis. Nenhum bloqueante`
- `[T4] TR julgou @sysloc/contracts sem consumidor como NÃO especulativo: a §3.1 da task o exige nominalmente, e a frase sobre "dependência sem consumidor" tem alvo diferente e nominal (@react-pdf/renderer, react, pdfjs-dist, adiados para a T6)`
- `[T4] TR avaliou a Iron Law #6 sobre normalizarParaComparacao (sem consumidor de produção) e a julgou NÃO configurada: a lei alcança símbolo criado como GANCHO para o teste alcançar produção; aqui a função é o OBJETO da prova`
- `[T4] rodada de ESCRITURAÇÃO: 4 dos 5 anotáveis FECHADOS (P1, P2, P3, P5) — o P2 era o mais grave (a normalização publicada sem aviso de que aplicá-la em produção colapsaria o layout EM SILÊNCIO, com a comparação continuando verde). O P4 não é do código (é ajuste de molde de task) e virou candidato a regra`
- `[T4] o executor REFINOU o P3 com medição própria mais precisa que a do gate: o mecanismo EXISTE (52 caracteres produzem espaço sob NFKC sem que \s os alcance — todos diacríticos isolados), mas nenhum dos 52 ocorre no oráculo (0 ocorrências medidas sobre o golden)`
- `[T4] staged: 8 arquivos do pacote novo + pnpm-lock.yaml. Suíte 1038 → 1050 (documentos: 12, pacote novo)`
- `[T4] rule_candidates: +2 (repeated_assertion_shape do QA, convention_drift do staff — fiação de teste de pacote novo)`
- `[T4] ⚠️ disco do host subiu para 96%`
[T5] base_sha=700d5461ccd513527fbf4a6529a028d8c62315cc
[T5] attempt_sha (rodada 1)=f1a5ac8e5d764b453399938d2189fde973d7cd42
- `[T5] QA rodada 1: REJEITADO — 1 ALTO tests (happy_path_only, BLOQUEANTE) + 2 MEDIO anotáveis + 1 BAIXO. O ALTO foi MEDIDO, não inferido: o gate aplicou 3 mutantes simultâneos e os TRÊS sobreviveram com 121/121 verdes`
- `[T5] EMENDA DE ARTEFATO pelo orquestrador (RN-15: "divergência medida exige emendar o artefato e escalar"): a DV-03 declarava absorção TOTAL pelo colapso de espaço; a medição da T5 mostrou absorção PARCIAL — colada à pontuação, sobra espaço no lado do ORÁCULO. Emendadas a §21.1 do tech_spec.md e a §3.6 da T5.md, nomeando os blocos alcançados. O QA julgou o tratamento do executor no teste como HONESTO ("não é tolerância disfarçada"), com três razões: o bloco não é pulado, a divergência é caracterizada nos dois sentidos, e o conjunto divergente é fechado por igualdade`
- `[T5] TR consultou: ADR-0016, ADR-0023, ADR-0025, ADR-0026, ADR-0030`
- `[T5] Tech Review rodada 2: PARCIAL — 1 ALTO adr_compliance + 1 MEDIO architecture (os dois bloqueantes) + 3 BAIXO. O ALTO é achado que NENHUM gate anterior viu: resolverValorTotal derivava valorMensal × prazoMeses NA APLICAÇÃO, e a ADR-0023 põe aritmética monetária derivada NO BANCO. Consequência medida: para RASCUNHO (estado que o CT-706 compõe), o JSON publicaria valorTotalContrato: null e o documento imprimiria R$ 30.000,00`
- `[T5] ⚠️ DÍVIDA CARREGADA PARA A T7: a consulta lerAgregadoDoContrato passa a dever COALESCE(valor_total_contrato, valor_mensal * prazo_meses) em numeric. Sem isso o RASCUNHO chega ao compositor sem total e NÃO COMPILA (TermosDoContrato foi estreitado para NonNullable)`
- `[T5] correção rodada 3: 138 → 141 (+3, nenhum removido). resolverValorTotal e multiplicarEmCentavos removidas; DECISÃO FECHADA nova em qualificarParte protegendo a divergência do fiador, com CT-704 (fiador) discriminante. Dois mutantes provados: derivação reintroduzida reprova 1/141; segundo molde por papel reprova 1/141`
- `[T5] o executor descobriu que a chamada DIRETA a qualificarParte NÃO alcança um segundo molde escolhido por papel — o mutante reprovou na asserção sobre o bloco `fiadores` do documento composto, não na chamada direta. Por isso o CT-704 (fiador) tem as DUAS asserções`
- `[T5] DÉBITO DE ESCRITURAÇÃO herdado da T3: packages/db/migracoes/0013:53 reproduz a cadeia canônica "DÉBITO COM GATILHO — D20" em prosa, criando falso marcador. A T3 está fechada; anotado na §2`
- `[T5] QA rodada 3 (DELTA): APROVADO_COM_OBSERVACOES — 10/10 critérios, zero bloqueantes. Os dois achados do Gate 2 verificados por mutante EXECUTADO pelo gate, não por declaração aceita`
- `[T5] QA confirmou a descoberta do executor: o mutante do segundo molde por papel reprova na asserção sobre o bloco `fiadores` do documento COMPOSTO (qualificacao.spec.ts:247) e a chamada DIRETA passa — a segunda asserção do CT-704 (fiador) é NECESSÁRIA. A razão é estrutural: qualificarParte recebe um argumento só e ParteDoContrato não carrega papel, logo um segundo molde só pode ser injetado no ponto de chamada da composição`
- `[T5] QA confirmou que a guarda ErroDeValorForaDeAlcance FICOU MAIS AMPLA, não menor: antes resolverValorTotal devolvia 0 em silêncio para fator não positivo; hoje um total negativo vindo da T7 chega a emCentavos e é RECUSADO`
- `[T5] EMENDA DE ARTEFATO pelo orquestrador (2ª desta task): a T7.md ganhou critério de aceite explícito exigindo o COALESCE na consulta e PROIBINDO recompor a conta em TypeScript. Razão do QA: o NonNullable<> impede a T7 de esquecer o CAMPO, mas não de reabrir a violação da ADR-0023 uma task adiante, sem teste do lado da T5 que a alcance`
- `[T5] Tech Review rodada 3: APROVADO — zero problems, zero bloqueantes. "Nada impede a T5 de fechar"`
- `[T5] ⚠️ TRÊS ACHADOS OPERACIONAIS DO GATE 2 CONTRA O PRÓPRIO ORQUESTRADOR, e os dois primeiros são falha minha:`
- `[T5]   (1) o attempt_sha_anterior que passei ao TR era o da RODADA 1, não o da rodada 2 — o gate revisou a UNIÃO das rodadas 2 e 3. Nada se perdeu (o escopo ficou MAIOR), mas eu não capturei attempt_sha antes do executor da rodada 3`
- `[T5]   (2) o Ledger em _run/tmp/T5.md ficou CONGELADO na rodada 1 — não registrei nele nenhum achado do Gate 2 (P1..P5 da rodada 2). O gate os re-verificou pelo meu prompt, não pelo Ledger. Corrigido no fecho`
- `[T5]   (3) achado de valor para a T7: derivarValorTotal(valorMensal, prazoMeses) JÁ EXISTE em packages/db/src/derivacao-de-contrato.ts:180, usada pela ativação em contrato.service.ts:746, com mutante registrado em contratos.e2e.spec.ts:1241. A T5 carregava uma TERCEIRA cópia da mesma conta. ⚠️ A T7 NÃO deve reusá-la na consulta — seria recair no que o critério novo proíbe; o COALESCE roda em numeric, no banco`
- `[T5] TR confirmou que a EMENDA DA T7 é suficiente, por três camadas: (a) o critério está na §4 (Aceite Técnico), que o QA da T7 confere item a item; (b) a T7 declara gates:[qa,tech_review] e risk:high, logo o mesmo gate a revê com a ADR-0023 em mãos; (c) o tipo torna o campo faltante erro de compilação`
- `[T5] SUGESTÃO NÃO-BLOQUEANTE do TR para a T7: dar ao critério do COALESCE uma prova executável — um grep-âncora (valorMensal * / * prazoMeses em packages/db/src/documento-de-contrato.ts devolve 0), no molde que a própria §4 já usa para o D36`
- `[T5] ledger: 9 achados totais | 5 originados em rodada >1 | 0 suspeitos de incompletude da rodada 1`
- `[T5] staged: 8 arquivos do pacote (5 src/contrato + 3 test) + index.ts. Suíte 1050 → 1179 (documentos 12 → 141)`
[T6] base_sha=700d5461ccd513527fbf4a6529a028d8c62315cc
- `[T6] QA: APROVADO_COM_OBSERVACOES — 9/9, 1 BAIXO. O gate reproduziu os 3 mutantes e CORRIGIU o registro do executor sobre a hifenização: com a frase commitada, um único deslocamento já discrimina; a sobrevivência era da PRIMEIRA redação, com outra frase`
- `[T6] TR consultou: ADR-0025, ADR-0030, ADR-0006`
- `[T6] Tech Review: APROVADO_COM_OBSERVACOES — 2 MEDIO project_pattern, ambos anotáveis. Arbitrou os 3 pontos delegados A FAVOR do executor: @types/react é fiação necessária; NÃO há speculative_complexity; o BAIXO-001 fica em BAIXO (o AP-29 pune a asserção infalível que SUBSTITUI garantia nomeada — aqui ela ACOMPANHA uma estritamente mais forte)`
- `[T6] TR verificou o encapsulamento no ARTEFATO EMITIDO, não só no fonte: packages/documentos/dist/renderizador-pdf.d.ts publica só criarRenderizadorPdf(): PortaDeRenderizacao — nenhum tipo de react atravessa a superfície. A T7 NÃO herda @types/react`
- `[T6] rodada de ESCRITURAÇÃO: os 2 anotáveis FECHADOS (classe R3 — o barrel afirmava "não conhece PDF", "a suíte não atravessa fronteira real alguma" e "a dependência é uma só", contradizendo um parágrafo que a própria T6 escreveu 25 linhas abaixo). Provado mecanicamente: 22 + 8 linhas não-comentário idênticas, diff vazio`
- `[T6] ⚠️ RISCO REGISTRADO PARA A T7 pelo TR: renderToBuffer devolve um Buffer (subclasse de Uint8Array). A T7 deve enviar o valor DIRETO ao reply.send (Fastify aceita Buffer/Uint8Array) e NUNCA passar bytes.buffer sem byteOffset/byteLength — é a única forma de esse detalhe morder`
- `[T6] staged: renderizador-pdf.ts + renderizador-pdf.spec.ts + package.json + index.ts + vitest.config.ts + pnpm-lock.yaml. Suíte 1179 → 1184 (documentos 141 → 146)`
- `[T6] rule_candidates: +3 (repeated_fixture e repeated_assertion_shape do QA; convention_drift do staff — "docblock de natureza do pacote envelhece", 3ª ocorrência desta classe no run)`

---

## ⏸️ PAUSA CONTROLADA — 2026-08-13

**Solicitada pelo usuário.** O run parou num **ponto limpo**: a T6 fechou aprovada nos dois gates, foi staged, e **nenhuma task está em progresso**. Não há memória lazy pendente (`_run/tmp/` vazio), nenhum executor em voo e nenhum gate aberto.

---

## ▶️ RETOMADA — 2026-08-13

- `[run] retomada do run pausado. Ponto de partida CONFERIDO: HEAD=700d546 (inalterado desde a T4), 83 arquivos staged sem commit, _run/tmp/ vazio, nenhuma task Em Progresso. O passo "Resume pós-interrupção" NÃO disparou — como o RETOMADA.md previa`
- `[run] executor resolvido: sysloc-backend-implementer (origem: argumento explícito)`
- `[run] executor_discipline injetado (fonte: references/executor-discipline.md)`
- `[run] ⚠️ AS DUAS AUTORIZAÇÕES DO USUÁRIO FORAM REPETIDAS no comando de retomada e valem para TODO este run, até a última task: (a) NÃO PAUSAR aguardando resposta — toda pergunta assume a opção recomendada; (b) o teto de 3 tentativas NÃO EXISTE neste run — a correção prossegue até não haver bloqueante`
- `[run] cleanup idempotente: _run/tmp/ já vazio, nada a remover. rm -rf /tmp/sysloc-banco-* executado (disco em 96%, 1,3 GB livres)`
- `[run] ⚠️ RESÍDUO STAGED DE TASKS ANTERIORES CONGELADO: 83 paths (T1..T6). O stage NÃO move o HEAD, logo `git diff <base_sha>` os inclui — a lista de "tocados" enviada ao QA da T7 subtrai esses 83, exceto os que a própria T7 declara (contrato.service.ts, CLAUDE.md, contratos.e2e.spec.ts, index.ts de @sysloc/db, pnpm-lock.yaml)`
- `[T7] base_sha=700d5461ccd513527fbf4a6529a028d8c62315cc`
- `[T7] executor: opus (declarado no frontmatter) · gates: [qa, tech_review] (declarado) · risk: high → os DOIS gates escalam para opus (e o CLAUDE.md já os fixa em opus, sem negociação)`
- `[T7] ADRs injetadas no executor: ADR-0028, ADR-0030, ADR-0011, ADR-0018, ADR-0017, ADR-0008, ADR-0025, ADR-0021 (fonte: task §7) + ADR-0023 acrescentada pelo orquestrador — ela NÃO está na §7 da task, mas é o fundamento do critério do COALESCE que a §4 carrega e a T5 deixou como dívida`
- `[T7] QA: APROVADO_COM_OBSERVACOES — 14/14 critérios, 3/3 CTs rastreados, ZERO bloqueantes, 1 BAIXO documentation (os 7 arquivos fora da §5.2 não escriturados). Rodada única`
- `[T7] antipadroes_verificados COMPLETO: 6/6 arquivos de teste tocados declarados, nenhum antipadrão detectado`
- `[T7] Ledger NÃO criado — rodada 1 aprovou sem rejeição, logo não há achado bloqueante a rastrear (é o caso legítimo previsto na rule, não lacuna)`
- `[T7] o QA VERIFICOU as três declarações do executor por REPRODUÇÃO, não por aceite: (1) MT-3 sobrevive (206 passed) e MT-3b reprova em 3 casos com o rótulo literal do CT-355 — o eixo da ADR-0018 É falsificável e TEM companheiro permanente; (2) a contradição do card do CT-714 CONFERE no código real (o @ApiOperation do PUT declara 422 sobre ATIVO), e o RASCUNHO é mais severo porque só imprime o total se o COALESCE derivou no banco; (3) a contagem por pacote confere unidade a unidade`
- `[T7] o QA mediu a rede do critério do COALESCE e a achou DUPLA e real: o CT-736 (c) discrimina ONDE a conta correu (500.03 × 13 = 6500.39 em numeric vs 6500.389999999999 em ponto flutuante) e o CT-736 (d) é a varredura estática que pega a forma arredondada que ACERTA o valor e atravessaria o (c) — com sensibilidade do predicado declarada antes`
- `[T7] achado mais forte da task, registrado pelo QA: o mutante de cache indexado pelo código reprova no cenário outra_empresa do CT-715 (expected 200 to be 404) — um cache por código ATRAVESSA a fronteira de empresa, porque o código é único por empresa e não globalmente`
- `[T7] CT-907 reprovou por TIMEOUT (5221 ms) na varredura completa e passou ISOLADO (219 passed) — é o flake pré-existente declarado no CLAUDE.md, não achado. O QA ainda conferiu o invariante À MÃO nas duas pontas: 20 identificadores de marcador vivos (um é o exemplo D99 do próprio spec) contra 19 linhas na tabela do CLAUDE.md, com o D5 · F3/T7 presente`
- `[T7] rule_candidates: +2 (repeated_fixture × 2 do QA — acessórios de arranjo E2E e extração de texto de PDF; o segundo já coberto pelo D5)`
- `[T7] TR consultou: ADR-0018, ADR-0023, ADR-0025, ADR-0028, ADR-0030 (as cinco abertas por inteiro). ADR-0008/0011/0016/0017/0021 julgadas pela linha do INDEX.md cruzada com a tabela de conformidade da §21.3 do tech spec — o gate REGISTROU a diferença por escrito, porque o repositório adverte que as linhas-resumo já divergiram do texto real`
- `[T7] Tech Review rodada 1: PARCIAL — 1 MEDIO performance (P1, BLOQUEANTE pela partição) + 2 BAIXO anotáveis (P2 scope_deviation, P3 code_quality)`
- `[T7] o P1 é MEDIÇÃO, não suspeita: o gate rodou 5 renderizações sobre o dist/ já construído de @sysloc/documentos e obteve 784,9 / 416,7 / 501,1 / 655,8 / 601,4 ms para um contrato MÍNIMO (1 fiador, 14.960 bytes). O manipulador embrulha as três etapas em sobContextoDaSessao → sql.begin, logo há BEGIN real e conexão FÍSICA reservada durante toda a renderização. Só a leitura do agregado precisa de tx: comporDocumentoDoContrato é pura e renderizar não toca o banco`
- `[T7] agravante medido pelo gate: o pool da API é o DEFAULT de postgres.js (abrirAcessoAoBanco sem maximoDeConexoes) e é UM só para o processo — as 86 rotas anteriores dividem as mesmas ~10 conexões. ~10 downloads simultâneos esgotam o pool por ~0,5 s cada e a degradação aparece LONGE da causa. Nenhum caso da suíte mede concorrência, então nada acusaria isto`
- `[T7] requires_qa_revalidation=true — o único bloqueante é category:performance, que está em revalidation_required; e os overrides tocou_area_critica=true e task_risk=high forçariam true de qualquer modo. A correção muda o DESENHO TRANSACIONAL do manipulador, e o CT-710/CT-714/CT-715 atravessam exatamente esse caminho`
- `[T7] TR confirmou a declaração "Garantias removidas: nenhuma" por método próprio: isolou o delta real da T7 com git diff SEM argumento de revisão (T1..T6 estão no ÍNDICE, a T7 na árvore de trabalho), o que separa limpo os três paths contaminados. Em contrato.service.ts a T7 remove EXATAMENTE dois blocos — as 13 linhas do marcador D36 e a assinatura antiga de exigir`
- `[T7] TR recontou a superfície INDEPENDENTEMENTE do arquivo de teste (grep dos decoradores de rota em *.controller.ts = 72 manipuladores) e confirmou (72-1)+7+9 = 87. AP-24 limpo: ZERO linhas removidas contendo it(/describe(/test(/expect no delta da T7`
- `[T7] TR NÃO abriu achado sobre as 8 ocorrências de "D36" que restam no código, embora contrariem a LETRA do critério §4 ("grep D36 devolve 0"): nenhuma é marcador, todas são prosa narrativa do fecho, e nenhuma usa a forma **Dnn** (F{n}/T{n}) que a §3-B proíbe fora da tabela — a checagem do sentido 2 as leria como falso órfão`
- `[T7] memória lazy CRIADA no formato completo e com o Ledger POPULADO na origem (4 linhas: QA-BAIXO-001, TR-P1, TR-P2, TR-P3), como a rule exige — nascer vazio faria a rodada 2 reinserir tudo com rodada_origem: 2 e corromper a métrica`
- `[T7] attempt_sha (rodada 1)=31141d435ac96f9b0db7ab36b597def4d5151a45`
- `[T7] correção rodada 2: documento() partido em agregadoDoDocumento(tx, codigo) — leitura + exigir, DENTRO da unidade — e renderizarDocumento(agregado) — composição + porta, FORA e SEM TransactionSql na assinatura. A separação é de TIPO, não de ordem: sem tx na assinatura não há caminho até o banco. O P3 (anotável) foi corrigido de passagem, no mesmo arquivo`
- `[T7] o executor acrescentou rede PRÓPRIA para um defeito que NENHUM caso da suíte alcançava — o CT-714 (b) — e a provou por dois mutantes: MT-4 (as três etapas de volta na mesma continuação) e MT-5 (_tx?: TransactionSql na assinatura, que compila e não muda comportamento). ⚠️ Sob MT-4 o CT-714 e o CT-715 seguem VERDES: é exatamente a razão de o caso novo existir`
- `[T7] QA rodada 2 (DELTA): APROVADO — zero problemas, 14/14 critérios. Ele REPRODUZIU os dois mutantes pelo script do pacote e ACRESCENTOU UM TERCEIRO de iniciativa própria (MT-2'), porque o MT-2 original media a forma ANTERIOR do manipulador e a correção reestruturou aquele trecho. Movendo os cabeçalhos para antes da unidade na forma NOVA: outra_empresa: expected 500 to be 404 no CT-715 — a rede sobreviveu à reestruturação`
- `[T7] QA refutou o AP-29 sobre o CT-714 (b) POR MEDIÇÃO: cada um dos 4 eixos tem estado falsificante demonstrado, e as 4 âncoras de não-vacuidade fazem um manipulador renomeado REPROVAR em vez de passar sobre recorte vazio — o modo de falha correto`
- `[T7] raio de impacto determinado com confiança pelos DOIS gates, sem fallback para FULL: grep por agregadoDoDocumento/renderizarDocumento/.documento( fora de dist não devolve consumidor algum fora dos três arquivos do delta`
- `[T7] Tech Review rodada 2 (DELTA): APROVADO_COM_OBSERVACOES — 1 BAIXO project_pattern (P4), anotável. TR-P1 e TR-P3 confirmados SANADOS por verificação própria; TR-P2 não reaberto`
- `[T7] TR julgou a COESÃO da partição e a aprovou pelo mecanismo do TIPO: renderizarDocumento exige um AgregadoDoDocumentoDoContrato, projeção produzida por UM único caminho (lerAgregadoDoContrato, que exige TransactionSql e corre sob RLS). A ordem não é convenção documentada — é imposta pelo compilador. O corte é por EXIGÊNCIA DE EXECUTOR (uma metade toca o banco, a outra não pode)`
- `[T7] TR conferiu a ADR-0026 explicitamente sobre o desenho novo: dataDeEmissao sai de to_char(negocio.data_corrente_da_operacao(), …) DENTRO da consulta, logo sob SET LOCAL e sob o relógio do banco; depois da unidade corre só função pura + renderização em memória. As duas ocorrências de new Date/Date.now em packages/documentos/src são PROSA advertindo contra ler o relógio do processo — zero executáveis`
- `[T7] TR julgou a fragilidade do CT-714 (b) (asserção estática sobre o fonte, forma do AP-02) e NÃO abriu achado, por quatro razões — a técnica é SANCIONADA pela testing-stack.md com prova de falsificação obrigatória (entregue e reproduzida); a fragilidade a renome falha na direção CERTA (recorte vazio REPROVA); o eixo 4 prova por TIPO, não por texto; e a alternativa comportamental exigiria fabricar concorrência ou dublar o renderizador sob prova (AP-10)`
- `[T7] RODADA DE ESCRITURAÇÃO (4ª do run): o P4 foi FECHADO em vez de anotado. Ele é classe R3 com consequência MECÂNICA — a detecção de regressão de decisão do próprio Gate 2 varre pelo LITERAL "DECISÃO FECHADA", e prosa sem o marcador é invisível a ela; a rede restante era só o CT-714 (b), que é teste, e teste se altera com SUT_IS_CORRECT_BECAUSE:. O marcador é a barreira que obriga a ESCALAR`
- `[T7] o executor MEDIU o alcance do CT-907 em vez de presumir: PADRAO_DE_MARCADOR só casa "DÉBITO COM GATILHO — Dn · Fn/Tn" e o CT-908 prova por contraexemplo que ele RECUSA "DECISÃO FECHADA". O marcador novo dá 0 casamentos e 0 campos ÍNDICE — logo o CLAUDE.md NÃO foi tocado, e isso está correto: o índice de lá é só de débito com gatilho`
- `[T7] o executor DIVERGIU deliberadamente da redação do REVERTER EXIGE sugerida pelo gate, em dois pontos: exige a medição SOB CONCORRÊNCIA (a série das 5 amostras é evidência do defeito, não da ausência dele, e citá-la de volta não pode satisfazer o contrato) e abre a segunda porta legítima (a renderização deixar de custar tempo da ordem da consulta), que a formulação do gate fechava sem razão`
- `[T7] prova mecânica da escrituração: filtrando comentários e linhas em branco, antes e depois são IDÊNTICOS — controller 444 linhas executáveis, service 351; todo o diff (35 + 6 linhas) começa com // ou *`
- `[T7] ledger: 4 achados totais | 0 originados em rodada >1 | 0 suspeitos de incompletude da rodada 1`
- `[T7] staged: 19 arquivos de código (3 criados + 16 modificados, dos quais 7 fora da §5.2) + task_plan.md + tasks/T7.md + _run/. Suíte 1184 → 1192 (db 152→156, api 203→207). Superfície 86/71 → 87/72`
- `[T7] ⚠️ DÍVIDA CARREGADA PARA AS T8..T12: a §5.2 das tasks que publicam rota está SUBDIMENSIONADA por construção — ela não conta as âncoras de inventário (cobertura-de-autorizacao.e2e.spec.ts, contexto.e2e.spec.ts) nem a fiação de manifesto/tsconfig que a aresta nova obriga a tocar. O docblock de contexto.e2e.spec.ts registra a MESMA divergência pela DÉCIMA task consecutiva (é o débito D26 · F2/T6). A T9 e a T11 publicam rota — espere o mesmo, e não o leia como scope_deviation do executor`

---

- `[T8] base_sha=700d5461ccd513527fbf4a6529a028d8c62315cc (o HEAD não se move — o pipeline nunca commita)`
- `[T8] executor: opus (declarado no frontmatter) · gates: [qa, tech_review] (declarado) · risk: high · tipo=crypto → os DOIS gates em opus`
- `[T8] resíduo staged congelado: 97 paths (T1..T7). A lista de "tocados" enviada ao QA subtrai esses 97, exceto os que a própria T8 declara — packages/db/src/{index.ts,cadastro-de-pessoa.ts}, packages/db/test/{banco-efemero.ts,cadastro-de-pessoa.spec.ts,execucao-da-regua.spec.ts} e CLAUDE.md`
- `[T8] ADRs injetadas no executor: ADR-0008, ADR-0024, ADR-0026, ADR-0022, ADR-0023, ADR-0014 (fonte: task §7)`
- `[T8] executor: 2 criados, 9 modificados. Suíte 1192 → 1201 (db 156 → 165, +9). Garantias removidas: nenhuma`
- `[T8] D13 (F3/T5) FECHADO nas duas pontas — o gatilho literal chegou com o CT-730. A cláusula de semearPoliticaDeAviso foi alinhada à da produção (ON CONFLICT (empresa_id)) e o ramo de conflito passou a EXECUTAR de fato; marcador fora de banco-efemero.ts e linha fora do índice do CLAUDE.md (18 linhas na tabela, "Dezoito débitos" no texto)`
- `[T8] par do comprimento 43 JULGADO e registrado como D7 na §2, SEM marcador — a razão está escrita: a divergência é RUIDOSA (qualquer outro número de bytes mata toda confirmação em 422), não silenciosa, e um décimo marcador seria o ruído que a §3-B adverte desarmar os que importam. Mitigação instalada: o CT-729 confere o segredo gerado contra esquemaDaApresentacaoDoPortador, de modo que a divergência passa a reprovar em packages/db`
- `[T8] D14 conferido e NÃO agrava (grep por America//AT TIME ZONE vazio nos dois arquivos novos); D1 e D26 intocados; NENHUM quarto marcador criado para o limitador de taxa`
- `[T8] packages/db/src/envio-de-cobranca.ts NÃO foi tocado — confirmado pelo orquestrador por git diff --stat vazio contra o base_sha`
- `[T8] fora da §5.2, DOIS arquivos: packages/db/test/unidade-de-trabalho.spec.ts (âncora do CT-012, que audita a superfície do pacote por IGUALDADE — os 6 símbolos novos a deixariam vermelha; entrou com SUT_IS_CORRECT_BECAUSE) e o _run/run-report.md (registro que a própria task manda fazer). É a MESMA classe do D6 da T7, e estava previsto`
- `[T8] ⚠️ DÍVIDA CARREGADA PARA A T9: gravarCadastroSobRestricaoDeUnicidade tem tipo concreto (PessoaCadastrada) por decisão registrada no docblock — emailMudou chega no VALOR, não no tipo, quando a escrita passa por ele. Alargar o retorno reabriria a conversão de UnwrapPromiseArray que aquele parágrafo recusa; o caminho é observar o resultado de alterarPessoa dentro do próprio escrever`
- `[T8] QA rodada 1: REJEITADO — 2 ALTO tests (os dois BLOQUEANTES) + 1 BAIXO documentation. 11/12 critérios, 4/4 CTs rastreados`
- `[T8] ALTO-001 (happy_path_only): o ramo \`undefined\` de consumirPortador — o mecanismo INTEIRO do uso único (RN-10) — nunca executa. As três chamadas existentes são todas sobre portador vivo. MEDIÇÃO do gate: uma implementação que trocasse WHERE … AND consumido_em IS NULL por WHERE derivado = \$1 passaria em 165/165. A §6.4 cenário 4 declara o caso COMO SENDO DESTA TASK, e a §6.5 o mapeia como cobertura do critério 5`
- `[T8] ALTO-002 (tautological_assertion, AP-29): depois de expect(recusas).toEqual({inexistente: undefined, vencido: undefined, invalidado: undefined}) passar, as duas linhas seguintes viram expect(undefined).toEqual(undefined) — infalíveis em QUALQUER estado do SUT. O dano não é ruído: o comentário acima delas apresenta a comparação PAR A PAR como a prova da RN-14, quando quem prova é a agregada`
- `[T8] o QA REPRODUZIU a prova de falsificação do CT-729 pelo script do pacote: mutante \${derivarSegredo(segredo)} → \${segredo} dá 8 failed | 157 passed e a varredura reprova NOMEANDO o arquivo. Revertido e conferido por diff -q`
- `[T8] o QA confirmou que o fecho do D13 NÃO afrouxou nada: politica_de_aviso_empresa_key é UNIQUE("empresa_id") na 0011:102, logo ON CONFLICT (empresa_id) INFERE exatamente essa restrição — mesmo conjunto, mesma linha alvo`
- `[T8] o QA julgou as duas escritas numa instrução só como CRAFT LEGÍTIMO e dentro do escopo (o critério 7 da §4 as declara literalmente), e mediu que o risco de meia-gravação do CTE é INALCANÇÁVEL — as duas tabelas compartilham empresa_id sob a mesma política e a FK composta`
- `[T8] attempt_sha (rodada 1)=60876cdfc0ae26d7f1cc818326011e7f6c7f9572`
- `[T8] correção rodada 2: os dois ALTO sanados. O caso de apoio da §6.4 cenário 4 nasceu com as três metades, e o executor escolheu a SEGUNDA saída para o AP-29 (asserções separadas) em vez da remoção — razão registrada: o docblock afirma que a indistinguibilidade é provada "entre as três, e não cada uma contra o vazio", e a remoção deixaria essa afirmação sem linha própria. O BAIXO da escrituração também foi fechado`
- `[T8] QA rodada 2 (DELTA): APROVADO — 12/12, zero problemas. Ele REPRODUZIU o mutante (1 failed | 165 passed, único vermelho é o caso novo, onde na rodada 1 a mesma troca passava em 165/165) e ainda julgou, a pedido do orquestrador, se a metade (c) era decorativa já que (b) aborta antes: NÃO é — medida isoladamente ela reprova sozinha, com os textos do banco denunciando a reescrita do carimbo`
- `[T8] QA confirmou que a troca do CT-728 é ESTRITAMENTE MAIS FORTE: new Set(valores).size === 1 executa PRIMEIRO, quando nada garante que os valores sejam vazios, e reprova a função que distingue causas sem pressupor o valor comum; a agregada preservou o poder integral (Object.values mantém a ordem e toEqual sobre array compara comprimento)`
- `[T8] TR consultou: ADR-0006, ADR-0008, ADR-0014, ADR-0017, ADR-0022, ADR-0023, ADR-0024, ADR-0026, ADR-0027, ADR-0030 (dez, com as Decisions abertas)`
- `[T8] Tech Review rodada 1 (escopo FULL — foi a PRIMEIRA passagem dele nesta task, porque a rodada 1 caiu no Gate 1): PARCIAL — 1 MEDIO security (P1, BLOQUEANTE) + 2 BAIXO anotáveis (P2 scope_deviation, P3 code_quality)`
- `[T8] o P1 é ACHADO NOVO que o Gate 1 não viu: o WHERE do CTE de consumirPortador é só (derivado = \$1 AND consumido_em IS NULL). Os outros DOIS predicados de validade — invalidado_em IS NULL e expira_em > now() — existem SÓ na função SECURITY DEFINER, que resolverPortador invoca em transação SEPARADA e sem contexto. A segurança do consumo passa a depender de um protocolo de chamada implícito ("sempre resolver antes") que NENHUM docblock enuncia`
- `[T8] a corrida que o P1 descreve é REAL e o modo de falha é grave: a T9 zera email_confirmado_em no PUT que troca o e-mail, invalida os portadores anteriores e emite um novo; um consumo em voo do portador ANTIGO, chegando logo depois, grava email_confirmado_em para um endereço que NINGUÉM confirmou. Sob READ COMMITTED o UPDATE bloqueia na trava de linha, reavalia o predicado depois do commit, e ele continua verdadeiro porque só olha consumido_em`
- `[T8] ⚠️ O TR ANTECIPOU o falso gatilho de parada e o desarmou por escrito: a correção NÃO conflita com a DECISÃO FECHADA — T3 da 0014, porque o marcador governa a RESOLUÇÃO (proíbe consumido_em no WHERE da FUNÇÃO, para não quebrar a RN-10) e não diz nada sobre acrescentar validade ao CONSUMO. A reapresentação de portador já consumido continua devolvendo undefined`
- `[T8] TR resolveu a tensão ADR-0024 × resolverPortador SEM achado, abrindo as duas Decisions: o alcance da 0024 é execução FORA de requisição, e a rota da confirmação é requisição SEM SESSÃO, não trabalho fora de requisição; a 0027, mais nova e específica, decide o ponto por escrito. Ele registrou que a advertência do CLAUDE.md ("citar ADR exige abrir a Decision") se confirmou útil — a linha-resumo do índice não bastaria`
- `[T8] TR julgou os quatro pontos delegados A FAVOR do executor: as duas escritas numa instrução só são a ÚNICA forma que fecha o estado irrecuperável "portador consumido e endereço não confirmado"; PRAZO_DE_VALIDADE é DURAÇÃO de negócio, não leitura de relógio; gerarSegredo/derivarSegredo NÃO são especulativos (constam da §1 e a T11 declara o segundo); e o RETURNING OLD./NEW. está registrado onde impede a reversão, SEM precisar de marcador (nenhum dos gatilhos obrigatórios da §3 está satisfeito)`
- `[T8] TR julgou a ausência de marcador para o D7 (par do 43) CORRETA: não há gatilho concreto, a divergência é ruidosa, e a mitigação faz a divergência reprovar NO PACOTE QUE SORTEIA em vez de virar 422 com a fatia inteira de pé`
- `[T8] requires_qa_revalidation=true — o único bloqueante é category:security, que está em revalidation_required; e tocou_area_critica=true e task_risk=high forçariam true de qualquer modo`
- `[T8] attempt_sha (rodada 2)=958188258b07b2268e29aac427d7ca32f7763712`
- `[T8] correção rodada 3: escolhida a PRIMEIRA saída do P1 — os dois predicados entram por CONJUNÇÃO no WHERE do CTE. Razão do executor: "a segunda apenas documentaria uma precondição que o eixo (b) do achado mostra ser violável — o símbolo é publicado no índice e alcançável por quem não leu o parágrafo. Com os predicados, a validade é propriedade da INSTRUÇÃO e não protocolo de chamada"`
- `[T8] rede própria do P1 com TRÊS mutantes DISJUNTOS, reproduzidos pelo QA: MT8b-2 (perde consumido_em), MT8b-3 (perde invalidado_em) e MT8b-4 (perde expira_em), todos 1 failed | 166 passed. A disjunção é o que prova que CADA predicado tem uma asserção que só ele mata — numa asserção única, um deles sumiria em silêncio`
- `[T8] QA rodada 3 (DELTA): APROVADO — 12/12, zero problemas. Ele conferiu a mecânica do EvalPlanQual e a topologia das duas transações`
- `[T8] ⚠️ FALHA DO ORQUESTRADOR apontada pelo QA da rodada 3, e ela é minha: eu NÃO gravei as linhas TR-P1/TR-P2/TR-P3 no Ledger ao interpretar o veredito do Gate 2 — o Passo 4.2 vale para os DOIS gates, e eu o apliquei só ao Gate 1. O gate julgou os achados pelo enunciado do prompt, não pelo Ledger. Corrigido antes do fecho, com a rodada_origem VERDADEIRA (2), e a inserção tardia declarada por escrito no próprio arquivo`
- `[T8] ⚠️ SEGUNDA falha correlata: o attempt_sha da rodada 2 foi logado aqui mas NÃO gravado na memória lazy, e o QA registrou que isso o impediu de diffar rodada 2 → rodada 3 por git. Ele contornou pela contagem por unidade e pela leitura integral do arquivo. Corrigido no fecho. É a MESMA classe de falha que o Gate 2 da T5 apontou contra o orquestrador — e voltou`
- `[T8] Tech Review rodada 3 (DELTA): APROVADO_COM_OBSERVACOES — 2 BAIXO anotáveis (P1 code_quality, P2 project_pattern). TR-P1 e TR-P3 confirmados SANADOS; TR-P2 não reaberto`
- `[T8] TR confirmou a mecânica do fecho da corrida: sob READ COMMITTED, o UPDATE que bloqueia na trava reavalia o WHERE contra a versão committed (EvalPlanQual), de modo que um invalidado_em gravado na janela agora PULA a linha, onde antes o predicado único continuaria verdadeiro`
- `[T8] TR julgou e NÃO transformou em achado a ressalva do QA sobre as metades (a)/(b) no mesmo it: o poder de detecção está integral (a discriminação veio da via mais forte, mutantes disjuntos), o custo é só de diagnóstico do relatório, e granularidade fina de caso é rubrica do Gate 1 — re-auditá-la com régua própria produziria severidade divergente entre os gates`
- `[T8] TR registrou por que pg_catalog.now() (instante de início da transação) é preferível a clock_timestamp(): é o MESMO relógio e a MESMA função que a 0014 usa na resolução, e clock_timestamp() quebraria a propriedade "uma instrução, um instante só" que o apoio da §6.4 afirma byte a byte`
- `[T8] EMENDAS DE ARTEFATO pelo orquestrador (RN-15 — a TERCEIRA invocação dela neste run, depois da DV-03 na T5 e do COALESCE na T7), a partir do P2 do Gate 2. QUATRO artefatos descreviam o WHERE do consumo com UM predicado: tasks/T8.md §3.1 e §4 (critério 7), tech_spec.md §7.4 e — o PRIORITÁRIO — tasks/T11.md §3.2`
- `[T8] ⚠️ A emenda da T11 é a que importa, e o risco era dirigido: o pseudocódigo dela enumerava DOIS desfechos de undefined (consumido_em IS NULL × NOT NULL) quando agora há TRÊS (já consumido · invalidado na janela · vencido na janela). Um executor que a seguisse à risca escreveria o mapeamento de resposta sobre premissa incompleta. As três continuam no MESMO 200 { confirmado: true }, e a emenda proíbe explicitamente escrever ramo para qualquer delas`
- `[T8] a emenda da §7.4 do tech spec corrigiu uma afirmação que a execução tornou FALSA: o bullet dizia "não há leitura-e-decisão vulnerável a corrida aqui", e na forma de um predicado só isso não era verdade`
- `[T8] as quatro emendas declaram, cada uma, que NÃO alcançam a DECISÃO FECHADA — T3 da 0014 e que não autorizam consumido_em na função SECURITY DEFINER — a advertência da T11.md permanece literalmente como estava`
- `[T8] RODADA DE ESCRITURAÇÃO (5ª do run): o P1 do Gate 2 rodada 3 FECHADO. A razão de publicação de gerarSegredo que o TR-P3 declarou errada sobrevivia em DOIS pontos não tocados e passou a CONTRADIZER o texto corrigido — e o pior deles era a justificativa de admissibilidade do símbolo dentro do SIMBOLOS_ESPERADOS do CT-012, isto é, exatamente o ponto onde alguém decidiria reinstalá-la no index.ts achando que corrige uma omissão`
- `[T8] prova da escrituração por DOIS caminhos independentes: (a) emissão esbuild --minify (todo comentário removido) IDÊNTICA byte a byte nos dois arquivos, com sha conferido; (b) texto filtrado de comentários e linhas em branco idêntico por diff (371 e 1119 linhas). Nenhum arquivo de produção aberto`
- `[T8] ledger: 8 achados totais | 5 originados em rodada >1 | 2 suspeitos de incompletude da rodada anterior`
- `[T8]   os 5 de rodada >1 são: TR-P1, TR-P2 e TR-P3 (rodada 2 — a PRIMEIRA passagem do Gate 2, porque a rodada 1 caiu no Gate 1) e os dois BAIXO do Gate 2 na rodada 3`
- `[T8]   os 2 suspeitos são os BAIXO da rodada 3: a prosa residual de gerarSegredo já existia na rodada 2 e o Gate 2 não a pegou ao rever o P3; e os quatro artefatos de spec desatualizados nunca estiveram em delta de código algum. Os TR-P1..P3 NÃO são suspeitos — eles nasceram na primeira vez que o Gate 2 olhou`
- `[T8] staged: 2 criados + 7 modificados (1 fora da §5.2) + os artefatos de spec emendados. Suíte 1192 → 1203 (db 156 → 167). Superfície INALTERADA em 87/72 — a T8 não publica rota`

---

- `[T9] base_sha=700d5461ccd513527fbf4a6529a028d8c62315cc (o HEAD não se move — o pipeline nunca commita)`
- `[T9] executor: opus (declarado no frontmatter) · gates: [qa, tech_review] (declarado) · risk: high · tipo=security + api_contracts + secrets/config → os DOIS gates em opus`
- `[T9] resíduo staged congelado: 101 paths (T1..T8). A lista de "tocados" enviada ao QA subtrai esses 101, exceto os que a própria T9 declara`
- `[T9] ADRs injetadas no executor: ADR-0029, ADR-0024, ADR-0011, ADR-0018, ADR-0016, ADR-0017, ADR-0008 (fonte: task §7)`
- `[T9] executor: 4 criados, 20 modificados. Suíte 1203 → 1214 (api 207→215, shared 219→222). Superfície 87/72 → 88/73 por dupla medição. Garantias removidas: nenhuma`
- `[T9] o orquestrador CONFERIU por conta própria dois critérios de aceite antes de despachar o gate: packages/auth/src/catalogo-de-permissoes.ts INTOCADO (git diff --stat vazio) e ZERO ramos "papel === 'locatario'" em superficie-de-cadastro.ts`
- `[T9] NOVE arquivos fora da §5.2 — o D6 ganha a TERCEIRA ocorrência, e ela é a maior das três. Dois deles são de INFRAESTRUTURA e não apareceram nas anteriores: .env.example e deploy/systemd/sysloc-api.service (sem caminho de provisionamento, URL_BASE_DA_CONFIRMACAO faria o CT-639 reprovar; entrou por Environment= inline, a partição documentada para coordenada revisável não-segreda, com valor substituto em domínio .invalid)`
- `[T9] QUATRO dos nove são âncoras de inventário que auditam POR IGUALDADE e ficariam vermelhas: contexto.e2e.spec.ts (ROTAS_PROTEGIDAS_ACEITAS — 11ª anotação do D26 · F2/T6), contrato-publicado.e2e.spec.ts (CT-327, 42 → 43), validacao.spec.ts (as duas âncoras de validar e ESQUEMA_DO_CORPO_VAZIO) e packages/db/test/cobranca.spec.ts (CT-512 (b) enumera as filas por igualdade)`
- `[T9] duas provas de falsificação executadas pelo executor e revertidas: disparo INCONDICIONAL no PUT → CT-718 reprova (expected 2 to be 1), demais 214 verdes; segunda definição de FILA_DA_CONFIRMACAO em apps/worker/src → o apoio de packages/shared/test/fila.spec.ts reprova NOMEANDO os dois arquivos`
- `[T9] ⚠️ AVISO DO EXECUTOR PARA A T10: URL_BASE_DA_CONFIRMACAO chega hoje só à unidade da api. Quando a T10 compuser o link no worker, ela precisará da MESMA variável em deploy/systemd/sysloc-worker.service — ou de descer para o arquivo de ambiente, que é o que as duas unidades leem`
- `[T9] QA: APROVADO_COM_OBSERVACOES — 15/15 critérios, 4/4 CTs, ZERO bloqueantes. 2 BAIXO anotáveis (documentation, tests/AP-05). Rodada única`
- `[T9] o QA REPRODUZIU as duas provas de falsificação e CORRIGIU o registro do executor numa delas: o mutante LITERAL do disparo incondicional NÃO COMPILA (TS6133: emailMudou declarado e nunca lido), o que é uma barreira a mais e não a prova. Ele aplicou então a forma que compila E é o defeito real que o CT-718 persegue — disparar porque "o corpo trouxe o campo" — e aí sim: CT-718 reprova com expected 2 to be 1, demais 214 verdes`
- `[T9] o QA confirmou as TRÊS declarações que o orquestrador mandou verificar: superfície 88/73 com semDeclaracao vazio; "segredo" na redação em ENTRADA ÚNICA (grep -c devolve 1) e NENHUM registro da task passa a carga adiante; e a URL_BASE_DA_CONFIRMACAO NÃO foi tratada como segredo — Environment= inline com valor .invalid (RFC 6761), .env.example com valor VAZIO, e o EnvironmentFile 0600 continua sendo o único caminho do que é credencial. Invariante 3 do CLAUDE.md preservado`
- `[T9] ⚠️ ACHADO DO QA QUE ELE DELIBERADAMENTE NÃO CLASSIFICOU COMO PROBLEMA, e que vai ao Gate 2: o critério §4 "falha do add não derruba a resposta do cadastro" está IMPLEMENTADO e correto (try/catch que engole e registra {erro, empresaId, locatarioId}, sem o segredo), mas NENHUM caso o exercita. A §6.5 não lhe atribuiu CT e a §6.6 não tem card — foi decisão de quem especificou. O risco é real: remover aquele catch não reprova caso algum, e o desfecho seria trocar mensagem perdida por CADASTRO perdido`
- `[T9] o QA mediu o teto de LIMITE_DA_VARREDURA_MS a pedido do orquestrador: 60 s contra 3967 ms isolado (~15× de folga), DENTRO do padrão do repositório (LIMITE_CASO_MS de 120 s, LIMITE_DE_MONTAGEM_MS de 240 s). O número está certo; o que está errado é a justificativa ao lado dele — virou o BAIXO-001`
- `[T9] ⚠️ o QA registrou que o CT-907 apareceu DUAS vezes por timeout (6003 ms e 9497 ms) nesta validação, e nomeou o agravante: a T9 acrescentou ao MESMO pacote um segundo arquivo que varre a árvore inteira (~4 s), e o CT-907 vizinho — que faz a MESMA varredura — segue no teto padrão de 5 s. Ele NÃO mexeu nele, por ser expansão de escopo (proibição 5 do Protocolo)`
- `[T9] ⚠️ NOTA OPERACIONAL do QA, e ela vale para todo o resto do run: ele mesmo causou uma execução falsa ao rodar rm -rf /tmp/sysloc-banco-* com uma suíte concorrente viva — a api reportou 15 falhas e 6 skipped, e isolada deu 215/215. Disco em 96% torna esse acidente mais provável. RODAR UM PACOTE POR VEZ`
- `[T9] rule_candidates: +2 (repeated_fixture — 8ª cópia do arranjo de sessão E2E; repeated_assertion_shape — 3º bloco do molde de barreira de partida)`
- `[T9] TR consultou: ADR-0008, ADR-0011, ADR-0016, ADR-0017, ADR-0018, ADR-0024, ADR-0026, ADR-0029 (as Decisions lidas integrais)`
- `[T9] Tech Review rodada 1: REJEITADO — 1 CRITICO security + 1 ALTO testability + 2 MEDIO bloqueantes (architecture, scope_deviation) + 1 BAIXO anotável`
- `[T9] ⚠️ O P1 É O ACHADO MAIS GRAVE DO RUN INTEIRO, e o gate o rastreou PELO FONTE DAS BIBLIOTECAS, não por suspeita: bullmq/scripts.js:151 empurra job.data como ARGUMENTO de comando Redis; job.js:322 o serializa; ioredis/DataHandler.js:40 anexa err.command={name,args} em QUALQUER erro de resposta (MISCONF, OOM, NOSCRIPT, LOADING); ioredis/event_handler.js:107 faz o mesmo no abortError. E a redação NÃO alcança: log.ts:572 varre Object.keys(erro), a chave "command" não casa RADICAIS_SENSIVEIS, e cada arg é uma CADEIA que só passa por mascararCredencial (log.ts:381), que trata usuario:senha@ e ?nome=valor — um JSON com o campo segredo atravessa BYTE A BYTE`
- `[T9] o gate mediu que o caminho NÃO é hipotético: é o modo de falha para o qual o próprio produtor foi desenhado (enableOfflineQueue: false + maxRetriesPerRequest finito ⇒ falha rápida), e o abortError é exatamente o cenário §6.4 "Redis indisponível no disparo". Pior: o MISCONF é CONCRETO NESTE HOST — Redis com AOF ligado (invariante 8) e disco em 96% ⇒ "MISCONF Errors writing to the AOF file" ⇒ vazamento`
- `[T9] o gate mediu ainda que a corrida do LIMITE_DE_ENFILEIRAMENTO_MS NÃO protege: o timeout só ganha quando o servidor NÃO RESPONDE; a rejeição por erro de resposta e por abortError é IMEDIATA e ganha a corrida, propagando o erro contaminado`
- `[T9] ⚠️ O P2 EXPLICA POR QUE O P1 ATRAVESSOU O GATE 1: o ramo do catch não é exercitado por caso algum, logo NENHUMA asserção sobre o conteúdo da linha de warn existe. O próprio gate escreveu: "a ausência do caso não é neutra — ela é o que tornou o P1 invisível". O QA havia NOMEADO esse ramo sem caso e deliberadamente não o classificara como problema, delegando ao Gate 2. A delegação funcionou`
- `[T9] o P3 é de TOPOLOGIA, não de exposição: URL_BASE_DA_CONFIRMACAO é exigida na partida da API mas NENHUM código da API a lê — quem compõe o link é o worker, e a T10 §3.4 já declara que ela é exigida "também aqui". O valor entrou por Environment= inline na unidade da api, e a do worker não o tem, de modo que a T10 precisaria de uma SEGUNDA declaração literal. Contraria o precedente escrito do próprio repositório para EMAIL_REMETENTE: "mora aqui porque é o mesmo arquivo que as duas unidades leem, e separá-lo criaria uma segunda fonte para o mesmo processo"`
- `[T9] ⚠️ O P4 é bloqueante pela partição (scope_deviation MÉDIO), mas o PRÓPRIO GATE escreveu "Para esta task: nenhuma reversão — as nove edições são corretas e estão justificadas em linha". O remédio dele é para a FATIA e é ação do ORQUESTRADOR (registro na §2 + candidato a regra), não do executor. Ele vai ao executor como CONTEXTO, não como item a corrigir — e isso está declarado por escrito na memória lazy`
- `[T9] o TR RESPONDEU a pergunta 4 do orquestrador a favor do executor: o desenho do gancho está CERTO. A inversão mantém a classe ignorante do papel, não é seam de teste, e as alternativas são piores — subclasse redeclararia as duas operações e reabriria a divergência entre papéis; emissor de eventos tiraria a continuação do controle transacional, que é o que Promise<() => Promise<void>> amarra. O P5 é só de FORMA (parâmetro posicional), e ele mesmo diz "nada a fazer nesta task"`
- `[T9] o TR confirmou que NÃO há IDOR na rota nova: reenviar lê o locatário por CadastroDePessoaService.ler ANTES de invalidar ou emitir, sob o contexto de tenant — locatário de outra empresa vira 404 indistinguível e NENHUM portador órfão nasce`
- `[T9] requires_qa_revalidation=true — o bloqueante de maior severidade é category:security (CRITICO)`
- `[T9] attempt_sha (rodada 1)=9934f2ef4a8df6bb845a18bde0b12eb0cb2c1c04`

---

## ⏸️ PAUSA CONTROLADA (2ª) — 2026-08-13

**Solicitada pelo usuário durante a execução.** ⚠️ **Diferente da primeira, este ponto NÃO é limpo.**

- `[run] a T9 está EM PROGRESSO: a rodada 2 de correção foi ENTREGUE pelo executor e NÃO foi validada por gate nenhum. requires_qa_revalidation=true, logo a retomada é QA → Tech Review, os dois em scan_scope: DELTA, com attempt_sha_anterior=9934f2ef4a8df6bb845a18bde0b12eb0cb2c1c04`
- `[run] ⚠️ HÁ CÓDIGO DE PRODUÇÃO NÃO APROVADO E NÃO STAGED na árvore. Não é descuido: o pipeline só faz git add depois que os gates aprovam. NÃO rodar git checkout/stash/restore — apagaria a correção de um CRÍTICO de segurança`
- `[run] a memória lazy _run/tmp/T9.md foi PRESERVADA e ENRIQUECIDA com a rodada 2 (o que o executor fez item a item, a divergência deliberada dele em relação à prescrição do gate, e os quatro pontos que os gates devem olhar com atenção). Ela é gitignored — existe no disco, não no git`
- `[run] o attempt_sha da rodada 1 está gravado NOS DOIS lugares (memória lazy e este arquivo), corrigindo a falha que o Gate 2 da T5 apontou e que voltou na T8`
- `[T9] rodada 2 entregue: 1 criado, 9 modificados. Suíte 1214 → 1217 (api 215 → 218). Superfície inalterada em 88/73`
- `[T9] P1 corrigido pela saída PRESCRITA (fronteira única): semRastroDeComando em produtor-de-fila.ts, nos TRÊS pontos por onde erro da biblioteca sairia — a rejeição do add, o conexao.on('error') e o confirmacao.on('error'). A causa entra como CADEIA, nunca o objeto. ConfirmacaoDeEmailService NÃO foi tocado. Nasceu com DECISÃO FECHADA e REVERTER EXIGE`
- `[T9] a prova do P1 foi feita com SERVIDOR REAL recusando escrita (CONFIG SET maxmemory 1 + noeviction ⇒ OOM, a MESMA CLASSE do MISCONF de disco cheio): o CT-738 exibe o erro CRU carregando JSON.stringify(carga) em command.args e a linha do registrador CONTENDO o segredo sentinela; o CT-739 afirma que o que o produtor rejeita tem Object.keys === [] e que a linha emitida NÃO o contém. Mutante: removida a envoltura ⇒ CT-739 reprova em expected [ 'command' ] to deeply equal []`
- `[T9] P2 corrigido em duas peças, com VERMELHO REGISTRADO ANTES: contra o produtor da rodada 1, o CT-738 PASSOU (o vazamento existe e é reprodutível) e o CT-739 REPROVOU. Mutante do P2: removido o try/catch de entregar ⇒ CT-737 reprova com expected 500 to be 201`
- `[T9] ⚠️ DIVERGÊNCIA DELIBERADA DO EXECUTOR em relação à prescrição do gate, e ela NÃO FOI JULGADA POR GATE NENHUM — é o primeiro item da retomada: o gate sugeria fechar o P1 pela asserção da linha de warn NO E2E; o executor não o fez, por duas razões MEDIDAS — (i) a queda da fila rejeita pelo caminho enableOfflineQueue: false, que NÃO anexa command, de modo que aquela asserção não ficaria vermelha com o código de hoje; (ii) capturar a linha exigiria a montagem instrumentada, cuja TERCEIRA suíte é o gatilho do D57`
- `[T9] P3 corrigido pela saída 1 (EnvironmentFile compartilhado), e a razão NÃO é preferência: a §16.3 do tech_spec declara LITERALMENTE "URL_BASE_DA_CONFIRMACAO no EnvironmentFile dos dois serviços". O campo Ambiente.urlBaseDaConfirmacao SAIU (a api não compõe link — ela cobra completude do arquivo compartilhado), o Environment= inline saiu da unidade, e provisionar-base.sh passa a semear a chave nos DOIS blocos`
- `[T9] ⚠️ LIMITAÇÃO DECLARADA, não fingida: verificar-provisionamento.sh exige sudo com senha interativa e NÃO foi executado. O executor validou o validável sem privilégio — shellcheck limpo, e REPLICOU a caixa de areia do CT-647 (que o próprio caso declara não exigir privilégio), medindo que as duas chaves nascem em linha própria e que a segunda passagem semeia []`
- `[T9] os dois BAIXO do Gate 1 foram FECHADOS na mesma rodada (o docblock do LIMITE_DA_VARREDURA_MS, medido em 3559 ms e não "folgado"; e as duas guardas do CT-718)`
- `[T9] QUATRO arquivos fora da §5.2 nasceram nesta rodada, os quatro por PRESCRIÇÃO do gate: apps/api/test/produtor-de-fila.spec.ts (novo), deploy/systemd/sysloc-worker.service, deploy/scripts/instalacao/provisionar-base.sh e verificar-provisionamento.sh`

---

## ▶️ RETOMADA (2ª) — 2026-08-13

- `[run] resume pós-interrupção: sinais confirmados (T9 Em Progresso; _run/tmp/T9.md de 15:48; diff não-staged nos paths declarados). Escolha (a) RETOMAR NOS GATES, assumida sem pausa por autorização explícita do usuário — a opção (b) destruiria a correção do CRÍTICO de segurança`
- `[run] autorizações do usuário reafirmadas para TODO o run: (a) não pausar aguardando resposta — toda pergunta assume o recomendado; (b) SEM TETO DE 3 TENTATIVAS — a correção prossegue até não haver bloqueante`
- `[run] executor resolvido: sysloc-backend-implementer (argumento explícito)`
- `[run] executor_discipline injetado (fonte: references/executor-discipline.md)`
- `[T9] base_sha=700d5461ccd513527fbf4a6529a028d8c62315cc (HEAD confirmado por git rev-parse — não se moveu)`
- `[T9] git add -N aplicado em apps/api/test/produtor-de-fila.spec.ts (untracked da rodada 2) — sem ele o arquivo NÃO apareceria no diff e a Camada 0 do QA reportaria falta falsamente`
- `[T9] delta rodada 1 → rodada 2 (git diff --name-only 9934f2e): 11 arquivos de código/infra — produtor-de-fila.ts, ambiente.ts, ambiente.spec.ts, confirmacao-de-email.e2e.spec.ts, produtor-de-fila.spec.ts (novo), provisionar-base.sh, verificar-provisionamento.sh, sysloc-api.service, sysloc-worker.service, .env.example, packages/shared/test/fila.spec.ts`
- `[T9] scan_scope=DELTA para os DOIS gates, attempt_sha_anterior=9934f2ef4a8df6bb845a18bde0b12eb0cb2c1c04; ordem QA → Tech Review (requires_qa_revalidation=true)`
- `[T9] executor: opus (declarado no frontmatter) | gates: [qa, tech_review] (declarado) | qa_model=opus, tech_model=opus (CLAUDE.md: Sonnet e Haiku PROIBIDOS, inclusive nos gates)`
- `[T9] disco em 95% (1,5 GB livres); nenhum resíduo /tmp/sysloc-banco-* no momento da retomada. Instrução de UM PACOTE POR VEZ repassada ao gate`
- `[T9] Gate 1 (QA) rodada 2: APROVADO_COM_OBSERVACOES — 15/15 critérios, 8/8 CTs rastreados, ZERO bloqueantes. 1 MÉDIO code_quality (AP-26, anotável pela partição) → débito. scan_scope=DELTA sustentado SEM cair para FULL (raio de impacto por SÍMBOLO)`
- `[T9] o QA confirmou os três achados de código do Gate 2 como SANADOS, ponto a ponto: P1 (semRastroDeComando nos três pontos — add:251, conexao.on('error'):212, confirmacao.on('error'):227 —, causa como CADEIA, DECISÃO FECHADA com REVERTER EXIGE nominando CT-738/CT-739); P2 (CT-737 + CT-738/CT-739 com Redis REAL em OOM); P3 (o campo Ambiente.urlBaseDaConfirmacao saiu, mas a variável SEGUE no ESQUEMA e em VARIAVEIS_EXIGIDAS — o critério §4 é de EXIGÊNCIA, não de publicação: ATENDIDO na substância)`
- `[T9] ⚠️ RESSALVA do QA que vai ao Gate 2 junto com a divergência deliberada: o CT-739 (b) RECONSTRÓI a linha de warn em vez de exercitar ConfirmacaoDeEmailService.entregar — o docblock afirma que a forma é "a MESMA que o catch usa" e essa igualdade não é asserida. O QA não o classificou como problema porque a propriedade que fecha a classe é a da FRONTEIRA (Object.keys(erro) === []), independente do chamador`
- `[T9] AP-24 LIMPO e MEDIDO: o diff contra o attempt_sha da rodada 1 sobre apps/api/test e packages/shared/test não tem UMA linha de remoção de it(/describe(. As três asserções de ambiente.spec.ts que mudaram de forma foram CONFIRMADAS por cruzamento dos dois diffs como introduzidas pela própria T9 na rodada 1 — nenhuma anterior à task, logo nenhuma é regressão de prova de fatia fechada. O QA-BAIXO-002 fechou por FORTALECIMENTO (toBeDefined → toBe(criado.id))`
- `[T9] Protocolo Antirregressão conferido pelo Gate 1 nas três pontas: nenhum it removido; guarda if (sessao.empresaId === null) de contexto-da-sessao.ts:83 INTACTA; apps/worker/src/fila.ts NÃO aparece no diff contra o base_sha (as duas DECISÃO FECHADA intactas)`
- `[T9] Ledger atualizado (Passo 4.2) nas DUAS pontas — os 5 achados do Gate 2 receberam rodada_ultima_verificacao=2, os dois BAIXO do QA passaram a corrigido, e QA-MED-001 entrou com rodada_origem=2`
- `[T9] veredito NÃO reclassificado: APROVADO_COM_OBSERVACOES é o veredito nativo do gate e não houve divergência com a partição`
- `[T9] Gate 2 (Tech Review) rodada 2: APROVADO_COM_OBSERVACOES — TR-P1 (CRÍTICO/security), TR-P2 (ALTO/testability) e TR-P3 (MÉDIO/architecture) SANADOS e re-verificados no fonte. Dois BAIXO novos, os dois de PRECISÃO DE TEXTO (alcance do "O QUÊ" do marcador novo; afirmação de igualdade num docblock). Zero bloqueantes. scan_scope=DELTA sustentado, raio de impacto por SÍMBOLO`
- `[T9] ⚠️ O GATE JULGOU A DIVERGÊNCIA DELIBERADA A FAVOR DO EXECUTOR, e o registro é o que importa: "a minha seria prova incapaz de falhar, pelo caminho enableOfflineQueue: false não anexar command". A saída escolhida é ESTRITAMENTE MAIS FORTE que a prescrita porque a asserção mede por IGUALDADE COM O CONJUNTO VAZIO (Object.keys(erro) === []) e não pela ausência da chave command — a próxima propriedade que a biblioteca anexar também reprova`
- `[T9] o gate conferiu no fonte que redigirErro (log.ts:552) varre Object.keys das PRÓPRIAS e lê erro.cause — e como cause de new Error(msg,{cause}) é própria NÃO-ENUMERÁVEL, a causa sai como texto pelo campo causa e nada mais atravessa. É a razão técnica de a causa entrar como CADEIA`
- `[T9] a ressalva que o Gate 1 delegou (CT-739 (b) reconstrói a linha de warn) foi julgada PROCEDENTE mas BAIXO: a propriedade que fecha a classe é a da FRONTEIRA, indiferente ao chamador, e a mutação imaginada ({ carga } no catch) esbarra na SEGUNDA barreira — segredo está em RADICAIS_SENSIVEIS. Elevar a asserção exigiria a montagem instrumentada, que é o gatilho do D57`
- `[T9] JULGAMENTO 3 — a exigência de URL_BASE_DA_CONFIRMACAO SOBREVIVEU à mudança de topologia, verificado ponta a ponta: ESQUEMA:171 → VARIAVEIS_EXIGIDAS deriva de Object.keys(ESQUEMA.shape):181 → partida falha fechado; provisionar-base.sh semeia nos DOIS blocos (:1499 e :767-770) em domínio .invalid; .env.example:209 declara; as DUAS unidades leem o MESMO EnvironmentFile. Conclusão para a T10: NENHUMA segunda fonte a criar`
- `[T9] D57 e D49 NÃO dispararam. O D57 QUASE disparou e não o fez por escolha do executor: produtor-de-fila.spec.ts monta por conectarProdutorDeFila, sem criarAplicacao. O D28 (F0/T5) ganhou UMA ocorrência nova com marcador canônico; a linha já existe no índice do CLAUDE.md — as duas pontas fechadas, sem escrituração pendente`
- `[T9] os quatro arquivos fora da §5.2 nascidos na rodada 2 NÃO são scope_deviation — os quatro foram prescritos pelo próprio gate. TR-P4 segue aceito_como_debito, sem evidência nova que eleve severidade. Registrado como TERCEIRA ocorrência do D6 (T7 e T8 foram as duas primeiras)`
- `[T9] ledger: 10 achados totais | 3 originados em rodada >1 | 0 suspeitos de incompletude da rodada 1 pelo critério mecânico`
- `[T9] ⚠️ o critério mecânico devolveu 0 mas ESCONDE UM CASO REAL: o QA-MED-001 (AP-26 em ambiente.spec.ts) nasceu do código da RODADA 1 e o sweep daquela rodada NÃO o viu — o próprio QA o admitiu por escrito. Ele escapa do {C} porque o arquivo ESTÁ no delta da correção. É evidência de que o critério de {C} subestima incompletude quando a correção reabre o mesmo arquivo`
- `[T9] staged: 27 paths (4 criados declarados + 1 criado não declarado + 10 modificados declarados + 12 modificados não declarados). Total do run: 120 arquivos staged, sem commit`
- `[T9] CONCLUÍDA em 2 rodadas. Suíte 1203 → 1217. Superfície 87/72 → 88/73`

### T10 — A entrega (mensagem no domínio + processador no worker)

- `[T10] Em Progresso. base_sha=700d5461ccd513527fbf4a6529a028d8c62315cc (HEAD inalterado — o pipeline nunca commita; o isolamento vem do filtro por paths)`
- `[T10] executor: opus (declarado no frontmatter) | gates: [qa, tech_review] (declarado) | risk: high`
- `[T10] deps satisfeitas: T4 (Concluído) e T9 (Concluído)`
- `[T10] ADRs injetadas no executor: ADR-0024, ADR-0029, ADR-0025, ADR-0008, ADR-0006 (fonte: task §7)`
- `[T10] o identificador do DÉBITO NOVO desta task foi resolvido pelo orquestrador como D12 · F3/T10 — a §2 do run-report desta fatia vai de D1 a D11 (a T9 acrescentou D8, D9, D10 e D11)`
- `[T10] Gate 1 (QA) rodada 1: APROVADO_COM_OBSERVACOES — 12/12 critérios, 1/1 CT + 2 apoios rastreados, ZERO bloqueantes. 3 BAIXO, os três de documentation (anotáveis) → débito. scan_scope=FULL (rodada 1)`
- `[T10] o QA conferiu POR SI as duas DECISÃO FECHADA de apps/worker/src/fila.ts, em vez de aceitar a declaração do executor: diff entre os blocos extraídos de git show HEAD:… e do arquivo em disco (marcador + 12 linhas seguintes de cada um) retorna VAZIO. As duas só se deslocaram de 57→59 e 74→76 pelas duas linhas de import acrescentadas acima delas`
- `[T10] o D12 fecha nas DUAS pontas com o ÍNDICE apontando para caminho que existe, e a COLISÃO com o D12 · F3/T4 da fatia irmã está declarada explicitamente em DOIS lugares do CLAUDE.md, nomeando o que cada um é — a §3-B é atendida (o identificador é o par, nunca o número)`
- `[T10] o D49 foi conferido e MANTIDO: marcador íntegro em apps/worker/test/ambiente.spec.ts:750, ÍNDICE na 762. O gatilho NÃO disparou — a T10 não é o terceiro processo`
- `[T10] SEGUNDA FONTE DA VARIÁVEL — ausência CONFIRMADA pelo gate: sysloc-worker.service não tem Environment= inline (a variável aparece ali só em COMENTÁRIO dizendo que chega pelo EnvironmentFile), provisionar-base.sh semeia uma única vez, e NENHUM dos dois está no diff da T10. A topologia que a T9 fixou permanece com uma fonte só`
- `[T10] função PURA provada por COMPORTAMENTO, não por leitura: o terceiro caso de apoio planta URL_BASE_DA_CONFIRMACAO=https://ambiente-que-nao-deve-prevalecer.invalid em process.env, restaura por onTestFinished, e assere que o corpo continua saindo do PARÂMETRO. grep process.env em packages/documentos/src devolve 1 ocorrência, dentro do docblock que explica por que a leitura não acontece`
- `[T10] ⚠️ o SEGREDO VAI NO FRAGMENTO do link (<base>/confirmar-email#<segredo>), e o gate julgou a decisão CORRETA e registrada em linha: o fragmento não é transmitido ao servidor, não entra na trilha do servidor de borda da F7, não viaja no Referer e não aparece no registro de acesso. A alternativa ?segredo= foi descartada POR NOME no cabeçalho. ⚠️ ISSO É INSUMO OBRIGATÓRIO DO HANDOFF (T12): a página da F6 lerá location.hash e apresentará o segredo no CORPO de POST /v1/confirmacoes-de-email`
- `[T10] o CT-734 usa bancoQueContaUnidades — implementação de AcessoAoBanco que delega tudo ao acesso real e só conta aberturas — para tornar observável a fronteira que "lança antes de qualquer leitura" exige, e ANCORA unidadesAposEntrega > 0 no ramo feliz ANTES de exigir igualdade exata no negativo. Sem essa âncora a igualdade seria satisfeita por uma borda que nunca abre unidade alguma`
- `[T10] CT-907 estourou o teto de 5000 ms (5205 ms) na suíte do pacote e passou 28/28 em 4,97 s isolado — flake PRÉ-EXISTENTE por TIMEOUT, não regressão. O gate registrou que a passagem isolada PROVA de quebra o que o caso audita: o D12 fecha nas duas pontas, sem marcador órfão nem linha órfã`
- `[T10] rule_candidates: +2 (repeated_fixture — 28ª cópia do literal DadosDaPessoa; repeated_assertion_shape — o molde de asserção por LINHA INTEIRA em corpo de e-mail)`
- `[T10] Gate 2 (Tech Review) rodada 1: PARCIAL — 1 MÉDIO bloqueante (error_handling) + 4 BAIXO anotáveis. requires_qa_revalidation=true (error_handling está em revalidation_required; e tocou_area_critica + task_risk:high forçariam de qualquer modo)`
- `[T10] ⚠️ O BLOQUEANTE É DE JANELA QUE NÃO REABRE, e é por isso que ele bloqueia em vez de virar débito: URL_BASE_DA_CONFIRMACAO tinha só presença/vacuidade conferidas, e a FORMA não era conferida em processo NENHUM — o ambiente.ts da api registra por escrito que DEFERE a conferência a "quem compõe o link (T10)". Base sem esquema (app.sysloc.com.br) sobe verde, a tarefa conclui, o logger grava "entregue", e todo locatário recebe link que não abre. provisionar-base.sh semeia .invalid e o valor REAL é digitado na F7, DEPOIS de este repositório fechar — não há task posterior que pegue o defeito`
- `[T10] os QUATRO julgamentos que o orquestrador pediu saíram TODOS a favor da implementação: (1) as duas DECISÃO FECHADA de fila.ts intactas byte a byte, e o DESLOCAMENTO POR IMPORT ACIMA NÃO É MOVIMENTAÇÃO — "o vínculo que a §3 protege é de adjacência, não de número de linha; a leitura contrária tornaria impossível acrescentar import a arquivo com marcador"; (2) bancoQueContaUnidades é SEAM LEGÍTIMO (implementa a interface de produção, delega ao acesso real, só conta aberturas; mesmo desenho de portaQueRecusaNaFaixa); (3) o segredo no FRAGMENTO é a escolha de segurança correta; (4) camposRecusados fica em BAIXO, sem marcador`
- `[T10] o gate verificou o segredo em claro em TRÊS eixos e NÃO o encontrou no journal: (a) a lista fechada doTrabalho={idTarefa,fila,empresaId,locatarioId}; (b) o adaptador SMTP ENVOLVE a exceção do nodemailer em new Error() SEM cause, de modo que o objeto cru (com to, response, command) nunca atravessa redigirErro; (c) o vetor da T9 (err.command.args) NÃO se reproduz no consumidor — o worker nunca envia a carga de volta como argumento de comando. E é ASSERIDO: o CT-734 afirma not.toContain sobre o segredo, o empresaId e o endereço`
- `[T10] ⚠️ REGISTRO DEIXADO PELO GATE PARA O FUTURO, e que não virou achado: produtor.on('error') e consumidor.on('error') de apps/worker/src/fila.ts CONTINUAM registrando o erro cru da biblioteca — o mesmo formato que a T9 fechou do lado da api com semRastroDeComando. Não é achado porque nenhum comando emitido por este processo carrega a carga, os dois são pré-existentes (T6/T8) e endurecê-los seria expansão de escopo. QUEM INSTALAR UM PRODUTOR REAL NO WORKER REABRE O VETOR DESTE LADO`
- `[T10] attempt_sha (rodada 1)=0f0fca1f350eb518742da0a2f8101128842a0c05`
- `[T10] divisão do trabalho da rodada 2 decidida pelo orquestrador: ao executor foram o TR-P1 (bloqueante) e os anotáveis que vivem NO CÓDIGO (TR-P5, QA-BAIXO-001, QA-BAIXO-002); os que vivem em ARTEFATO DE RUN (TR-P2, TR-P3, TR-P4) foram fechados pelo próprio orquestrador, e o prompt do executor o proibiu de tocar o run-report para não colidir`
- `[T10] TR-P4 fechado: a entrada ### D12 da §2 prometia proteção do compilador nas DUAS direções e falhava justamente no exemplo que dava — enviar(destinatario, mensagem: MensagemDeAviso) recebe uma VARIÁVEL do tipo MensagemDeEmail, não um literal, então a checagem de propriedades excedentes não se aplica. Texto corrigido para declarar a assimetria; o marcador no fonte nunca fez a afirmação e ficou intocado`
- `[T10] TR-P3 fechado como QUARTA ocorrência do D6, e ela REFUTOU a delimitação escrita nas três anteriores: o gatilho do subdimensionamento NÃO é publicar rota (a T10 não publica nenhuma e caiu no padrão mesmo assim) — é criar ARESTA NOVA ENTRE PACOTES ou BORDA NOVA SOB INVENTÁRIO ESTÁTICO. A T11 faz as duas`
- `[T10] TR-P2 fechado: o contrato do fragmento (location.hash → corpo do POST) entrou na §4 do run-report como insumo obrigatório do handoff da T12, porque packages/documentos/src NÃO é entrada de gerador de handoff`
- `[T10] rodada 2 entregue: 4 modificados, 0 criados. Suíte 1227 → 1239 (worker 53 → 65). P1 fechado pela SAÍDA 1 (conferência da forma na partida do worker)`
- `[T10] ⚠️ O EXECUTOR MEDIU EM VEZ DE CONFIAR NA FORMA, e a medição mudou o desenho: URL.parse ACEITA mailto:, javascript:, file: e ftp:// como absolutas — por isso a lista fechada {http:, https:} não é decorativa. E ele NÃO escreveu ramo para servidor vazio, com razão declarada: com esquema http/https o interpretador JÁ EXIGE host (https:// e http://:8080 levantam), e o ramo seria asserção incapaz de falhar`
- `[T10] o executor conferiu que https://sysloc.invalid — o valor que provisionar-base.sh semeia — é ACEITO, de modo que a instalação de máquina nova continua subindo. Está como caso POSITIVO na tabela`
- `[T10] prova de falsificação da rede do P1, pelo script do pacote: mutante 1 (degrau de forma removido) ⇒ 8 casos reprovam; mutante 2 (URL.parse sem a lista de esquemas) ⇒ 4 reprovam (ftp:, mailto:, javascript: e o "não aborta"). Árvore íntegra 65/65. Os dois revertidos`
- `[T10] Gate 1 (QA) rodada 2: APROVADO — 12/12 critérios, ZERO problemas de qualquer severidade. O QA REPRODUZIU os dois mutantes em vez de aceitá-los por declaração, e os números bateram exatamente (8 e 4). Nota metodológica dele: o mutante 1 na forma LITERAL não compila (TS6133), e ele o mediu como degrau INERTE (false && …) — semanticamente equivalente e mais conservador`
- `[T10] Gate 2 (Tech Review) rodada 2: APROVADO — zero problemas. TR-P1 confirmado sanado NA CAUSA, não no contorno`
- `[T10] o Gate 2 respondeu o item 4 do prompt do orquestrador: NÃO nasceu terceira forma de conferir coordenada externa na partida. O molde seguido é o de ehCadeiaDeFilaValida + EXIGENCIA_DA_CADEIA_DE_FILA (packages/shared/src/ambiente.ts:33) — o precedente do MESMO acumulador (problemas.push) no MESMO arquivo. O molde de coordenadasDoTransporte é de um CONSTRUTOR que precisa levantar, e adotá-lo dentro de lerAmbiente quebraria o acúmulo de problemas que o CT-006 prova`
- `[T10] ⚠️ ACHADO DE SEGURANÇA QUE O GATE 2 REGISTROU E QUE NÃO ESTAVA NO PEDIDO: a escolha de URL.parse sobre new URL é REFORÇO DE SEGURANÇA e não estilo — o TypeError do new URL no Node 24 carrega a cadeia recusada na propriedade input, que é o vazamento que coordenadas-do-transporte.ts teve de capturar e substituir. URL.parse devolve null e o vetor não existe`
- `[T10] o gate julgou as TRÊS decisões que a medição do executor produziu, e as três se sustentam: (a) a lista fechada {http:, https:} não é decorativa (URL.parse aceita mailto:, javascript:, file: e ftp:// como absolutas); (b) a AUSÊNCIA de ramo para servidor vazio é ACERTO — para esquema especial o host vazio já é erro de análise, e o ramo seria asserção incapaz de falhar; (c) https://sysloc.invalid conferido CONTRA O FONTE REAL (provisionar-base.sh:266), não contra a declaração`
- `[T10] o gate registrou que NÃO exportar o predicado é acerto: a api DELIBERADAMENTE não confere a forma, e publicá-lo em @sysloc/shared criaria a porta que convida o segundo ponto de decisão. Mesmo raciocínio do D25 em coordenadas-do-transporte.ts:92-97 — a ausência de export converte em impossibilidade estrutural o que a prosa só proibia`
- `[T10] o gate examinou e DISPENSOU deliberadamente uma fronteira da lista fechada: https://usuario:senha@host é aceito. Não é achado (variável declaradamente não-secreta, barreira não ecoa valor, e o ramo seria complexidade especulativa) — registrado para deixar claro que o limite foi examinado, não ignorado`
- `[T10] os DOIS marcadores de apps/worker/test/ambiente.spec.ts foram reconferidos NA RUBRICA DO GATE 2, não por herança: DECISÃO FECHADA — T8 (linha 571) e DÉBITO COM GATILHO — D49 (linha 851) sem uma única linha "-" no delta; o último hunk termina em linha antiga 415, muito antes das duas âncoras`
- `[T10] ledger: 8 achados totais | 0 originados em rodada >1 | 0 suspeitos de incompletude da rodada 1`
- `[T10] TODOS os 8 anotáveis foram FECHADOS — nenhum sobra para a §2. Os 5 de código pelo executor, os 3 de artefato de run pelo orquestrador`
- `[T10] CONCLUÍDA em 2 rodadas. Suíte 1217 → 1239. Superfície inalterada em 88/73 (a T10 não publica rota)`

### T11 — O ato do titular (a única rota de negócio sem sessão)

- `[T11] Em Progresso. base_sha=700d5461ccd513527fbf4a6529a028d8c62315cc (HEAD inalterado)`
- `[T11] executor: opus (declarado) | gates: [qa, tech_review] (declarado) | risk: high`
- `[T11] deps satisfeitas: T8 (Concluído) e T10 (Concluído)`
- `[T11] ADRs injetadas no executor: ADR-0027, ADR-0024, ADR-0008, ADR-0011, ADR-0017, ADR-0022, ADR-0026, ADR-0021 (fonte: task §7)`
- `[T11] Gate 1 rodada 1: REJEITADO — 1 ALTO (tests/AP-29: a asserção "diferente do sucesso" do CT-723 é infalível) + 2 BAIXO. Gate 1 rodada 2: APROVADO. Gate 2 rodada 1: PARCIAL — 1 MÉDIO bloqueante (adr_compliance) + 2 BAIXO. Gate 1 rodada 3: APROVADO_COM_OBSERVACOES. Gate 2 rodada 2: APROVADO_COM_OBSERVACOES`
- `[T11] attempt_sha (rodada 1)=a801fb2f54c2a42af8d1797981d90c76ffa58754 | (rodada 2)=25db1de192e87d1e63784f0999dec623f2c02936`
- `[T11] ⚠️ O EXECUTOR REFUTOU A PRESCRIÇÃO DO GATE 1 POR MEDIÇÃO, E O GATE SE RETRATOU POR ESCRITO. A saída 1 (comparar contra confirmacaoReal) seria IGUALMENTE infalível: o gate reinseriu a linha na posição exata sob o mutante e ela NUNCA FOI ALCANÇADA — o it abortou na asserção literal (:1026). Veredito literal dele: "A prescrição que eu dei na rodada 1 estava errada, e o executor está certo"`
- `[T11] o AP-24 da remoção de asserção foi ABSOLVIDO POR MEDIÇÃO nos DOIS gates, independentemente — remover asserção é a forma canônica do weakening test to pass, e a absolvição dependia inteiramente do poder de detecção ser ZERO`
- `[T11] ⚠️ O BLOQUEANTE DO GATE 2 NÃO TINHA LINHA DE CÓDIGO A MUDAR: a Decision da ADR-0024 afirmava que a enumeração de tenants é a ÚNICA leitura legítima sem contexto de empresa, e a T11 instalou a SEGUNDA — citando a própria 0024 como autorização em CINCO pontos do diff. O código estava certo (a 0027, posterior e específica, institui o mecanismo); o TEXTO da 0024 é que envelheceu`
- `[T11] o impacto era MEDIDO, não teórico: a F4 é a integração bancária, e "retorno de integração externa" está NOMEADO no Context da própria 0024. Lida como proibição, bloqueia mecanismo já instituído; lida como autorização por analogia, abre leitura sem contexto SEM a maquinaria que torna esta segura. O repositório já mediu esse custo — a emenda da ADR-0021 registra que "um gate futuro leria violação onde houve decisão, e leu, no Gate 2 da T7"`
- `[T11] a ADR-0024 foi EMENDADA (não superseded), pela autorização permanente do usuário e com o precedente literal da ADR-0021 na T7 da fatia irmã. O Gate 2 confirmou nas quatro alíneas: (a) a emenda RESOLVE e não apenas documenta; (b) o discriminador é OPERACIONAL — as cinco condições são verificáveis por consulta ao catálogo do Postgres, e o nosso_numero do boleto da F4 NÃO satisfaz nenhum dos quatro atributos que a 0027 exige de um portador, logo não passa por analogia; (c) a decisão NÃO mudou, com evidência MECÂNICA independente: o INDEX regenerado por script manteve a linha-resumo da 0024 BYTE A BYTE; (d) a §21.3 fecha, "Duas" → "Três" sem resíduo`
- `[T11] ⚠️ RESSALVA HONESTA que o Gate 2 registrou e que vale guardar: a emenda NEGA A LETRA de uma frase da Decision, e ampliar conjunto permitido é estritamente mexer em alcance. Ele não abriu achado por três razões concretas — a frase negada era afirmação de COMPLETUDE SOBRE O ESTADO DO MUNDO na data (e tudo que a emenda acrescenta é RESTRITIVO); a segunda leitura já estava instituída por ADR aceita MAIS NOVA (0027, de 2026-08-12, contra 2026-08-11 da 0024), de modo que a emenda RECONCILIA em vez de inverter; e superseder produziria ADR nova com a mesma decisão, o churn que o projeto já recusou no caso da 0006`
- `[T11] o CT-740 nasceu da correção (não estava na §6 da task) com TRÊS mutantes; o Gate 1 REPRODUZIU DOIS pelo script do pacote, com controle 226 passed e reversão conferida por md5sum. O quarto mutante foi DESCARTADO com razão registrada: motivo: derivado deixa a constante sem leitor e NÃO COMPILA (TS6133) — o compilador é a primeira barreira, e por isso os mutantes escolhidos PRESERVAM a constante. O Gate 2 julgou o raciocínio procedente: "um mutante que o compilador já pega não testaria a asserção, testaria o tsconfig"`
- `[T11] o Gate 2 conferiu a defesa contra o modo de falha nomeado na testing-stack.md: sem semComentarios a asserção casaria MENÇÃO em vez de USO (o docblock do serviço cita motivo: 'nao-resolvido' em prosa na linha 78) e o mutante C não seria discriminável. "A defesa é real e necessária, não decorativa"`
- `[T11] ⚠️ O GATE 2 CORRIGIU O GATE 1 no achado de texto: a razão que o Gate 1 dera para MANTER a guarda ("ela reprova se o acessório voltar a tolerar leitura ausente") é IGUALMENTE FALSA — arquivos.length continuaria 1. Quem reprovaria é a asserção seguinte. Registrado no D14 para que a correção não reintroduza o erro com outra redação`
- `[T11] ledger: 8 achados totais | 2 originados em rodada >1 | 0 suspeitos de incompletude da rodada 1`
- `[T11] staged: 12 paths. CONCLUÍDA em 3 rodadas. Suíte 1239 → 1247. Superfície 88/73 → 89/74, publicas +1`

### T12 — O fecho (superfície 89/74 por dupla medição + isolamento da verificação)

- `[T12] Em Progresso. base_sha=700d5461ccd513527fbf4a6529a028d8c62315cc (HEAD inalterado)`
- `[T12] executor: opus (declarado) | gates: [qa, tech_review] (declarado) | risk: high`
- `[T12] deps satisfeitas: T7, T9 e T11 (as três Concluídas) — as três rotas existem`
- `[T12] ADRs injetadas no executor: ADR-0011, ADR-0018, ADR-0027, ADR-0006, ADR-0005 (fonte: task §7)`
- `[T12] ⚠️ a §5.2 desta task DECLARA o _run/run-report.md (o registro da página inexistente, §4) — o orquestrador PARA de editar esse arquivo enquanto a T12 roda, para não colidir`
