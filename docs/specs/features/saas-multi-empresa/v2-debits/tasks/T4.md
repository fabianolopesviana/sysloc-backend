# T4 — Cleanup de evidência e inventário da T9 (D-004, D-008, D-009, D-011 + partes-T9)

## 1. Identificação

- **ID**: T4
- **Nome da Task**: Cleanup de evidência e inventário da T9 — §5.1, §7.A.20, seção de evidência ausente, frases sobre mapa e três espelhamentos
- **model**: sonnet
- **risk**: low
- **gates**: [qa]
- **Status**: Concluído
- **Fase**: 1
- **Dependências**: Nenhuma (coordenação **textual** com T2 e T3 — ver §4.4)
- **Símbolos públicos criados**: N/A
- **Símbolos consumidos de outras tasks**: N/A
- **Critério de Conclusão**: os quatro débitos e os três espelhamentos corrigidos no `T9.md`, sem tocar outro arquivo; coerência com T2/T3 conferida; Gate 1 aprova.
- **Feature**: saas-multi-empresa
- **Versão**: v2-debits
- **Parent task**: T9 (de v1)
- **Débitos origem**: D-004 (`MEDIO`), D-008 (`MEDIO`), D-009 (`MEDIO`), D-011 (`BAIXO`) + partes-T9 de D-005, D-007, D-012
- **Source**: agent-spec-debt-resolution

> É a task com mais débitos (7 itens) e o maior custo estimado (~37min), porque sete dos doze débitos da v1 caíram neste arquivo.

---

## 2. Objetivo da Task

Corrigir o inventário de entregáveis, a seção de aprendizado, a evidência ausente e as afirmações absolutas do `T9.md`, mais três espelhamentos cujos gêmeos estão em T2 e T3.

O `T9.md` é a **evidência de execução** de uma task operacional — nela a prosa **é** o artefato entregue, e é o documento que a T2 da v1 vai herdar como contrato.

---

## 3. Arquivos Impactados

### 3.1 Arquivos a Criar

_Nenhum._

### 3.2 Arquivos a Modificar

| Arquivo | Descrição |
|---------|-----------|
| `docs/specs/features/saas-multi-empresa/v1/tasks/T9.md` | §5.1 + checklist (D-004); §7.A.20 (D-008); seção de evidência nova (D-009); frases sobre mapa de IPs (D-011); espelhamentos de D-005/D-007/D-012 |

### 3.3 Arquivos de Referência

- `docs/specs/features/saas-multi-empresa/v1/_run/run-report.md` §2 — os débitos.
- `docs/specs/features/saas-multi-empresa/v1/_run/workflow-report.md` — a telemetria das 8 rodadas da T9, incluindo as medições que o D-009 precisa registrar.
- `deploy/scripts/veredito_suite.sh` e `deploy/scripts/portao_orfaos.py` — **leitura apenas**, para espelhar redação. São da T2 e T3.

---

## 4. Detalhes de Implementação

### 4.1 Correções — os quatro débitos próprios

- [x] **D-004 (MEDIO)** — §5.1 (linha ~59) e checklist (linha ~1302) listam **dois** arquivos criados; a task criou **três**.
  - Falta `deploy/scripts/veredito_suite.sh` (73 linhas, modo 755). Ele **está** documentado na §7.A.19 com justificativa, então não é artefato oculto — o defeito é de escrituração.
  - Por que importa: a §5.1 é a lista que a **Camada 0 de qualquer gate futuro** lê para cruzar declarado × entregue. Sem linha lá, o script fica sem task de origem rastreável — e ele é o sinal que T2/T3 da v1 e CI vão herdar.
  - Acrescentar linha na tabela §5.1 com path e justificativa da promoção (mesma forma já usada para o `portao_orfaos.py`), e corrigir o checklist para enumerar os **três**.

- [x] **D-008 (MEDIO)** — §7.A.20 (linha ~1239) diz *"Cinco variações"* sobre uma tabela de **seis** linhas, e a conclusão está superdeclarada.
  - O QA auditou as seis linhas: **todas são reais e corretamente descritas**. Só a contagem na prosa está errada.
  - Nas palavras dele: *"o erro de não varrer o vizinho ocorrendo dentro do parágrafo sobre não varrer o vizinho"*.
  - Além da contagem, as rodadas 7 e 8 produziram a **sétima** e a **oitava** variação:
    - **7ª** — o wrapper corrigido no compose ficou **travado em `exit 1`** (o placar sai em stderr; o pipe carregava stdout vazio), e a medição foi feita só do lado vermelho, onde "funciona" e "está travado" são indistinguíveis.
    - **8ª** — ao ganhar a capacidade de reportar sucesso, o wrapper abriu **falso-verde**: suíte de zero testes (`Ran 0 tests` + `OK`) e linha `OK` espúria com placar `FAILED` saíam `exit 0`.
  - Corrigir a contagem para **seis**, acrescentar as variações 7 e 8, e **reescrever a conclusão**: a mudança de classe fechou **uma** superfície (o portão, que virou arquivo executado por stdin); as demais continuam abertas porque dependem de sincronização manual entre artefatos.
  - Esta é a seção que a T2 da v1 herda como aprendizado. Publicada como está, ensina que o problema acabou.

- [x] **D-009 (MEDIO)** — as correções de bootstrap e de piso **não têm seção de evidência**, e o checklist (linha ~1243) remete a §7.A.16/§7.A.19, que não as contêm.
  - A §7.A.16 é de rodada anterior e lista apenas invalidações antigas; a §7.A.19 trata exclusivamente do `bench run-tests`.
  - Todas as outras correções do run ganharam seção numerada; estas duas não.
  - Abrir seção nova com as **medições literais** dos quatro caminhos de `RODADA_INVALIDA`, incluindo o cenário do **interno renomeado** — que é o que distingue "o portão não pode afirmar nada" de "achei um órfão". Corrigir a referência do checklist.
  - **Este é o item mais caro (~25min) e o único que exige ambiente**: as medições precisam ser **reproduzidas**, não copiadas do `workflow-report.md`. Ver §4.5.

- [x] **D-011 (BAIXO)** — frases absolutas sobre mapa de IPs (linhas ~1188 e ~613) são **literalmente falsas**.
  - *"Não há mais mapa publicado em lugar nenhum"* (`:1188`) é desmentida pelo bloco imediatamente abaixo (saída de `network inspect`).
  - *"Não há mapa publicado aqui de propósito"* (`:613`) é desmentida dez linhas depois, na mesma seção (mapa de rodada anterior, com valores que já não valem).
  - Os dois blocos são **legítimos como evidência datada** — o defeito é a afirmação absoluta.
  - Trocar por afirmação verificável: *"nenhum mapa é publicado como estado atual; onde há saída capturada, ela está datada e vem acompanhada do comando que a deriva"*. Idem na nota da `:613`, reconhecendo o mapa logo abaixo como captura de rodada anterior.

### 4.2 Os três espelhamentos (gêmeos em T2 e T3)

- [x] **D-005 (parte-T9)** — remover `com arquivo temporario` de `T9.md:1250`. O lado script (`veredito_suite.sh:25`) é da **T2**.
- [x] **D-007 (parte-T9)** — se a T2 alterar a redação da condição (c), **espelhar** onde o predicado é reenunciado (`T9.md:1248`). Ver §4.4.
- [x] **D-012 (parte-T9)** — atualizar as **três amostras de saída** do portão (`:571`, `:1062`, `:1101`) com o campo `piso em vigor`, que a correção do piso introduziu. Formato real medido: `PORTAO_TOTAL_ALVOS: 739 (piso em vigor: 700; ...)`. O lado docstring é da **T3**.

### 4.3 O que NÃO corrigir neste arquivo

O `T9.md` tem seções deliberadamente preservadas como **registro histórico** — não são débito:

- `:1223` e o registro da forma antiga do wrapper (`grep -qE '^OK'` sem `2>&1`) — preservados de propósito para documentar o defeito.
- §7.A.4, marcada como *"Superado por §7.A.14"* — o padrão correto de superação, mantido.
- Os blocos de saída datados com IPs de rodadas anteriores — legítimos como captura histórica (é a **afirmação absoluta** sobre eles que o D-011 corrige, não os blocos).

**Não "limpe" nada disso.** Apagar registro histórico é perder a trilha de auditoria que dá valor a este arquivo.

### 4.4 Guardrail cruzado com T2 e T3 (obrigatório)

Três débitos têm lado gêmeo em arquivo de outra task:

| Débito | Lado seu (T4) | Lado da outra task |
|---|---|---|
| D-005 | `T9.md:1250` | `veredito_suite.sh:25` (T2) |
| D-007 | `T9.md:1248` | `veredito_suite.sh:61` + cabeçalho `:34` (T2) |
| D-012 | `T9.md:571`, `:1062`, `:1101` | docstring do `portao_orfaos.py` (T3) |

**A redação final tem de ser coerente.** Para o D-007 em particular: a T2 vai **declarar no output** a forma final adotada para a condição (c) — espelhe **essa** forma, não a sugerida no débito, caso divirjam.

Se a T2 rodar em paralelo e você não tiver a forma dela, **duas opções válidas**: (a) espelhar a forma sugerida e registrar que precisa de conferência contra a T2; ou (b) reportar dependência e deixar o espelhamento do D-007 para uma passada de conferência. **Não invente uma terceira redação.**

Isto é a lição da v1 aplicada: o padrão dominante lá, em **oito ocorrências**, foi *corrigir num lugar e não varrer o vizinho*. O agrupamento por arquivo que gerou estas tasks resolve o paralelismo e reintroduz esse risco exatamente aqui.

### 4.5 Pré-requisito de ambiente (só para o D-009)

O D-009 exige **reproduzir** os quatro caminhos de `RODADA_INVALIDA` do portão para colar evidência literal. Isso precisa da stack nova de pé:

```
docker compose -f deploy/compose/docker-compose.stack-nova.yaml -p frappe-stack-nova up -d
```

Estado no momento da geração desta versão: 4 contêineres `up`, site `verificacao` provisionado, marcador `.provisionado` presente, rede `internal=true`, portão VERDE com 739 alvos.

**Se o ambiente não estiver de pé e você não conseguir subi-lo**, reporte — não invente as medições. Copiar do `workflow-report.md` **não** satisfaz o débito: o defeito é justamente afirmação sem seção que a sustente.

### 4.6 Guardrails de Execução

**DEVE**:

- Ler as seções citadas antes de editar. O arquivo tem ~1307 linhas — não reescreva o que não está listado.
- Para o D-009, **reproduzir** as medições e colar a saída literal.
- Conferir a coerência dos três espelhamentos com T2 e T3 (§4.4).

**NÃO DEVE**:

- NÃO tocar `deploy/scripts/veredito_suite.sh` (T2), `deploy/scripts/portao_orfaos.py` (T3) nem `T8.md` (T1).
- NÃO apagar registro histórico (§4.3).
- NÃO alterar o veredito de nenhum AC — AC-3 e AC-4 estão aprovados; AC-1/AC-2/AC-5 seguem pendentes de T2/T3 da v1, e isso **não** é débito.
- NÃO reescrever a §7.A.20 inteira — corrigir a contagem, acrescentar duas linhas e ajustar a conclusão. O resto foi auditado e está correto.
- NÃO adicionar testes.

---

## 5. Testes

**N/A — task é cleanup de documentação.**

O Gate 1 (QA) deve executar a suíte. **O oráculo é o placar idêntico**: `Ran 169 tests · FAILED (failures=9, errors=161, skipped=1)` — vermelho pré-existente por ausência dos 14 DocTypes que a T2 da v1 vai versionar. Desvio = regressão.

```
docker compose -f deploy/compose/docker-compose.stack-nova.yaml -p frappe-stack-nova \
  exec -T backend bench --site verificacao run-tests --app locacao_automation
```

> Esta task não toca código do app — a não-execução da suíte é defensável se declarada. Mas as medições do D-009 **não** são opcionais.

**Se você plantar artefato no ambiente para as medições do D-009**, remova-o pela receita completa (`delete_doc` + `DROP TABLE` + purga de `tabDeleted Document`/`tabVersion`/`tabComment`) e faça a varredura por nome **como última ação**. A v1 registrou resíduo remanescente duas vezes por confirmação intermediária.

---

## 6. Notas / Observações

_Vazio._

> Se identificar OUTROS débitos no `T9.md`, **não os resolva** — registre em `docs/specs/features/saas-multi-empresa/v1/_run/workflow-report.md` e siga.

---

## 7. Checklist Final

- [x] D-004: §5.1 lista os **três** arquivos; checklist corrigido
- [x] D-008: contagem corrigida (tabela ficou com **oito** linhas após acrescentar 7 e 8), variações 7 e 8 acrescentadas, conclusão reescrita
- [x] D-009: seção nova (§7.A.21) com as **medições reproduzidas** dos 4 caminhos, incluindo o interno renomeado; referência do checklist corrigida
- [x] D-011: as duas frases absolutas trocadas por afirmação verificável
- [x] D-005 (parte-T9), D-007 (parte-T9) e D-012 (parte-T9) espelhados, com coerência conferida contra T2/T3 (§4.4) — formas extraídas byte a byte de `veredito_suite.sh`/`portao_orfaos.py` já concluídos, apenas leitura
- [x] Registro histórico preservado (§4.3) — `:1223` (forma antiga do wrapper), §7.A.4 e os blocos de IP datados não foram tocados, só as frases absolutas ao redor deles (D-011)
- [x] Diff afeta APENAS `docs/specs/features/saas-multi-empresa/v1/tasks/T9.md` (confirmado via `git status --porcelain`; a única outra diferença no arquivo é a linha de `Status` pré-existente, não autorada por esta task)
- [x] Se plantou artefato: N/A — nenhum artefato foi plantado no banco para as medições do D-009 (3 dos 4 caminhos falham antes de qualquer escrita; o 4º só lê). Nada a varrer
- [x] Suíte com placar idêntico (ou não-execução justificada) — não-execução justificada: task de cleanup de documentação, não toca código do app (§5)
- [ ] Gate 1 (QA) aprovou
- [ ] Staged para commit (git add pelo orquestrador)
