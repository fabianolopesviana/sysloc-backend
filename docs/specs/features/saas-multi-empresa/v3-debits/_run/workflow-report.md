# Workflow report — saas-multi-empresa/v3-debits

- [run] executor resolvido: `__default__` (origem: **determinado, não perguntado** — `.claude/agents/` contém apenas os 3 agentes de gate, todos excluídos como executores pela skill; a descoberta interativa ofereceria uma única opção. Mesma razão registrada na v2-debits e na invocação da debt-resolution)
- [run] executor_discipline injetado (fonte: references/executor-discipline.md)
- [run] git ok · `_run/tmp/` no .gitignore · nenhuma memória lazy · nenhuma task `Em Progresso` no início · `portao_orfaos.py` staged **sem** modificação pendente → **sem sinal de resume**
- [run] Ambiente verificado antes de despachar: stack `frappe-stack-nova` com 4 serviços `running`; site `verificacao` saudável (portão VERDE, 739 alvos, resíduo zero)
- [run] Lote paralelo: **não se aplica** — versão de task única. Flag `N/A`, não `Sim`.
- [T1] base_sha=5a4e5197ee1d4e1cd262b6886c054ddf9a0da9b2
- [T1] gates: [qa, tech_review] (declarado) · model: sonnet (declarado) · risk: medium (declarado). Desvio do `[qa]` padrão de cleanup justificado em `scope.md` §3.3 e `task_plan.md` §4.3 — com evidência empírica da v2-debits, onde o Tech Review sobre estes dois arquivos achou coisa real nas duas tasks em que rodou.
- [T1] **Testemunha do baseline de prosa, capturada na geração da versão e reconfirmada agora**: `DOCSTRING_MD5=fd8874e9433063970f161cce45a0f332`, 195 linhas; `python3 -W error::SyntaxWarning` → SEM_WARNING. A §4.4 da task manda o executor capturar antes de editar; ter a testemunha independente permite detectar divergência entre geração e execução.
- [T1] executor retornou: 1 arquivo modificado (`30 +/2 -`), `import traceback` + localização no `except` do corpo + `try/except` interno no `finally`. 6/6 casos provados, 3 provas de não-regressão verdes, resíduo zero.
- [T1] **O executor CORRIGIU a premissa da task com medição — e a imprecisão era MINHA.** Eu escrevi, no D3 do run-report da v2-debits e nos três artefatos da v3-debits, que o defeito produzia `exit 1` *"sem nenhum `PORTAO_VEREDITO` na saída"*. **Falso.** Ele mediu e reportou: o `PORTAO_VEREDITO` **é** impresso, porque o CPython faz flush do stdio na finalização do interpretador mesmo com exceção não tratada. O defeito real é **só** o exit code — `sys.exit(main())` sai `1` (o genérico de exceção não tratada) **independente** do veredito real. Confirmei por execução própria contra a versão do index: `EXIT=1` **com** `PORTAO_VEREDITO: VERDE` presente no stdout.
- [T1] Essa correção **aumenta** a gravidade do débito em vez de diminuir, e é o ponto mais fino do run: o docstring do arquivo instrui *"códigos de saída são o veredito — não confie só no texto"*. Uma automação que **obedeça a instrução do próprio arquivo** lê `1` e trata como VERMELHO, quando o veredito real era VERDE. O texto correto não protege — protege só quem desobedece o contrato. Registrado porque a descrição do débito na fonte (`v2-debits/_run/run-report.md` §2, D3) precisa ser corrigida.
- [T1] **Caso 5 medido por mim, antes e depois, com a mesma injeção** (`frappe.destroy()` → nome inexistente, no stream, arquivo em disco intocado): **ANTES** (versão do index) `EXIT=1` com `PORTAO_VEREDITO: VERDE` + `AttributeError` no traceback; **DEPOIS** `EXIT=0` com `PORTAO_VEREDITO: VERDE` + `PORTAO_TEARDOWN_FALHOU: AttributeError: ... — veredito acima permanece valido (destroy() nao participa da varredura)`. O exit code voltou a corresponder ao veredito.
- [T1] Decisão de design do executor, com a qual concordo: falha de teardown **não invalida** a rodada (o veredito já foi computado; `destroy()` só libera conexão e reseta locals), mas fica **visível** — não `except Exception: pass`, que trocaria exit code errado por silêncio novo. O comentário no código documenta a mecânica exata (`Python troca o valor pendente pela excecao do finally`) e foi reescrito para refletir a medição, não a expectativa da task.
- [T1] Varredura de vizinhos confirmada por mim com padrão genérico: **1 único `finally`** no arquivo inteiro (`/usr/bin/grep -c '^\s*finally:'` = 1), e o inventário de `return`/`except` não mostra outro ponto de descarte de retorno pós-computado.
- [T1] **Verificação minha dos 6 casos**: 1) `EXIT=0` VERDE, 739 alvos ✔; 3) `EXIT=2` `PORTAO_ESPERADO_PULADOS=999, medido 0` ✔; 6) `EXIT=2` com `em linha 293 em main()` — **a localização funciona** ✔; 5) antes/depois acima ✔. Casos 2 e 4 conferidos no relatório do executor (o 2 exige plantio; não replantei, pelo risco de resíduo, e o gate vai reprovar/confirmar).
- [T1] **As 3 provas de não-regressão conferidas por mim**: hash do docstring `fd8874e9433063970f161cce45a0f332`, **195 linhas — idêntico** ao baseline capturado na geração da versão (prosa intocada, provado e não afirmado); `python3 -W error::SyntaxWarning` → **SEM_WARNING** (o `import traceback` e as strings novas não introduziram escape inválido); escopo isolado a `portao_orfaos.py` + o checklist da própria `T1.md`.
- [T1] **Consistência de stream conferida por mim** (candidato natural a achado do Tech Review): o arquivo tem **15 `print`** e **zero** com `file=sys.stderr` — `_invalida` inclusive imprime em stdout. O `PORTAO_TEARDOWN_FALHOU` novo segue a mesma convenção, e confirmei que veredito e teardown saem **juntos no stdout** com stderr descartado. Consistente, não divergente.
- [T1] Ambiente: portão VERDE, 739 alvos, **resíduo zero** verificado por mim depois da varredura do executor (a lição da v1 é que confirmação intermediária envelhece).
- [T1] Gate 1 despachado em **sonnet** (`risk: medium`, sem critical path canônico). Nota: a v2-debits escalou este arquivo a opus nas rodadas em que a mudança alterava **como o fluxo é consumido**; aqui a mudança é um `try/except` de teardown e uma string de diagnóstico, com prosa provada intocada por hash — não justifica opus.
- [T1] Gate 1 (QA, sonnet) tentativa 1: **REJEITADO** — 1 ALTO (`error_handling`), 0 médios, 0 baixos. criterios 11/12. `tocou_area_critica: true`.
- [T1] **D-003 aprovado, e o QA foi além do que eu pedi para confirmá-lo**: leu o código-fonte de `frappe.destroy()` (`frappe/__init__.py:427-433` → só `db.close()` + `release_local(local)`) em vez de aceitar a afirmação do executor de que `destroy()` não participa da varredura. E testou o **caso patológico** que eu tinha levantado (exceção no corpo **E** falha no teardown simultâneas): `EXIT=2` com o veredito correto preservado e o `PORTAO_TEARDOWN_FALHOU` depois. Também confirmou que uma conexão pendurada por `close()` falho não é risco prático: o script é CLI de vida curta, invocado por processo novo a cada chamada.
- [T1] **ALTO-001 — a localização do D-006 fica ENGANOSA quando a exceção nasce no framework, e a causa é uma afirmação absoluta num comentário.** O comentário novo diz que *"o `filename` vira sempre `<stdin>`"* e por isso o descarta incondicionalmente. **Falso.** Confirmei por execução própria injetando `pluck="campo_inexistente_xyz"` em `frappe.get_all`: o frame mais interno é `/home/frappe/frappe-bench/env/lib/python3.11/site-packages/pymysql/err.py`, **não** `<stdin>`, e a mensagem sai `... em linha 143 em raise_mysql_exception()` — sem dizer de qual arquivo. **A linha 143 do `portao_orfaos.py` é uma frase do docstring**: *"Se (2) nao ficar vermelho, o portao virou decoracao. Ver T9 §7.A.16."* Ou seja, aponta uma linha real do arquivo errado.
- [T1] Isso é **pior que não ter localização**, e é o cenário que a própria task marcou como *"mais provável na prática"* — o corpo chama `get_controller`, `get_all` e `get_value` do framework repetidamente. Nona ocorrência do padrão dominante desta feature, e a **quarta** originada numa afirmação absoluta de comentário que a medição refuta.
- [T1] **Observação minha que vai além do fix do QA, e que pode mudar a forma da correção**: o fix sugerido (incluir o `filename` quando ≠ `<stdin>`) remove o engano, mas talvez **não responda o D-006**. O débito pergunta *"em qual dos ~130 pontos do corpo quebrou"* — e quando a exceção nasce no framework, o frame mais interno responde "em `pymysql/err.py`", não "na linha do meu script que chamou o framework". A informação que o débito pede é o **frame do próprio script** (o último com `filename == "<stdin>"`). Vou passar as duas opções ao executor e deixá-lo decidir com justificativa — ele já corrigiu uma premissa minha com medição nesta task, o julgamento dele merece o espaço.
- [T1] QA verificou o que eu pedi e ainda acrescentou: `import traceback` é stdlib e não pertence ao inventário de `ACOPLAMENTO A INTERNOS` (que documenta acoplamento a internos do **Frappe**), e não quebra a invocação por stdin (testado em 5+ execuções). Hash do docstring reconfirmado idêntico entre a versão do index e o arquivo atual; `-W error::SyntaxWarning` limpo; D-004 e D-005 intocados, com o hash como prova. **Todas as outras afirmações técnicas dos comentários novos** (semântica do `finally`, troca do valor pendente, exit code genérico 1, veredito computado antes do teardown) **foram verificadas por medição e se sustentam** — só a do `filename` não.
- [T1] Armadilha registrada pelo QA para quem implementar a **T2 da v1**: `PORTAO_TEARDOWN_FALHOU` é impresso **depois** do `PORTAO_VEREDITO`. Um consumidor que faça grep pela linha `PORTAO_VEREDITO:` não quebra; um que assuma *"a última linha é o veredito"* quebraria. Não é defeito desta task (nenhum consumidor existe ainda — `grep -rn PORTAO_` só acha o próprio arquivo e um comentário no compose), mas é o tipo de coisa que a v1 vai herdar.
- [T1] attempt_count=1 → correção despachada. `requires_qa_revalidation`: rejeição veio do QA → sempre re-QA.
- [T1] executor tentativa 2: forma **(b)** — reportar o frame do **próprio script** como localização principal, com o frame mais interno como profundidade extra quando vier de outro arquivo. E **melhorou a minha sugestão**: em vez de comparar com o literal `"<stdin>"`, deriva `main.__code__.co_filename` **em runtime** — então a lógica continua correta se o arquivo algum dia for executado por outra via, em vez de virar a próxima afirmação absoluta a envelhecer. Diff cresceu para `46 +/2 -`.
- [T1] **Verificação independente do orquestrador — o achado fechou e a localização agora responde o débito**: caso do framework (`pluck` inválido em `frappe.get_all`) → `em linha 288 em main() (origem: /home/frappe/frappe-bench/env/lib/python3.11/site-packages/pymysql/err.py:143 em raise_mysql_exception())`. Conferi o **conteúdo** da linha 288, não só o número: é `alvos = frappe.get_all("DocType", {"custom": 0}, pluck="name")` — exatamente o ponto do corpo que chamou o framework, que é o que o D-006 pergunta. Caso 6 (exceção nascida no próprio script) → `em linha 293 em main()` **sem** "origem", e a linha 293 é `clear_controller_cache()`. Bate nos dois.
- [T1] Casos 1/3/5-depois inalterados (`EXIT=0` VERDE / `EXIT=2` RODADA_INVALIDA / `EXIT=0` VERDE + `PORTAO_TEARDOWN_FALHOU`). Provas de não-regressão verdes: docstring `fd8874e9433063970f161cce45a0f332` com 195 linhas (idêntico pelas duas rodadas) e `-W error::SyntaxWarning` limpo.
- [T1] **Questão que levo ao gate, e que eu não consegui fechar por medição**: o fallback `frames_do_corpo[-1] if frames_do_corpo else frames[-1]`. Se a lista ficasse vazia, o fallback **silenciosamente reintroduz o defeito original** (reporta frame do framework sem `filename`). Meu raciocínio é que é inalcançável — a exceção é capturada em `main()`, logo o frame de `main` está sempre no traceback —, mas isso é argumento, não medição, e argumento não-medido é exatamente o que reprovou 5 rodadas nesta feature. Pedi ao gate para decidir: inalcançável e benigno, ou alcançável (defeito silencioso) ou defensivo demais (`speculative_complexity`).
- [T1] Re-QA despachado em sonnet, escopado à mudança da localização.
- [T1] Gate 1 (QA, sonnet) tentativa 2: **REJEITADO** — **1 CRÍTICO** (`error_handling`). criterios 1/2.
- [T1] **ALTO-001 da rodada 1 FECHADO**, confirmado pelo gate com medição própria e **conferindo o conteúdo** das linhas, não só os números: caso do framework → `em linha 288 em main() (origem: .../pymysql/err.py:143 em raise_mysql_exception())`, e a 288 é de fato o `frappe.get_all(...)`; caso 6 → `em linha 293 em main()` sem "origem", e a 293 é `clear_controller_cache()`.
- [T1] Ele respondeu as três sub-perguntas do item 3 com medição: **(a)** `main.__code__.co_filename == '<stdin>'` sob a invocação publicada, confirmado por print instrumentado; **(b)** sem risco de `NameError` — `main` é resolvido por `LOAD_GLOBAL` durante a própria execução de `main()`, disparada por `sys.exit(main())`; **(c)** a comparação por `filename` é o design **correto** para funções aninhadas e comprehensions, que em Python 3.11 têm code object próprio mas herdam o `co_filename` do módulo.
- [T1] **CRIT-001 — o fallback reintroduz a classe D-003, e a minha análise estava pela metade.** Eu argumentei que o fallback era inalcançável porque o frame de `main()` está sempre no traceback. O gate mostrou que a **premissa está correta e a conclusão errada**: existe um segundo caminho, **ortogonal** a `frames_do_corpo`, que esvazia `frames` **inteiro** — `traceback.extract_tb()` consulta `sys.tracebacklimit` quando `limit` é omitido, e com o valor `0` ou negativo retorna `[]`. Aí `frames[-1]` levanta `IndexError` **dentro do próprio `except`**, propaga, atravessa o `finally` (que só protege o teardown) e o processo sai **1 sem nenhum `PORTAO_VEREDITO`**.
- [T1] **Reproduzi por conta própria, nos dois níveis**: na stdlib isolada (`sys.tracebacklimit = 0` → `extract_tb` devolve **0 frames** → `frames[-1]` levanta `IndexError`) e no portão real via stream (`tracebacklimit=0` + `pluck` inválido) → **`EXIT=1`, zero linhas `PORTAO_VEREDITO`**, com `IndexError: list index out of range` em "During handling of the above exception". É exatamente o defeito que esta task existe para eliminar, renascido do próprio fix do outro débito.
- [T1] Observação do gate que **muda como o achado deve ser lido**: ele é **ortogonal à escolha entre forma (a) e (b)**. Qualquer implementação que indexe `extract_tb(...)[-1]` sem guarda contra lista vazia tem o mesmo buraco — **inclusive a forma mínima que ele próprio sugeriu na rodada 1**. Logo não é argumento contra a proporcionalidade da forma (b); é débito de guarda que precisaria ser corrigido em qualquer das duas. Ele também julgou explicitamente as ~10 linhas de frame-filtering como **proporcionais**, sem abstração especulativa.
- [T1] **Discordo da severidade CRÍTICO, e registro em vez de discutir**: confirmei por grep em `apps/` e `env/lib/python3.11/site-packages/` que **nada na stack instalada seta `sys.tracebacklimit`** — o próprio gate reconheceu isso. O caminho exige um ator externo mutando estado global no mesmo interpretador, num script CLI de processo próprio e vida curta. Minha leitura é **ALTO**, não CRÍTICO. A diferença é irrelevante para o fluxo (as duas classes bloqueiam) e o fix são 2 linhas, então não gasto tentativa discutindo — mas registro para quem ler o run-report não calibrar risco pela palavra errada.
- [T1] O gate também nomeou com precisão o que o comentário fez de errado, e é uma variação nova da classe: **não é afirmação falsa, é omissão** — o comentário apresenta o `else frames[-1]` como alternativa segura *"sem substituir a localizacao principal"* **sem declarar a suposição implícita de que `frames` nunca é vazio**. Quinta ocorrência da classe nesta feature, primeira por omissão em vez de asserção.
- [T1] attempt_count=2 → **última tentativa**. `requires_qa_revalidation`: rejeição veio do QA → re-QA.
- [T1] executor tentativa 3 (**última**): adotou **as duas defesas juntas** — `limit` explícito em `extract_tb` (remove a causa conhecida) **e** guarda `if not frames` que trata lista vazia como mais um caminho de `RODADA_INVALIDA` (cobre causa desconhecida). O comentário agora **declara a suposição** em vez de escondê-la: diz que o `limit` explícito não prova que `frames` seja sempre não-vazio para qualquer causa futura, e que por isso a guarda existe. Diff em `68 +/2 -`.
- [T1] **Verificação independente do orquestrador — a afirmação sobre a stdlib, medida e não aceita**: com `sys.tracebacklimit` em **0** e em **-1**, `extract_tb` sem `limit` devolve **0 frames**; com `limit` explícito devolve **2**. O global é de fato ignorado quando o `limit` é passado.
- [T1] **Vetor do CRIT-001 fechado nos dois valores**: `tracebacklimit=0` → `EXIT=2`, **1** linha `PORTAO_VEREDITO`, `RODADA_INVALIDA`; `tracebacklimit=-1` → idem. Antes era `EXIT=1` com **zero** vereditos e `IndexError`.
- [T1] Regressões conferidas com **conteúdo** das linhas: framework → `em linha 288 em main() (origem: .../pymysql/err.py:143 em raise_mysql_exception())`, e a 288 é `alvos = frappe.get_all("DocType", {"custom": 0}, pluck="name")`; caso 6 → `em linha 293 em main()`; caso 1 → `EXIT=0` VERDE. Provas de prosa: docstring `fd8874e9433063970f161cce45a0f332` com 195 linhas (idêntico pelas **três** rodadas) e `-W error::SyntaxWarning` limpo.
- [T1] **Ponto que levo ao gate por iniciativa própria**: o `limit=1_000_000` é número mágico. A justificativa do comentário é que é "grande o bastante para nunca truncar um traceback real deste script" — e ela **se sustenta na medição**: o `recursionlimit` é **1000** tanto no host quanto no contentor, então um traceback real não passa de ~1000 frames, três ordens de grandeza abaixo do limite. Registro para o gate julgar se a constante merece ficar explícita ou derivada, mas o raciocínio está medido, não afirmado.
- [T1] Re-QA despachado em sonnet, escopado à guarda e ao `limit`.
- [T1] Gate 1 (QA, sonnet) tentativa 3: **APROVADO_COM_OBSERVACOES** — criterios **9/9**, 0 críticos, 0 altos, 0 médios, **1 baixo**. O ciclo de duas rejeições fechou.
- [T1] Ele deu **julgamento explícito** nas quatro perguntas, e duas respostas trazem informação que nem eu nem o executor tínhamos:
- [T1] **(2) A guarda é ALCANÇÁVEL, não defesa morta.** Medido: `traceback.extract_tb(None, limit=1_000_000)` devolve `[]` **independente do limit** — ou seja, a guarda dispara sempre que `e.__traceback__` for `None`, que é mecanismo real da stdlib, não hipótese. Julgamento dele: não é `speculative_complexity`; o `limit` explícito remove a causa **conhecida** e a guarda cobre a lacuna residual a ~3 linhas, proporcional ao contrato do arquivo.
- [T1] **(4) A semântica do `limit` positivo é contra-intuitiva, e ele mediu**: `extract_tb` com limite positivo pega os **PRIMEIROS** N frames (os mais **externos**), não os últimos. Provou com uma cadeia de 3 chamadas aninhadas: `limit=2` manteve `<module>`/`nivel1` e **descartou** o frame mais interno. Consequência: se um traceback deste script algum dia excedesse o limite, o `frames[-1]` usado como "origem" apontaria um frame intermediário truncado, não o real. Estruturalmente impossível aqui (`recursionlimit=1000`, três ordens de grandeza abaixo), mas é uma propriedade que o comentário do executor **não** tinha verificado e que eu também não tinha pensado ao sugerir o `limit`.
- [T1] **BAIXO-001 → débito anotado**: a própria guarda pode falhar. Ela monta a mensagem com `getattr(sys, 'tracebacklimit', 'ausente')!r` numa f-string, sem proteção — e o gate mediu que, com um `tracebacklimit` cujo `__repr__` levanta, a conversão `!r` propaga, atravessa o `finally` e volta ao exit 1 sem veredito, **num terceiro ponto do mesmo arquivo**. Calibrado BAIXO com justificativa que eu endosso: exige a composição de **duas** condições adversariais (objeto não-inteiro **com** repr quebrado **e** frames vazio), sem caminho operacional plausível no stack atual — diferente do CRIT-001, cujo gatilho (`tracebacklimit=0/-1`, inteiro documentado) é padrão real de ferramentas de supressão de traceback. **Onde**: `deploy/scripts/portao_orfaos.py:421`.
- [T1] Ele também comparou o `except Exception` do **bootstrap** byte a byte contra a versão pré-task e confirmou **intocado** — o guardrail de não expandir escopo por simetria foi respeitado.
- [T1] Gate 2 (Tech Review) despachado.
- [T1] Gate 2 (Tech Review, sonnet): **APROVADO_COM_OBSERVACOES** — 1 BAIXO (`architecture`), zero bloqueantes. ADR consultada: ADR-0006, conformidade preservada (o diff não toca resolução de site, criação/destruição de dados nem caminho de escrita).
- [T1] **P1 (BAIXO, architecture) — o TR achou o QUARTO ponto latente da mesma classe, e propôs fechar a categoria em vez do ponto.** O `except` do teardown imprime `str(e)[:160]`; se `str(e)` levantar (um `__str__` quebrado), a própria impressão propaga, sai do `finally` sem imprimir nada e o processo termina 1 sem veredito — **não coberto por nenhum dos 6 casos do oráculo**. Diagnóstico dele: *"não é um bug isolado, é um padrão de correção reativa ponto-a-ponto dentro de `main()`, e cada rodada de gate encontrou o próximo ponto não coberto"*.
- [T1] **A proposta estrutural, e por que ela é boa**: um safety-net **aditivo** em `if __name__ == "__main__":` envolvendo `sys.exit(main())` num `try/except` que imprime `RODADA_INVALIDA` e sai 2. O TR **verificou** que não conflita com o invariante do D-003 — o `try/except` interno do `finally` já intercepta a falha de teardown **dentro** de `main()` e devolve o veredito original sem propagar, então o wrapper nunca a vê; ele só atua sobre o que escapa de toda a lógica interna. Fecha a categoria inteira em vez de aguardar o quinto ponto. É BAIXO, então virou débito anotado — mas é o achado mais valioso da versão.
- [T1] Respostas do TR às outras quatro perguntas: **(Q2)** concorda com BAIXO para o achado do QA e **não** pede correção — a recorrência de classe motivou o P1, que é finding distinto, não reclassificação. **(Q3)** mediu 45 linhas de comentário para 23 de código no diff (~1,96:1), contra 0,98:1 pré-`v2-debits` e 1,49:1 pós — "ainda não é ata de reunião" porque não há duplicação entre parágrafos, mas a tendência é sinal de atenção. **(Q4)** o literal `1_000_000` é **melhor** que `sys.getrecursionlimit()` derivado, porque um valor derivado de global mutável reintroduziria a própria classe de fragilidade que gerou o CRIT-001 — o literal só precisa ser "grande o bastante", não exato. **(Q5)** nenhuma injeção nem vazamento; a mensagem de `OperationalError` pode conter fragmento de SQL, aceitável num CLI operacional sem rota de rede e sem log acessível a terceiros. **(Q7)** a `T1.md` no diff é ruído aceitável do framework, não incoerência a registrar.
- [T1] **CONCLUÍDA** — ambos os gates aprovados. `git add`: `deploy/scripts/portao_orfaos.py` + `docs/.../v3-debits/tasks/T1.md`.
- [run] **VERSÃO CONCLUÍDA — 1/1 task.** `task_plan.md` §1 → `Concluído`; os 9 critérios da §7 marcados (o da suíte marcado `[~]` com a não-execução justificada, não como feito); `minispec_state.yaml` → `execution: completed`. `_run/run-report.md` gerado com 2 débitos anotados e as notas de revisão humana.
- [run] rule_candidates: 0 sinais — nenhum gate emitiu `rule_candidates_emitidos[]` nesta versão. Arquivo não criado (regra de criação lazy).
- [run] Nada commitado — apenas `git add`, conforme a restrição do usuário.

## agent-spec-debt-resolution — 2026-07-30

- Débitos coletados: **2** (ambos `BAIXO`, da §2 do `_run/run-report.md` da v3-debits)
- Recomendados pela LLM: **1** (D-002 — o safety-net do `if __name__`)
- Perfumaria: **1** (D-001 — o `!r` desprotegido)
- Selecionados pelo usuário: **0**
- Output: **nenhum** — `v4-debits/` **não foi criada**, por decisão do usuário
- Comando para executar: n/a

### Por que a cadeia de cleanup parou aqui

- **O fato que reenquadrou a decisão, e que nenhum gate tinha tornado explícito**: o pior caso dos dois débitos cai do lado **seguro** do contrato. Uma exceção não capturada faz o Python sair `1`, e `1` é o código que a automação lê como **VERMELHO ("não migre")** — não como VERDE. O raio de dano real deste arquivo (apagar a definição de `Imovel`/`Locatario`/`Contrato`) exigiria um **falso-verde**, e nenhum dos dois débitos o produz. Os dois produzem **falso-vermelho**, que o próprio cabeçalho do arquivo já sanciona: *"um falso-vermelho ocasional custa uma investigacao; um falso-verde deixa passar migracao que apaga dados."*
- Consequência retroativa, registrada por honestidade: as **3 rodadas de gate** que a v3-debits gastou perseguindo esta classe estavam perseguindo um defeito que **falha fechado**. Isso não os torna não-defeitos — diagnóstico enganoso tem custo real e um gate deve ser honesto sobre o que sabe — mas significa que a urgência atribuída a eles (incluindo o `CRITICO` da rodada 2) foi calibrada acima do que o raio de dano justifica.
- **Relação entre os dois débitos, confirmada pelo especialista**: o **D-002 torna o D-001 inofensivo**. O wrapper envolve `sys.exit(main())` inteiro, então a exceção do `!r` seria capturada na fronteira e convertida em `RODADA_INVALIDA`/exit 2 — mesmo desfecho contratual que a correção local, só com mensagem genérica em vez de específica. Com o D-002 no lugar, o D-001 vira granularidade de diagnóstico, não comportamento.
- **Custo de oportunidade, que o especialista levantou sem indução**: a **v1 tem 7 das 10 tasks A Fazer** — a migração das regras de negócio e dos endpoints, o valor real da feature. Esta seria a **quarta** rodada de cleanup em cadeia (v1 → v2-debits → v3-debits → v4-debits) sobre um script de suporte cujo pior caso já é fail-safe.
- **Sugestão registrada para quando o arquivo for tocado de novo**: um upgrade de Frappe já obriga a revalidar o teste de falsificabilidade (o próprio docstring manda). Nessa janela, aplicar **só o D-002** — é aditivo, fecha a classe inteira em vez do próximo ponto, e dispensa o D-001.

### Rastreabilidade dos débitos não selecionados

Nenhum `scope.md` foi criado (a versão não nasceu), então o registro de auditoria fica aqui. Os dois seguem **anotados e acionáveis** na §2 do `_run/run-report.md` da v3-debits, com `file:linha` e fix sugerido:

- **D-001** · baixo · `error_handling` · `deploy/scripts/portao_orfaos.py:421` — motivo de não-seleção: **perfumaria**; torna-se redundante se o D-002 for aplicado.
- **D-002** · BAIXO · `architecture` · `deploy/scripts/portao_orfaos.py` (bloco `if __name__`) — motivo de não-seleção: **não selecionado nesta rodada**; recomendado pelo especialista, adiado por custo de oportunidade frente à v1.

Nenhum bloco `### D{n}` foi marcado com `— ✓ em cleanup`, porque nenhum entrou em cleanup. O snapshot da v3-debits permanece intacto.
