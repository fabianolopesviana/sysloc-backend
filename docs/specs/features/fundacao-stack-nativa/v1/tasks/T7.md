# TASK – Detalhamento da Task

## 1. Identificação
- **ID**: T7
- **Nome da Task**: Unidades de serviço, instalação idempotente e prova de recuperação
- **model**: opus
- **risk**: high
- **gates**: [qa, tech_review]   # tipo=secrets/config — define o arranque do servidor e o arquivo de segredos; erro aqui pode impedir o boot
- **Status**: Concluído
- **Fase**: 5
- **Dependências**: T5, T6
- **Símbolos públicos criados**: `sysloc-api.service`, `sysloc-worker.service` (unidades de serviço); `instalar-unidades.sh`, `verificar-fundacao.sh` (procedimentos); `mprocs.yaml` (ciclo de desenvolvimento)
- **Símbolos consumidos de outras tasks**: artefatos construídos de `apps/api` ← T5 e `apps/worker` ← T6; `provisionar-base.sh` ← T2 (as unidades declaram dependência dos serviços que ele instala)
- **Critério de Conclusão**: as duas unidades sobem os serviços, `instalar-unidades.sh` executado duas vezes seguidas termina com sucesso sem duplicar; encerrar o processo do serviço de aplicação faz o supervisor trazê-lo de volta; após reinício completo do servidor, aplicação, processador **e o ambiente legado** voltam sozinhos; `verificar-fundacao.sh` executa a bateria de aceitação e devolve código de saída.

---

## 2. Objetivo da Task

Fechar a fatia: colocar os dois serviços sob o supervisor do sistema operacional com reinício automático e dependências declaradas, entregar o ciclo de trabalho do desenvolvedor, e **provar a recuperação** — de queda de processo e de reinício completo do servidor.

Esta é a task que produz a prova central da INTENT.

---

## 3. Arquivos Impactados

### 3.1 Arquivos a Criar

| Arquivo | Descrição |
|---------|-----------|
| `deploy/systemd/sysloc-api.service` | Unidade do serviço de aplicação: reinício sempre, dependências de banco e fila declaradas, segredos por arquivo de ambiente externo, arranque automático |
| `deploy/systemd/sysloc-worker.service` | Unidade do processador, mesmas propriedades |
| `deploy/scripts/instalacao/instalar-unidades.sh` | Posiciona e habilita as unidades; idempotente |
| `deploy/scripts/instalacao/verificar-fundacao.sh` | Executa a bateria de aceitação da fatia e devolve código de saída |
| `mprocs.yaml` | Painéis do ciclo de desenvolvimento: serviço de aplicação, processador e capturador de e-mail |

### 3.2 Arquivos a Modificar

| Arquivo | Modificação |
|---------|------------|
| `package.json` | Confirmar que o script `dev` aponta para o multiplexador agora que `mprocs.yaml` existe (criado em T1 apontando para este arquivo) |

### 3.3 Arquivos de Referência

| Arquivo | Motivo da Consulta |
|---------|-------------------|
| `deploy/scripts/instalacao/provisionar-base.sh` | Criado em T2 — nomes dos serviços de base que as unidades declaram como dependência, e onde o arquivo de ambiente foi posicionado |
| `deploy/scripts/caracterizacao/preparar-site-efemero.sh` | Exemplar de idempotência aprovado nesta base de código |
| `docs/adr/0005-rotinas-operacionais-versionadas-com-instalacao-idempotente.md` | **Decisão integral** — condição de entrada sobre credencial e exigência de idempotência |
| `apps/api/src/main.ts` e `apps/worker/src/main.ts` | Criados em T5/T6 — como cada processo é iniciado e como trata desligamento gracioso |
| `docs/specs/features/fundacao-stack-nativa/v1/scope.md` | §4 (CA-8, CA-9, CA-11, CA-12, CA-16) |

---

## 4. Detalhes de Implementação

> **Legenda desta seção**: `[x]` = implementado e exercitado. `[~]` = **implementado, pendente
> de execução privilegiada** — `sudo` neste host exige senha interativa, que nenhum subagente
> consegue responder; a execução é conduzida pelo orquestrador junto ao operador, com a saída
> preservada para os gates auditarem. Nada foi marcado como provado sem ter sido executado.

- [x] Criar as duas unidades com **reinício automático** (`Restart=always`, `RestartSec=5`),
  **dependência declarada** dos serviços de base (`After=`/`Wants=` de `postgresql.service` e
  `redis-server@sysloc.service` na aplicação; de `redis-server@sysloc.service` no processador) e
  **arranque automático** no boot (`WantedBy=multi-user.target`). Acrescentado além do enunciado,
  por serem condição de correção e não capacidade especulativa: `StartLimitIntervalSec=60` +
  `StartLimitBurst=5` (sem eles os padrões da distribuição **não fecham** o laço de reinício com
  `RestartSec=5` — ver §6) e `TimeoutStopSec=30` (cobrado nominalmente pelo marcador
  `DECISÃO FECHADA` de `apps/worker/src/fila.ts`).
- [x] Apontar os segredos para **arquivo de ambiente com permissão restrita, fora do repositório**
  — `EnvironmentFile=/etc/sysloc/backend.env`, o mesmo que T2 posicionou (0600 root). Nenhuma
  credencial nas unidades: as diretivas `Environment=` carregam apenas coordenadas revisáveis
  (`NODE_ENV`, `PORT`, `LOG_LEVEL`).
- [x] Criar `instalar-unidades.sh`: posiciona as unidades, recarrega o supervisor, ativa e habilita.
  **Idempotente** — escreve só quando o destino diverge da fonte, e **não reinicia serviço saudável**.
  A decisão de idempotência (`unidade_diverge`) é função pura, exercitada sem privilégio pelo
  **CT-007** — caso próprio, e não uma ponta do CT-001, porque ela não depende de nenhuma execução
  privilegiada ter dado certo (ver §6.2).
- [x] Criar `mprocs.yaml` com os painéis do ciclo de desenvolvimento (aplicação, processador,
  capturador de e-mail). `package.json` já apontava `dev` para o multiplexador desde T1 —
  **conferido, sem alteração necessária**.
- [x] Criar `verificar-fundacao.sh` executando a bateria de aceitação da fatia e devolvendo
  **código de saída**. Cobre as duas rotas de saúde, a publicação do contrato e o consumo de tarefa
  (subcomando `servicos`), a recuperação após encerrar o processo (CT-002) e a ausência de
  credencial em arquivo versionado (CT-005). Agrega, na ordem e sem paralelizar, os verificadores
  de T1, T2 e T4 — reemitindo os **dois** canais de cada um e tratando `CA-14 … EM ABERTO` como
  falha da fatia.
- [~] **Provar CA-8**: encerrar o processo do serviço de aplicação e confirmar que o supervisor o
  traz de volta sem intervenção. **Implementado no CT-002**; exige `sudo`.
- [~] **Provar CA-9**: reiniciar o servidor por completo. **Implementado no CT-006**, em duas
  metades explícitas (`reinicio-preparar` / `reinicio-conferir`), com asserção de pré-condição que
  compara o instante do boot com o do retrato — o que torna impossível substituir o reinício por
  `systemctl restart` e declarar o critério cumprido. **Não executado**: exige `sudo` e a janela.
- [x] Deixar todo o resto da fatia pronto e verificado **antes** de consumir a janela. A bateria
  principal **não** executa o CT-006, e `reinicio-preparar` **recusa** consumir a janela enquanto a
  bateria não sair 0.
- [~] Confirmar que nada do ambiente legado foi alterado, exceto a janela. **Implementado na ponta
  (c) do CT-006**, que deriva o conjunto de unidades legadas do retrato pré-reinício e reprova
  **nomeando individualmente** cada uma que não voltar; e na ponta (h), que compara o conjunto
  (porta, processo dono).

### 4.1 Exemplo de Payload

N/A — sem payload parcial. Esta task não expõe endpoint.

---

## 5. Testes

> Gerado pelo agente `agent-spec-qa-test-generator` em 2026-07-31. 6 casos — 4 integracao, 1 e2e, 1 seguranca.

### 5.0 Padrões de Teste (detectados)

- **Framework**: Nenhum framework aplicável — a task entrega unidades de serviço e scripts. Convenção: shell com asserções, contagem de falhas e código de saída (precedente aprovado: deploy/scripts/caracterizacao/verificar-golden.sh). O próprio `verificar-fundacao.sh` é o entregável e a bateria
- **Stack**: systemd (unidades de serviço, reinício automático, dependências declaradas, EnvironmentFile externo) · aplicação NestJS+Fastify e processador BullMQ construídos em T5/T6 · scripts de instalação e verificação em shell
- **Comando**: `bash deploy/scripts/instalacao/verificar-fundacao.sh`
- **Convenção de nomes**: um bloco por `CT-NNN`, com o ID literal no nome do caso — preserva a rastreabilidade `CT → teste` auditada pelo gate de QA.
- **Fixture/Setup**: instâncias efêmeras próprias de banco e fila (T4); nenhuma verificação toca a instância provisionada (ADR-0006).
- **Mocks**: evitados — a fatia verifica infraestrutura real; mock aqui não provaria recuperação nem persistência.

**Lacunas declaradas pelo gerador:**

- Não há framework nem convenção formalizada de teste para shell no projeto — a convenção usada aqui é derivada do único precedente aprovado. Formalizar em `.claude/rules/testing-stack.md`.
- Não está declarado o limite de tempo aceitável para a recuperação após queda de processo (CA-8) nem para o retorno completo após reinício (CA-9). Foram arbitrados 60 s e 300 s respectivamente; convém que a operação confirme ou ajuste, porque são esses números que definem quando a bateria reprova.

### 5.1 Testes Unitários

N/A — esta task não tem caso de owning layer `unit`; o que ela entrega só é verificável contra recurso real (ver 5.2).

### 5.2 Testes de Integração

Setup: instâncias efêmeras (T4) e/ou sistema de arquivos real · Fronteiras exercidas: filesystem, http

| CT | Teste | Objetivo | Fluxo | Validação | Setup (caminho legítimo) |
|----|-------|----------|-------|-----------|--------------------------|
| CT-001 | Instalação das unidades executada duas vezes seguidas não duplica nem sobrescreve | `instalar-unidades.sh` executado duas vezes seguidas sai 0 nas duas e deixa exatamente um link de habilitação por unidade, com os arquivos de unidade  | Executar o instalador pela 1ª vez; registrar código de saída → Capturar o retrato (SHA-256 das unidades, is-enabled, contagem de links, NRestarts) → Executar o  | Ambas as execuções saem com código 0. Após a 2ª: `systemctl is-enabled` devolve `enabled` para `sysloc-api` e `sysloc-worker`; a contagem de links apo | — |
| CT-002 | Encerrar abruptamente o processo da aplicação faz o supervisor trazê-lo de volta sozinho | Encerrar abruptamente o processo do serviço de aplicação faz o supervisor trazê-lo de volta sem intervenção: novo identificador de processo, unidade a | Registrar o identificador do processo principal (P1) e `NRestarts` (N) → Encerrar P1 com sinal KILL → Sondar em laço, com limite de 60 s, até a unidade voltar a | Dentro do limite de 60 s, sondando por estado observável e nunca por espera fixa: `systemctl is-active sysloc-api` devolve `active`; o identificador d | — |
| CT-003 | Configuração obrigatória ausente faz a partida falhar nomeando a variável, sem laço de rei | Faltando variável de ambiente obrigatória, o processo falha na partida nomeando a variável ausente e o supervisor desiste após o limite de tentativas  | Registrar estado e `NRestarts` da unidade de produção → Instalar a unidade de teste e o arquivo de ambiente incompleto; recarregar o supervisor → Tentar iniciar | A unidade de teste converge para estado terminal dentro do limite: `systemctl show -p ActiveState --value` devolve `failed` e `-p Result --value` devo | imite `NENHUM`: Copiar a unidade real para uma unidade de teste com nome próprio (ex.: `sysloc-api-prova-config.service`) apontando para um `EnvironmentFile |
| CT-004 | Código de saída de `verificar-fundacao.sh` é fiel — verde só quando tudo passa | `verificar-fundacao.sh` sai 0 somente quando todas as asserções passam: com uma dependência derrubada ele sai != 0 nomeando o caso que falhou, e volta | Executar a bateria no estado íntegro; registrar código de saída e a linha final → Armar o trap de restauração e parar o processador → Executar a bateria; regist | (a) Estado íntegro: a bateria sai 0 e a linha final declara todos os casos aprovados, com a contagem de falhas em 0. (b) Com o processador parado: a b | imite `NENHUM`: Parar o processador pelo mesmo comando de operação que qualquer administrador usaria, e restaurá-lo em seguida — o serviço é novo e ainda nã |

### 5.3 Testes E2E

#### Fluxo: Após reinício completo do servidor, a base nova e o ambiente legado voltam sozinhos (CT-006)

- **Objetivo**: Após reinício completo do servidor, sem nenhuma intervenção manual, a aplicação, o processador e todas as unidades do ambiente legado voltam ativas, as dependências estão alcançáveis, e a tarefa enfileirada antes do reinício é consumida.
- **Pré-condições**:
  - CT-001 a CT-005 aprovados e `verificar-fundacao.sh` saindo 0 — todo o restante da fatia verificado ANTES de consumir a janela
  - Janela de indisponibilidade combinada previamente com o responsável pela operação, com data e hora registradas
  - Execução ÚNICA — o caso não é repetível à vontade; repeti-lo custa outra janela, e é por isso que ele é o último da bateria e não o único
  - Retrato pré-reinício capturado em arquivo persistente (fora de `/tmp`): unidades ativas legadas e novas, conjunto (porta, processo dono), espaço livre em disco
  - Uma tarefa de eco enfileirada e deliberadamente NÃO consumida imediatamente antes do reinício, com o processador parado após o enfileiramento — é o que prova CA-10 ponta a ponta
  - Acesso de console fora da rede disponível e plano de retorno definido caso alguma unidade não suba
  - _Precondição privilegiada_: Consumir a janela de indisponibilidade combinada previamente, uma única vez, com CT-001 a CT-005 já aprovados e `verificar-fundacao.sh` já saindo 0 — a janela é a última prova, nunca a primeira. Proibido substituir o reinício por `systemctl restart` das unidades e declarar o critério cumprido: reiniciar unidades não exercita ordem de arranque no boot, resolução de dependências a frio, nem o retorno do ambiente legado — provaria outra coisa com o mesmo nome. Se a prova precisar ser repetida, agendar nova janela em vez de relaxar a asserção. Ter em mãos, antes de reiniciar, acesso de console fora da rede e o comando de desabilitação das unidades novas, para o caso de o boot não convergir. (análogo: `NENHUM`)
- **Passos**:
  1. Confirmar que a bateria sai 0 e que CT-001 a CT-005 estão aprovados
  2. Capturar o retrato pré-reinício em arquivo persistente
  3. Enfileirar a tarefa de eco e parar o processador, deixando-a pendente
  4. Reiniciar o servidor por completo, dentro da janela combinada
  5. Após o boot, sem executar nenhum comando de partida, sondar até a convergência ou até o limite de 300 s
  6. Comparar o estado de cada unidade do retrato com o estado atual
  7. Consultar as duas rotas de saúde
  8. Conferir o consumo da tarefa pendente e o registro do seu término
  9. Executar `verificar-fundacao.sh` e registrar o código de saída
  10. Comparar portas e espaço em disco com o retrato
- **Validações**: Após o boot, sem nenhum comando de partida manual e dentro de 300 s: `systemctl is-system-running` devolve `running` — não `degraded`, que é o sinal de que algo mais no servidor não subiu; `systemctl is-active` devolve `active` para `sysloc-api`, `sysloc-worker` e para cada unidade que constava do retrato pré-reinício, inclusive as do ambiente legado (docker, mysql, clp-nginx, clp-php-fpm, memcached, containerd); `systemctl list-units --state=failed` devolve conjunto vazio; `GET /saude` responde 200; `GET /saude/pronto` responde 200 com estado alcançável declarado para cada dependência; a tarefa de eco enfileirada antes do reinício é consumida e registra término, com o mesmo identificador gerado antes do reinício; `verificar-fundacao.sh` sai 0. O conjunto (porta, processo dono) das portas legadas é idêntico ao do retrato. Qualquer unidade fora de `active` reprova nomeando individualmente a unidade — nunca com mensagem agregada.

### 5.4 Cenários de Erro

| Cenário | Objetivo | Trigger | Código/Status | Log Esperado |
|---------|----------|---------|---------------|--------------|
| CT-002 — Encerrar abruptamente o processo da aplicação faz o supervisor trazê-lo de volta | Encerrar abruptamente o processo do serviço de aplicação faz o supervisor trazê-lo de volta sem intervenção: novo identificador de processo, unidade a | unidade partindo com variável de ambiente obrigatória ausente | a unidade converge para `failed` com `Result` em {`exit-code`, `start-limit-hit`}, o journal nomeia a variável ausente e `NRestarts` para de crescer — | A asserção `NRestarts == N+1` (e não apenas `is-active == active`) é o que dá valor ao cas |
| CT-003 — Configuração obrigatória ausente faz a partida falhar nomeando a variável, sem l | Faltando variável de ambiente obrigatória, o processo falha na partida nomeando a variável ausente e o supervisor desiste após o limite de tentativas  | este é o caso negativo — arquivo de ambiente sem uma variável obrigatória (ex.: a de conexão com o banco) | ver resultado_esperado | É o negative companion de CT-002 e o caso de maior blast radius desta task. `Restart=alway |
| CT-004 — Código de saída de `verificar-fundacao.sh` é fiel — verde só quando tudo passa | `verificar-fundacao.sh` sai 0 somente quando todas as asserções passam: com uma dependência derrubada ele sai != 0 nomeando o caso que falhou, e volta | este é o caso negativo — processador parado deliberadamente, quebrando o consumo de tarefa | ver resultado_esperado — a transição 0 → != 0 → 0 é a asserção | Sem este caso, `verificar-fundacao.sh` poderia retornar 0 incondicionalmente e a fatia int |
| CT-005 — Nenhuma credencial nas unidades de serviço, na árvore versionada ou no journal | A credencial existe apenas no arquivo de ambiente com modo 0600 fora do repositório, referenciado por `EnvironmentFile`; ela não aparece nas unidades  | credencial plantada deliberadamente num arquivo versionado de cópia de trabalho, para provar que a varredura consegue re | a varredura acusa a ocorrência no formato `arquivo:linha`, omite o valor e sai != 0 | A asserção (g) impede a asserção infalível (AP-29): varredura com regex errada devolve 0 o |

### 5.5 Rastreabilidade: Critério de Conclusão → Testes

| # | Critério (seção 1 / aceite) | Teste(s) Correspondente(s) | Tipo |
|---|-----------------------------|----------------------------|------|
| 1 | ADR-0005 — instalação idempotente | CT-001 | Integração |
| 2 | ADR-0005 — nenhum script/unidade entra no repositório carregando credencial | CT-005 | Segurança |
| 3 | CA-10 | CT-006 | E2E |
| 4 | CA-11 | CT-001 | Integração |
| 5 | CA-12 | CT-005 | Segurança |
| 6 | CA-15 | CT-003 | Integração |
| 7 | CA-16 | CT-006 | E2E |
| 8 | CA-16 (a prova não pode degradar o servidor compartilhado) | CT-003 | Integração |
| 9 | CA-3 | CT-006 | E2E |
| 10 | CA-5 | CT-006 | E2E |
| 11 | CA-5 (consumo de tarefa é um dos casos da bateria) | CT-004 | Integração |
| 12 | CA-8 | CT-002 | Integração |
| 13 | CA-8 (guarda — reinício automático não pode virar laço) | CT-003 | Integração |
| 14 | CA-9 | CT-006 | E2E |
| 15 | CLAUDE.md — invariante 3 | CT-005 | Segurança |
| 16 | CLAUDE.md — invariante 7 (tudo sobe sozinho após reboot, testado com reboot real) | CT-006 | E2E |
| 17 | CLAUDE.md — invariante 7 (tudo sobe sozinho) | CT-002 | Integração |
| 18 | T7 — Critério de Conclusão (encerrar o processo faz o supervisor trazê-lo de volta) | CT-002 | Integração |
| 19 | T7 — Critério de Conclusão (instalar-unidades.sh duas vezes sem duplicar) | CT-001 | Integração |
| 20 | T7 — Critério de Conclusão (verificar-fundacao.sh executa a bateria e devolve código de saída) | CT-004 | Integração |
| 21 | ADR-0005 — instalação idempotente (a DECISÃO, exercitada como função pura) | CT-007 | Unit |
| 22 | Guardrail do servidor compartilhado — o guarda de colisão de porta decide a posse pelo estado real | CT-005 de `verificar-provisionamento.sh`, ponta (g) | Unit |

### 5.6 Detalhamento dos Casos de Teste

#### CT-001 — Instalação das unidades executada duas vezes seguidas não duplica nem sobrescreve

- **Tipo**: Integração | **Categoria**: caminho_feliz
- **Arquivo**: `(a criar — NO_SUITE_FOUND)` (criar)
- **Invariant**: `instalar-unidades.sh` executado duas vezes seguidas sai 0 nas duas e deixa exatamente um link de habilitação por unidade, com os arquivos de unidade inalterados e sem derrubar serviço que já estava saudável.
- **Owning layer**: `service-integration` | **Real execution boundary**: `filesystem`
- **Pré-condições**:
  - T5 e T6 concluídas: os artefatos construídos de `apps/api` e `apps/worker` existem no caminho que as unidades declaram
  - T2 concluída: os serviços de base estão de pé e o arquivo de ambiente está posicionado
  - Retrato capturado antes da 2ª execução: SHA-256 de cada arquivo de unidade, saída de `systemctl is-enabled`, contagem de links em `*.wants/`, e `NRestarts` de cada unidade
  - A 2ª execução ocorre imediatamente após a 1ª, sem alteração manual entre elas
- **Dados de entrada**: Duas execuções consecutivas do instalador, mais uma cópia deliberadamente inválida de unidade para o controle de falibilidade
- **Passos**:
  1. Executar o instalador pela 1ª vez; registrar código de saída
  2. Capturar o retrato (SHA-256 das unidades, is-enabled, contagem de links, NRestarts)
  3. Executar o instalador pela 2ª vez; registrar código de saída
  4. Recapturar o retrato e comparar item a item
  5. Executar `systemd-analyze verify` sobre as duas unidades reais
  6. Executar `systemd-analyze verify` sobre a cópia deliberadamente inválida e conferir que ela reprova
  7. Remover a cópia inválida
- **Resultado esperado**: Ambas as execuções saem com código 0. Após a 2ª: `systemctl is-enabled` devolve `enabled` para `sysloc-api` e `sysloc-worker`; a contagem de links apontando para cada unidade sob `/etc/systemd/system/*.wants/` é exatamente 1 (não 2 — duplicar link é o modo de falha clássico de instalador não idempotente); o SHA-256 de cada arquivo de unidade é idêntico entre a 1ª e a 2ª execução; `NRestarts` de cada unidade é idêntico antes e depois da 2ª execução, provando que reinstalar não derrubou serviço saudável; `systemd-analyze verify` sai 0 para as duas unidades reais, sem emitir aviso. Controle de falibilidade: sobre a cópia com diretiva inválida, `systemd-analyze verify` sai != 0 — sem esse controle, um `verify` que sempre retorna 0 (binário ausente, caminho errado) passaria despercebido.
- **Negative companion**: este é o caso negativo (`ct_id: self`) — entrada: cópia da unidade em diretório temporário contendo diretiva inválida, submetida à mesma validação. Asserção: `systemd-analyze verify` sai != 0 sobre a cópia inválida, provando que a asserção de validade aplicada às unidades reais não é infalível
- **Critérios validados**: CA-11; T7 — Critério de Conclusão (instalar-unidades.sh duas vezes sem duplicar); ADR-0005 — instalação idempotente
- **Obs**: `NO_SUITE_FOUND` legítimo — o repositório não tem suíte alguma. Estes casos vivem em `deploy/scripts/instalacao/verificar-fundacao.sh`, que é entregável desta própria task: a bateria de aceitação e a suíte são o mesmo artefato, o que é adequado aqui porque o objeto sob teste é o sistema operacional, não código chamável. A asserção sobre `NRestarts` inalterado é o que distingue idempotência real de um instalador que 'funciona' reiniciando tudo a cada execução — comportamento que, em produção, transforma um `git pull` em micro-indisponibilidade.

#### CT-002 — Encerrar abruptamente o processo da aplicação faz o supervisor trazê-lo de volta sozinho

- **Tipo**: Integração | **Categoria**: tratamento_erro
- **Arquivo**: `(a criar — NO_SUITE_FOUND)` (criar)
- **Invariant**: Encerrar abruptamente o processo do serviço de aplicação faz o supervisor trazê-lo de volta sem intervenção: novo identificador de processo, unidade ativa e `GET /saude` respondendo 200 dentro do limite, com exatamente um reinício contabilizado.
- **Owning layer**: `service-integration` | **Real execution boundary**: `http`
- **Pré-condições**:
  - CT-001 aprovado: unidades instaladas, habilitadas e válidas
  - `sysloc-api` ativa e `GET /saude` respondendo 200 antes do caso
  - Identificador do processo principal e `NRestarts` capturados imediatamente antes do encerramento
  - Nenhum comando manual de partida é executado durante o caso — é justamente isso que está sob prova
- **Dados de entrada**: Encerramento abrupto (sinal não capturável) do processo principal da unidade da aplicação
- **Passos**:
  1. Registrar o identificador do processo principal (P1) e `NRestarts` (N)
  2. Encerrar P1 com sinal KILL
  3. Sondar em laço, com limite de 60 s, até a unidade voltar a `active` e `GET /saude` responder 200
  4. Registrar o novo identificador de processo (P2) e `NRestarts`
  5. Conferir que nenhum comando de partida manual foi executado no intervalo
  6. Aguardar 30 s adicionais e reler `NRestarts` para descartar laço de reinício
- **Resultado esperado**: Dentro do limite de 60 s, sondando por estado observável e nunca por espera fixa: `systemctl is-active sysloc-api` devolve `active`; o identificador do processo principal é P2, com P2 != P1 e P2 != 0; `NRestarts` é exatamente N+1 — um reinício, não vários (mais de um indica que o serviço subiu e caiu de novo); `GET /saude` responde 200 com corpo não-vazio. Numa releitura 30 s depois, `NRestarts` continua em N+1, descartando laço de reinício. Nenhum comando de partida manual é executado em nenhum momento do caso.
- **Negative companion**: → CT-003: unidade partindo com variável de ambiente obrigatória ausente — a unidade converge para `failed` com `Result` em {`exit-code`, `start-limit-hit`}, o journal nomeia a variável ausente e `NRestarts` para de crescer — sem laço de reinício indefinido
- **Critérios validados**: CA-8; T7 — Critério de Conclusão (encerrar o processo faz o supervisor trazê-lo de volta); CLAUDE.md — invariante 7 (tudo sobe sozinho)
- **Obs**: A asserção `NRestarts == N+1` (e não apenas `is-active == active`) é o que dá valor ao caso: um serviço em laço de reinício também aparece como `active` num instante afortunado da sondagem. A releitura 30 s depois separa recuperação de oscilação. O sinal precisa ser não capturável — encerramento gracioso testaria o desligamento da aplicação, não a recuperação pelo supervisor.

#### CT-003 — Configuração obrigatória ausente faz a partida falhar nomeando a variável, sem laço de reinício

- **Tipo**: Integração | **Categoria**: teste_negativo
- **Arquivo**: `(a criar — NO_SUITE_FOUND)` (criar)
- **Invariant**: Faltando variável de ambiente obrigatória, o processo falha na partida nomeando a variável ausente e o supervisor desiste após o limite de tentativas — a unidade termina em `failed`, sem laço de reinício indefinido martelando o servidor compartilhado.
- **Owning layer**: `service-integration` | **Real execution boundary**: `filesystem`
- **Pré-condições**:
  - CT-001 e CT-002 aprovados
  - Unidade de teste criada como cópia da unidade real, com nome próprio e `EnvironmentFile` apontando para arquivo temporário
  - `NRestarts` e estado da unidade de PRODUÇÃO registrados antes do caso, para conferir ao final que ela não foi afetada
  - A porta declarada para a unidade de teste é distinta da porta de produção, para não disputar socket
  - Trap de limpeza armado antes de instalar qualquer arquivo
- **Dados de entrada**: Arquivo de ambiente temporário completo, exceto por uma variável obrigatória removida
- **Passos**:
  1. Registrar estado e `NRestarts` da unidade de produção
  2. Instalar a unidade de teste e o arquivo de ambiente incompleto; recarregar o supervisor
  3. Tentar iniciar a unidade de teste; registrar o código de saída
  4. Sondar até a unidade convergir para estado terminal, com limite de tempo
  5. Registrar `ActiveState`, `Result` e o código de saída do processo
  6. Ler o journal da unidade de teste e procurar o nome exato da variável ausente
  7. Ler `NRestarts` duas vezes, separadas por 30 s, e comparar
  8. Varrer o journal da unidade de teste pela credencial
  9. Remover unidade de teste, drop-in e arquivo temporário; recarregar o supervisor; reconferir a unidade de produção
- **Resultado esperado**: A unidade de teste converge para estado terminal dentro do limite: `systemctl show -p ActiveState --value` devolve `failed` e `-p Result --value` devolve `exit-code` ou `start-limit-hit`; o processo sai com código != 0. O journal da unidade contém o **nome exato** da variável ausente (a mesma string declarada em `.env.example`) — mensagem genérica do tipo 'configuração inválida' reprova, porque não permite corrigir sem depurar. `NRestarts` estabiliza em valor menor ou igual ao limite de tentativas configurado na unidade e é idêntico nas duas leituras separadas por 30 s — prova de que não há laço indefinido consumindo o servidor compartilhado. O journal contém 0 ocorrências da credencial. Ao fim, a unidade de PRODUÇÃO segue `active`, com `NRestarts` inalterado, e nenhum arquivo temporário permanece.
- **Negative companion**: este é o caso negativo (`ct_id: self`) — entrada: este é o caso negativo — arquivo de ambiente sem uma variável obrigatória (ex.: a de conexão com o banco). Asserção: ver resultado_esperado
- **Precondição privilegiada**: Copiar a unidade real para uma unidade de teste com nome próprio (ex.: `sysloc-api-prova-config.service`) apontando para um `EnvironmentFile` temporário incompleto, ou instalar um drop-in em `/etc/systemd/system/<unidade de teste>.d/`. Nunca editar a unidade de produção, nunca apagar variável do arquivo de ambiente real, e nunca adicionar à aplicação flag, variável ou branch que só o teste usa para simular configuração ausente — a validação de partida já existe como requisito de produção (CA-15) e é ela que está sob prova. Remover a unidade de teste, o drop-in e o arquivo temporário ao fim, inclusive em falha (trap), seguidos de recarga do supervisor; conferir ao final que a unidade de produção segue `active` e com `NRestarts` inalterado. Análogo: `NENHUM`
- **Critérios validados**: CA-15; CA-8 (guarda — reinício automático não pode virar laço); CA-16 (a prova não pode degradar o servidor compartilhado)
- **Obs**: É o negative companion de CT-002 e o caso de maior blast radius desta task. `Restart=always` combinado com erro de configuração produz laço de reinício que consome CPU e enche o journal — num servidor que atende a operação e está com 79% de disco, isso é incidente, não inconveniente. A dupla leitura de `NRestarts` é a única asserção que distingue 'falhou e parou' de 'falha para sempre'. A varredura de credencial no journal está aqui porque falha de partida é exatamente quando aplicações costumam despejar a configuração inteira no log.

#### CT-004 — Código de saída de `verificar-fundacao.sh` é fiel — verde só quando tudo passa

- **Tipo**: Integração | **Categoria**: teste_negativo
- **Arquivo**: `(a criar — NO_SUITE_FOUND)` (criar)
- **Invariant**: `verificar-fundacao.sh` sai 0 somente quando todas as asserções passam: com uma dependência derrubada ele sai != 0 nomeando o caso que falhou, e volta a sair 0 quando ela é restaurada.
- **Owning layer**: `service-integration` | **Real execution boundary**: `http`
- **Pré-condições**:
  - Todos os serviços da fatia de pé e `verificar-fundacao.sh` já saindo 0 no estado íntegro
  - O processador (`sysloc-worker`) ainda não atende a nada em produção — a parada é inócua para a operação
  - Trap de restauração armado antes de parar o serviço
  - Limite de tempo declarado para a execução da bateria, de modo que travamento seja reprovação e não espera indefinida
- **Dados de entrada**: Três execuções da bateria, sobre três estados do sistema
- **Passos**:
  1. Executar a bateria no estado íntegro; registrar código de saída e a linha final
  2. Armar o trap de restauração e parar o processador
  3. Executar a bateria; registrar código de saída, saída de erro e contagem de falhas
  4. Restaurar o processador e sondar até a fila voltar a ser consumida
  5. Executar a bateria novamente; registrar código de saída
  6. Conferir que o serviço está `active` ao encerrar o caso
- **Resultado esperado**: (a) Estado íntegro: a bateria sai 0 e a linha final declara todos os casos aprovados, com a contagem de falhas em 0. (b) Com o processador parado: a bateria sai != 0; a saída de erro nomeia especificamente o caso de consumo de tarefa (não uma mensagem agregada do tipo 'a verificação falhou'); a contagem de falhas é maior ou igual a 1; a bateria termina dentro do limite de tempo declarado, sem travar à espera de uma tarefa que nunca será consumida. (c) Após restaurar o processador e a fila voltar a ser consumida: a bateria sai 0 novamente. A transição 0 → != 0 → 0 é a asserção central — ela é o que prova que o código de saída é fiel e não constante.
- **Negative companion**: este é o caso negativo (`ct_id: self`) — entrada: este é o caso negativo — processador parado deliberadamente, quebrando o consumo de tarefa. Asserção: ver resultado_esperado — a transição 0 → != 0 → 0 é a asserção
- **Precondição privilegiada**: Parar o processador pelo mesmo comando de operação que qualquer administrador usaria, e restaurá-lo em seguida — o serviço é novo e ainda não atende a nada, então a parada não interrompe operação alguma. Restaurar inclusive em falha (trap) e confirmar o retorno a `active` antes de encerrar o caso. Proibido: introduzir na bateria flag, variável ou modo que force a reprovação artificialmente — isso provaria que a flag funciona, não que a bateria detecta a falha real. Análogo: `NENHUM`
- **Critérios validados**: T7 — Critério de Conclusão (verificar-fundacao.sh executa a bateria e devolve código de saída); CA-5 (consumo de tarefa é um dos casos da bateria)
- **Obs**: Sem este caso, `verificar-fundacao.sh` poderia retornar 0 incondicionalmente e a fatia inteira seria declarada aprovada por um script que não sabe reprovar — Iron Law #1, e o cenário mais caro possível numa fatia cuja entrega É a prova. A escolha do processador como dependência a derrubar não é arbitrária: é o serviço novo cuja parada não afeta nada, ao contrário do banco (compartilhado com a verificação) e da fila (usada por T2/T4).

#### CT-005 — Nenhuma credencial nas unidades de serviço, na árvore versionada ou no journal

- **Tipo**: Segurança | **Categoria**: seguranca
- **Arquivo**: `(a criar — NO_SUITE_FOUND)` (criar)
- **Invariant**: A credencial existe apenas no arquivo de ambiente com modo 0600 fora do repositório, referenciado por `EnvironmentFile`; ela não aparece nas unidades de serviço, na árvore versionada, no ambiente exposto pelo supervisor nem no journal dos dois serviços.
- **Owning layer**: `service-integration` | **Real execution boundary**: `filesystem`
- **Pré-condições**:
  - Unidades instaladas e serviços ativos, de modo que o journal já tenha registro de partida
  - Janela de tempo do caso registrada, para delimitar a leitura do journal
  - Trap de limpeza armado para o arquivo com a credencial plantada
  - As varreduras nunca imprimem o valor — apenas `arquivo:linha` ou contagem
- **Dados de entrada**: A credencial em uso, usada como agulha e nunca impressa
- **Passos**:
  1. Varrer as duas unidades por atribuição literal de segredo
  2. Conferir que cada unidade declara `EnvironmentFile=` apontando para caminho fora da raiz do repositório
  3. Conferir modo e dono do arquivo apontado
  4. Ler a credencial em runtime e varrer a árvore versionada por stdin
  5. Consultar o ambiente que o supervisor expõe para as duas unidades e varrer pela credencial
  6. Varrer o journal das duas unidades na janela do caso
  7. Plantar a credencial num arquivo versionado de cópia de trabalho, rodar a varredura, conferir que reprova, e remover
- **Resultado esperado**: (a) `grep -cE '(PASSWORD|SENHA|SECRET|TOKEN|CREDENCIAL)[[:space:]]*=[[:space:]]*[^$]' deploy/systemd/*.service` = 0 — nenhuma atribuição literal de segredo nas unidades; (b) cada unidade declara ao menos um `EnvironmentFile=` cujo caminho não é prefixado pela raiz do repositório, e a contagem de diretivas `Environment=` carregando valor de segredo embutido é 0; (c) `stat -c '%a'` do arquivo apontado devolve `600`; (d) a varredura da árvore versionada pela credencial retorna 0 ocorrências; (e) a varredura do ambiente exposto pelo supervisor para as duas unidades retorna 0 ocorrências; (f) a varredura do journal das duas unidades na janela do caso retorna 0 ocorrências; (g) prova de falibilidade: com a credencial plantada em arquivo versionado de cópia de trabalho, a varredura acusa `arquivo:linha`, omite o valor e sai != 0. Em nenhuma saída do caso a credencial aparece.
- **Negative companion**: este é o caso negativo (`ct_id: self`) — entrada: credencial plantada deliberadamente num arquivo versionado de cópia de trabalho, para provar que a varredura consegue reprovar. Asserção: a varredura acusa a ocorrência no formato `arquivo:linha`, omite o valor e sai != 0
- **Precondição privilegiada**: Ler o valor em runtime com privilégio, mantê-lo em variável de shell não exportada e trafegá-lo para o varredor exclusivamente por entrada padrão — nunca por argv, nunca ecoado, nunca gravado em temporário. Jamais ligar rastreio verboso do shell no verificador. Proibido versionar o valor ou seu hash como agulha alternativa. Para a asserção de falibilidade, plantar a credencial apenas em cópia de trabalho descartável e removê-la via trap. Análogo: `deploy/scripts/caracterizacao/verificar-golden.sh — função `ct_013_estatico` (linhas 286-437), e deploy/scripts/caracterizacao/preparar-site-efemero.sh — `ler_credencial_db` (linhas 57-75)`
- **Critérios validados**: CA-12; ADR-0005 — nenhum script/unidade entra no repositório carregando credencial; CLAUDE.md — invariante 3
- **Obs**: A asserção (g) impede a asserção infalível (AP-29): varredura com regex errada devolve 0 ocorrências para sempre. A asserção (f) é a que costuma pegar vazamento real — a aplicação registra a cadeia de conexão inteira na partida, credencial embutida, e ninguém olha o journal. A asserção (e) cobre um caminho distinto do (a): a unidade pode não ter literal algum e ainda assim expor o segredo se alguém usar `Environment=` em drop-in.

#### CT-006 — Após reinício completo do servidor, a base nova e o ambiente legado voltam sozinhos

- **Tipo**: E2E | **Categoria**: integracao
- **Arquivo**: `(a criar — NO_SUITE_FOUND)` (criar)
- **Invariant**: Após reinício completo do servidor, sem nenhuma intervenção manual, a aplicação, o processador e todas as unidades do ambiente legado voltam ativas, as dependências estão alcançáveis, e a tarefa enfileirada antes do reinício é consumida.
- **Owning layer**: `e2e` | **Real execution boundary**: `http`
- **Pré-condições**:
  - CT-001 a CT-005 aprovados e `verificar-fundacao.sh` saindo 0 — todo o restante da fatia verificado ANTES de consumir a janela
  - Janela de indisponibilidade combinada previamente com o responsável pela operação, com data e hora registradas
  - Execução ÚNICA — o caso não é repetível à vontade; repeti-lo custa outra janela, e é por isso que ele é o último da bateria e não o único
  - Retrato pré-reinício capturado em arquivo persistente (fora de `/tmp`): unidades ativas legadas e novas, conjunto (porta, processo dono), espaço livre em disco
  - Uma tarefa de eco enfileirada e deliberadamente NÃO consumida imediatamente antes do reinício, com o processador parado após o enfileiramento — é o que prova CA-10 ponta a ponta
  - Acesso de console fora da rede disponível e plano de retorno definido caso alguma unidade não suba
- **Dados de entrada**: O servidor inteiro, reiniciado uma vez, com uma tarefa pendente na fila e o retrato do estado anterior preservado
- **Passos**:
  1. Confirmar que a bateria sai 0 e que CT-001 a CT-005 estão aprovados
  2. Capturar o retrato pré-reinício em arquivo persistente
  3. Enfileirar a tarefa de eco e parar o processador, deixando-a pendente
  4. Reiniciar o servidor por completo, dentro da janela combinada
  5. Após o boot, sem executar nenhum comando de partida, sondar até a convergência ou até o limite de 300 s
  6. Comparar o estado de cada unidade do retrato com o estado atual
  7. Consultar as duas rotas de saúde
  8. Conferir o consumo da tarefa pendente e o registro do seu término
  9. Executar `verificar-fundacao.sh` e registrar o código de saída
  10. Comparar portas e espaço em disco com o retrato
- **Resultado esperado**: Após o boot, sem nenhum comando de partida manual e dentro de 300 s: `systemctl is-system-running` devolve `running` — não `degraded`, que é o sinal de que algo mais no servidor não subiu; `systemctl is-active` devolve `active` para `sysloc-api`, `sysloc-worker` e para cada unidade que constava do retrato pré-reinício, inclusive as do ambiente legado (docker, mysql, clp-nginx, clp-php-fpm, memcached, containerd); `systemctl list-units --state=failed` devolve conjunto vazio; `GET /saude` responde 200; `GET /saude/pronto` responde 200 com estado alcançável declarado para cada dependência; a tarefa de eco enfileirada antes do reinício é consumida e registra término, com o mesmo identificador gerado antes do reinício; `verificar-fundacao.sh` sai 0. O conjunto (porta, processo dono) das portas legadas é idêntico ao do retrato. Qualquer unidade fora de `active` reprova nomeando individualmente a unidade — nunca com mensagem agregada.
- **Negative companion**: este é o caso negativo (`ct_id: self`) — entrada: o próprio reinício é o evento de falha — o caso reprova se qualquer unidade, nova OU legada, não voltar sozinha. Asserção: `systemctl is-system-running` diferente de `running`, qualquer unidade do retrato pré-reinício fora de `active`, ou `list-units --state=failed` não-vazio reprovam nomeando individualmente cada unidade
- **Precondição privilegiada**: Consumir a janela de indisponibilidade combinada previamente, uma única vez, com CT-001 a CT-005 já aprovados e `verificar-fundacao.sh` já saindo 0 — a janela é a última prova, nunca a primeira. Proibido substituir o reinício por `systemctl restart` das unidades e declarar o critério cumprido: reiniciar unidades não exercita ordem de arranque no boot, resolução de dependências a frio, nem o retorno do ambiente legado — provaria outra coisa com o mesmo nome. Se a prova precisar ser repetida, agendar nova janela em vez de relaxar a asserção. Ter em mãos, antes de reiniciar, acesso de console fora da rede e o comando de desabilitação das unidades novas, para o caso de o boot não convergir. Análogo: `NENHUM`
- **Critérios validados**: CA-9; CA-10; CA-16; CA-5; CA-3; CLAUDE.md — invariante 7 (tudo sobe sozinho após reboot, testado com reboot real)
- **Unidade ativada sob demanda — distinção registrada após a janela real de 2026-08-01**: a ponta (c) exige que **toda** unidade do retrato esteja `active`, e a execução real reprovou por `polkit.service`, que é **ativado sob demanda** (`Type=dbus`, `BusName=org.freedesktop.PolicyKit1`, `static`): antes do reinício ele estava de pé apenas porque a bateria acabara de executar dezenas de comandos `systemctl`, que passam por ele; depois do boot, nada ainda tinha pedido autorização. **O sistema estava correto — a asserção é que classificava toda unidade do retrato como persistente.** A ponta (c) passa a distinguir, e **`Type=dbus` é o critério que discrimina** (não "declara `BusName`", não "é `static`" — `systemd-logind`, `systemd-networkd` e `systemd-resolved` casam esses filtros largos e são persistentes): unidade do retrato que esteja inativa e seja `Type=dbus` tem o **nome D-Bus acionado** (`StartServiceByName` no barramento do sistema — o mesmo caminho de qualquer consumidor, não um comando de partida) e a asserção passa a ser que ela chega a `active` dentro de `LIMITE_ATIVACAO_SOB_DEMANDA_S`. **Isentar unidade inativa, ou pular `Type=dbus`, está proibido**: um polkit genuinamente quebrado (mascarado, arquivo de ativação D-Bus removido, `Type=dbus` sem `BusName` legível) passaria a passar, e a ponta perderia o poder que existe para ter — nesses três casos a ponta **reprova, nomeando individualmente a unidade**, como o resto do card exige. Marcador `DECISÃO FECHADA` no ponto do código (`veredito_da_unidade_do_retrato`).
- **Obs**: É a prova central da INTENT e o único caso desta task que consome janela de indisponibilidade. Por isso o grosso da verificação está em CT-001 a CT-005, que não dependem dela: se algo estiver errado, é neles que aparece, antes de a janela ser gasta. Duas asserções merecem destaque. `is-system-running` = `running` é o apanhador de rede da fatia: ele reprova quando qualquer unidade do servidor (não só as nossas) não subiu, que é exatamente o risco de o provisionamento de T2 ter desalinhado o arranque do legado. E a tarefa enfileirada antes do reinício é o que torna CA-10 uma prova ponta a ponta em vez de uma consulta de configuração — ela atravessa persistência em disco, reinício de máquina, arranque do processador e consumo, numa única asserção. O identificador gerado em runtime atravessa uma fronteira real (disco + reboot) entre a escrita e a leitura: enquadra-se na exceção (b) da Mock Budget Rule, não é assertiva sobre valor auto-plantado em dublê.

#### CT-007 — A decisão de idempotência do instalador, exercitada como função pura

- **Tipo**: Unit | **Categoria**: caminho_feliz
- **Arquivo**: `deploy/scripts/instalacao/verificar-fundacao.sh` (criar)
- **Invariant**: `unidade_diverge` — a função do instalador que decide se o destino precisa ser reescrito — devolve "não diverge" **apenas** quando conteúdo, modo e dono coincidem, e "diverge" em cada uma das formas de divergência.
- **Owning layer**: `service-integration` | **Real execution boundary**: `filesystem`
- **Pré-condições**:
  - Nenhuma. O caso **não** depende de execução do instalador ter dado certo, nem de serviço de pé — é o que o separa do CT-001.
- **Dados de entrada**: A função real, extraída de `instalar-unidades.sh` por `sed` + `eval`, e quatro destinos sintéticos num diretório temporário 0700
- **Passos**:
  1. Extrair `unidade_diverge` do arquivo real e confirmar que o símbolo carregou como função
  2. Exercitá-la contra um destino idêntico em conteúdo, modo e dono
  3. Contra um destino com o mesmo modo e dono e **conteúdo** diferente
  4. Contra um destino com o mesmo conteúdo e dono e **modo** diferente
  5. Contra um destino **inexistente**
- **Resultado esperado**: (a) a função carrega — se não carregar, o caso **reprova** em vez de passar com tabela vazia; (b) destino idêntico: NÃO diverge; (c) conteúdo diferente: DIVERGE; (d) modo diferente: DIVERGE; (e) destino inexistente: DIVERGE.
- **Negative companion**: as pontas (c), (d) e (e) são o companheiro negativo de (b) — sem elas, uma função que devolvesse "não diverge" para tudo passaria, e o instalador nunca escreveria nada
- **Critérios validados**: ADR-0005 — instalação idempotente (a DECISÃO, não o efeito)
- **Obs**: Promovido a caso próprio pelo Gate 2 (ver §6.2). Era a nona ponta do CT-001, que fixou teto de oito. Enquanto viveu lá, uma falha da 1ª execução privilegiada do instalador levava junto, pelo `return` antecipado, a única prova que não precisava dela.

### Testes Existentes a Modificar

> Nenhum teste existente impactado — o repositório não possui suíte; ela nasce nesta feature.
---

## 6. Notas / Observações

**Risco alto e justificado.** Esta task define o arranque do servidor. Unidade mal escrita — dependência circular, ordem errada, reinício em laço — pode impedir o boot ou deixar o ambiente legado sem subir. É o cenário mais caro da fatia.

**CA-9 exige janela combinada e não é repetível à vontade.** Toda a verificação possível deve estar feita antes. Se o reinício revelar problema, corrigir e repetir custa outra janela — daí a ordem: `verificar-fundacao.sh` verde primeiro, reinício por último.

**A prova inclui o ambiente legado.** Não basta a base nova voltar: o guardrail da INTENT exige que o sistema que atende a operação também volte sozinho. Se o provisionamento de T2 tiver desalinhado algo no arranque dele, é aqui que aparece.

**Existe precedente de idempotência aprovado nesta base.** O `preparar-site-efemero.sh` passou pelos dois gates com a mesma exigência.

**Esta task é a última da fatia.** Depois dela, o critério "a base existe e se sustenta" está provado, e a fatia seguinte pode começar.

---

### 6.1 Registro da execução (2026-08-01)

#### 🔴 BLOQUEADOR — a aplicação recusa a `DATABASE_URL` que o provisionamento grava

Descoberto ao exercitar o `ExecStart` real desta task, **antes** de qualquer execução privilegiada.
É o resíduo não fechado do débito **D7** (`_run/run-report.md`), cujo endereço de resolução foi
definido como **T5**: a reconciliação de **esquema** foi feita lá (a regex exige `postgresql://`),
a de **forma** não.

- `provisionar-base.sh` (P06) grava a forma de socket de domínio Unix:
  `DATABASE_URL=postgresql://sysloc_app:SEGREDO@/sysloc?host=/var/run/postgresql&port=5432`
- `apps/api/src/configuracao/ambiente.ts` valida com `.refine(URL.canParse, …)`, e essa forma
  **não passa** — o host vazio entre `@` e `/` é rejeitado pelo interpretador de URL do runtime.
- Consequência: `systemctl start sysloc-api` falha na partida com
  `configuração inválida na partida: DATABASE_URL: não é uma cadeia de conexão interpretável`.

Reprodução, sem privilégio e sem tocar o arquivo real:

```
node -e 'console.log(URL.canParse("postgresql://u:p@/sysloc?host=/var/run/postgresql&port=5432"))'
# -> false
```

`apps/api/test/ambiente.spec.ts` só exercita a forma `HOSPEDEIRO:PORTA`, e por isso a lacuna
atravessou os dois gates de T5. **A decisão de qual lado corrigir NÃO foi tomada aqui** — ela tem
duas leituras incompatíveis e nenhuma delas está em arquivo desta task:

| Opção | O que muda | Custo |
|---|---|---|
| **A** — relaxar a validação em `apps/api/src/configuracao/ambiente.ts` | trocar `URL.canParse` por uma verificação que aceite a forma de socket, com caso novo em `apps/api/test/ambiente.spec.ts` | toca entregável de T5, com os gates de T5 |
| **B** — `provisionar-base.sh` passar a gravar uma forma interpretável (ex.: `postgresql://u:p@localhost/sysloc?host=…&port=…`, que é aceita pelas duas pontas) | toca entregável de T2 **e** exige reescrever `/etc/sysloc/backend.env` já posicionado | operação, não só código |

Enquanto não for resolvido, a bateria reprova no CT-001 (o instalador aborta ao confirmar a
partida) — que é exatamente o que se espera dela.

#### ✅ Fechamento do bloqueador (2026-08-01) — opção **B**, decidida pelo usuário

O vão é **maior** do que a tabela acima supunha, e isso muda a decisão. As três formas candidatas
foram exercitadas contra o cliente que a aplicação de fato usa (`postgres.js`, que constrói as
opções de conexão com `new URL()`):

| Forma | `URL.canParse` | O que o cliente faz | Conecta? |
|---|---|---|---|
| `@/sysloc?host=/var/run/postgresql&port=5432` | ❌ | recusa com `Invalid URL` | **não** |
| `@%2Fvar%2Frun%2Fpostgresql:5432/sysloc` | ✅ | **não decodifica** → resolve como nome de máquina | **não** |
| `@localhost:5432/sysloc?host=/var/run/postgresql` | ✅ | **ignora o `?host=`** → vai ao endereço | **não** |

**A opção A não resolveria**: o cliente só alcança socket de domínio Unix pelo **objeto de
opções** (`host: '/var/run/postgresql'`), nunca por cadeia de conexão. Relaxar a validação faria o
processo passar na partida e quebrar na primeira consulta — o oposto do que `carregarAmbiente`
existe para garantir.

Implementada a **opção B**, na forma que o usuário decidiu: o cluster provisionado passa a escutar
em **`127.0.0.1`** *além* do socket, e a `DATABASE_URL` passa a ser
`postgresql://sysloc_app:SEGREDO@127.0.0.1:5432/sysloc` — uma URL de verdade, que `postgres.js`,
`psql` e o migrador da fatia seguinte leem **sem tradução**. A alternativa (manter o socket e
traduzir na aplicação) foi descartada porque exigiria a mesma tradução em toda ferramenta futura.

- **`provisionar-base.sh`**: `listen_addresses = '127.0.0.1'`; regra `host sysloc sysloc_app
  127.0.0.1/32 scram-sha-256` no bloco gerido do `pg_hba.conf`, que passou a ser idempotente por
  **conteúdo** (por presença do marcador, nenhuma mudança de bloco chegaria a uma máquina já
  provisionada); a cadeia de conexão num lugar só (`montar_url_do_banco`); **migração** do
  `/etc/sysloc/backend.env` já posicionado, reescrevendo só a linha da cadeia e **preservando a
  credencial** que o banco conhece; P09 passou a validar pelo caminho que a aplicação percorre; e
  a porta do cluster entrou no guarda de colisão, derivada da configuração do próprio cluster.
- **`verificar-provisionamento.sh`**: pontas **(m)** e **(n)** novas no CT-003 — a forma gravada é
  auditada nas duas frentes que falharam (estrutural e **contra o cliente real**), com três
  falsificadores; e a migração é exercitada com companheiro negativo.
- **`apps/api` não precisou de mudança**: `URL.canParse` aceita a forma nova, e era essa validação
  que estava certa desde o início.
- **`.env.example`** documenta a forma nova e **por que** loopback e não socket.

`apps/api/test/ambiente.spec.ts` segue exercitando a forma `HOSPEDEIRO:PORTA` — que agora é a
única em uso. A lacuna que atravessou os dois gates de T5 não era o caso ausente: era não haver
asserção alguma ligando o que o provisionamento **grava** ao que a aplicação **consome**. É essa
ponte que a ponta (m) instala.

#### Endurecimento do instalador decorrente do bloqueador

`instalar-unidades.sh` **ativa antes de habilitar**, e confirma que a unidade permaneceu `active`
(`Type=simple` devolve sucesso assim que o processo é criado — a falha de configuração acontece um
instante depois). Se a partida não se confirmar, ele imprime as últimas linhas do journal,
**desabilita** a unidade caso ela já estivesse habilitada, e aborta. Sem isso, uma falha de
configuração deixaria uma unidade quebrada **marcada para subir no boot**, e o efeito apareceria
como `is-system-running = degraded` num servidor compartilhado com o ambiente que atende a operação.

#### Divergência deliberada do card do CT-003

O card da §5.6 é escrito no **singular** e usa como dado de entrada a remoção da "variável de
conexão com o banco". `DATABASE_URL` é justamente a variável que o **processador não lê**
(`apps/worker/src/main.ts` valida `LOG_LEVEL` e `REDIS_URL`, e só): executado contra
`sysloc-worker` com esse dado, o caso veria o processo subir e **provaria o contrário do que
afirma**. O CT-003 implementado roda contra as **duas** unidades, cada uma com uma variável que o
seu próprio processo exige. Verificado sem privilégio, executando os dois artefatos construídos com
o ambiente da unidade menos a variável:

```
api    -> "configuração inválida na partida: DATABASE_URL: ausente."  código 1, 1,6 s
worker -> "configuração inválida na partida: REDIS_URL: ausente."     código 1, 0,5 s
```

Segunda divergência, menor: o arquivo de ambiente de prova conserva apenas as chaves que **aquele**
processo consome, menos a removida — de modo que a credencial real **nunca** é copiada para um
temporário. O poder discriminante é idêntico.

#### `TimeoutStopSec` — o requisito nominal do marcador de T6

O marcador `DECISÃO FECHADA` de `apps/worker/src/fila.ts` (T6 / Gate 2) diz, no `REVERTER EXIGE`,
que "T7 declara `TimeoutStopSec` com folga sobre este valor", sendo o valor o
`LIMITE_DE_DESLIGAMENTO_MS` de **15 s**. Até aqui o par se sustentava pelo padrão da distribuição
(`DefaultTimeoutStopUSec=1min 30s` nesta máquina) — **por acidente**. Declarado explicitamente
`TimeoutStopSec=30` nas **duas** unidades: o dobro do prazo interno, e bem abaixo dos 90 s
herdados, de modo que um desligamento do servidor não pague 90 s por unidade. O marcador **não foi
alterado**.

#### `StartLimitIntervalSec` / `StartLimitBurst` — sem eles, `Restart=always` é laço infinito

Os padrões da distribuição são 5 partidas em **10 s**. Com `RestartSec=5`, cada ciclo custa mais de
5 s e **nunca** se chega a 5 partidas dentro de uma janela de 10 s: o limite jamais é atingido e uma
unidade com configuração inválida reinicia **para sempre**. Com a janela em 60 s, as cinco partidas
cabem nela e a sexta é recusada — a unidade converge para `failed` com `Result=start-limit-hit`, que
é o que a CA-15 e o CT-003 cobram.

#### `ExecStart` depende de artefato construído — a decisão

`dist/main.js` é produzido por `pnpm build` e barrado pelo `.gitignore`. **Não** há `ExecStartPre`
de construção: com `Restart=always`, compilar a cada reinício custaria dezenas de segundos no
caminho de recuperação que a CA-8 exige em 60 s, e uma falha de compilação viraria laço de
reinício. Quem confere o artefato é o **instalador**, uma vez, com um humano olhando, e ele se
recusa a prosseguir sem ele dizendo o comando a executar.

#### `verificar-workspace.sh` (T1) — atualizado, não afrouxado

Ele reprovava **4 asserções** contra o HEAD atual, porque congelou o estado de T1 (`apps/` e
`packages/` vazios) e o clone efêmero é do HEAD, que hoje traz `packages/shared`, `apps/api` e
`apps/worker`. O invariante "o ciclo tolera membro de workspace vazio" **perdeu o objeto**. As
asserções passaram a ser **espelho** do conjunto real de membros (`MEMBROS_DO_WORKSPACE`): contagem
exata por diretório e cada membro conferido pelo **nome e pelo caminho**. Perder um pacote reprova,
ganhar um pacote não declarado reprova, e um pacote registrado no lugar errado reprova — é
estritamente mais forte do que a asserção anterior, não mais frouxa.

### 6.2 Correções do Gate 2 — Tech Review (2026-08-01)

#### P2 — o guarda de colisão da porta do cluster era inerte na execução de transição

`conferir_colisao_de_porta` decidia a posse em dois passos: alguém escutando? a nossa unidade está
ativa? O segundo tratava **"unidade ativa" como prova de que a porta é nossa**. Isso vale para
6380/1025/8025 — as unidades delas *vinculam* a porta ao subir — e é **falso para o cluster**, que
fica `active` servindo só pelo socket enquanto `listen_addresses` estiver vazio. Estado real do host
conferido pelo gate: drop-in com `listen_addresses = ''`, `postgresql@18-main.service` ativa,
`ss -lntH` sem 5432 — **unidade ativa, porta TCP sem dono**. Com um terceiro nela, o guarda passaria
alegando "nossa instância", o P03 reescreveria o sobreposto e o `systemctl restart` falharia ao
**vincular**: um aborto limpo de pré-condição viraria um aborto de meio de execução com o cluster
desligado, dentro da janela.

**Forma escolhida**: `conferir_colisao_de_porta` ganhou um 4º parâmetro **opcional** — uma *prova
extra de posse*, exigida em conjunção com `unidade_ativa`. Só a porta do cluster a recebe
(`cluster_escuta_na_porta`); as três portas fixas seguem exatamente como estavam, porque endurecê-las
quebraria a 2ª execução que a ADR-0005 exige idempotente. A prova é feita ao **cluster em execução**
(`SHOW listen_addresses` + `SHOW port`), não ao arquivo de configuração, e são duas condições
necessárias: escutar em TCP (se `listen_addresses` está vazio, nenhum ouvinte de porta TCP pode ser
nosso) **e** a porta viva ser a porta sob guarda (o `port` de `postgresql.conf` pode ter mudado sem
reinício). Deliberadamente **não** se compara `listen_addresses` com `127.0.0.1`: `localhost`, `*` e
`0.0.0.0` também são escuta em TCP, e recusá-los transformaria reexecução legítima em aborto.

**Rede**: ponta **(g)** nova no CT-005 de `verificar-provisionamento.sh` — sete asserções que
carregam o guarda e a prova do arquivo real e injetam dublês de `ss`, `unidade_ativa` e do cliente do
cluster. Falsificação demonstrada com três mutantes em cópia: guarda antigo (`unidade_ativa` sozinha)
reprova (g)-transição e (g)-outra-porta; prova sem a comparação de porta reprova (g)-outra-porta;
guarda que anuncia a recusa e segue reprova todas as pontas de aborto. Controle limpo nas sete.

#### P1 — a saída do P03 mentia ao operador durante a janela

`criado`/`ja_ok` do P03 diziam *"socket local, sem escuta de rede"* — a linha de auditoria que o
operador lê no terminal, e o único ponto do fluxo privilegiado onde a postura de exposição é afirmada
em voz alta. As duas passaram a ser derivadas de `postura_de_escuta_do_cluster()`, que compõe a frase
a partir de `${HOSPEDEIRO_DB}` e `${DIR_SOCKET_PG}` — assim a mensagem acompanha a constante na
próxima mudança em vez de vencer de novo em silêncio.

**Varredura da classe inteira** (mensagens e comentários que descrevam a postura de escuta ou o
caminho de conexão, no arquivo todo): **quatro** afirmações vencidas, duas além das apontadas —
(1) `criado "P03"`, (2) `ja_ok "P03"`, (3) o comentário do P06 que justificava descobrir a porta
"porque ela nomeia o arquivo do socket", quando hoje ela existe para a `DATABASE_URL` e para o P09
conectar por `${HOSPEDEIRO_DB}`, e (4) o comentário de `conferir_coordenadas_do_ambiente`, que dizia
que a divergência faria as unidades apontarem "para um socket inexistente". Corrigidas as quatro.

#### P3 — CT-001 tinha nove pontas

A ponta (i) virou **CT-007 — a decisão de idempotência do instalador, exercitada como função pura**,
com o bloco reaproveitado como estava e o par `caso`/`fechar_caso` do padrão. CT-001 fica com oito
(a–h). Ganho além da contagem: a prova da função pura deixou de depender do `return` antecipado que a
falha da 1ª execução privilegiada do instalador dispara.

#### P4 — NÃO aplicado, por decisão explícita do Gate 2 (débito com gatilho)

`sysloc-worker.service` compartilha `EnvironmentFile=/etc/sysloc/backend.env` com o serviço de
aplicação, de modo que a `DATABASE_URL` — que o processador **não usa** — entra no ambiente dele e
fica legível em `/proc/<pid>/environ`. **Hoje não é fronteira de privilégio**: as duas unidades rodam
como `User=sysloc` e quem lê o `environ` de uma lê o da outra. Partir o arquivo tocaria o P06 de
`provisionar-base.sh`, `verificar-provisionamento.sh` e a ponta (c) do CT-005 — mudança grande demais
para a véspera da janela.

> **Gatilho**: quando o processador ganhar `User=` próprio, dividir em `/etc/sysloc/backend.env` e
> `/etc/sysloc/worker.env` (só `REDIS_URL`), ambos 0600.

### ADRs Aplicáveis nesta Task

- ADR-0005 — as unidades de serviço e os scripts vivem no repositório e são posicionados por procedimento de instalação **idempotente**; **nenhum entra carregando credencial** — o segredo vive em configuração não versionada, e isso é condição de entrada — docs/adr/0005-rotinas-operacionais-versionadas-com-instalacao-idempotente.md

---

## 7. Checklist Final
- [x] Implementada conforme Scope — os 5 arquivos da §3.1 criados; `package.json` conferido (o
  script `dev` já apontava para o multiplexador desde T1, nenhuma alteração necessária).
- [x] Testes unitários criados/atualizados — **N/A declarado na §5.1**: esta task não tem caso de
  camada `unit`; o que ela entrega só é verificável contra recurso real.
- [x] Testes de integração criados/atualizados — CT-001 a CT-007 implementados em
  `deploy/scripts/instalacao/verificar-fundacao.sh`, com a convenção de asserção de
  `.claude/rules/testing-stack.md`. **A execução de CT-001 a CT-006 é pendente de privilégio**
  (ver §6.1). Exercitado sem privilégio: as duas unidades em `systemd-analyze verify` (código e
  ausência de aviso) com os dois falsificadores; as funções puras do instalador; as varreduras de
  segredo do CT-005 com os quatro falsificadores de unidade e o falsificador da árvore versionada;
  a premissa do CT-003 nos dois processos construídos; `verificar-workspace.sh` completo.
- [ ] Critério de conclusão atendido — **implementação completa, prova pendente de execução
  privilegiada**. O bloqueador da cadeia de conexão está **fechado** (§6.1, "Fechamento do
  bloqueador": opção B — o cluster escuta em `127.0.0.1` e a `DATABASE_URL` é uma URL de endereço
  de rede, que `apps/api` aceita sem mudança). O que resta não é implementação: CT-001 a CT-005
  exigem `sudo`, que neste host pede senha interativa, e o CT-006 exige além disso a **janela de
  indisponibilidade combinada** com o operador (`reinicio-preparar` → `reboot` real →
  `reinicio-conferir`). Enquanto as duas execuções não acontecerem, "as duas unidades sobem os
  serviços e o servidor volta sozinho" está **provado por construção e não por execução** — por
  isso a caixa segue aberta.
- [x] Rastreabilidade Critério → Testes preenchida (seção 5.5)
- [x] Detalhamento dos Casos de Teste preenchido (seção 5.6 — 1 card por CT)
- [x] Revisada
