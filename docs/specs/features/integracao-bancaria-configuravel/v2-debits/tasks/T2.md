# T2 — Resolver D-002: corrigir docstring de `montar_payload_emissao`

## 1. Identificação

- **ID**: T2
- **Nome da Task**: Resolver D-002 — docstring de `montar_payload_emissao` desatualizada após a correção de equivalência
- **model**: sonnet
- **risk**: low
- **gates**: [qa, tech_review]  # path em Critical Path (payments — adaptador de cobrança)
- **Status**: Concluído
- **Fase**: 2 (núcleo canônico — sequencial)
- **Dependências**: Nenhuma
- **Símbolos públicos criados**: N/A
- **Símbolos consumidos de outras tasks**: N/A
- **Critério de Conclusão**: docstring coerente com o código; nenhuma linha executável alterada; suíte passa sem regressão.
- **Feature**: integracao-bancaria-configuravel
- **Versão**: v2-debits
- **Parent task**: T5 (de v1)
- **Débito origem**: D-002 (code_quality, BAIXO) — classificação: `recomendado_corrigir`
- **Source**: agent-spec-debt-resolution

---

## 2. Objetivo da Task

Resolver o débito técnico D-002 alinhando a docstring ao comportamento real da função, **sem tocar nenhuma linha executável**.

---

## 3. Arquivos Impactados

### 3.1 Arquivos a Criar

_Nenhum._

### 3.2 Arquivos a Modificar

| Arquivo | Descrição |
|---------|-----------|
| `app-sync/locacao_automation/locacao_automation/cobranca_bancaria/adaptadores/sicoob/mapeamento.py` | Corrigir a docstring de `montar_payload_emissao` (somente comentário — nenhuma linha de código) |

> ⚠️ **Arquivo compartilhado**: `mapeamento.py` também é tocado por **T4** (popular `codigo_erro` em `interpretar_consulta`). São tasks separadas — não antecipe.

### 3.3 Arquivos de Referência

- `docs/specs/features/integracao-bancaria-configuravel/v1/_run/run-report.md` — débito original (§2, linha 35).
- `docs/specs/features/integracao-bancaria-configuravel/v1/tasks/T5.md` — task que gerou o débito.

---

## 4. Detalhes de Implementação

### 4.1 Contexto do Débito

**Origem**: task `T5` da `v1` — registrado em `docs/specs/features/integracao-bancaria-configuravel/v1/_run/run-report.md` (linha 35).

**Descrição original**: a docstring afirma que `codigoNegativacao`/`codigoProtesto`/`tipoJurosMora`/`gerarPdf` "entram pelo escape, **NUNCA hardcoded**". Após a correção de equivalência aplicada em T5 da v1 (restaurar os 8 defaults técnicos que o legado sempre enviava), esses campos passaram a ser **defaults `*_PADRAO` da camada de dialeto** — o escape `parametros_provedor` apenas os **sobrescreve**. A docstring contradiz o código logo abaixo dela.

**Categoria**: `code_quality` (BAIXO)

**Classificação do especialista**: `recomendado_corrigir`
**Justificativa**: "Custo: 5min (só docstring em mapeamento.py, nenhuma linha de código executável tocada). Risco: nenhum. Valor: evita leitura errada do mecanismo de escape/ADR-0001 por quem mexer no adapter Sicoob depois."
**Custo estimado**: ~5min
**Risco de regressão**: `nenhum`

### 4.2 Correção

- [x] Ajustar a docstring de `montar_payload_emissao` para descrever a natureza dual pós-correção: os campos técnicos fixos entram como **defaults do dialeto** (equivalência com o legado) e `parametros_provedor` os **sobrescreve** quando presente.
- [x] Trocar o exemplo de "campo que entra pelo escape" por um campo **sem default** (ex.: `rateioCreditos`), coerente com o teste negativo do CT-010.

### 4.3 Guardrails de Execução

**DEVE**:

- Ler o bloco de constantes `*_PADRAO` e o corpo de `montar_payload_emissao` para descrever o comportamento com precisão (o comentário do bloco de constantes já está correto — use-o como referência).
- Alterar **somente texto de docstring/comentário**.
- Rodar a suíte completa antes de retornar a task.

**NÃO DEVE**:

- NÃO alterar nenhuma linha executável (constantes, ordem do merge, defaults). O diff deve ser **100% comentário**.
- NÃO antecipar **D-004/T4** (popular `codigo_erro` em `interpretar_consulta`, mesmo arquivo) — tem task própria.
- NÃO refatorar a função nem renomear constantes.
- NÃO adicionar testes novos.

---

## 5. Testes

**N/A — task é cleanup técnico**. Não cria testes novos.

O Gate 1 (QA) DEVE executar a **suíte completa** após a modificação:

```
docker compose exec -T backend bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend run-tests --app locacao_automation'
```

- ✅ Os **113 testes** continuam passando (esperado trivialmente — o diff é só comentário).
- ❌ Qualquer teste regredindo → sinal de que uma linha executável foi tocada por engano → task rejeitada.

---

## 6. Notas / Observações

_Vazio. Tasks de débito não geram débito novo._

---

## 7. Checklist Final

- [x] Correção aplicada exatamente como descrita em §4.2 — sem expansão de escopo
- [x] Diff afeta APENAS `adaptadores/sicoob/mapeamento.py` e contém **somente** texto de docstring
- [x] Nenhuma antecipação de T4 (mesmo arquivo, task própria)
- [x] Suíte completa passa sem regressão (113 testes)
- [x] Gates aprovaram (QA + Tech Review)
- [x] Staged para commit (git add pelo orquestrador)
