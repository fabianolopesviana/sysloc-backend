# T3 — Resolver D-004: renomear `_obter_pendente` → `_preparar_pendente`

## 1. Identificação

- **ID**: T3
- **Nome da Task**: Resolver D-004 — `_obter_pendente` tem nome de leitura e contrato de escrita
- **model**: sonnet
- **risk**: low
- **gates**: [qa, tech_review]  # Critical Path: security + payments
- **Status**: Concluído
- **Fase**: 1
- **Dependências**: Nenhuma (colide em arquivo com T1, T2 e T4)
- **Símbolos públicos criados**: `_preparar_pendente` (rename de `_obter_pendente`)
- **Símbolos consumidos de outras tasks**: N/A
- **Critério de Conclusão**: função renomeada e os 3 call sites atualizados; nenhuma referência órfã; suíte sem regressão.
- **Feature**: integracao-bancaria-configuravel
- **Versão**: v4-debits
- **Parent task**: T1 (de v3)
- **Débito origem**: D-004 (code_quality, BAIXO) — classificação: `recomendado_corrigir`
- **Source**: agent-spec-debt-resolution

---

## 2. Objetivo da Task

Alinhar o nome da função ao que ela faz — escrita, não leitura —, seguindo a convenção que o próprio módulo já pratica.

---

## 3. Arquivos Impactados

### 3.1 Arquivos a Criar

_Nenhum._

### 3.2 Arquivos a Modificar

| Arquivo | Descrição |
|---------|-----------|
| `app-sync/locacao_automation/locacao_automation/integracao_bancaria_api/service.py` | Renomear `_obter_pendente` → `_preparar_pendente` (definição ~477) e atualizar os 3 call sites (~668, ~905, ~979) |

> ⚠️ **Arquivo compartilhado** com T1, T2 e T4. Não antecipe nenhuma delas.

### 3.3 Arquivos de Referência

- `docs/specs/features/integracao-bancaria-configuravel/v3/_run/run-report.md` — débito original (§2, bloco D4).

---

## 4. Detalhes de Implementação

### 4.1 Contexto do Débito

**Origem**: task `T1` da `v3` — Gate 2 (Tech Review).

`_obter_pendente` não apenas obtém: pode **criar** o pendente, **deletar** e **inserir** `File`s (efeitos commitáveis no banco e no filesystem) e mutar o documento em memória. O docstring declara o efeito e os 3 call sites salvam logo em seguida — não há bug —, mas o nome esconde a escrita de quem lê a chamada.

Os demais helpers do módulo usam verbo de escrita: `_criar_pendente_de_ativa`, `_ressincronizar_ex_ativo`, `_replicar_vinculo_certificado`.

**Verificação do especialista**: a função é chamada em exatamente 3 lugares, todos privados ao módulo (`salvar_configuracao`, `enviar_certificado`, `remover_certificado`); nenhum teste faz mock/patch pelo nome — há apenas menção em comentário. Qualquer chamada esquecida quebraria a suíte imediatamente com `NameError`.

**Categoria**: `code_quality` (BAIXO) · **Custo**: ~10min · **Risco de regressão**: `nenhum`

### 4.2 Correção

- [x] Renomear a definição para `_preparar_pendente`.
- [x] Atualizar os **3** call sites.
- [x] Manter o docstring atual (ele já declara corretamente os efeitos).
- [x] Atualizar menções ao nome antigo em comentários, se houver — inclusive no arquivo de teste, se for apenas comentário.

### 4.3 Guardrails de Execução

**DEVE**:

- Grep por `_obter_pendente` em **todo** o repo antes e depois, para garantir zero referência órfã (inclusive comentários e docstrings de outros módulos).
- Rodar a suíte completa antes de retornar.

**NÃO DEVE**:

- **NÃO** alterar o corpo da função — apenas o nome e as chamadas.
- **NÃO** alterar a assinatura (parâmetros e retorno permanecem).
- **NÃO** renomear outras funções "de carona".
- **NÃO** antecipar T1, T2 ou T4.
- **NÃO** adicionar testes.

---

## 5. Testes

**N/A — cleanup técnico.** Não cria testes novos.

O Gate 1 (QA) DEVE executar a suíte completa:

```
docker compose exec -T backend bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend run-tests --app locacao_automation'
```

- ✅ **120 testes** continuam passando.
- ❌ `NameError` ou qualquer regressão → call site esquecido → task rejeitada.

---

## 6. Notas / Observações

_Vazio. Tasks de débito não geram débito novo._

---

## 7. Checklist Final

- [x] Definição renomeada para `_preparar_pendente`
- [x] Os **3** call sites atualizados
- [x] Grep confirma zero ocorrência de `_obter_pendente` no repo (código e comentários)
- [x] Corpo e assinatura da função inalterados
- [x] Nenhuma antecipação de T1/T2/T4
- [x] Suíte completa passa sem regressão (120 testes)
- [x] Gates aprovaram (QA + Tech Review)
- [x] Staged para commit (git add pelo orquestrador)
