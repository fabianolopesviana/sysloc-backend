# T2 — Resolver D-002: tornar o CT-039 auto-contido quanto a `frappe.local.response`

## 1. Identificação

- **ID**: T2
- **Nome da Task**: Resolver D-002 — asserção de "nenhum PDF gerado" depende de estado global limpo por outro arquivo
- **model**: sonnet
- **risk**: low
- **gates**: [qa]  # altera APENAS arquivo de teste; nenhum código de produção
- **Status**: Concluído
- **Fase**: 1
- **Dependências**: Nenhuma
- **Símbolos públicos criados**: N/A
- **Símbolos consumidos de outras tasks**: N/A
- **Critério de Conclusão**: o teste inicializa e restaura `frappe.local.response` por conta própria; passa isolado e na suíte; nenhuma asserção enfraquecida.
- **Feature**: integracao-bancaria-configuravel
- **Versão**: v6-debits
- **Parent task**: T2 (de v5)
- **Débito origem**: D-002 (tests, BAIXO) — classificação: `perfumaria` (incluído por decisão do usuário)
- **Source**: agent-spec-debt-resolution

---

## 2. Objetivo da Task

Eliminar o acoplamento entre arquivos de teste: a prova de que `salvar_configuracao` não gera PDF deve depender **só** do que este teste monta.

---

## 3. Arquivos Impactados

### 3.1 Arquivos a Criar

_Nenhum._

### 3.2 Arquivos a Modificar

| Arquivo | Descrição |
|---------|-----------|
| `app-sync/locacao_automation/locacao_automation/tests/test_integracao_bancaria_api.py` | Inicializar e restaurar `frappe.local.response` no setUp de `_BaseDecisaoTrocaTest` |

> ⚠️ **Arquivo compartilhado** com T1 desta versão (que acrescenta testes ao mesmo arquivo). O orquestrador decide a ordem.

### 3.3 Arquivos de Referência

- `docs/specs/features/integracao-bancaria-configuravel/v5/_run/run-report.md` — débito original (§2, bloco D2).
- `app-sync/locacao_automation/locacao_automation/tests/test_boletos_abertos.py:272-274` — **o padrão a imitar** (já faz exatamente isso).

---

## 4. Detalhes de Implementação

### 4.1 Contexto do Débito

Em `test_ct039_decisao_aceitar_com_consolidado_grava_e_nao_produz_pdf` (~linha 636), a prova de que nenhum PDF foi produzido é:

```python
assertIsNone(frappe.local.response.get("filecontent"))
```

Mas `frappe.local.response` é **estado global do processo**, e o teste não o inicializa no próprio `setUp`. Hoje ele passa em qualquer ordem apenas porque `test_boletos_abertos.py:274` restaura `frappe.local.response = frappe._dict()` via `addCleanup` depois do teste de streaming.

Se um teste futuro escrever ali sem cleanup, este caso passa a falhar por motivo **alheio ao SUT** — a asserção deixaria de provar o comportamento de `salvar_configuracao` e passaria a medir a higiene de outro arquivo.

**Não é falha atual**: a suíte roda verde e o caminho isolado também.

**Categoria**: `tests` (BAIXO) · **Custo**: ~10min · **Risco de regressão**: `nenhum`

> **Incluído por decisão explícita do usuário**, ciente de que o especialista o classificou como **perfumaria** — acoplamento hipotético, sem incidente registrado.

### 4.2 Correção

- [x] No `setUp` de `_BaseDecisaoTrocaTest` (ou no próprio teste, antes da chamada ao SUT): definir `frappe.local.response = frappe._dict()`.
- [x] Registrar `addCleanup` restaurando o valor **anterior** — não apenas zerando, para não estragar o estado de quem vier depois.
- [x] Imitar o padrão já existente em `test_boletos_abertos.py:272-274`, em vez de inventar outro.

### 4.3 Guardrails de Execução

**DEVE**:

- **Ler `test_boletos_abertos.py:272-274` antes** e seguir o mesmo padrão — consistência vale mais que preferência pessoal aqui.
- Preservar **todas** as asserções existentes do CT-039. A mudança é de **setup**, não de asserção.
- Verificar que o teste passa **isolado** (rodando só o módulo) e **na suíte completa** — o ponto da task é justamente a independência de ordem.
- Rodar a suíte completa.

**NÃO DEVE**:

- **NÃO** alterar código de produção. Esta task é só teste — se ela revelar defeito no SUT, **pare e reporte**.
- **NÃO** enfraquecer nem remover asserção alguma (AP-24).
- **NÃO** mexer no `addCleanup` de `test_boletos_abertos.py` — aquele arquivo está correto; o problema é a dependência deste.
- **NÃO** criar testes novos — o escopo é tornar o existente auto-contido.
- **NÃO** antecipar T1 (o log de chaves ignoradas em `service.py`).

---

## 5. Testes

**N/A — cleanup técnico.** Não cria testes novos; torna um existente auto-contido.

O Gate 1 (QA) DEVE executar a suíte completa:

```
docker compose exec -T backend bash -lc 'cd /home/frappe/frappe-bench && bench --site frontend run-tests --app locacao_automation'
```

- ✅ Os **136 testes** continuam passando.
- ✅ **Verificação adicional**: rodar o módulo `test_integracao_bancaria_api` **isolado** e confirmar que o CT-039 passa sem depender de `test_boletos_abertos`. É a prova de que o acoplamento foi removido — sem ela, a task não demonstrou nada.
- ❌ Qualquer regressão → task rejeitada.

---

## 6. Notas / Observações

_Vazio. Tasks de débito não geram débito novo._

---

## 7. Checklist Final

- [x] `frappe.local.response` inicializado no setUp e **restaurado** (valor anterior) por `addCleanup`
- [x] Padrão de `test_boletos_abertos.py:272-274` seguido
- [x] Todas as asserções do CT-039 preservadas; nenhuma enfraquecida
- [x] CT-039 passa com o módulo rodando **isolado** — prova de que o acoplamento saiu
- [x] Nenhum código de produção alterado
- [x] Nenhuma antecipação de T1
- [x] Suíte completa passa sem regressão (139 testes)
- [x] Gate aprovou (QA)
